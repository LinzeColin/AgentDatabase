interface:
  display_name: "Dynamic Personal Profile Update"
  short_description: "Update one time-aware profile Markdown from derived data"
  default_prompt: "Use $dynamic-personal-profile-update to update and validate the single profile Markdown from allowlisted derived data, then report only material changes and reusable asset candidates."
