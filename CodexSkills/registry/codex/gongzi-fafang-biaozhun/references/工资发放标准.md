# 发放表整理任务｜ChatGPT + Codex 统一执行模板（精确金额差异 + ABExcel 校验）

> 用途：把一个或多个“当月原始发放 Excel”分别整理成“模板文件 / 上月最终正确版”的结构、样式、公式和 Sheet 顺序，并在交付前完成精确金额、归属、公式、格式、文件完整性、ABExcel 和跨原始数据一致性复核。
> 适用对象：ChatGPT、Codex、工程型 Agent、人工复核人员。  
> 最高优先级：每份原始文件都是独立唯一数据源；模板文件只提供结构、样式、公式和版式；金额以精确十进制判断、以整数分辅助展示；任意非零差异都要报告；多原始数据任务固定交付跨原始数据一致性报告。

---

## 0. 使用方式

### 0.1 给 ChatGPT 使用

把本文件全文、当月原始数据集、模板文件一起发送给 ChatGPT，并替换第 1 节中的占位信息。

```text
read-only → plan → implement → verify → deliver
```

不得跳过 read-only 分析，不得未列出计划就修改或生成正式文件。

---

## 1. 本次任务输入

```text
你现在要处理一个高重要性 Excel 发放表任务。请严格按以下规则执行。

【输入文件】
1. 当月原始数据集：{来源标识: 当月原始文件名.xlsx}，至少一份
2. 模板文件 / 上月最终正确版：{模板文件名.xlsx}
3. 当前运行授权的打开口令候选集：{仅运行时提供，不写入交付物或仓库}
4. 输出保护策略：{默认沿用成功打开的模板口令}
5. 目标月份：{例如：2026.05月发放}
6. 发给 Agent 的当天日期：{YYYYMMDD，例如：20260703}
7. 历史复演组：{可选；历史原始文件 + 历史处理结果}

【单源无差异通过时最终交付】
1. {目标月份}-{YYYYMMDD}_最终发放.xlsx
2. {目标月份}-{YYYYMMDD}_校验ABExcel检查.xlsx

【单源有差异 / 未通过 / 无法确认时最终交付】
1. {目标月份}-{YYYYMMDD}_最终发放.xlsx
2. {目标月份}-{YYYYMMDD}_校验ABExcel检查.xlsx
3. {目标月份}-{YYYYMMDD}_差异报告.md
4. {目标月份}-{YYYYMMDD}_差异明细.xlsx

【多原始数据任务固定追加交付】
1. {目标月份}-{YYYYMMDD}_跨原始数据一致性报告.md
2. {目标月份}-{YYYYMMDD}_跨原始数据一致性明细.xlsx

多个原始文件位于同一目录时，每份专属交付物在 `{YYYYMMDD}` 后加 `{来源标识}`，例如：
`2026.05月发放-20260703-source_01_最终发放.xlsx`。

【示例】
目标月份 = 2026.05月发放
发给 Agent 的当天日期 = 20260703
最终发放文件名 = 2026.05月发放-20260703_最终发放.xlsx
ABExcel 校验文件名 = 2026.05月发放-20260703_校验ABExcel检查.xlsx
```

---

## 2. 总目标

把每份“当月原始文件”独立整理成“模板文件 / 上月最终正确版”的格式和样子，并在多源任务中报告相互一致性。

最终文件必须满足：
2个来源齐全，5个维度一致，2检验，12类检查，ABExcel通过，20项测试通过，才算是准确无误能交付验收

| 项目 | 必须满足 |
|---|---|
| 数据来源 | 每份最终发放版全部来自对应当月原始文件 |
| 模板来源 | 样式、结构、公式、Sheet 顺序、分区来自模板文件 |
| 金额一致 | 按精确十进制比较，精确差异与金额分差异均为 0 |
| 明细一致 | 源数据有效行与最终文件有效行一一对应 |
| 归属一致 | 主体公司、B公司、报销/发票路由均符合规则 |
| 公式一致 | 模板公式未写死，范围随当月行数调整 |
| 样式一致 | 字体、对齐、行高、列宽、合并、边框、填充符合模板 |
| 文件完整 | 按输出保护策略可打开；保存重开无损坏；Excel 不提示修复 |
| 校验通过 | 2 轮 × 6 类检查、ABExcel、20 项测试均通过 |

关键原则：

```text
总额一致不等于发放表正确。
总额一致 + 精确金额一致 + 行级一致 + 分区一致 + 路由一致 + 公式一致 + 格式一致 + 文件完整，才允许判定通过。
```

---

## 3. 文件角色规则

| 文件 | 作用 | 绝对规则 |
|---|---|---|
| 当月原始数据集 | 每份都是独立唯一数据源 | 每份最终文件的姓名、金额、备注、主体公司、B公司/报销、扣借支、提前发、实发、发票/报销数据都必须来自对应原始文件 |
| 模板文件 / 上月最终正确版 | 样式、结构、分区、公式、版式来源 | 只提供最终长相，不允许沿用旧月份人员、旧金额、旧备注 |
| 最终发放版 | 正式交付文件 | 当月数据 + 模板样式 + 公式正确 + 校验通过 |
| ABExcel 校验文件 | 人工/自动复核文件 | 展示 A/B 值差异、公式差异、错误值、核心金额回勾、格式指纹差异 |
| 差异报告 MD/Excel | 有差异时的说明文件 | 只有存在差异、未通过项或无法确认项时才作为最终交付物输出 |
| 跨原始数据一致性报告 MD/Excel | 多源任务固定交付 | 展示共同业务键金额、仅源 A、仅源 B、字段、路由、分区和覆盖范围差异 |

---

## 4. 执行流程

### 4.1 ChatGPT 执行流程

```text
1. 验证运行时口令并读取原始数据集、模板和历史复演组
2. 识别 Sheet、表头、分区、公式、样式、核心字段
3. 对每份原始文件构建标准化账本 Canonical Ledger
4. 保留精确十进制金额并生成金额分视图
5. 根据规则路由主体公司支付、B公司支付、报销/发票金额
6. 以模板为基底分别生成最终发放版
7. 从每份原始数据独立生成 B 校验版 / Oracle Ledger
8. 分别生成 ABExcel 校验检查文件
9. 执行两轮六类检查、二十项测试和变异验证
10. 生成跨原始数据一致性报告
11. 按条件交付规则输出文件
```

### 4.2 Codex 执行流程

```text
read-only:
  只读原始数据集、模板和历史复演组，输出计划，不修改任何文件。

plan:
  列出将读取的文件、将写入的文件、将处理的 Sheet、业务规则、测试命令、回滚方案。

implement:
  为每份原始文件独立生成结果，只向输出目录写入结果。

verify:
  执行精确金额、行级、覆盖、路由、公式、格式、文件完整性、ABExcel、跨源一致性和变异验证。

deliver:
  根据是否存在差异决定交付 2 个文件或 4 个文件。
```

### 4.3 Read-only 阶段必须记录

| 项目 | 要求 |
|---|---|
| Sheet 信息 | 原始数据集、模板和历史复演文件的 Sheet 名称和顺序、used range |
| 模板结构 | 合并单元格、行高、列宽、表头、分区标题、公式位置 |
| 模板格式 | 字体、字号、边框、填充、数字格式、水平/垂直对齐 |
| 原始字段 | 姓名、主体公司、社保公司、B公司/报销、实发、主体支付、B公司支付、报销/发票、扣借支、提前发、备注 |
| 异常项 | 空值、重复行、重复姓名、无法识别公司名、任意精度金额差异、金额超过两位小数、文本金额 |
| 回滚方案 | 不修改输入文件；删除输出目录即可回滚 |

---

## 5. 精确金额一致规则

### 5.1 金额底层规则

1. 金额必须使用原始十进制文本或等价十进制口径读取；二进制浮点数不参与最终判断。
2. 每个金额同时保留 `amount_exact` 和 `amount_cents`：前者用于精确判断，后者用于人民币分视图与传统汇总。
3. 原始金额超过两位小数时，标记为“金额精度异常”，保留原始精度并写入差异报告。
4. 显示可以为两位小数，精确金额、精确差异和原始精度必须在账本与 ABExcel 中可追溯。
5. 金额差异通过标准为：

```text
source_exact = final_exact = oracle_exact
exact_delta = 0
cents_delta = 0
```

6. 任意 `exact_delta != 0` 都是差异；`0.00001` 元与更小非零差异同样进入差异报告。
7. 人工认可的历史差异保留“已获人工认可”业务状态，并保留技术差异与精确差异值。
8. 不允许为了让汇总一致而修改明细金额。
9. 不允许只看汇总，不查明细。

### 5.2 金额字段最少覆盖

| 字段 | 标准字段名 | 校验规则 |
|---|---|---|
| 实发金额 | `net_pay_exact` / `net_pay_cents` | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 主体公司支付 | `main_company_pay_exact` / `main_company_pay_cents` | 原始 = 最终 = 分表小计 = 汇总 = Oracle，精确差异 = 0 |
| B公司支付 | `b_company_pay_exact` / `b_company_pay_cents` | 原始 = 最终 = 分区小计 = 汇总 = Oracle，精确差异 = 0 |
| 报销/发票金额 | `reimburse_or_invoice_exact` / `reimburse_or_invoice_cents` | 原始 = 最终 = 分区小计 = 汇总 = Oracle，精确差异 = 0 |
| 扣借支 | `advance_deduction_exact` / `advance_deduction_cents` | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 提前发 | `next_month_advance_exact` / `next_month_advance_cents` | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |

### 5.3 独立 Oracle 校验

必须从每份“当月原始文件 + 本模板规则”独立重建 B 校验账本。B 校验账本不得从最终发放文件复制单元格值。

```text
A = 最终发放版抽取结果
B = 原始数据 + 规则独立重建结果
通过 = A 与 B 在行、字段、精确金额、金额分、路由、公式、格式指纹上全部一致
```

---

## 6. 标准化账本 Canonical Ledger

每条原始有效行必须转换为一条标准账本记录。

```text
source_id
row_uid
source_file
source_sheet
source_row
name
main_company
social_security_company
b_or_reimburse_raw
b_or_reimburse_normalized
b_pay_target_company
b_pay_route_status
invoice_target_company
invoice_route_status
route_notes
net_pay_exact
net_pay_cents
main_company_pay_exact
main_company_pay_cents
b_company_pay_exact
b_company_pay_cents
reimburse_or_invoice_exact
reimburse_or_invoice_cents
advance_deduction_exact
advance_deduction_cents
next_month_advance_exact
next_month_advance_cents
remark
final_sheet
final_section
final_row
validation_status
```

### 6.1 row_uid 建议

```text
row_uid = source_id + "|" + source_sheet + "|" + source_row + "|" + normalized_name + "|" + net_pay_exact + "|" + main_company + "|" + b_or_reimburse_normalized
```

### 6.2 确定性逐字段比较

重复与映射使用来源标识、来源 Sheet、来源行号及关键业务字段的逐字段比较。关键字段包括：姓名、主体公司、社保公司、B公司/报销、精确金额、备注、源 Sheet、源行号。

### 6.3 行级验收

| 检查 | 通过标准 |
|---|---|
| 源到最终 | 每条原始有效行都能在最终文件找到 |
| 最终到源 | 最终文件没有对应原始文件不存在的有效行 |
| 重复检查 | 重复行、重复 row_uid、重复关键业务字段组合必须进入差异清单 |
| 行数检查 | `source_only_rows = 0`，`final_only_rows = 0`，`duplicate_rows = 0` |

### 6.4 多原始数据一致性

跨原始数据比较先建立业务键匹配层级：优先使用稳定员工标识；缺少稳定标识时使用标准化姓名、主体公司、岗位或用户确认字段，并把重复或歧义匹配列为差异。报告同时给出共同业务键的精确金额一致性和双方全体覆盖一致性；两项均为独立结论。

---

## 7. B公司/报销与金额列路由规则（最高优先级）

本节是最高优先级业务规则。Agent 必须先看金额列是否有数字，再用“B公司/报销”字段决定归属。不得只看字段文本，也不得用社保公司覆盖“B公司/报销”的含义。

### 7.1 基础定义

| 名称 | 定义 |
|---|---|
| 主体公司 | 该行用于主体公司支付归属的公司。若原始文件字段名为“社保公司”，也按该行主体支付公司处理。 |
| 公司名称 | 能从当月原始文件公司字段、模板分区名、或用户明确给出的公司清单中识别出的公司。 |
| 有数字 | 金额列非空，且可解析为数字。金额为 0 时保留原值参与校验，但不得凭空新增其他金额。 |
| 公司名称/报销 | 公司名称与“报销”之间有 `/` 或 `／`，例如 `曦悦/报销`。 |
| 公司名称报销 | 公司名称与“报销”直接相连，中间没有 `/` 或 `／`，例如 `曦悦报销`。 |

必须区分 `公司名称/报销` 和 `公司名称报销`。两者不能互相改写，不能按同一种规则处理。

### 7.2 字段解析顺序

解析“B公司/报销”字段时，按以下顺序执行：

1. 清理前后空格、全角空格、不可见字符。
2. 保留 `/`、`／`，不得删除斜杠。
3. 优先匹配 `公司名称/报销`。
4. 再匹配 `公司名称报销`。
5. 再匹配纯 `报销`。
6. 再匹配纯 `公司名称`。
7. 无法识别时，标记为差异 / 无法确认项，不允许猜测。
8. 不允许内置人员姓名、历史月份人员、历史金额作为路由依据。

### 7.3 主体公司支付列

如果 `主体公司支付列` 有数字，则该金额归该行 `主体公司`。

如果 `主体公司支付列` 无数字，则不生成主体公司支付金额。

`B公司/报销` 字段不改变主体公司支付列的归属。

### 7.4 B公司支付列

如果 `B公司支付列` 有数字，且 `B公司/报销 = 公司名称`，则 B公司支付金额归该 `公司名称`。

如果 `B公司支付列` 有数字，且 `B公司/报销 = 公司名称/报销`，则 B公司支付金额归该 `公司名称`。

如果 `B公司支付列` 有数字，且 `B公司/报销 = 公司名称报销`，则 B公司支付金额归该 `公司名称`。

如果 `B公司支付列` 有数字，但 `B公司/报销` 为空、纯 `报销`、或无法识别公司名称，则标记为差异 / 无法确认项。

如果 `B公司支付列` 无数字，则不生成 B公司支付金额。

### 7.5 发票金额列

如果 `发票金额列` 有数字，按下表决定发票金额归属：

| B公司/报销字段 | B公司支付列 | 发票金额列 | 发票金额归属 |
|---|---|---|---|
| 空白 | 任意 | 有数字 | 差异 / 无法确认 |
| 公司名称 | 任意 | 有数字 | 差异 / 无法确认 |
| 报销 | 任意 | 有数字 | 主体公司 |
| 公司名称/报销 | 有数字 | 有数字 | 主体公司 |
| 公司名称/报销 | 无数字 | 有数字 | 该公司名称 |
| 公司名称报销 | 任意 | 有数字 | 该公司名称 |
| 无法识别文本 | 任意 | 有数字 | 差异 / 无法确认 |

如果 `发票金额列` 无数字，则不生成发票金额。

### 7.6 组合场景

如果 `主体公司 = 武汉`，且 `B公司/报销 = 曦悦`，且 `主体公司支付列` 有数字，且 `B公司支付列` 有数字，则：

- 主体公司支付金额归 `武汉`；
- B公司支付金额归 `曦悦`；
- 若发票金额列无数字，则不生成发票金额。

如果 `主体公司 = 武汉`，且 `B公司/报销 = 曦悦/报销`，且 `主体公司支付列` 有数字，且 `B公司支付列` 有数字，且 `发票金额列` 有数字，则：

- 主体公司支付金额归 `武汉`；
- B公司支付金额归 `曦悦`；
- 发票金额归 `武汉`。

如果 `主体公司 = 武汉`，且 `B公司/报销 = 曦悦/报销`，且 `主体公司支付列` 有数字，且 `B公司支付列` 无数字，且 `发票金额列` 有数字，则：

- 主体公司支付金额归 `武汉`；
- 发票金额归 `曦悦`。

如果 `主体公司 = 武汉`，且 `B公司/报销 = 曦悦报销`，且 `主体公司支付列` 有数字，且 `发票金额列` 有数字，则：

- 主体公司支付金额归 `武汉`；
- 发票金额归 `曦悦`；
- 如果 B公司支付列也有数字，则 B公司支付金额同时归 `曦悦`。

### 7.7 Agent 执行伪代码

```text
main_target = none
b_pay_target = none
invoice_target = none

if main_company_pay_has_number:
    main_target = main_company

if b_company_pay_has_number:
    if field_form in [公司名称, 公司名称/报销, 公司名称报销]:
        b_pay_target = parsed_company
    else:
        error = 差异/无法确认

if invoice_amount_has_number:
    if field_form == 报销:
        invoice_target = main_company
    elif field_form == 公司名称/报销:
        if b_company_pay_has_number:
            invoice_target = main_company
        else:
            invoice_target = parsed_company
    elif field_form == 公司名称报销:
        invoice_target = parsed_company
    else:
        error = 差异/无法确认
```

### 7.8 路由验收标准

| 检查项 | 通过标准 |
|---|---|
| 主体公司支付 | 有数字即归主体公司，差异 = 0 |
| B公司支付 | 有数字且可识别公司时归对应 B 公司，差异 = 0 |
| 发票金额 | 严格按 7.5 表格归属，差异 = 0 |
| 未定义组合 | 必须进入差异报告，不允许猜测 |
| 分表归属 | 总额一致但分表归属不同，仍判定不通过 |

---

## 8. 分区落表规则

同一原始行可以同时产生多类金额归属。每一列独立落表，不能互相覆盖。

| 金额列 | 落表目标 | 规则 |
|---|---|---|
| 主体公司支付列 | 主体公司支付分区 | 归该行主体公司 |
| B公司支付列 | B公司支付分区 | 归 7.4 识别出的公司 |
| 发票金额列 | 发票/报销分区 | 归 7.5 计算出的主体公司或 B 公司 |

禁止为了总额一致而移动分区。分表小计、汇总页金额、Oracle 账本必须逐行、逐金额分、逐归属回勾一致。

---

## 9. 样式和格式规则

最终文件必须继承模板的：

1. Sheet 名称。
2. Sheet 顺序。
3. 表头结构。
4. 分区标题。
5. 合并单元格。
6. 行高。
7. 列宽。
8. 边框。
9. 填充色。
10. 字体名称。
11. 字号。
12. 加粗。
13. 字体颜色。
14. 数字格式。
15. 打印设置。
16. 页面方向。
17. 页边距。
18. 打印区域。
19. 冻结窗格，如模板存在。

强制要求：

| 项目 | 要求 |
|---|---|
| 字体 | 使用模板主字体或模板对应位置字体，不能混入默认字体 |
| 水平对齐 | 全部有效单元格必须水平居中 |
| 垂直对齐 | 全部有效单元格必须垂直居中 |
| 新增行 | 继承所在分区模板明细行样式 |
| 金额列 | 保持数字金额格式，不得转文本 |
| 空白样式区 | 保留边框、字体、居中，不出现裸露默认样式 |

---

## 10. 公式规则

1. 模板中原本是公式的位置，最终必须仍是公式。
2. 分表小计、分表合计、汇总页合计必须使用公式。
3. 行数变化后，公式范围必须同步调整到实际明细范围。
4. 不允许把公式写死成数值。
5. 不允许出现以下错误：

```text
#REF!
#VALUE!
#DIV/0!
#NAME?
#N/A
#NULL!
#NUM!
```

6. 如果工具无法可靠计算 Excel 公式缓存，必须用独立 Python/脚本账本重算金额，并在 ABExcel 中保留公式差异和公式错误检查。

---

## 11. 生成最终发放版

1. 以模板文件为基底复制。
2. 清除模板旧月份明细，不清除公式框架、样式框架、分区结构。
3. 写入当月标准化账本数据。
4. 按业务归属写入对应分表/分区。
5. 调整公式范围。
6. 复制新增行样式。
7. 设置有效区域水平居中、垂直居中。
8. 单源保存为：

```text
{目标月份}-{YYYYMMDD}_最终发放.xlsx
```

9. 多源同目录时，在文件名中加入来源标识：

```text
{目标月份}-{YYYYMMDD}-{来源标识}_最终发放.xlsx
```

10. 文件必须按当前运行的输出保护策略打开。若当前工具只能设置工作簿保护而不能生成“打开密码”，必须在结果中明确标记失败或无法满足，不得伪装通过。

---

## 12. ABExcel 校验检查文件规范

每份原始文件都必须生成：

```text
{目标月份}-{YYYYMMDD}_校验ABExcel检查.xlsx
```

### 12.1 A/B 定义

| 名称 | 含义 |
|---|---|
| A 文件 | 待检查最终发放版 |
| B 文件 | 由原始数据 + 模板规则独立重建出来的校验版，或用户指定的参考正确版 |
| ABExcel 检查 | 把 A/B 同名 Sheet 复制到校验工作簿中，检查值、公式、错误、核心金额和格式指纹 |

如果任务是比较两个用户提供的文件：

| 名称 | 含义 |
|---|---|
| A 文件 | 版本 1 |
| B 文件 | 版本 2 |
| 正确性判断 | 必须回到当月原始文件，按业务规则判断谁正确 |

### 12.2 ABExcel 必须包含的 Sheet

| Sheet 名称 | 内容 |
|---|---|
| AB_控制台 | 每个 Sheet 的差异数量和总通过状态 |
| AB_核心金额回勾 | 实发、主体、B公司、报销/发票、扣借支、提前发的精确金额与金额分回勾 |
| AB_精确金额差异 | 原始精度、A 精确金额、B 精确金额、精确差异和金额精度异常 |
| AB_差异清单 | 所有差异单元格、A值、B值、差异类型、是否修复 |
| AB_格式指纹 | 字体、字号、水平居中、垂直居中、数字格式、边框、填充、合并信息 |
| A_汇总、A_各分表 | A 文件各 Sheet 副本 |
| B_汇总、B_各分表 | B 文件各 Sheet 副本 |

### 12.3 AB_控制台列结构

| 列 | 字段 |
|---|---|
| A | Sheet 名 |
| B | 比对范围 |
| C | 值差异数 |
| D | 公式差异数 |
| E | 公式错误数 |
| F | 核心金额差异数 |
| G | 格式指纹差异数 |
| H | 行数差异数 |
| I | 结论 |

### 12.4 值差异数量公式

在 `AB_控制台!C2` 放入并向下填充：

```excel
=LET(s,$A2,r,$B2,a,IFERROR(INDIRECT("'A_"&s&"'!"&r),""),b,IFERROR(INDIRECT("'B_"&s&"'!"&r),""),d,IF(ISNUMBER(a)*ISNUMBER(b),a<>b,NOT(EXACT(a&"",b&""))),SUM(--d))
```

说明：金额使用单元格原始数值直接比较；外部精确十进制账本是金额精度的最终依据。非金额文本按完全一致比较。

### 12.5 公式差异数量公式

在 `AB_控制台!D2` 放入并向下填充：

```excel
=LET(s,$A2,r,$B2,ra,INDIRECT("'A_"&s&"'!"&r),sr,ROW(INDEX(ra,1,1)),sc,COLUMN(INDEX(ra,1,1)),rows,ROWS(ra),cols,COLUMNS(ra),SUM(MAKEARRAY(rows,cols,LAMBDA(i,j,--NOT(EXACT(IFERROR(FORMULATEXT(INDIRECT("'A_"&s&"'!"&ADDRESS(sr+i-1,sc+j-1,4))),""),IFERROR(FORMULATEXT(INDIRECT("'B_"&s&"'!"&ADDRESS(sr+i-1,sc+j-1,4))),"")))))))
```

### 12.6 公式错误数量公式

在 `AB_控制台!E2` 放入并向下填充：

```excel
=LET(s,$A2,r,$B2,x,INDIRECT("'A_"&s&"'!"&r),y,INDIRECT("'B_"&s&"'!"&r),SUM(--ISERROR(x))+SUM(--ISERROR(y)))
```

### 12.7 总结论公式

在 `AB_控制台!I2` 放入并向下填充：

```excel
=IF(AND(C2=0,D2=0,E2=0,F2=0,G2=0,H2=0),"通过","不通过")
```

总结果单元格：

```excel
=IF(COUNTIF(I:I,"不通过")=0,"ABExcel检查通过","ABExcel检查不通过")
```

### 12.8 格式指纹

普通 Excel 函数不能完整读取字体、字号、边框、填充色、垂直居中和合并单元格，因此 Agent 必须用脚本生成格式指纹。

每个有效单元格格式指纹至少包含：

```text
font_name | font_size | bold | italic | font_color | fill_color | border_left | border_right | border_top | border_bottom | num_format | horizontal | vertical | wrap_text | merged_range
```

格式指纹差异数必须为 0。

---

## 13. 2 轮 × 6 类检查

### 第一轮：生成后检查

| Agent | 检查内容 | 通过标准 |
|---|---|---|
| A1 数据源检查 | 原始文件行数、姓名、金额、备注完整读取 | 无漏行、无多行、无旧数据 |
| A2 归属检查 | 主体公司、B公司、报销/发票归属 | 严格符合 B公司/报销字段规则 |
| A3 金额检查 | 实发、主体、B公司、报销/发票、扣借支、提前发 | 精确金额、金额分、明细、分表、汇总、Oracle 全部回勾 |
| A4 公式检查 | 小计、合计、汇总公式 | 公式存在、范围正确、未写死 |
| A5 样式检查 | 字体、居中、行高列宽、合并单元格 | 与模板一致，且水平/垂直居中 |
| A6 文件检查 | 密码、可打开、Sheet 顺序、无公式错误 | 全部通过 |

### 第二轮：独立复核

| Agent | 检查内容 | 通过标准 |
|---|---|---|
| B1 源到最终 | 原始文件逐行查最终文件 | 每行都能找到 |
| B2 最终到源 | 最终文件反查原始文件 | 没有额外伪造行 |
| B3 分表到汇总 | 各分表小计合计到汇总页 | 精确差异 = 0，金额分差异 = 0 |
| B4 汇总到原始 | 汇总页金额回勾原始文件 | 精确差异 = 0，金额分差异 = 0 |
| B5 公式到显示值 | 公式计算结果与显示值 | 一致，无错误值 |
| B6 版式到模板 | 最终文件与模板外观 | 字体一致、双向居中、结构一致 |

---

## 14. 20 项测试要求

| 序号 | 测试项 | 通过标准 |
|---:|---|---|
| 1 | 输出文件能否按输出保护策略打开 | 可以打开 |
| 2 | Sheet 名称和顺序 | 与模板一致 |
| 3 | 原始明细行是否全部进入最终文件 | 无漏行 |
| 4 | 最终明细行是否都能反查原始文件 | 无多余行 |
| 5 | 主体公司支付合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 6 | B公司支付合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 7 | 报销/发票金额合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 8 | 实发金额合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 9 | 扣借支合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 10 | 提前发合计 | 原始 = 最终 = 汇总 = Oracle，精确差异 = 0 |
| 11 | 分表小计 | 等于分表明细 |
| 12 | 汇总页金额 | 等于各分表合计 |
| 13 | 公式错误扫描 | 无错误值 |
| 14 | 模板公式是否被写死 | 未写死 |
| 15 | 字体一致性 | 有效区域字体一致 |
| 16 | 水平居中 | 有效区域全部水平居中 |
| 17 | 垂直居中 | 有效区域全部垂直居中 |
| 18 | 合并单元格 | 与模板一致 |
| 19 | 行高列宽 | 与模板一致 |
| 20 | 保存并重新打开 | 文件不损坏，Excel 不提示修复 |

---

## 15. 负向测试 Mutation Test

必须验证校验器真的能抓错。做法：复制最终发放文件，制造一个小错误，重新跑校验，校验器必须失败。

最低负向测试：

| 测试 | 故意改动 | 期望 |
|---|---|---|
| MT-001 | 任意金额 +0.00001 | 精确金额差异失败 |
| MT-002 | 把一个可识别路由文本改成无法识别文本 | 路由差异失败 |
| MT-003 | 删除最终文件一条有效明细 | source_only_rows 失败 |
| MT-004 | 复制最终文件一条有效明细 | duplicate 或 final_only 失败 |
| MT-005 | 把小计公式改成数值 | formula_missing 失败 |
| MT-006 | 把金额改成文本 | type_check 失败 |
| MT-007 | 修改有效区域字体或对齐 | style_fingerprint 失败 |

如果故意改错后校验器没有失败，则校验体系无效，禁止声称通过。

---

## 16. 内部审计胶囊 Audit Capsule（可选内部产物）

无论最终是否交付，Agent 内部建议形成审计胶囊，便于复核和追责。用户没有要求时，无差异情况下不作为最终交付物输出。

```text
audit_capsule/
  00_inputs/
    source.xlsx
    template.xlsx
  01_extracted/
    raw_ledger.csv
    final_extracted_ledger.csv
    template_structure.json
  02_oracle/
    oracle_ledger.csv
    oracle_summary.csv
  03_compare/
    precise_amount_reconciliation.csv
    row_bijection_report.csv
    cross_source_consistency.csv
    ab_cell_diff.csv
    ab_formula_diff.csv
    ab_style_fingerprint_diff.csv
  04_tests/
    test.log
    mutation_test.log
    save_reopen_check.log
  05_reports/
    validation_summary.md
```

---

## 17. 条件交付规则（最高优先级）

### 17.1 单源无差异通过时，只输出 2 个文件

当且仅当以下全部成立：

1. 2 轮 × 6 类检查全部通过。
2. ABExcel 检查全部通过。
3. 20 项测试全部通过。
4. Mutation Test 证明校验器能抓错。
5. 差异清单为空。
6. 无无法确认项。
7. 最终文件可按输出保护策略打开。
8. 精确金额差异数与金额精度异常数均为 0。

只交付：

```text
{目标月份}-{YYYYMMDD}_最终发放.xlsx
{目标月份}-{YYYYMMDD}_校验ABExcel检查.xlsx
```

不要额外交付差异报告 MD、差异明细 Excel、审计胶囊、JSON，除非用户另行要求。

### 17.2 单源有差异 / 未通过 / 无法确认时，输出 4 个文件

只要出现任一情况：

1. A/B 文件存在值、金额、公式、格式、行数差异。
2. 原始数据与最终文件存在任意非零精确金额差异。
3. 公式、格式、金额检查存在差异。
4. 路由归属存在冲突或无法确认。
5. 任何 2 轮 × 6 类检查失败。
6. 任何 20 项测试失败。
7. ABExcel 控制台出现“不通过”。
8. 无法确认密码打开、公式重算、文件完整性或数据来源。
9. 存在金额精度异常或历史人工认可的技术差异。

必须交付：

```text
{目标月份}-{YYYYMMDD}_最终发放.xlsx
{目标月份}-{YYYYMMDD}_校验ABExcel检查.xlsx
{目标月份}-{YYYYMMDD}_差异报告.md
{目标月份}-{YYYYMMDD}_差异明细.xlsx
```

差异报告必须逐项说明：

| 字段 | 要求 |
|---|---|
| 差异位置 | Sheet、行、列、单元格 |
| 差异类型 | 金额 / 归属 / 公式 / 样式 / 行缺失 / 多余行 / 备注 / 密码 / 文件损坏 |
| 原始值 | 来自原始文件 |
| A值 | 待检查最终发放版值 |
| B值 | Oracle / 独立校验版值 |
| 正确值 | 按原始数据和规则确定 |
| 精确差异 | 必须写明 `exact_delta`，任意非零值都是差异 |
| 金额分差异 | 必须写明 cents_delta |
| 处理状态 | 已修复 / 未修复 / 无法判断 / 已获人工认可 |
| 判断依据 | 引用本模板具体规则编号 |
| 剩余风险 | 如有必须写明 |

### 17.3 多原始数据任务

多原始数据任务按每份原始文件分别适用 17.1 或 17.2，并固定交付：

```text
{目标月份}-{YYYYMMDD}_跨原始数据一致性报告.md
{目标月份}-{YYYYMMDD}_跨原始数据一致性明细.xlsx
```

跨源报告必须分别列示：共同业务键精确金额差异、共同业务键路由差异、仅源 A、仅源 B、覆盖范围差异、歧义匹配和历史复演差异。任一跨源差异保持总体“存在差异”，不改变各独立原始文件的来源事实。

---

## 18. Codex 可选机器可读结果

如使用 Codex 或工程脚本，允许在内部生成 `verification_result.json` 作为机器可读审计结果；但无差异通过时，除非用户要求，不作为最终交付物输出。

```json
{
  "source_id": "",
  "source_file": "",
  "template_file": "",
  "output_file": "",
  "agent_date": "YYYYMMDD",
  "credential_verification": "passed/failed",
  "output_protection_verification": "passed/failed",
  "final_output_name": "",
  "ab_excel_check_file": "",
  "differences_found": false,
  "unknown_items": [],
  "checks_2_round_6_agents": {
    "A1_source_data": false,
    "A2_routing": false,
    "A3_amounts": false,
    "A4_formulas": false,
    "A5_styles": false,
    "A6_file_integrity": false,
    "B1_source_to_output": false,
    "B2_output_to_source": false,
    "B3_sheets_to_summary": false,
    "B4_summary_to_source": false,
    "B5_formula_to_value": false,
    "B6_layout_to_template": false
  },
  "ab_excel_check": {
    "value_diff_count": 0,
    "formula_diff_count": 0,
    "formula_error_count": 0,
    "exact_amount_diff_count": 0,
    "amount_precision_anomaly_count": 0,
    "amount_diff_count": 0,
    "style_fingerprint_diff_count": 0,
    "row_count_diff_count": 0,
    "passed": false
  },
  "twenty_tests": {
    "01_password_open": false,
    "02_sheet_order": false,
    "03_source_rows_covered": false,
    "04_output_rows_traceable": false,
    "05_main_company_pay": false,
    "06_b_company_pay": false,
    "07_reimburse_invoice": false,
    "08_net_pay": false,
    "09_advance_deduction": false,
    "10_next_month_advance": false,
    "11_sheet_subtotals": false,
    "12_summary_to_sheets": false,
    "13_formula_error_free": false,
    "14_formula_preserved": false,
    "15_font_consistent": false,
    "16_horizontal_center": false,
    "17_vertical_center": false,
    "18_merged_cells": false,
    "19_row_height_column_width": false,
    "20_save_reopen": false
  },
  "amounts_exact": {
    "net_pay": "0",
    "main_company_pay": "0",
    "b_company_pay": "0",
    "reimburse_or_invoice_amount": "0",
    "advance_deduction": "0",
    "next_month_advance": "0"
  },
  "amounts_cents": {
    "net_pay": 0,
    "main_company_pay": 0,
    "b_company_pay": 0,
    "reimburse_or_invoice_amount": 0,
    "advance_deduction": 0,
    "next_month_advance": 0
  },
  "mutation_test": {
    "validator_failed_as_expected": false
  },
  "differences": [
    {
      "type": "amount/routing/formula/style/row/remark/password/file",
      "sheet": "",
      "cell_or_row": "",
      "source_value": "",
      "a_value": "",
      "b_value": "",
      "correct_value": "",
      "exact_delta": "0",
      "cents_delta": 0,
      "rule_reference": "",
      "status": "fixed/unfixed/unknown"
    }
  ]
}
```

---

## 19. 最终回复格式

### 19.1 无差异通过时

```markdown
结论：已通过，未发现差异。

下载：
- 最终发放版：{链接}
- ABExcel 校验检查：{链接}
- 跨原始数据一致性报告：{多源任务提供}
- 跨原始数据一致性明细：{多源任务提供}

核心金额：
| 项目 | 金额 |
|---|---:|
| 实发金额 | |
| 主体公司支付 | |
| B公司支付 | |
| 报销/发票金额 | |
| 扣借支 | |
| 提前发 | |

检查结果：
- 2 轮 × 6 类检查：通过
- ABExcel 检查：通过
- 20 项测试：通过
- Mutation Test：通过，校验器可抓出故意错误
- 精确金额差异：0
```

### 19.2 有差异 / 未通过 / 无法确认时

```markdown
结论：未完全通过，存在差异 / 未通过项 / 无法确认项。

下载：
- 最终发放版：{链接}
- ABExcel 校验检查：{链接}
- 差异报告 MD：{链接}
- 差异明细 Excel：{链接}
- 跨原始数据一致性报告：{多源任务提供}
- 跨原始数据一致性明细：{多源任务提供}

核心差异摘要：
| 差异类型 | Sheet | 行/列/单元格 | 原始值 | A值 | B值 | 正确值 | 精确差异 | 状态 |
|---|---|---|---|---|---|---|---|---|

检查结果：
- 2 轮 × 6 类检查：{通过/不通过}
- ABExcel 检查：{通过/不通过}
- 20 项测试：{通过/不通过}
- Mutation Test：{通过/不通过}
```

---

## 20. 禁止事项

1. 不允许跳过原始数据逐行检查。
2. 不允许只做版式，不做金额校验。
3. 不允许只看汇总，不查明细。
4. 不允许把公式写死成数值。
5. 不允许沿用模板旧人员、旧金额、旧备注。
6. 不允许把 `公司名称/报销` 和 `公司名称报销` 混为同一种规则。
7. 不允许忽略金额列是否有数字，单靠“B公司/报销”字段文本入账。
8. 不允许把可识别公司的 B公司支付金额排除在 B公司支付之外。
9. 不允许把 `公司名称/报销` 且 B公司支付列有数字时的发票金额归 B 公司；此时发票金额应归主体公司。
10. 不允许把 `公司名称/报销` 且 B公司支付列无数字时的发票金额归主体公司；此时发票金额应归该 B 公司。
11. 不允许按社保公司覆盖“B公司/报销”字段。
12. 不允许不生成 ABExcel 检查文件。
13. 不允许未通过 20 项测试仍声称最终正确。
14. 不允许发现差异却不输出 MD 和 Excel 差异报告。
15. 不允许金额使用 float 作为最终判断依据。
16. 不允许原始金额超过两位小数时静默四舍五入。
17. 不允许把文本金额当作正常金额通过。
18. 不允许在无真实测试日志或真实校验文件时声称“通过”。
19. 不允许内置历史人员姓名、历史月份个案、历史金额作为规则。
20. 不允许输出文件名缺少发给 Agent 的当天日期。
21. 不允许无法生成密码打开文件时伪装成已加密。
22. 不允许因为总额一致就忽略分表、分区、行级或路由差异。
23. 不允许把任何非零精确金额差异、金额精度异常或人工认可差异归类为“无差异通过”。
24. 不允许在技能、Git 仓库、报告、日志、交接或机器可读结果中保存口令明文。
25. 不允许让多原始文件的专属交付物互相覆盖，或把跨源一致性替代独立原始事实。

---

## 21. 最终验收标准

只有同时满足以下条件，才允许输出“已通过，未发现差异”：

```text
source_only_rows = 0
final_only_rows = 0
duplicate_rows = 0
unknown_routes = 0
amount_diffs = 0
exact_amount_diffs = 0
amount_precision_anomalies = 0
route_diffs = 0
formula_diffs = 0
formula_errors = 0
style_fingerprint_diffs = 0
sheet_order_diffs = 0
password_open = true
save_reopen_ok = true
ab_excel_passed = true
twenty_tests_all_passed = true
mutation_test.validator_failed_as_expected = true
cross_source_exact_amount_diffs = 0（多源任务）
cross_source_route_diffs = 0（多源任务）
cross_source_coverage_diffs = 0（多源任务）
```
原始数据集、模板、精确金额、路由、公式、样式、文件完整性、ABExcel、二十项测试和跨源一致性全部闭环后，才算准确无误能交付验收。

任一条件不满足，单源按“有差异 / 未通过 / 无法确认”交付 4 个文件；多源任务同时交付跨原始数据一致性报告和明细。
