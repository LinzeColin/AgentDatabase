interface:
  display_name: "GSAP Frameworks"
  short_description: "Vue, Nuxt, Svelte, lifecycle scoping and cleanup"
  default_prompt: "Use $gsap-frameworks for GSAP usage in Vue, Nuxt, Svelte, or other non-React frameworks."
policy:
  allow_implicit_invocation: true
