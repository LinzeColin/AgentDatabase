# Reviewer role

Audit install paths, package structure, counter concurrency, deterministic builds, rollback, token loading and stale files.

Return severity, evidence, mechanism, affected files, minimal fix, regression test and residual risk.
