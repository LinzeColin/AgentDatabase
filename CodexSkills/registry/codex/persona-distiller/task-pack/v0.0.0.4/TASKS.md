# Codex implementation tasks v0.0.0.4

1. Remove runtime identity prompts and user-provided identity arguments.
2. Route distilled identity facets and scenarios automatically from each task.
3. Replace invocation counters with optional unnumbered audit records.
4. Allocate `product_version` per canonical person, continuously from 1 to 999.
5. Lock registration, reject gaps/reuse/exhaustion, and preserve seven-folder uniqueness.
6. Update templates, schemas, quality gates, reviewer checks, docs, and all three repository README surfaces.
7. Rebuild manifests/checksums and run unit, integration, concurrency, packaging, installation, and registry gates.
8. Replace the local v0.0.0.3 installation with v0.0.0.4 and publish the verified commit to GitHub `main`.
