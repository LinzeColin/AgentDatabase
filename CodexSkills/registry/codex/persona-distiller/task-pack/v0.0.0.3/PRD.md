# PRD v0.0.0.3

## Goal

Given only target name and identity, orchestrate exhaustive-as-practical evidence research and produce one installable target-person Skill that can perform real tasks with identity gating, safe memory, evaluation, correction and immutable invocation versions.

## Non-goals

Model-weight training; deceptive impersonation; unauthorized private connectors; absolute proof of web exhaustiveness; automatic promotion of runtime experience; replacement of accountable experts.

## Acceptance

All contracts in root `SKILL.md`, tests and `audit/` pass; final ZIP has one top-level `persona-distiller/`; fresh install works at `.agents/skills`; target scaffold and generated target package smoke tests pass.
