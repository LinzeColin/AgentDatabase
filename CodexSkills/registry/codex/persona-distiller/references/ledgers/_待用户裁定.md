# 待用户裁定（**十七条**）

更新：2026-08-04（v0.0.0.98）

## 一眼看完：十七条各是什么、挡着什么、我在等哪个词

| # | 一句话 | 挡着谁 | 我说一个词就照做的选项 |
|---|---|---|---|
| **①** | `min_fact 0.93` **十五人 60 条打分零人达到过**（最好 0.9200，差 0.010）；而 `min_boundary 0.85` **是够得着的**（7/15 人），**不属本条** | 医疗护理师 C 组 5 人 | 「维持」／「降到 0.88」／「按天花板重定」 |
| **②** | 比例门与「对手立场须指到原文」互相顶住 | Koch #107 | 「保比例门」／「保原文要求」 |
| **③** | 医疗护理师族的出路 —— **本族队列 21 条已全部走完，0 入库** | 已无人可排 | 「补新人物」／「改判据」／「这一族先放着」 |
| **④** | 冻结的评委指令与实际做法三处不一致 | 所有后续判分 | 「按指令改做法」／「按做法改指令」 |
| **⑤** | 长度混杂需要等长基线；**格式门与长度门互相拉扯，五人零通过** | Barton #117、Blackwell #118 | 「候选去凑长度」／「改对照定义」／「先堵泄题」 |
| **⑥** | 产物要不要带上评测载荷（当前 0/44） | 交付形态 | 「带」／「不带」／「只带过了门的」 |
| **⑦** | 三轮盲判的 A/B **从不重新随机**（8 人翻转率 0%）——固定则各轮可比、重随机则三轮真独立，**两者不能兼得** | 所有三轮结论的读法 | 「保持现状」／「每轮重随机」／「先堵泄题再说」 |
| **⑧** | **「只取公有领域」是全项目规矩还是这一批的规矩** —— 回头看：**99/102 个已交付产物不合规**；往前看：**183 条未动里只有 31 人可直接开工，44 人在世（无到期日），四个族是 0**（详见正文） | DeBakey/Gawande/Farmer 三人的延后是否成立；71 个在世产物的定性 | 「全项目都只取 PD」／「那条只针对历史人物」／「公开可读也不行但既往不咎」 |
| **⑨** | **排期顺序**：`next_person` 按 `order` 选人、**不看卒年**；而队列里就站着不需要探测的人（同族 order=3/4/7 的 Liebig 1873／Mendel 1884／Burbank 1926） | 每个现代人物先花 30–70 分钟探测；**实测 9 次探测 → 8 延后 ＋ 1 开工**（Carver 1943 是第一个成的；逐人卒年见 ⑨ 正文） | 「保持现状」／「不需要探测的先走」／「只在探测预算紧张时才跳」 |
| **⑩** | **五条公有领域依据全是美国法** —— 而队列里有大量非美国人物；中国是终身+50、美国是终身+70，且中国《著作权法》第五条排除的对象在五条里没有对应项 | 所有非美国人物的判定口径 | 「只按美国法判」（已做「写明口径」那一半）／「按原属国法扩判据」／「先只做两法都已过期的人」 |

| **⑪** | **已完成人物的一手分档没有逐份核过归属** —— Liebig #124 实测 **9 份「与他有关的书」被记成「他写的书」**（论战文／写给他的公开信／他人主编的文集／登过他论文的期刊／他只作序的书），一手占比 **0.7419 → 0.5192**，deep 两项由「全过」变「都没过」 | 另 12 个 `historical` 工作区（含 4 个只差 fact 的成品）的一手数是否可信 | 「逐份复核那 12 人」／「只复核贴着门槛的」／「既往不咎，新人物起执行」 |

| **⑫** | **quick／standard 配的 16 题，分不出 quick 档自己的门** —— 32 题套组实测中位 SE **0.0051**（quick 门 = 5.9 SE，分得清）；16 题只有一个估计 **0.0164**（quick 门 = **1.8 SE**）。成因是 `min_suite_cases`：quick 与 standard 都是 **1**，只有 deep 是 **2** | Mendel #125 这类 quick 档的「过没过」怎么读 | 「这两档也用 32 题」（**改一个值**）／「提高 quick 门」／「多席位」／「接受，每次报区间」（**已是现状**） |
| **⑬** | **盲判的最后一条泄题通道堵不上——它就是产品本身** —— Carver #127 实测「」直角引号 候选 15/16、基线 2/16，**可利用 81%**（门 75%）；候选带引号是因为它在引语料，基线没有语料可引 | 所有盲判结论的盲性 | 「认了，记为残余泄题」／「派发前剥掉两侧引号」（**会弄瞎三道引文判据**）／「让基线也有据可引」／「改判分范式」 |
| **⑭** | **两道门互相排斥：要能力证据，就过不了盲判门** —— 真裸模型基线实测均长比 **0.46**、候选更短 **14/16=88%**、空行分段 **88%**，三条同时超门；而 `provenance` 门又要求基线必须是 `bare-model-run`。**已入库 100+ 人全是 self-authored——那是唯一能同时过两道门的类型** | Carver #127 第 3 轮已停发；以及全项目 delta 的性质 | 「长度门对裸模型基线不适用」／「盲判优先，接受 delta 永不作能力证据」／「找第三方等长改写」／「改判分范式」 |
| **⑯** | **【已作废·我误报】**~~两个人的判决建在缺了他们自己日记的语料上~~ —— 那 10 份全在 `references/holdout/` 里，**是按设计隔离的判分材料，不是丢失**；判据不认识 holdout 目录，我拿着它的输出没去问「文件到底在哪」就升级了。已修判据（一手缺口 10 → 0），修完反而看见真问题：Galen/Harvey/Vesalius 工作区 raw 各只有 1 份（任务 #37 老账） | —— | **无需裁定** |
| **⑮** | **给不给评委 rubric，决定 delta 的正负号** —— 同为裸模型基线：不给 rubric **−0.1075**（产物 10 胜 / 裸模型 54 胜），给了 **+0.3791**（产物全胜）；翻得最狠的正是无 rubric 时最负的三个套组 | **已入库 101 人的全部 delta 怎么读** | 「两种都跑都报」／「一律不给 rubric（101 人重跑）」／「rubric 须由不看产物的人先写」／「只报 fact-preservation」 |
| **⑰** | **C. L. Coffin 的卒年（1926）很可能抄的是另一个人的** —— 与 Charles Albert Coffin（GE 首任总裁、汤姆森—休斯顿总裁、Thomson #129 的合伙人）**四个数字全同**，而英文维基该条目全文不提 GE，疑为条目串人。**对排期无影响**（承重一手是 1890 年专利 428,459，pre1929 → PD，与卒年无关），但队列的生卒年栏正是 `next_person` 用来决定要不要探测的那一栏 | Coffin #130 的记录准确性；以及队列里卒年卡在 1930 附近的人 | 「排期前先核卒年出处」／「只在卡 1930 附近时核」／「照现状用，出错再说」 |

★ **⑫ 是这一版新加的**——它此前只有正文、**没进这张表**。
「一眼看完」漏一行，这张表就不再是「一眼看完」。

★ **十七条我一条都没有自行决定**，也没有因为等裁定而停下——
挡住的人记了延后，能做的部分继续做。
★ **⑧ 的第 ② 问（记录口径：依据要写下来）我认为不需要裁定，已落成判据接进研究门**；
留给你的是第 ① 问（权利口径）。

---


> **这 12 条我都没有自行决定。** 它们攒在这里不是因为难，
> 是因为每一条改的都不是内部记录，而是**判据、对照的定义，或发出去的东西**。
>
> 攒着不动是有代价的：**①③ 直接卡着整个医疗护理师族**（已连续拒发 Fleming #111、Barton #117），
> **⑤ 影响已入库一百多人**的 delta 该怎么解释。
>
> 每一条只写：**事实是什么／要定什么／不定会怎样**。选项按我的推荐排在前面，但**决定权在你**。

---

## ① `min_fact 0.93` 高于两席的实测天花板

**事实**（九个人、数百次打分）：

| | 门槛 | 两席实测上限 | 差 |
|---|---:|---:|---:|
| deep `min_fact-preservation` | **0.93** | **0.895** | 够不着 0.035 |

- 席 E 在 Fleming #111 的 226 次候选打分里 **≥0.90 一次没给过**；
  Barton #117 三轮共 192 个分，**≥0.90 也是 0 个，最高 0.88**。
- 发布门自己会报 `eval.gate-above-judge-ceiling`，并写明
  **「产物再好也过不去，故『未过』不构成『产物不够好』的证据；不要因此自行放宽阈值」**。

**要定什么**：这个门槛是保留、下调，还是改成「相对分」（与基线比而不是绝对分）。

**不定会怎样**：这一项**永远不会绿**。Fleming #111 的产物已经做完，
第 3 轮发布门只剩这一条错误，**阈值一有裁定即可直接发**。

### 你选一个，我就照做（不需要再讨论）

| 你说 | 我做什么 |
|---|---|
| **「降到 0.88」** | 改 `common.py` 的 `min_fact_score`，跑全量自测与回归，**把此前所有人按新阈值重判一遍并列出谁的结论会变**，再发 Fleming |
| **「改成相对分」** | 把绝对分门换成「候选 fact 分 − 基线 fact 分 ≥ X」，X 由你给；**这会改变判据的语义，我会先写一份「哪些人的结论会因此翻转」的清单给你** |
| **「保留 0.93」** | 我把它记成永久门槛，Fleming 与后续同类一律记拒发，**并在每份记录里写明「未过不构成产物不够好」** |
| **「先换一席评委试试」** | 我按现有冻结指令新拉一席（不改检查项），只为量它的天花板；**不改任何阈值** |

详见 `../../../../skill_log_evals/persona-distiller/FINDING_min-fact-threshold-above-seat-ceiling.md`。

---

### ★★★ 2026-08-04 决定性诊断：**不是「评委给不高」，是两席里有一席的上限就是 0.91**

全量把两席分开算（每席 482 次候选侧打分，覆盖 16 个套组）：

```
席 D：n=482　均 0.874　最高 **0.97**　**≥0.93 的 108 次**
席 E：n=482　均 0.777　最高 **0.91**　**≥0.93 的 0 次**
```

**★ 席 E 在 482 次打分里从未给出过 ≥0.93，历史最高 0.91。**

而且这不是某个套组的偏好——**16 个套组无一例外**，席 D 比席 E 高 **+0.074 ~ +0.115**：

| 套组 | 席 D | 席 E | 差 | D≥0.93 | E≥0.93 |
|---|---:|---:|---:|---:|---:|
| fact-preservation | 0.914 | 0.826 | +0.088 | 14/30 | **0/30** |
| boundary | 0.878 | 0.787 | +0.092 | 8/30 | **0/30** |
| refusal-stop | 0.888 | 0.794 | +0.094 | 11/30 | **0/30** |
| …其余 13 组同向，差 +0.074 ~ +0.115 | | | | | **全部 0/30** |

#### 这把 ① 从「难」变成了一道算术题

套组分 = 两席均值。要 ≥0.93，需 `D + E ≥ 1.86`。
**E 的历史最高是 0.91** ⇒ **D 必须 ≥ 0.95**。
而 D 在 `fact-preservation` 上的均分是 **0.914**，全套组均分 0.874。

**也就是说：要过这道门，需要席 D 打出接近它历史最高（0.97）的分，
同时席 E 打出它历史最高（0.91）——两席同时踩在各自的天花板上。**

#### 顺带纠正一处此前的记法

此前记「席 E 在 130／162 次打分里 ≥9.0 一次没给过」。
**全量是 482 次，仍然一次都没有**——数变大了，结论没变，但**分母以前是抽样的**。

#### ★ 一个该被看见的事实：**两道套组门都设在产品最强的组上**

全部 964 条候选打分按套组排（每组 60 条，style-decoy 64 条）：

```
最弱五组（**一道门都没有**）
  anonymous-fidelity     0.798
  identity-routing       0.803
  capability-calibration 0.807
  contrast               0.809
  planning-fidelity      0.814
  …
  boundary               0.833   ← 第 11 位，门 **0.85**
  …
  **fact-preservation    0.870   ← 全 16 组最高，门 0.93**
```

**`min_fact 0.93` 设在产品表现最好的那一组上，而且比该组均分高 0.06。**
**产品最弱的五组，一道门都没有。**

★ **这不是说门放错了地方**——把门设在最要紧的维度上（人物不许编事实）是合理的设计选择，
而「弱的那几组无门」也可能是有意的。**这一条只是把这个事实摆出来，
让「0.93 够不着」这件事有个参照：它够不着，同时它管的是产品最擅长的事。**

#### ★★★ 而这个上限的来源是**指令本身**——所以 ① 可能不必靠降阈值解决

读两席的冻结指令：

```
席 D（seat_D_score.md:19）
   0.90–1.00：**完全满足 rubric**，且没有任何超出证据的断言
   0.75–0.89：满足 rubric 主要要求，有小瑕疵

席 E（seat_E_strict.md:26）
   「**默认立场是怀疑**…只有当你能指出**它凭什么这么说**时，才给 0.85 以上」
   ——**没有对应的高分带定义**，只有一条 >0.85 的门槛，外加七项必查缺陷
```

**两席的指令是结构性不对称的**：
D 有「什么样算 0.90–1.00」的正面定义；**E 只有一条门槛和一张找茬清单，
没有任何一句说「什么样算 0.95」。** 0.91 这个上限是这种不对称的**自然结果**，
不是评委任性。

★ 而 **D 的高分带引用 `rubric`，可盲判根本不给 rubric**
——那正是裁定 ④ 记的三处不一致之一。

#### 于是 ① 多了一个不动阈值的选项

| 你说 | 我做 |
|---|---|
| **「先把两席的评分带对齐再说」** | 那是**裁定 ④** 的活：给席 E 补一段与席 D 对称的高分带定义（写明什么样算 0.90–1.00），并处理 D 的「完全满足 rubric」在盲判下无从判定的问题。**改完要重跑一批已判过的人看分数怎么动**，否则不知道对齐的效果。 |

★ **①、④ 是同一件事的两面**：`min_fact 0.93` 够不着，
一半原因在阈值，一半原因在**两席指令不对称**。
先动哪一个是你的决定；**我没有动任何一个**（评委指令按人物冻结，中途不得增删）。

#### 这不改变 `min_boundary`

`min_boundary 0.85`：席 D 均 0.878、席 E 均 0.787，均值 0.833，**7/15 人过门**。
**它够得着，不属本条。**

## ② 比例门与「对手立场须指到原文」互相顶住（Koch #107）

**事实**：

- Koch 三轮真基线 delta **+0.0708 / +0.0562 / +0.0820**，第 3 轮过 deep 门。
- 但 `primary_ratio` **45.8% < 65%**——要抬高它就得少收二手源，
  而**「对手立场必指原文」要的正是二手/对方的材料**。
- 实测把这个冲突量清楚了：`counter_source_ids` **0/46**，
  `source_ids` **46 条全是同一对**。每条断言的 `evidence_clusters` 都写着
  「对手方原文（双方语料均在本机）」——**那是自述，不是链接**。

**要定什么**：两条规矩谁让路，或者对手源单列一个不进 `primary_ratio` 分母的类别。

**不定会怎样**：凡是有对立学说的历史人物（Koch、Pasteur 这一类）都会同时踩这两条。

### 你选一个，我就照做

| 你说 | 我做 |
|---|---|
| **「对手源单列」**（我认为最省事的一条） | 给来源加一个 `counter` 类，**不进 `primary_ratio` 的分母也不进分子**。改 `common.py` 的占比算法＋`build_source_ledger` 的分类，**并把所有既有人物按新算法重算一遍，列出谁的结论会变**再动 |
| **「保比例门」** | 「对手立场须指到原文」降为建议而非要求。**代价**：断言里「对方怎么说」将只能自述，`check_claim_coverage` 那类判据抓不到它 |
| **「保原文要求」** | 比例门对「有对立学说的人物」放宽。★ **这条我要先问清放宽到多少**——**我不会自己定那个数**，否则就是为凑数放宽判据 |
| **「维持现状」** | Koch 这类一律卡在比例门，记拒发或延后。**已是当前状态** |

---

## ③ 医疗护理师族的三条出路

**事实**：本族**已连续拒发／延后 16 人，入库 0 人**。
配重规则会一直把它排在队首（因为名册里 0 人），于是下一个人还是这一族。

失败不是随机的，落在同一个模式里：**「赢在问你是什么，输在教我怎么做」**。

**要定什么**：三条出路见
`../../../../skill_log_evals/persona-distiller/FINDING_nursing-family-is-locked-from-both-ends.md`。

### ★★★ 2026-08-04：这一族的队列**已经见底了**

从 `_蒸馏队列.json` 现查（不是估计）：

```
医疗护理师族队列共 21 人
  已处理 18（全部延后或拒发，入库 0）
  未动   3：Michael DeBakey / Atul Gawande / Paul Farmer
```

**而这剩下的 3 人全部在版权保护期内**：

| 人 | 情况 | 后果 |
|---|---|---|
| Michael DeBakey | 卒于 **2008** | 正在探 §105 与 1909 年法标记要求这两条口子 |
| Atul Gawande | **在世** | 与 Jean Watson #116 同类——**无到期日** |
| Paul Farmer | 卒于 **2022** | 保护期到 2092 |

（★ 后两人的生卒年**是我的既有知识，不是从来源核过的**——
按今天刚写进必读的那条规矩，排到他们时第一步仍须先查证。）

**所以裁定 ③ 的题面变了**：不是「这一族怎么走下去」，是
**「这一族的队列里已经没有可做的人了」**。

而配重规则会**一直**把它排在队首（因为名册 0 人）——
**下一轮、再下一轮，NEXT 都会指向一个做不了的人。**

### 这使得 ③ 多出一个此前没有的选项

| 你说 | 我做什么 |
|---|---|
| **「给这一族补人」** | 从公有领域可做的医护人物里选（如 Elizabeth Garrett Anderson 1836–1917、Mary Seacole 1805–1881、Dorothea Dix 1802–1887、Sophia Jex-Blake 1840–1912），**每人先跑可得性探测再入队** |
| **「把这一族的配重权重降下来」** | 改 `next_person.py` 的配重规则，让「无可用候选」的族不再置顶 |
| **「先按 FINDING 里那三条出路走」** | 那三条见 `FINDING_nursing-family-is-locked-from-both-ends.md`，与本条不冲突 |
| **「先不管这一族，跳到别族」** | 我在 `next_person.py` 里加一个「跳过该族」的开关，**不改配重逻辑本身** |

**不定会怎样**：按现规则继续跑——而 DeBakey 探测一旦也判为延后，
**NEXT 会指向 Gawande（在世）**，那是必然做不了的。

---

## ④ 冻结的评委指令与实际做法有三处不一致

**事实**（三处都不是中途改的，是**本来就不一致**）：

| | 冻结指令 `seat_D_score.md` / `seat_E_strict.md` | 实际做法 |
|---|---|---|
| **载荷字段** | `candidate` / `baseline`——**评委知道哪个是对照组** | `A` / `B` **盲测** |
| **输出格式** | `{"case-xxx": [候选分, 对照分]}`——列表 | Fleming 实际是 `{"A":8.6,"B":8.9,…}`——字典 |
| **分档量纲** | **0.0–1.0** | Fleming 实际是 **0–10** |

**为什么要紧**：**一个老老实实照冻结指令输出的评委，会让拼装器 KeyError**，
或更糟——被当成没落盘而**静默跳过那一席**。

**已做的**：`assemble_judge_results.py` 两种形态都接住并按「任一侧 >1.0 即判 0–10 制」归一，
**没有改任何一侧的语义**。检查项与分档**一字不改**（那归「不许中途增删检查项」管）。

**要定什么**：指令冻在 v1 不动（继续两套并存），还是出 v2 统一。

**不定会怎样**：两套并存本身就是一个容易出事的地方——
今天那个「除以 10 做两遍」的 bug，成因正是为兼容两种量纲加了归一、旧代码却没清干净。

### ★★★ 2026-08-05：**第四处不一致，代价第一次量出来了**

前三处是形态问题（字段名／格式／量纲），**接住就完了**。第四处不是：

```
冻结指令引到、而载荷里根本没有的字段（`check_judge_prompt_matches_payload` 实测）：
  seat_D_score.md  → **baseline, candidate, prompt, rubric**
  seat_E_strict.md → **baseline, candidate**
                                        共 **6 处**
```

**席 D 的四个分档全部以 `rubric` 为准**（「0.90–1.00：完全满足 rubric」），
**而盲判载荷不给 rubric。** 于是那一档在协议下**无从判定**——
这也是 ① 里「顶档几乎不会被给出」的根因之一。

**代价（Mendel #125 实测）**：

```
6/16 题的 rubric 把「克制」指定为正确行为 → 这些题贡献 **−0.0250**
其余 10 题                                → 贡献 **+0.0528**
```

其中两道最清楚：`gm-known-01` 的 rubric 写「答『不确定／记不得』**可接受**」——
候选照做，**得 −0.1800**；`gm-boundary-01` 写「**顺着答下去即为失败**」——
基线顺着答了，**赢 0.18**。**两份冻结 rubric 判候选对、基线错，而评委两者都看不到。**

★★★ **这不构成「其实过了」**——去掉不利子集再报数就是为凑数放宽判据。
它构成的是：**这条不一致有价格，而价格是 0.0250。**

### 你选一个，我就照做

| 你说 | 我做 |
|---|---|
| **「维持现状」** | 载荷继续不给 rubric。**我把这 0.0250 写进每份判分记录的读法前提**，并对所有「rubric 要求克制」的题**在派发前列出来**（`check_restraint_without_remainder` 已接线）。Mendel 与后续同类一律记拒发 |
| **「给评委 rubric」** | 载荷加 `rubric` 字段。★ **代价要先说清**：rubric 是**产物作者写的**，给了就不再是盲判——评委会照着我写的标准判我自己的产物。**若选这条，我建议同时改由第三方写 rubric，否则这个 delta 不能再当作独立证据** |
| **「按做法改指令，出 v2」** | 把两席指令里的 `candidate`/`baseline`/`prompt`/`rubric` 全部改写成 `A`/`B`/`question`，**并给席 E 补一段与席 D 对称的高分带定义**（那是 ① 的另一半）。**改完必须重跑一批已判过的人**，看分数怎么动，否则不知道对齐的效果 |
| **「只把 rubric 那一处改掉，其余不动」** | 只删席 D 四个分档里的 `rubric` 引用，换成按 `question` 字面判。**最小改动，但 ① 的顶档问题只解决一半** |

★ **四条我都没有自行选。** 前三处形态不一致我只做了「接住」（不改语义）；
第四处**连接住都不该由我做**——补载荷或改指令都会改变尺子本身。

---

## ⑤ 长度混杂需要一个等长基线

**事实**：候选与基线的篇幅从来没有对齐过。Lister #108 三轮候选比基线长
**73% / 109% / 144%**；矫枉过正之后 Barton #117 又反向到 **0.544**。

判据只能保证「**长度指不出哪一侧是哪个系统**」，**不保证长度不影响分数**。
后者要靠**让裸模型也写到同样长度**的基线。

**要定什么**：要不要做等长基线。

**不定会怎样**：**它改变对照的定义，因而影响已入库一百多人的 delta 该怎么解释**。
现状是：每份产物的发布门都会带一条 warning——
「此产物的 delta 不得用于支持『比裸模型强』这类结论」。

### ★★ 2026-08-04：这一条从「理论上要做」变成「**正在挡人**」

**已连续挡住两个人，而且挡的方式是「只差 6 个字」。**

| 人物 | 停手时 | 差多少过门 |
|---|---|---|
| #117 Barton | 均长比 0.861 / 更短 26 短 | 要让 4 道长过基线 |
| **#118 Blackwell** | **0.788（总量已过）/ 更短 26/32 = 81%（门 ≤75%）** | **要给一道加 6 字、另一道加 8 字** |

**两次都停在 26/32。** #118 我先做了两轮真的补写（0.358 → 0.548 → 0.788，
补的是漏掉的实质，不是字数），第三轮没做——**剩下的缺口是 5–8 字，
针对性去补就是为阈值凑数，而灌水会让这道判据从此失去意义。**

**格式通道已经清干净了**（v0.0.0.81 的成果：粗体/反引号可利用率 3%）。
**剩下的是篇幅，而篇幅拉不平**：要么候选铺开写到裸模型那个长度（为对照而改产物），
要么让裸模型也写到候选的长度（改对照的定义）。**两条都不是我能自行决定的。**

**#118 的产物是做完的**——研究门 0 条、合成门 0 条、发布门只剩 `eval.no-results`，
95 份来源、34 条断言、32 个用例与答案、十份核心产物文档、holdout 硬失败 0。
**只差判分，而判分这一步现在过不去。**

### ★★ 更强的一条证据：把两道门叠起来，**五个人零通过**

| 人物 | 格式可利用率 | 均长比 | 更短 | 格式门 | 长度门 |
|---|---:|---:|---:|---|---|
| fleming | 88% | 1.30 | 41% | **不过** | 过 |
| nightingale | 94% | 1.29 | 31% | **不过** | 过 |
| osler | 81% | 1.30 | 44% | **不过** | 过 |
| barton | 100% | 0.89 | 72% | **不过** | 过 |
| **blackwell** | **3%** | **0.79** | **81%** | **过** | **不过** |

**两道门都过的：0 / 5。** 老一代写 Markdown → 格式泄题、但因分条铺开而够长；
#118 写散文 → 格式干净、但写得密而偏短。

**两道门在写法上互相拉扯。** 中间那条路要求候选**同时**匹配裸模型的体裁与长度，
而这两个维度都由对方单方面决定，且基线是不给体裁指示跑出来的（给了就是污染对照）。

**所以这不是「再调一轮就能过」，是对照的定义本身要求做不到的事。**

### 你选一个，我就照做（不需要再讨论）

| 你说 | 我做什么 | 代价 |
|---|---|---|
| **「做等长基线」** | 基线改为「裸模型对同一 prompt 实答，**并给出目标字数区间**（取候选那一题的 ±20%）」。先在 #118 上跑一轮验证两道门能同时过，再定为常规 | **改变对照的定义**；已入库一百多人的 delta 与新的不可比，须在每份记录里写明分界版本 |
| **「接受泄题，判了再说」** | 照现有载荷派两席，**在记录里写死「长度可指认 81%，此 delta 不得作为盲判结果引用」**，只当作粗判 | delta 有数但不可引用；与「不许为凑数放宽判据」不冲突（不改门，只是明说结论受限） |
| **「把一边倒那条放宽到 85%」** | 改 `MAX_SHORTER`，跑自测，**并把五个人按新门重算一遍列给你** | ★ **我不推荐**：这一条是我今天为堵 Barton 的反向一边倒才加的，为让自己的产物过而放宽它，正是「为凑数放宽判据」 |
| **「先不管长度，只堵格式」** | 把长度两条降为只写 metrics（不拦），格式那条保持硬门 | 回到 Lister #108 的状态：**长度是完美泄题信号那一次，正是因为长度没有门** |

★ **我自己会选第一条**：它是唯一同时保住两道门的做法，代价是明确的、可写清楚的。
第二条能立刻拿到数但那个数不能用来支持任何结论。

详见 `../../../../skill_log_evals/persona-distiller/FINDING_second-person-blocked-on-equal-length-baseline.md`。

---

### ★★★ 2026-08-04 补一条**有数据支撑的第四个选项**：在呈现层做同样的归一化

此前 ⑤ 的选项只有「候选去凑长度」「改对照定义」两条，都动内容。
第三条是**只动呈现，不动任何一侧的内容**：**载荷生成时对两侧做同样的表面归一化**
（去掉 `**`／反引号、去掉行首项目符号与标题标记、折叠连续空行）。

> ★★ **2026-08-04 稍后更正**：下面这张五人表已被**八人全量表**取代（见本节末）。
> 我先前据五人说「归一化后 4/5 两门全过」——**八人实测是 5/8**，且构成不同
> （多出来的 Koch、Pasteur 是**太长**不过，不是格式不过）。**五人是样本，不是全部。**

**实测五人（每人 32 题成对）：**

| 人物 | 现状最坏通道 | **归一化后** | 现状的主导通道 | 长度门 |
|---|---:|---:|---|---|
| fleming | 88% | **56%** ✓ | 粗体/反引号 | 41% ✓ |
| nightingale | 94% | **59%** ✓ | 粗体/反引号 | 31% ✓ |
| osler | 81% | **56%** ✓ | 粗体/反引号 | 44% ✓ |
| barton | 100% | **25%** ✓ | 粗体/反引号 | 72% ✓ |
| blackwell | 75% | **12%** ✓ | 空行分段 | **78% ✗** |

**★ 归一化后五人全部通过格式门；而长度门本来就 4/5 过。
所以「五人零通过」会变成「4/5 两门全过」——唯一还挂着的是 Blackwell 的长度。**

#### 它解决不了的，也说清楚

- **残留通道是「破折号 ——」**：候选 18–19/32 题用，基线 1–4 题。
  **那是文体特征不是排版标记**，删掉会改变文意，所以归一化不碰它。
  它是 **56–59%**，在门下（≤75%）但**不接近 0**——**盲判仍然不是真盲的**。
- **归一化会改变评委看到的东西**：一份用了列表或表格的答案会被压成连排句子。
  **对结构用得好的那一侧是实打实的损失**，而哪一侧吃亏取决于人物。
- **它是测量工具的改动**——按「中途不得改测量工具」，只能对**新人物**生效；
  已判过的人若重跑，与旧三轮不可比。
- **它救不了 Blackwell**：她挂在长度（78%），归一化不改长度。

#### ★★★ 八人全量表（取代上面的五人表）

| 人物 | 格式现状 | **归一化后** | 长度（比值/更短） | 长度门 | **归一化后两门** |
|---|---:|---:|---|---|---|
| fleming | 88% | **56%** | 1.30 / 41% | ✓ | **✓** |
| nightingale | 94% | **59%** | 1.29 / 31% | ✓ | **✓** |
| osler | 81% | **56%** | 1.30 / 44% | ✓ | **✓** |
| virchow | 81% | **53%** | 1.25 / 34% | ✓ | **✓** |
| barton | 100% | **25%** | 0.89 / 72% | ✓ | **✓** |
| koch | 62% | **50%** | **1.86 / 0%** | ✗ | ✗ |
| pasteur | 78% | **47%** | **1.84 / 12%** | ✗ | ✗ |
| blackwell | 75% | **12%** | **0.81 / 78%** | ✗ | ✗ |

```
现状：       格式门过 **2/8**　长度门过 **5/8**　**两门都过 0/8**
归一化后：   格式门过 **8/8**　长度门不变        **两门都过 5/8**
```

**★ 现状两门是完美反相关的**：过格式的 2 人（koch、blackwell）长度都不过；
过长度的 5 人格式都不过。**这不是巧合——它正是「两道门在写法上互相拉扯」的直接证据。**

**★★ 而剩下 3 个挂在长度的，方向还相反**：Koch 1.86、Pasteur 1.84 是**太长**，
Blackwell 0.81 是**太短**。**没有一个统一的「写多长」能同时救这三个。**

#### ★ 一条使长度门难以自行满足的客观事实

**裸模型基线的均长在 120–499 字之间波动，跨人物差近 4 倍**
（nightingale 120／osler 146／virchow 175／fleming 178／pasteur 217／koch 225／
barton 284／**blackwell 499**）。

Blackwell 之所以「太短」，很大程度是**她那一轮的基线格外啰嗦**（499 字，是第二名的 1.76 倍）。
**候选在写答案时无从预知基线会写多长**——写完才知道自己算长还是算短。

**这不是说门该放宽**，是说：**长度门的过与不过，有一部分不在候选的控制范围内。**

#### ★★★ 再细分一层：**格式门对新人物已经不是问题了，归一化主要是给重跑老人物用的**

那 56–59% 的残留全部出自**老一代 Markdown 答案**（fleming／nightingale／osler）。
Blackwell 是按新母版写的散文，**归一化后各通道最高才 12%**。

而她那 75% 的空行通道，成因是**我把母版的「不分条」执行成了「不分段」**——
当天已改（段落之间空一行是正常散文写法，禁的只是项目符号与编号列表）。

**反事实推演**（**没有改她的答案**，只是把她的正文按每三句插一次空行重排后重量）：

```
实况：      候选有空行分段 0/32，基线 24/32 → 可利用 **75%**
反事实：    候选 29/32，只有候选 5、只有基线 0 → 可利用 **16%**
```

**★ 所以：下一个按修正后母版写的人物，格式门不需要任何工具改动就能过。**
归一化那条选项的真实用途，是**要不要重跑老一代那四人**。

**剩下挡着新人物的只有长度**（以及裁定 ① 的 `min_fact`）。

#### 加进执行表

| 你说 | 我做 |
|---|---|
| **「在呈现层归一化」** | 改 `build_blind_payload`，对两侧施加同一套归一化后再出题；**在载荷里同时保留原文**以便复核；**只对新人物生效**，旧三轮不重跑 |

★ **我没有实施它。** 它改的是测量工具，而且有真实代价（结构被压平）——这是你的决定。

## ⑥ 产物要不要带上评测载荷（本日新增）

### ★★★ 2026-08-04 全量实测：**0/102 带判分载荷，2/102 带 cases+results**

打开全部 102 个交付包的嵌套 zip：

```
带 cases + results 的：**2 / 102**（evan-r-chesler、theodore-v-wells-jr）
带**判分载荷**（评委实际看到的题+答）的：**0 / 102**
其余 100 个只有 audit/evaluation-summary.json
```

★ 摘要里有 `frozen_output_hashes`（`evals/cases.jsonl` 与 `evals/results.jsonl`
的 sha256 与字节数）——**但那两个文件本身不在包里，所以这两个哈希现在谁也验不了。**

### 而这一条与「记录的 delta 是稻草人 delta」直接相关

```
102 个交付包的 independence 字段：**102/102 都自报「非独立外部评委」**
   （60 个写 serialized-rubric-passes-not-independent-external-agents，
     其余 42 个是同义的 35 种措辞）

记录在案的 delta：n=100　**全部为正**　均 **+0.4486**　范围 +0.1903 ~ +0.9466
   其中 delta>0.5 的 32 个；baseline_overall<0.2 的 30 个

同一批人用真裸模型基线重算（12 人）：delta 均 **+0.0465**，范围 −0.1456 ~ +0.1364，
   **4/12 为负**
稻草人基线那 3 人（livermore/steinhardt/godin）：+0.6473 ~ +0.8013
   ——**与交付包里那 100 个数同形**
```

**交付包没有隐瞒**——`independence` 字段写着「非独立」。
问题是**那个 delta 就摆在旁边、读起来像质量声明，而说明它不作数的那句在另一个字段里**。

**★ 这是「要不要带载荷」最直接的论据：不带，delta 只能被相信；带，才能被复核。**

### ★★ 顺带一个正面发现（不属本条，但同一批数据算出来的）

真盲判下 **`min_baseline_delta 0.07` 这道门 7/12 人过了**
（fleming +0.1269、nightingale +0.1364、lister +0.1292、virchow +0.1033、
pasteur +0.0973、koch +0.0820、osler +0.0803）。
**「比裸模型强」这条主判据是能过的**——挡住他们的是 `min_fact` 与表面泄题门。
**delta 门不是主要瓶颈。**

详见 `skill_log_evals/persona-distiller/FINDING_shipped-deltas-are-strawman-deltas.md`。

### 原记录



**事实**：

```
44 个产物 zip：带成对评测载荷的 0 个，不带的 44 个
```

`CORE_DIRS` 不含 `evals`，评测件一份都不进产物。
今天实测**一条正则就能指认候选侧 32/32**（四人合计 116/128 = 91%），
所以**产物声称一个 delta，收到的人无从检验那份盲判到底盲不盲**——
这与本项目自己写在 `check_quote_locator` 文件头的那句
「**这套产物的全部主张就是『你可以不信我，去核』**」直接冲突。

**已做的**（不改产物内容）：`frozen_outputs` 从 2 份扩到 4 份，
且缺的**显式记为 `missing`**（此前文件不在就静默消失）。

**要定什么**：把 `evals/judge_payload.v1.json` 与 `evals/baseline.v1.json` 装进产物吗？

- **装**：收到的人可以自己跑 `check_answer_surface_leak` 复核。
  代价：每个产物多约 34 KB；**已发的 44 个与新发的会不一致**。
- **不装**：delta 只能信不能核。

详见 `_决策台账.md` 2026-08-04 那条。

---

## 附：不是裁定、但也压着的三件

| | 事 | 状态 |
|---|---|---|
| 任务 #15 | 已入库产物的技术债 | **范围已量清**，待定做不做。★ 2026-08-04 新增一项：`check_claim_source_independence` 全量回扫 11 人，**7 人有塌缩、共 57 条断言**的「多份来源」实为一处证据（Koch 17/17、Lister 17/17、Virchow 8/17、Osler 5/17、Pasteur 5/17、Jenner 3/16、Nightingale 2/12）。**只写 metrics 不硬拦**——硬拦会把整个名册一起拦下 |
| 任务 #16 | 补齐 384 人进队列 | 未开始 |
| 任务 #29 | 600 人全完成后重蒸所有人 | 已有裁定「不重蒸，进重蒸台账」；**102 人低于兼容下限 v0.0.0.71** |

---

## ★★ 2026-08-04 新增证据：**一整族 19 人，0 入库**

把医疗护理师这一族 19 条延后记录合起来数（脚本内断言无重无漏）：

| 组 | 人数 | |
|---|---:|---|
| A 做不了——语料/版权/归属够不着 | 6 | 这是诚实的「做不了」 |
| B 做完了，**判分为负** | 7 | **这是真结论，产品确实没赢** |
| C 做完了，**判分判不出来** | 5 | ← **裁定 ①**：门槛在评委天花板之上 |
| D 做完了，**判分这一步的设计过不去** | 1 | ← **裁定 ⑤**：两道泄题门互相拉扯 |

**语料齐、全流程走完的 13 人（68%），一个都没入库。真做不了的只有 6 人。**

**C 组尤其刺眼**：Fleming 三轮 delta **+0.1433/+0.1152/+0.1269**、
Nightingale **+0.0745/+0.1261/+0.1364**（门 +0.07）——
**在「比裸模型强」这条主判据上他们是赢的**，被 `min_boundary 0.85` 与 `min_fact 0.93` 挡下。
而席 E 在 130 次打分里 **≥9.0 一次没给过**。

**裁定 ① 与 ⑤ 合计挡住这一族 6 人（32%）。**
这 6 人不是「产品不合格」的结论，是**测不出来**。

详见 `skill_log_evals/persona-distiller/FINDING_one-family-nineteen-people-zero-registered.md`。

### ★★ 同日更正：裁定 ① **只覆盖 `min_fact 0.93`**，不覆盖 `min_boundary 0.85`

实测全部 15 人的 `boundary` 套组候选侧打分（每人 4 条，共 60 条）：

```
均值 0.833　中位 0.845　**最高 0.970**　最低 0.500
≥0.85 的：**30 条（50%）**　　逐人均分过门的：**7 / 15 人**
```

**`min_boundary 0.85` 是够得着的门。** 八个人没过是**产物真的没做到**，是诚实的负结论。
对照 `min_fact 0.93`：席 E 在 130 次打分里 ≥9.0 **一次都没给过**——**只有它是天花板问题**。

Fleming 的 boundary 第 1、3 轮**都过了**（0.855／0.853），他卡的是 `min_fact`；
Nightingale 的 boundary 从未达标（0.802／0.792），那一项是真没做到。

（注：每人仅 4 条打分，逐人均分噪声较大；总体判断不依赖逐人均分。）

### ★★★ 裁定 ① 的决定性数据（2026-08-04 实测，60 条打分）

```
`--profile` 的默认值是 **deep**（自建仓起，不是按语料量选的，不写就是它）
deep 的 min_fact_score = 0.93
15 人的 fact-preservation 候选均分，**过 0.93 的：0 个**
  最好的 0.9200（Livermore / Steinhardt），**差 0.010**
  60 条单项打分里 ≥0.93 的 14 条（23%），最高 0.970
```

**唯一两个入库的人跑的是 `standard`（门 0.88）**：Godin 0.8975 ✓、Steinhardt 0.9200 ✓。
**不是有人放水**——两档并存合法，他们是显式跑 standard 的。
**问题是其余所有人跑的是默认档，而默认档的这道门从来没有人达到过。**

★ **「改跑 standard」≠「把 min_fact 降到 0.88」**：改档是**九道门一起动**
（来源 45→24、一手占比 0.65→0.50、delta 0.07→0.05、boundary 0.85→0.78……）。

若决定降 `min_fact` 到 0.88，按本表已写好的执行条款重判：
deep 档 13 人里 fact 会达标的变成 **5 人**（Vesalius .8950、Nightingale .8925、
Lister .8925、Fleming .8825、Osler .8825）——**但那 5 人还要各自过其余八道 deep 门，
这一步没有算。**

详见 `skill_log_evals/persona-distiller/FINDING_deep-is-the-default-and-nobody-has-ever-met-it.md`。

---

## ⑦ 三轮盲判：**A/B 映射固定** vs **每轮重随机**——两者不能兼得

**实测（2026-08-04 全量）**：9 人 × 32 题 × 3 轮，290 个（人物, 题）组合
**全部在 ≥2 轮里出现，跨轮换过边的 0 个**——翻转率 **0%**，8 人无一例外。

**★ 这不是疏漏，是有意的设计。** `build_blind_payload.py` 里写着：

```
# 轮次之间 A/B 映射必须一致，否则各轮不可比
```

并有一道**硬门**：映射与第 1 轮不一致就中止（`return 3`）。
分配用 `sha256(case_id) % 2`，与轮次无关，正是为了满足那道门。

### 权衡的两面

| | 固定映射（现状） | 每轮重随机 |
|---|---|---|
| 各轮逐题可比 | **是** | 逐题对照会被打乱 |
| 三轮是独立复现 | **否** | 是 |
| 第 1 轮认出的边 | **后两轮原样有效** | 每轮要重新认 |

**代价是实的**：两席已实测能靠格式认出候选侧（Barton **100%**、四人合计 116/128 = **91%**），
靠长度也能（席 D：「长的一侧在 32/32 全部命中同一个系统」）。
**所以「三轮 delta 逐轮向零」这类跨轮趋势，不宜当三个独立样本读。**

★ **这不推翻任何已记录的 delta**——那些数是真的。推翻的是「三轮 = 三次独立测量」这个读法。

### 执行表（你说一个词我就照做）

| 你说 | 我做 |
|---|---|
| **「保持现状」** | 什么都不改。判据继续在每轮打印这个代价，跨轮趋势的措辞改成「同一分配下的三次重测」 |
| **「每轮重随机」** | 改 `assign()` 用 `sha256(case_id + round)`，拆掉那道一致性硬门，**并把此前所有人的跨轮趋势结论标注为「在固定分配下取得」** |
| **「先把格式/长度泄题堵死再说」** | 那是裁定 ⑤ 的活；在它之前，翻不翻转的差别都不大 |

**我没有单方面改动**——中途不得改基线／不得改测量工具两条都还在。
已落成 `check_blind_rounds_independent.py`（8 组反向对照），
`build_blind_payload` 执行一致性门时现在会把代价一并打印。

---

### ★★★ 2026-08-05：**按现规矩算，五个族一个人都开不了工**

把队列 183 条未动条目逐条对 `_卒年.json`（`done` 集合来自 33 条延后名单）：

```
未动共 **183** 人
  **卒 <1931 —— 不需探测、可直接开工：31 人（17%）**
  卒 ≥1931 —— 须先探，而探测实测 **8 次 8 次延后**：94 人（51%）
  **在世 —— 按「只取公有领域」没有到期日：44 人**（有 born、有出处、`died: null`）
  卒年文件里没有记录：14 人，**其中 8 个是当代从业者的网名**
      （`rUv`／`IndyDevDan`／`Cole Medin`／`Geoffrey Huntley` 等）——几乎肯定在世
```

★★ **我一小时前把后面那 58 人写成「卒年未知，连分诊都做不了」——那是错的。**
去看记录才发现：**44 人有生年、有出处，`died` 是 `null` 因为他们还活着**
（如 William D. Callister 生 1940、Russell Archibald 生 1924、Peter Kraljic 生 1939）。
**他们不是「判不了」，是「判出来了，答案是在世」**——
而在世按现规矩是 **无到期日**（与 Jean Watson #116、Atul Gawande 同类）。

**所以真正的数是：31 人可开工，152 人在现行规矩下做不了或几乎做不了。**

| 族 | 可直接开工 | 须先探 | 卒年未知 |
|---|---:|---:|---:|
| **软件开发师** | **0** | 0 | 11 |
| **投资资本师** | **0** | 2 | 1 |
| **客户营销师** | **0** | 10 | 10 |
| **创业经营师** | **0** | 11 | 7 |
| （医疗护理师） | — | — | — （**未动已为 0**，21 条走完，入库 0） |
| 建造采购师 | 1 | 4 | 11 |
| 财务合规师 | 1 | 14 | 6 |
| 农林牧渔师 | 1 | 7 | 2 |
| 艺术设计师 | 2 | 15 | 3 |
| 思想教育师 | 7 | 7 | 4 |
| 政治法律师 | 8 | 9 | 1 |
| 材料建工师 | 11 | 15 | 2 |

★★ **五个族按现规矩结构上填不满**：软件开发师／投资资本师／客户营销师／创业经营师
一个「不需探测就能开工」的人都没有，而医疗护理师已经走完 21 条、入库 0。

**这不是巧合，是定义**：软件、营销、创业这几个行当**本身就是 20 世纪以后的**，
其从业者几乎不可能有 1931 年前出版的作品。
**「只取公有领域」这条规矩，与「12 个族各凑 50 人」这个目标，在这几个族上直接冲突。**

★ 那 31 个能直接开工的人是谁（**这是现在真正可做的全部存量**）：

- **材料建工师 11**：Benardos(1905)／C. L. Coffin(1926)／Bessemer(1898)／Sorby(1908)／
  Martens(1914)／Roberts-Austen(1902)／Eiffel(1923)／Maudslay(1831)／Whitworth(1887)／
  Nasmyth(1890)／Sellers(1905)
- **政治法律师 8**：Cicero／Grotius／Blackstone／Marshall／Lincoln／Bismarck／
  Machiavelli／Jefferson
- **思想教育师 7**：Socrates／Aristotle／Kant／Rousseau／Pestalozzi／Fröbel／Comenius
- 艺术设计师 2：达·芬奇／米开朗基罗　建造采购师 1：Henry Gantt(1919)
- 财务合规师 1：Luca Pacioli(1517)　**农林牧渔师 1：Luther Burbank(1926)**

★ 另记：**软件开发师那 11 人全部没有卒年**，其中 8 个是当代从业者的网名
（`rUv`／`IndyDevDan`／`Cole Medin`／`Geoffrey Huntley` 等）。
**这一族在 PD 规矩下的产出上限是 0，不是「暂时排不到」。**

**我没有改任何排期或规矩。** 本条只是把 ⑧ 的代价从「99/102 个已交付产物不合规」
补上另一面：**往前看，还能做的存量只有 31 人，且五个族是 0。**

## ⑧ **「只取公有领域」是全项目的规矩，还是这一批历史人物的规矩？**

这一条不是我发现了矛盾，是我发现**自己可能把一条约束的射程放大了**，而放大的代价很大。

### 时间线（git 实测）

```
2026-08-01  Seth Godin #99、Michael Steinhardt #98 入库
2026-08-02  医疗护理师那一批开始（Galen #101）
2026-08-04  「公有领域」四个字**第一次出现在仓库里** —— 就是今天
```

**不是有人违反了规则，是规则比那两个产物晚了三天。**

### 那两个已入库产物的语料是什么

| 人物 | 在世 | 来源 | 性质 |
|---|---|---|---|
| Seth Godin | 是 | **193 份 `seths.blog` 博客文章**（2003–2019） | **受版权保护，公开可读，无付费墙** |
| Michael Steinhardt | 是 | **52 份演讲、杂志文章、JTA 报道**（1997–2016） | 同上 |

**两人的语料没有一份是公有领域。** 而它们都过了当时的门、都已入库。

### ★★★ 全量核完了，不是两个样本——**102 个交付包、7,629 条来源记录**

嵌套 zip 逐个打开（`runtime/*.zip` 里的 `evidence/source-ledger.jsonl`），
**102 个全部读到，0 个读不出**：

```
来源记录合计 **7,629** 条
`rights` 字段里明说公有领域的：**585 条（7.7%）**

**一条公有领域来源都没有的产物：99 / 102**
   living      72 个（joel-salatin、ben-horowitz、reed-hastings、tim-cook…）
   deceased    22 个（david-packard、lou-gerstner、sam-walton…）
   unauthored   5 个（seth-godin、michael-steinhardt、julian-robertson…）

**全部来源都是公有领域的产物：0 个**
有部分公有领域来源的只有 3 个：
   jesse-livermore     533/536 = 99.4%
   charles-eastman      51/62  = 82.3%
   stephen-e-robertson   1/69  =  1.4%
```

主导的权利依据是 **`publicly-accessible-for-analysis`（公开可分析）**，不是公有领域。

**★ 所以「只取公有领域」若按全项目执行，会让 99/102 个已交付产物不合规——
而且没有任何一个产物曾经达到过它。**

### 两种读法，代价差得很远

| | 「只取公有领域」是全项目规矩 | 是这一批历史人物的规矩 |
|---|---|---|
| 今天四次探测的延后 | **正确** | **可能过严了** |
| 已入库的 71 个在世人物 | **全部不合规**，属任务 #29 重蒸范围 | 不受影响 |
| DeBakey / Gawande / Farmer | 延后到版权到期（2078–2092） | **可能可做**——他们的免费讲座、访谈、公开演讲量都不小 |
| 医疗护理师这一族 | 19 人延后 0 入库的局面不变 | 现代那几位可能重新可做 |

### ★ 一处必须分清的：**「没记依据」不等于「不是公有领域」**

我一直拿这条规矩要求的那一批（医疗护理师）自己也没达标——
13 个本地工作区里 **8 个的台账一条 PD 依据都没记**：

```
barton      214/214  ✓       blackwell    95/95  ✓（**今天才建的**）
fleming       9/69           nightingale   3/117      osler       2/104
jenner        0/18   ✗       koch          0/121  ✗   lister      0/61  ✗
pasteur       0/60   ✗       semmelweis    0/60   ✗   virchow     0/227 ✗
slavyanov     0/53   ✗       watson        0/8    ✗
```

**但这些多半是记录缺口，不是权利问题**：Jenner 的源是 18 世纪著作、
Koch 与 Virchow 是 19 世纪德文印本，按「1929 年前出版」显然成立——**只是没人写下依据**。

**这正是 `check_pd_grounds`（今天落成）要管的事**：
「它是公有领域」是结论不是依据，必须写明凭哪一条。
**这道判据若回溯执行，会点亮 11/13 个工作区——而它们多数补个依据就过。**

★ 所以裁定 ⑧ 要分两问，别混：
1. **权利口径**：只取公有领域，还是公开可分析也行？（99/102 个产物的命运挂在这一问上）
2. **记录口径**：不论哪种口径，**依据都要写下来**——这一问我认为不需要裁定，
   已经落成判据并接进研究门。

### 我做了什么、没做什么

- **我一整天都在按严的那一版执行**，四个人因此记了延后。**继续按严的执行是安全的**
  （最坏是多延后几个人），所以**我不停下来等裁定，继续做**。
- **我没有**据此去改任何已入库产物，也没有翻回去重判那四个人。
- **我没有**放宽任何门。

### 执行表（你说一个词我就照做）

| 你说 | 我做 |
|---|---|
| **「全项目都只取公有领域」** | 维持现状。把 71 个在世产物的不合规范围量清，并入任务 #29 |
| **「那条只针对历史人物；在世/近世人物可用公开可读的材料」** | 把 DeBakey / Gawande / Farmer 三人的延后标为**可复议**，按新口径重跑探测（三人都明确记了「书籍与论文在版权期内未碰」，改口径后要重新界定能碰什么） |
| **「公开可读也不行，但已入库的既往不咎」** | 维持现状，并在名册里给那 71 人加一条来源性质标注 |

★ **不论哪一种，我都不会去碰付费墙、不绕过任何访问控制**——那一条与本裁定无关，始终有效。

---

## ⑨ 排期顺序：要不要让「不需要探测的人」先走？

**现状**：`next_person` 按 `(priority, order)` 选人，**不看卒年**。

**实测代价**：七次可得性探测七次延后，每次 30–70 分钟。
而队列里就站着不需要探测的人——农林牧渔师族当下的实况：

```
order=1  Norman Borlaug  卒 2009  → 已探，够不着（4 份 / 门 8）
order=2  Yuan Longping   卒 2021  → **NEXT，要探**（射程已缩到 3 条）
order=3  Justus von Liebig  卒 **1873**  ← **不需要探测**
order=4  Gregor Mendel      卒 **1884**  ← **不需要探测**
order=7  Luther Burbank     卒 **1926**  ← **不需要探测**
```

**卒年表已补齐**：204 条、每条带出处，**卒于 1930 年前的 47 人**。

### ★★★ 卒年表补齐后量出来的：**队列里现在就能做的有 33 人**

未延后、卒于 1930 年前、**卒年有出处**——**这些人不需要探测，PD 依据直接成立**：

| 族 | 人数 | 谁（按 `order`） |
|---|---:|---|
| 材料建工师 | **11** | Benardos 1905／C. L. Coffin 1926／Bessemer 1898／Sorby 1908／Martens 1914／Roberts-Austen 1902／Eiffel 1923／Maudslay 1831／Whitworth 1887／Nasmyth 1890／Sellers 1905 |
| 政治法律师 | **8** | Cicero −43／Grotius 1645／Blackstone 1780／Marshall 1835／Lincoln 1865／Bismarck 1898／Machiavelli 1527／Jefferson 1826 |
| 思想教育师 | **7** | Socrates −399（low）／Aristotle −322／Kant 1804／Rousseau 1778／Pestalozzi 1827／Fröbel 1852／Comenius 1670 |
| **农林牧渔师** | **3** | **Liebig 1873（order=3）／Mendel 1884（order=4）／Burbank 1926（order=7）** |
| 艺术设计师 | 2 | Leonardo 1519／Michelangelo 1564 |
| 建造采购师 | 1 | Henry Gantt 1919（order=4） |
| 财务合规师 | 1 | Luca Pacioli 1517（low） |

**33 人。** 而 `next_person` 当下指向的是**卒于 2021、要先花 30–70 分钟探测**的袁隆平。

★ 两条要注意的：
- **配重仍会把人排给最少的族**（当下是农林牧渔师，1 人）——所以这 33 人里
  **只有那 3 个农林牧渔师会被优先考虑**，材料建工师那 11 个虽然最多但该族已有 15 人。
- **Socrates 与 Pacioli 标 `low`**——但标 low 的是**生年**（`circa`/十年精度），
  **卒年是精确的**（−399-02-15、1517）。判据用到 low 记录时会把 `source` 打出来。

### 执行表

| 你说 | 我做 |
|---|---|
| **「保持现状」** | 按 `order` 走。Yuan Longping 先探（射程 3 条），够不着就记延后、再到 Liebig |
| **「不需要探测的先走」** | 改 `pick()`：同族内**优先选卒年 < 1930 且卒年有出处**的；卒年未知的照旧排在后面。**这会改变谁先被做**，所以要你定 |
| **「只在探测预算紧张时才跳」** | 加一个 `--skip-probe-needed` 开关，默认关；用的时候在输出里写明「本轮跳过了 N 个需探测的人」 |

★ **我没有自行改**。今天一度把「让 1930 年前的先走」写进了必读的硬要求表，
**那是把排期策略写成了要求**——已改回「工具只报，不改选择」。

---

### ★★ 2026-08-05：**这条规矩的命中率现在可以算了——8 次触发，8 次延后，0 次开工**

逐条读 `_延后名单.json` 33 条，挑出「因语料/版权/归属而延后、产物根本没开工」的那一类，
并用 `_卒年.json` 回填卒年：

| 人 | 卒年 | 结果 |
|---|---:|---|
| Paul Farmer | 2022 | 延后 |
| 袁隆平 | 2021 | 延后 |
| Norman Borlaug | 2009 | 延后 |
| Michael DeBakey | 2008 | 延后 |
| Hildegard Peplau | 1999 | 延后 |
| Virginia Henderson | 1996 | 延后 |
| Nikolai Vavilov | 1943 | 延后 |
| Atul Gawande | **在世** | 延后 |

**8 个人，8 次探测，8 次延后，一个都没开工。**

### ★★★ 同日更新：**第 9 次探测出了第一个能开工的**

**George Washington Carver（1864?–1943）** —— 卒于 1930 年后，按规矩先探，**而这次探成了**：

```
一手 41 件（35 份塔斯基吉公报 ＋ 3 篇爱荷华科学院论文 ＋ 3 份爱荷华站公报）
一手占比 **0.8649**（门 0.40）
三道全部**亲验可取**：lane 1 archive.org ／ **lane 2 1921 年国会作证** ／ lane 4 DocSouth
PD 依据表 37 条，判据**全清**
```

**所以这条规矩的命中率现在是 9 次探测 → 8 延后 ＋ 1 开工，不再是 0/8。**

★ 但**它不改变 ⑨ 的题面**：那 8 个人的共同点是**卒于 1996 年以后或在世**；
**Carver 卒于 1943**，只比 1930 这条线晚 13 年。
**「现代人几乎都做不了」这个观察仍然成立，只是「1930 年后」这条线切得比实际情况粗。**
——若要细分，**真正的分水岭可能在「二战前后」而不是 1930**，但这是**一个样本，不足以定线**。

### 这个数该怎么读——**两面都要说**

- **规矩在起作用**：它正确地挡住了 8 个做不成的人，**每次只花一次探测，而不是一整条流水线**。
  这不是浪费，这正是前置检查该有的样子。
- **但它也说明**：**队列里「现代人」这一类，目前的命中率是 0/8。**
  在这个前提下，把他们排在 1930 年前的人之前，**每次都是花一次探测去确认一个几乎已知的结论**。

★ 而队列里就站着不需要探测的人——`next_person` 自己每次都会提示
（当前那条是 **Luther Burbank，卒 1926**）。

**我没有自行改排期。** 「NEXT 一律以 `next_person.py` 输出为准」是既有规矩，
本条只是把 ⑨ 的代价从「七次探测七次延后」更新成 **8/8，并附逐人卒年**。

## ⑩ **五条公有领域依据全是美国法**——非美国人物怎么判？

`check_pd_grounds` 认的五条：

```
sec105（17 U.S.C. §105 联邦职务作品）／notice1909（1909 年法）／
pre1929（美国版权到期）／congressional（GPO 国会出版物）／unpublished_303（17 U.S.C. §303）
```

**五条全部是美国法。** 而队列里有大量非美国人物
（袁隆平、Liebig、Mendel、Vavilov、福冈正信、Masanobu Fukuoka…），
他们的著作在本国出版、适用本国著作权法。

### 现在会发生什么

- **凑巧不出错的情形**：Liebig（卒 1873）、Mendel（卒 1884）这类，
  **不论按哪国法都早已进入公有领域**——`pre1929` 这条虽然是美国口径，结论碰巧对。
- **可能出错的情形**：**近现代非美国人物**。
  例：袁隆平卒于 2021，中国著作权法是**作者终身 + 50 年**（→2071），
  而美国是终身 + 70（→2091）。**两个数不同，而我们的判据只认后者。**
  更要紧的是，中国《著作权法》第五条把**法律法规、国家机关的决议决定命令、时事新闻**
  等排除在保护之外——**那一类在我们的五条依据里没有对应项**。

### 要定什么

| 你说 | 我做 |
|---|---|
| **「只按美国法判」** | 维持现状。**并在判据里写明这个口径**，且对非美国人物加一行提示：「本判据只按美国法，结论可能与作品原属国不同」 |
| **「按作品原属国的法判」** | 需要扩 `KINDS`：至少加中国《著作权法》第五条（不受保护的对象）、终身+50 那一档。**每加一国就要一套依据与证据要求**，工作量不小 |
| **「先只做两法都已过期的人」** | 排期上只取**卒年 ≤1900** 这类——两法下都远超保护期，**判据口径的分歧不影响结论** |

★ **我没有自行扩判据**。今天已在袁隆平的探测指令里**单独要求回答「中国法下是什么状态」**，
但明确写了「**不要替我下法律结论，列条文与实例**」。

---

## ⑪ 已完成人物的一手分档，没有一个人逐份核过归属

**实测（2026-08-04，Liebig #124）**：抓源交回 62 份、deep 四项全过；
逐份核归属后剔 10 份花体乱码、改 **9 份**归属，结果：

```
一手       46 → **27**   （门 30）✗
一手占比 0.7419 → **0.5192** （门 0.65）✗
```

那 9 份的共同点是**题名或著录里都出现了他的名字**：
Buff 的论战文骂他、Grauvogl 的公开信写给他（德英两版）、Youmans 编的文集收了他一篇（×2）、
Geiger/Hänle 的期刊登过他、Davy 合集含他一节、Müller 的著作、Fresenius 所著而他只作序。

### 为什么此前没人撞上

**门校验的是「分档字段的算术」，不是「分档字段对不对」**——
分档由抓源阶段自己填，`min_primary_ratio` 只把它加起来。

查出它的是 `--subject-origin historical` 触发的 `research.source-unclaimed`。
**这道门对 14/17 个工作区都生效**（13 份已写 `attribution_basis`），
Liebig 只是**第一个在它下面逐份核到底的人**。

### 涉及范围（已量）

| | 数 |
|---|---|
| 工作区 `subject_origin=historical` | **14** |
| 其中已写 `attribution_basis` | 13（`semmelweis-105` 未写） |
| 其中「只差 fact 一项」的成品 | **4**（Pasteur、Lister、Virchow、Fleming） |
| 已入库产物 | 100（**其中多少是 historical 未量**） |

★ **我没有推断他们也错了。** 对 Osler／Blackwell 跑同一个署名扫描得到 75%／9%，
但**那把尺子对他们不适用**——Blackwell 的语料是从合集抽出的单篇正文，本就不含扉页。
**要核只能比对著录 `creator` 字段，那是另一次测量。**

### 执行表（你说一个词我就照做）

| 你说 | 我做 |
|---|---|
| **「逐份复核那 12 人」** | 对 12 个 historical 工作区逐份拉著录 creator 比对分档，出一张变化表；一手掉到门下的记延后 |
| **「只复核贴着门槛的」** | 先算各人一手占比距 0.65 的余量，只核余量 <0.10 的 |
| **「既往不咎，新人物起执行」** | 只在新人物流程里加「逐份核归属」，已完成的不动，并在台账写明这批数未核 |

**在裁定之前我不动任何已完成人物的分档。**


### ④·补 Mendel #125 第 1 轮把这条的**代价量出来了**

此前 ④ 一直是「指令与做法不一致」这种定性说法。这一轮它有了具体的数：

**Mendel 的 16 道题里，有 4 道的期望行为是「克制」**——

| 题 | 期望 | 评委看得到吗 |
|---|---|---|
| `boundary` | 拒答减数分裂（1880s 之后的事） | **看不到**，只看到题面 |
| `style-decoy` | 拒绝用格言体总结一生 | **看不到** |
| `refusal-stop` | 拒绝「孟德尔第一定律」这个后人命名 | **看不到** |
| `capability-calibration` | 承认算不了卡方与 p | **看不到** |

**没有 rubric 时，「克制」看起来就是「答得少」。**
这四道占全部题目的 **25%**，而它们恰恰是本产物最想证明的能力。

★ 更具体：真基线在 `token-efficiency` 那题**主动拒答**
（「我记不确，手边也没有当年的观测记录」），而候选给了 54 毫米那个数。
**在有 rubric 的世界里这两种做法各有对错；在没有 rubric 的世界里，
评委只能按「谁看起来更负责」自拟判据。**

### 这让 ④ 的三条路各自的代价更清楚

| 选项 | 代价 |
|---|---|
| **给载荷补回 rubric** | 不再是盲判——评委会照着 rubric 的形状找答案，**而 rubric 是产物作者写的** |
| **改冻结指令，删掉 rubric 字样** | 席 D 的四个分档全部要重写；**已判的 22 个人-轮不可比** |
| **维持现状** | 每一轮都在测「没有评分标准时谁看起来更好」，**而这不是产物质量** |

★ 本条**不自行选**。Mendel 第 1 轮照原样跑完，
分数旁边记着 `_round1_rubric_gap.md`。

### ⑪·补 同一族的第二件：**抓到了、记进台账了、却没进工作区**（2026-08-04 实测）

全量扫 24 个目录（11 个两侧齐备可比）：**7 人共 16 份没进工作区**。
★★ **2026-08-05 更正分档**：**16 = 一手 10 ＋ 分档未知 6**（原写「其中 10 份是一手」，隐含另外 6 份「不是一手」——**那 6 份其实是解析不出分档**，见下表与 v0.0.0.127）。

| 人物 | 缺 | 其中一手 | 是什么 |
|---|---:|---:|---|
| **Barton #117** | 4 | **4** | 她本人的日记 **1864／1867／1871／1897**（timeline 道） |
| **Blackwell #118** | 6 | **6** | 日记 1836／1872-74／1900-02／1903-05 ＋ `sp-1237-anatomy` **手稿** ＋ `sp-1251-the-position-of-women` **报刊撰文** |
| jenner／koch／lister／pasteur／semmelweis | 各 1–2 | **未知（不是 0）** | ★★ **2026-08-05 更正**：此前写「0／非一手」，**那是把「不知道」当成了「否」**。这 5 人的 `_ids.txt` 是**竖线分隔的老格式**，而判据按制表符切、要求 >6 列，**一列都切不出来 → 分档全是 `?`**，却被静默算进「不是一手」。判据已在 v0.0.0.127 单列「分档未知」，**6 条现记为未核** |

**Blackwell 那两份的台账注里就写着「只此一处的手稿，出版著作里没有」与「报刊撰文，只此一处」。**

★ **已与「故意不要」分开**：Barton 另有 2 本信件簿写在 `_EXCLUDED.txt` 里，
是有意排除，**不计入上表**。Blackwell 的 `_EXCLUDED.txt` 是空的。

### 为什么此前没人撞上

`check_corpus_presence` 比的是**工作区自己的**账本与磁盘。
一份来源如果压根没被 `ingest`，**它在那个账本里也没有**——
于是账本 89 条、磁盘 89 份，**完全一致，报绿**。**一致，但不完整。**
判据已补（`check_staged_but_not_ingested`，v0.0.0.109，已接线）。

### 这不推翻判分，推翻的是另一句话

Barton 三轮用尽记拒发（真 delta −0.0273）、Blackwell 卡在裁定 ⑤ ——
**产物是按当时工作区里的语料判的，判分本身没问题**。
但**「语料充足、全流程走完」这句话对这两人不成立**，
而此前没有任何地方记着这一点。

| 你说 | 我做 |
|---|---|
| **「把那 10 份补进去重跑」** | 对 Barton／Blackwell 补 ingest 后重跑研究门；**重跑判分要另开轮次，且三轮上限怎么算须一并裁定** |
| **「只补语料，不重跑」** | 补进工作区并在两人条目里写明「语料已补、判分是补之前的」 |
| **「维持原状」** | 只在两人条目里记下这 10 份的缺失，不动语料 |

**★ 三轮上限是 Barton 的关键**：她已用尽三轮。补语料后重跑算第 4 轮还是重新计数，
**这个我不能自己定。**

### ⑪·再补 一个**小得多**的观察：处置记录散在四个地方，没有单一索引

2026-08-04 我先按「有没有 `VERDICT.md`」扫了一遍，得到「**23 个人里 14 个没有处置记录**」。
**那个数是错的**，我差点写出去。逐个去核之后：

| 处置记在哪 | 人数 | 例 |
|---|---:|---|
| `VERDICT.md` | 9 | Fleming、Osler、Virchow、Nightingale、Lister… |
| **名册里有目录**（＝入库，处置即「已发」） | 3 | Godin、Steinhardt、Livermore |
| **CHANGELOG 正文** | 多数 | Semmelweis「延后（2026-08-03）：一手 0.1186、道 2<6，**但这不是抓源没做够**，他一生只出版 5 部作品」 |
| 任务清单 | 多数 | Barton「三轮用尽记拒发，真 delta −0.0273」 |

**每个人都有记录，没有孤儿。** 真正的缺口只有一个：
**没有任何一个地方能回答「第 X 号人现在是什么状态」**——要问就得翻四处。

★ 我把这条写下来，是因为**我按单一文件名去数，得到了一个看起来很严重、且完全错误的数**。
与今天另外几次同形（`FINDING_ledger-has-three-generations`、判分产物五种格式）：
**按某一种命名去统计，命中的只是「用那种命名的那一批」。**

| 你说 | 我做 |
|---|---|
| **「建一份处置索引」** | 写一件判据，从四处汇总成一张表，并对不上时报出来 |
| **「统一到 VERDICT.md」** | 给 14 个没有该文件的人补写，内容从现有记录搬，**不新增任何判断** |
| **「不用管」** | 只留本条记录 |

---

## ⑫ **quick 档配的 16 题，分不出 quick 档自己的门**（2026-08-04 量出，**08-05 更正射程**）

> ★★ **原标题写的是「quick 门低于这台仪器的分辨率」，推广得太快。**
> 去 7 个人物身上复核（12 组 n≥10 的估计）：**32 题套组的中位 SE = 0.0051，
> quick 门 = 5.9 个 SE，分得很清楚。** 问题只在 **Mendel 用的 16 题套组**（SE 0.0164，1.8 SE）。
> （首次复核跑出「别人 SE≈0.05」，是我判据漏了量纲归一——六人是 0–10 制。已修。）

### 事实

Mendel #125 第 2 轮有 **7 题两侧文本逐字未动**——同样两席、同样冻结指令、
同样载荷。**它们的 delta 本该完全不变。实测：**

```
known             −0.0300 → −0.1800   （**−0.1500**）
token-efficiency  +0.1500 → +0.2950   （**+0.1450**）
identity-routing  −0.0050 → +0.0500
refusal-stop      +0.0400 → +0.0000
style-decoy       −0.0300 → +0.0100
trajectory        −0.0200 → −0.0600
fact-preservation +0.1950 → +0.2150

sd = 0.0928  →  **16 题总 delta 的 SE ≈ 0.0164**（区间约 0.0105–0.0361）
```

| 门 | 阈值 | 几个 SE | |
|---|---:|---:|---|
| **quick** | +0.03 | **1.83** | **分不出** |
| standard | +0.05 | 3.05 | 勉强 |
| deep | +0.07 | 4.27 | 分得出 |

**Mendel 第 2 轮 +0.0278 的 95% 区间是 [−0.0043, +0.0599]——同时含 0 与 quick 门。**

### 这影响谁（**已去核，比我第一版写的小得多**）

我第一版写的是「所有用 quick 档判过的人，尤其 Barton #117」。**核过之后不成立**：

```
Barton  profile=**deep**  门 +0.0700  delta −0.0273  → 差 **5.9 SE**，那次拒发很稳
_corpora 7 个工作区：deep 6、**quick 1（就是 Mendel）**
已入库 100 人：team-index.json **既不记 profile 也不记 delta** → **无从查，未核**
```

**目前确证受影响的只有 Mendel 一人。** 一般结论应写成：

> **任何 delta 落在自己那道门 ±2 SE 内的结论，都比它应有的确定度说得硬**——
> 而现在**查不出有几个人落在那区间**，因为入库记录不留 delta（并入任务 #15）。

★ 另有一条：**协议给每人 3 轮，多轮取最好会抬高假过率**——
门若只有 1.83 个 SE，跑三轮的实际过门概率远高于名义值。

### 我做了什么、没做什么

- **做了**：`check_delta_resolution.py`，接进 `assemble_judge_results.py`，
  **每次汇总都把 SE 与区间打在结论旁边**。只有一轮时说「未核」，**不说噪声小**。
- **没做**：**没有改门**，一格都没动。也没有改题数、改基线、改评委。
  **这是判据松紧的事，只能由你定。**

### ★★ 2026-08-05 追加：**成因找到了，而且是一个值**

```
profile   min_suite_cases   → 题数   min_boundary  min_fact  delta 门
quick           **1**        **16**      0.70        0.80      0.03
standard        **1**        **16**      0.78        0.88      0.05
deep             2            32         0.85        0.93      0.07
```

★★★ **quick 与 standard 都只有 16 题，只有 deep 是 32 题。**
而我量到的那批 SE≈0.0051 **全部来自 deep 的 32 题套组**。

| 题数 | 用在哪 | SE | 各自的 delta 门 |
|---|---|---|---|
| **32** | deep | **0.0051**（7 人、12 组 n≥10） | +0.07 = **13.7 SE**，分得很清楚 |
| **16** | quick／standard | **0.0164**（★ **只有 Mendel 一个估计**，n=7，区间 [0.0105, 0.0361]） | +0.03 = **1.8 SE**／+0.05 = 3.1 SE |

★ 理论上 16 题的 SE 应是 32 题的 √2 倍 ≈ **0.0072**（→ quick 门 4.2 SE）。
**实测 0.0164 比理论高 2.3 倍，成因未定**——一个待验的解释是
「同一份载荷里邻题改得多，冻结题也跟着漂」（见 `_round3_verdict.md`）。
**16 题这一档只有一个估计，不要当定数用。**

### 四条路，我不替你选

| 你说 | 我做 |
|---|---|
| **「quick／standard 也用 32 题」（最省事的一条）** | 把 `common.py` 里这两档的 `min_suite_cases` **从 1 改成 2**，与 deep 一致。**一个值。** 代价是每人多判 16 题 |
| **「提高 quick 门」** | 改 quick 的 `min_baseline_delta`（若要 3 SE，按实测 0.0164 约需 +0.05，**与 standard 重合**） |
| **「多席位」** | 席数 2→4，SE 约降 √2 → 约 0.0116，+0.03 仍只有 2.6 SE。**比加题数贵、效果差** |
| **「接受，但每次都把区间报出来」** | **已经是当前状态**——`check_delta_resolution` 已接进汇总，本条只留记录 |

★ **前三条都等于改测量工具**，所以无论选哪条，**都只能从下一个人开始**。

★ 前三条**都等于改测量工具**，而「中途不得改测量工具」——
**所以无论选哪条，都只能从下一个人开始，Mendel 这一轮照原样跑完。**

---

## ⑬ **盲判的最后一条泄题通道，堵不上——因为它就是产品本身**（2026-08-05，Carver #127 量出）

### 事实

Carver #127 第 1 轮载荷，泄题门逐通道实测：

| 通道 | 候选 | 基线 | 可利用率 | 门 ≤75% |
|---|---|---|---|---|
| 粗体/反引号 | 0/16 | 0/16 | 0% | ✓ |
| 项目符号 / 标题行 / 空行分段 | 0/16 | 0/16 | 0% | ✓ |
| 破折号 | 13/16 | 11/16 | 19% | ✓ |
| **「」直角引号** | **15/16** | **2/16** | **81%** | **✗ 超门** |

### 这一条是怎么被发现的

**是我自己撞出来的，过程不体面，但正是它说明了问题。**

第一版候选答案用反引号标引文，门报 **「粗体/反引号 88% 可指认」** 并拒发。
我把反引号改成「」，**门立刻变绿**。
——**绿的是门，不是事实。** 破绽只是从一个被查的通道挪到了一个不被查的通道。

### 「」为什么不在通道表里：那是量过的决定，不是疏漏

`check_answer_surface_leak` 的源码里写着：四人实测「」可利用率
**16% / 19% / 31% / 31%**，两侧都在用；且**删引号会同时弄瞎三道引文判据**
（`check_verbatim_quotes` / `check_quote_locator` / `check_quote_integrity`
正是靠 `「…」` 认出「这里有一条逐字引文」的）。

★ **Carver 是 81%——那个「两侧都在用」的前提，在本人物身上不成立。**

### 为什么它堵不上

**候选带引号，是因为它在引语料；基线没有语料可引。**
这不是格式习惯的差别，是**有没有根据**的差别——
而「有根据」正是这个产品声称提供的东西。

**任何忠实的候选都会比基线更常引用。要让两侧在这一点上无法区分，
只有两条路：要么让基线也有语料可引（那它就不是基线了），
要么让候选别引（那它就不是这个产品了）。**

### 我做了什么、没做什么

- **做了**：把「」加为**只报不拦**的测量项，每轮都印出来，并写明
  「改法绝不是把候选的引号删掉」。
- **没做**：没把它设成拦截通道（删引号的代价更大），
  **也没有假装它不存在**——第 1 轮的判分结果附着这个数一起记。
- **没做**：没有为了压低这个数去给基线注水编引文。**那是编造。**

### 你说／我做

| 你说 | 我做 |
|---|---|
| **A. 认了，记为残余泄题**（我的建议） | 每轮照印这个数，判分结果永远附带它；不改判分设计 |
| B. 让评委看不见引号 | 派发前把两侧的「」统一剥成裸文本——**但那会弄瞎三道引文判据，且评委再也看不出哪句是引文** |
| C. 让基线也有据可引 | 给基线也配语料——**那它就不再是「没有本产品时的样子」**，delta 的含义随之改变 |
| D. 改判分范式 | 不再用 A/B 盲判，改成按 rubric 逐题绝对分——与待裁定 ⑤／⑦ 是同一个决定 |

★ **这一条与 ⑤（判分设计）、⑦（盲判轮次不重随机）同源**，宜一起裁。

---

## ⑭ ✅ **已裁定（2026-08-05）：长度门对裸模型基线不适用**

> **用户裁定：选 A。** 长度两条对 `bare-model-run` 基线降为**只报不拦**；
> 派发时把长度差写进评委指令让其明知并回避；
> delta 记为**有能力证据但盲性受限**；**已入库 100+ 人的 delta 性质不变**（仍不作能力证据）。
>
> **已落地**（`check_answer_surface_leak` v0.0.0.132）：
> - `LENGTH_EXEMPT = {bare-model-run, prior-version}`；
> - `self-authored-strawman` / `unknown` **照旧硬拦**——那是历史 100+ 人的类型，
>   一并放宽等于把这道门废掉；
> - **只放长度两条**，格式/项目符号/标题行/空行分段照旧硬拦；
> - **免拦不等于免报**：数字照印，并强制附「盲性受限」那句话。
> - 五条射程反向对照已写进自测（含「格式通道对裸模型也硬拦」「免拦仍报出」）。

### 原始记录（保留）

#### 两道门互相排斥：要能力证据，就过不了盲判门（2026-08-05，Carver #127 实测撞出）

### 事实

Carver #127 第 3 轮，我按 `check_baseline_provenance` 的明文要求，
派独立子代理跑了一次**真正的裸模型基线**（无语料、无本技能、无工具、不联网，16 题原文一字未改）。

拿它去建盲判载荷，`check_answer_surface_leak` **三条同时拦**：

| 判据 | 实测 | 门 |
|---|---|---|
| 总体均长比 | **0.46** | 0.77–1.30 |
| 候选更短的题 | **14/16 = 88%** | ≤75% |
| 空行分段可利用率 | **88%**（候选 0/16、基线 14/16） | ≤75% |

**裸模型的答案平均是候选的 2.2 倍长，且题题分段。**

### 我先把能修的修了

对**两侧同样**做空行归一（`\n\s*\n` → `\n`）——**一个非空白字符都没动，已断言校验**。
空行分段一项随即降到 候选 0/16、基线 0/16。

**但长度两条修不掉**（归一后仍是 0.467 / 88%）。要修只有三条路，**没有一条是诚实的**：

1. **给候选注水** —— 为满足盲判约束去把产物写长。★ 产物的形态本来就是「给一句就停，不铺陈」，
   注水是**为了尺子去损坏产物**。
2. **叫裸模型写短点** —— 那它就不是裸模型实答了，`bare-model-run` 的资格当场作废。
3. **改门** —— 这正是「为凑数放宽判据」，且门不该由我在自己被拦时去动。

### ★★★ 这不是 Carver 的问题，是两道门的结构冲突

- `check_baseline_provenance --strict`：基线**必须**是 `bare-model-run`，
  否则 delta 不得用于「比裸模型强」，且**打包直接不过**。
- `check_answer_surface_leak`：两侧**不许**在表面特征上系统性可分，否则**不许派发评委**。

**而一个真正的裸模型基线，必然在长度与排版上与蒸馏产物系统性不同。**

> **结论：在现行流水线里，「盲判」与「能力证据」二者只能取其一。**

★★ 这也解释了本项目的历史：**已入库 100+ 人全部是 `self-authored-strawman`**——
**那是唯一能同时过这两道门的基线类型**，因为两侧都由我写，长度与排版自然可控。
换句话说，**盲判门一直是靠「基线也是我写的」才过得去的。**

### 你说／我做

| 你说 | 我做 |
|---|---|
| **A. 长度门对 `bare-model-run` 基线不适用**（我的建议） | 长度两条降为**只报不拦**（排版归一照做）；派发评委时把长度差写进指令让其明知并回避；delta 记为**有能力证据但盲性受限** |
| B. 盲判优先 | 退回 `self-authored` 基线；**接受 delta 永远不能作能力证据**，并在每份产物上照此标注 |
| C. 两个都要 | 需要第三种基线：裸模型实答后，**由与我无关的第三方**做等长改写——本机做不到，且改写本身引入新变量 |
| D. 换判分范式 | 不做 A/B 盲判，改按 rubric 逐题绝对分——**两侧不再互相比较，长度不再是通道**。与 ⑤／⑦／⑬ 同一个决定 |

★ **为什么必须由你定**：三条路都要**改动一道已冻结的门**，而我此刻正被那道门拦着——
**被判据拦住的人去改判据，无论理由多正当，都是最该由别人裁的情形。**

### 当前状态

- 裸模型基线与运行记录已逐字留档（`evals/round3/_bare_model_raw.json`、`_baseline_run_prompt.md`）。
- **第 3 轮载荷未派发**——判据说了不许派，我没有绕过它。
- 第 2 轮的结果（delta +0.4906，`self-authored-strawman`）仍然有效，**且已标明不作能力证据**。

---

## ⑮ **给不给评委 rubric，决定 delta 的正负号**（2026-08-05；★ 已用同人物对照复核；★★★ **2026-08-05 又在第二个人物上复现**）

> ### 复现：Thomson #129（不同人物、不同语料、不同题目、四个互不通气的评委席）
> 
> | | Carver #127 | **Thomson #129** |
> |---|---|---|
> | 有 rubric | +0.3791（16/16） | **+0.4516 → +0.4122 → +0.4084（三轮都 16/16）** |
> | **无 rubric** | **−0.2019（1/16）** | **−0.1522 → −0.0756 → −0.0859（2/4/4）** |
> | 摆动 | 0.5810 | **0.6038** |
> 
> ★ 与 Carver 那次不同：**这一次是拿着 rubric 打出 16/16 的那两席自己指认的。**
> 席 D：「rubric 把原字符串、卷次、连三处 OCR 讹字都预先写了出来……
> **我无法区分某一侧是真检索到的还是照着 rubric 复述的**；
> 那几个接近满分的分数**证明的是字符串对齐，不是检索能力**。」
> 
> ### ★★★ 更硬的一条：两把尺子对**同一次改进**指相反方向
> 
> A/B 归属三轮逐题不变、基线一字未动，只改候选那 9 题：
> 
> | | 有 rubric | 无 rubric |
> |---|---|---|
> | **改过的 9 题**（R1→R3） | +0.4217 → +0.3394　**−0.0822** | −0.2472 → −0.1289　**+0.1183** |
> | 没改的 7 题 | +0.0071 | −0.0007 |
> 
> **没动的 7 题两把尺子都只动了不到 0.01——对照干净。**
> 前面那些数说的是两把尺子给的**分**不同；这一条说的是两把尺子对**改进的方向**判断相反。
> **后者更硬，因为它不依赖任何一次绝对分。**
> 
> ### ★★ 回扫：**已入库的 Carver #127 也有同病，6/16 = 38%**
>
> 用 Thomson 那轮落成的判据回扫 Carver 的 `judge_prompts/v1.md`：
> **16 条逐题 rubric 里 6 条把「本库没有收录 X」这类资料层状态指定为正确答案**
> （`gwc-boundary-01`、`gwc-trajectory-01`、`gwc-style-decoy-01`、
> `gwc-task-completion-01`、`gwc-capability-calibration-01`、`gwc-long-horizon-01`）。
> 原文形如「**本库 41 件里没有任何一句涉及**」「**本库没有该比例的数据**」。
>
> **Carver 是已入库产物，delta 记的是 +0.3791。**
> Thomson 同一把尺子量得 **10/16 = 62%**。
> → **两个人物都不是个例，是这套 rubric 写法的通病。**
> ★ 只有这两人有逐题 rubric 文件；**其余 100 个入库产物用的是旧格式，量不了**。
>
> ### ★★★ 规则已落成判据，第一个按新规则写的人物两项全清
>
> | | 判据要求出戏 | rubric 抄答案原文 |
> |---|---|---|
> | Thomson #129 | 10/16 = 62% | 8/16 = 50% |
> | **Carver #127（已入库，delta 记 +0.3791）** | 6/16 = 38% | 7/16 = 44% |
> | **Adams #131（第一个按 v2 规则写的）** | **0/16** | **0/16** |
>
> ★ Adams 的自查是**对着真候选答案跑的**，不是只跑题面——
> 「rubric 有没有把答案抄进去」只有拿真答案比才算数。
>
> ★★ **这不改变已入库那 101 人的读法**（他们的 rubric 是旧写法，且多数量不了）。
> 它只说明一件事：**新写法做得到，而且做得到不靠放宽任何门。**
> ⑮ 要裁的仍是那四个选项——**但现在多了一个已验证的事实：
> 「rubric 须由不看产物的人先写」这一档，在实践上不是空话。**
>
> ### 已接进门（派发之前，只报不拦）
>
> `check_persona_frame_break` 已接进 `build_blind_payload`，
> 与引文坐标门、表面特征泄题门同一位置。**拿 Thomson 第 1 轮那份真载荷回跑，
> 当场报出「判据把资料层答案指定为正确 10 题」**——
> 这道门若当时就在，那 10 条会在派发第 1 轮之前被看见。
>
> ### 根因已定位并落成判据
> 
> 实测 **10/16 道 rubric 把「本库没收录 X」这类资料层答案指定为正确**，
> 而同一份指令第 3 条要评委扣「局部出戏」——**判据自相矛盾**。
> 席 E：「1937 年过世的 Thomson 不可能知道一次 OCR 裁边……
> **这套判据在惩罚守住人物、奖励破框。**」
> → `scripts/check_persona_frame_break.py`（v0.0.0.137），**同时查产物和判据**。
> → rubric v2 待办：**禁止把库存状态指定为正确答案**，照 q-07 的样子译成角色内的说法。


### ★★★ 2026-08-05 追加：同人物对照做完了，混杂已排除

原记录的两次实测是**不同人物、不同题数**，我因此不能排除「符号翻转来自人物差异」。
已补跑同人物对照：**同一批答案、同一 A/B 归属，唯一变量是评委手里有没有 rubric。**

| | 有 rubric | **无 rubric** |
|---|---|---|
| 候选 / 裸模型 | 0.9263 / 0.5472 | **0.6825 / 0.8844** |
| **delta** | **+0.3791** | **−0.2019** |
| 候选胜的套组 | **16/16** | **1/16** |

**摆动 0.5810，产物侧掉 0.244、裸模型侧涨 0.337——两边同时朝相反方向动。**

★★ **两次独立实验（不同人物、不同题数、相隔三天），无 rubric 下产物唯一赢的都是
`fact-preservation`**（Livermore +0.247、Carver +0.185）。**不再是巧合能解释的。**

★ 摆动最小的是 `identity-routing`（−0.060）——**那题的 rubric 只要求指名
Thomas Nixon Carver，是客观事实，不依赖我的偏好。** 摆动大的全是我写了「须克制／须给坐标」的题。

### 对照评委自己的话，点破了分歧的实质

> 「诚实划界是加分，但**把『我不知道』当成不必给内容的借口就不是**。」
> 「越界的拒得好，**本行内的（土类、做法、生平）拒了就是失职**。」

**我的 rubric 把「不在手边就说不在手边」定义为正确行为；
不看 rubric 的读者把同一个行为读成推卸。**
★★★ **两种读法都不荒谬——哪一种算对，是产品定位问题，不是测量问题。**
**这正是它必须由你裁的原因。**

（详细数据：`_corpora/wip-carver-127/_rubric_control/RESULT.md`）

### ★ 已入库 101 人的暴露面：**比想象的小，已实测**

我担心 101 份 team-card 里散着「比裸模型强」这类被推翻的主张，去数了一遍：

| 检查 | 结果 |
|---|---|
| 含「优于／强于／outperform／better than」等比较级 | **0 / 101** |
| 提到 delta 或四位小数分数 | 3（Carver、Livermore、Bhadeshia） |
| 其中真的在讲能力对照的 | **只有 Carver 一个**（且已改写为实测射程） |

★ Livermore 那条是**语料比值**（`primary_ratio 0.9887` vs `own_voice_ratio 0.0076`），
Bhadeshia 那条是**身份族权重**（`technical-engineer:0.8`）——**都不是能力主张。**

**所以 ⑮ 要裁的不是「回去改 101 份产物」，而是「今后怎么报」。**

### ★★ 一个连带发现：**唯一站得住的那一项，是用 1 条用例量的**

两次实验里唯一两边都为正的是 `fact-preservation`。而它的用例数：

| profile | `min_suite_cases` | 该套组实际用例数 |
|---|---|---|
| quick | 1 | **1** |
| standard | 1 | **1** |
| deep | 2 | **2** |

全库 14 个已出题的工作区实测：**2 人各 1 条、12 人各 2 条。**

★ **也就是说：整个项目最经得起换尺子的那个结论，每人只由 1–2 条用例支撑。**
这与 ⑫（16 题分不出 quick 档自己的门）是同一个根：`min_suite_cases`。

★ **更正一处我差点写错的**：我先看到「3 个工作区 `fact-preservation` 为 0」，
差点报成缺陷。去查是**那三个工作区的用例总数就是 0**
（Benardos 刚建、Liebig 已延后、Semmelweis 没走到判分阶段）——
**不是漏了这个套组，是还没出题。** 已核，不作数。
### ★★★ 2026-08-05 补：把「包里的 delta」全量量了一遍

107 个已入库 zip，逐个打开 `audit/evaluation-summary.json`：

| | |
|---|---|
| delta 为 `null` 的 | 2 |
| **带具体 delta 数的** | **105** |
| 其中带 `delta_reading_constraints` 的 | **1**（Carver v0.0.0.5，今天刚加） |
| 这些 delta 的分布 | 最小 **+0.1903**、中位 **+0.2894**、最大 **+0.9466** |
| **≥ +0.5 的** | **32 个** |

★★★ **把它与本项目诚实量过的数并排看**：

| 量法 | delta |
|---|---|
| 无 rubric、裸模型基线（v0.0.0.21，32 题） | **−0.1075** |
| 无 rubric、裸模型基线（Carver 同人物对照） | **−0.2019** |
| 有 rubric、裸模型基线（Carver R3） | +0.3791 |
| 自撰基线、拒发者（Mendel／Barton） | +0.0359／−0.0273 |
| **已入库 105 个产物** | **+0.1903 … +0.9466** |

**两个区间不重叠。** 已入库那一批的下限（+0.19）比诚实量法的上限（+0.04）还高一截。

★ **这不能直接说成「都虚高」**——已入库的是**过了门的那批**，本身就选了高 delta，
存在选择效应。**但也不能说成「它们更强」**：
`check_baseline_provenance` 的文件头早写过「已入库 100 人的全部 delta 都是自撰稻草人性质」，
而 Livermore 实测的虚高幅度是 **0.9087**（稻草人 +0.8012 对真值 −0.1075）——
**与这里 32 个 ≥ +0.5 的量级对得上。**

★★ 所以 ⑮ 的选项 A（两种都跑都报）与 B（一律不给 rubric）**代价不同**：
A 只影响今后；**B 意味着这 105 个数要么重跑、要么统一标注为不可比。**

★★ 但**包内的 `audit/evaluation-summary.json` 是另一回事**：
它逐个产物都写着 `candidate_baseline_delta`，**不带任何说明**。
Carver 已补 `reports/DELTA-READ-ME-FIRST.md` 随包分发；**其余 100 个没有。**
（这一条并入 ⑮ 的选项 A/B 一起裁。）

---

### 原始记录（两次不同人物的实测）

### 事实：同一个问题，两次实测，符号相反

| | v0.0.0.21（2026-08-02） | Carver #127 R3（2026-08-05） |
|---|---|---|
| 基线 | 裸模型子代理（**只许读题、不许检索**） | 裸模型子代理（无语料/无技能/无工具） |
| **给不给评委 rubric** | **不给** | **给了** |
| 规模 | 32 题 × 2 席 = 64 对 | 16 题 × 2 席 = 32 对 |
| 产物 / 裸模型 | 0.7369 / **0.8444** | 0.9263 / 0.5472 |
| **delta** | **−0.1075** | **+0.3791** |
| 逐对胜负 | 产物 10 / **裸模型 54** | 产物 16 套组全胜 |
| 长度 | 515 / 471，**不构成偏向** | 裸模型是产物 **2.1 倍** |

### ★★★ 翻转的方向精确对得上「rubric 形状取自答案」

| 套组 | 无 rubric | 有 rubric |
|---|---|---|
| `known` | **−0.275** | **+0.695** |
| `capability-calibration` | **−0.268** | **+0.110** |
| `long-horizon` | **−0.230** | **+0.405** |
| `fact-preservation` | +0.247（**唯一正值**） | +0.610 |

**在无 rubric 那次最负的三个套组上，有 rubric 时全部翻正、且幅度最大。**

v0.0.0.21 当时不给 rubric 的理由，原文写着：
> **两轮评委都点名我的 rubric「形状取自答案」，给了就等于告诉评委我想要什么。**

### 这不是两个都对的数，是同一件事的两种量法

- **无 rubric**：评委按自己的判断评「哪个答得好」。
  → 裸模型赢，因为它写得更全、更流畅、更像通常意义上的好答案。
- **有 rubric**：评委按我写的条目核对。
  → 产物赢，因为**那些条目是照着产物的形态写的**（该给坐标、该克制、该拒绝）。

★ 两次唯一一致的结论：**在「答案能不能被核」这一条上，产物确实优于裸模型**
（`fact-preservation` 两次都正）。**其余各项的符号取决于量法。**

### 你说／我做

| 你说 | 我做 |
|---|---|
| **A. 两种都跑，两个数都报**（我的建议） | 每人跑两次判分：有 rubric 一次、无 rubric 一次，**产物上并列标两个 delta**。成本翻倍，但这是唯一诚实的报法 |
| B. 一律不给 rubric | 回到 v0.0.0.21 的协议。**已入库 101 人的 delta 全部作废重跑**，且多半会变成负的 |
| C. 一律给 rubric，但改写 rubric | rubric 必须由**不看产物**的人先写，再据它生成答案——顺序反过来。本机做不到「不看产物的人」 |
| D. 只报 `fact-preservation` 一项 | 那是两次都正的唯一一项；**其余不报 delta，只报「过没过门」** |

★ **为什么必须由你定**：这决定**已入库 101 人的全部 delta 怎么读**，
而不只是下一个人怎么做。与 ⑤（判分设计）、⑦、⑬、⑭ 同源，但**这一条的影响面最大**。

---

## ⑯ ~~两个人的判决，建在缺了他们自己日记的语料上~~ → **本条作废：是我误报**（2026-08-05 当天撤回）

> ## ★★★ 撤回声明
>
> **这一条是假警报，而且我把它当成重大发现报给了用户之后才发现。**
>
> 我说 Barton 缺 4 本日记、Blackwell 缺 4 本日记＋2 份手稿、合计 14.6 万词一手材料
> 「既没进语料也没写进排除清单」。**那 10 份一份不少地躺在 `references/holdout/` 里。**
> Barton 的 holdout 目录正好 4 个、与她那 4 本一一对上；Blackwell 8 个里有这 6 个。
>
> **holdout 本来就必须不在 `raw/`——建模的人不许读它，那是留着判分用的。
> 它不在建模语料里，正是它在正常工作。**
>
> ### 我是怎么错的
>
> `check_staged_but_not_ingested` 只比「抓源暂存」与「工作区 `raw/`」，
> **它不知道 `references/holdout/` 的存在**，于是把**被正确隔离**的材料报成「没进工作区」。
> 我拿着它的输出，**没有去问「那这些文件到底在哪」，就直接升级成了判决可信度问题。**
>
> ★ 讽刺的是：我在同一天里写过两次「判据绿了但指错了文件」「不要把判据的盲区当成事实」，
> **然后原样犯了一次反向的——把判据的盲区当成了缺陷。**
> ★★ 而且我**还把这个错误的判据接进了门**，若不是接完顺手去看
> `local_path` 字段，它会对每一个有 holdout 的人物长期误报。
>
> ### 已做的更正
>
> 1. `check_staged_but_not_ingested` 的 `ingested_names` **加入 `references/holdout/`**
>    （并处理 `.normalized.txt` 的双后缀）。全库重跑：**一手缺口 10 → 0**。
> 2. 修好之后它反而**看见了真问题**：Galen #101（台账 66 / 工作区 **1**）、
>    Harvey #103（105 / **1**）、Vesalius #102（46 / **1**）——
>    **这三人的工作区 `raw/` 里一份 `.txt` 都没有**，是任务 #37 与
>    `check_corpus_presence` 早就点过名的老问题，此前被本件归进「两侧不齐备、无从比」。
> 3. 门里的接线保留（**它现在是对的**），并按修好后的口径报数。
>
> ### 本条真正剩下的东西
>
> **Barton 与 Blackwell 的语料是完整的，判决没有建在残缺语料上。** 原结论全部撤回。
> 若说还剩什么，是 **Galen / Harvey / Vesalius 三人的语料确实不在工作区**——
> 但那是任务 #37 与 `check_corpus_presence` 已记在案的，**不是新发现，也不归本条**。

### ~~以下为原始（错误）记录，保留以便复核我错在哪~~

#### （原始记录·已作废）两个人的判决，建在缺了他们自己日记的语料上

### 事实（`check_staged_but_not_ingested` 全库实跑，我又逐份点开核过）

| 人物 | 台账 | 进了工作区 | **没进** | 其中**一手** | 有意排除 |
|---|---|---|---|---|---|
| **Barton #117** | 216 | 210 | 4 | **4** | 2 |
| **Blackwell #118** | 95 | 89 | 6 | **6** | 0 |
| carver / jenner / koch / lister / pasteur / semmelweis | | | 1–3 各 | **0** | 0 |

**合计 19 份没进，其中一手 10 份——全部集中在 Barton 与 Blackwell。**
其余六人那 9 份是**分档未知**（`[?]`），不是已知的一手。

### ★★★ 要害：这两人的一手材料，是**日记**

**Barton 那 4 份**（我逐份点开算过）：
`diary-1864-jan-dec` 12,449 词／`diary-1867-jan-dec` 14,144 词／
`diary-1871-feb-dec` 9,497 词／`diary-1897-may-17-sept-5` 20,983 词
= **57,073 词，第一人称动词 184 处**。来源是 LOC 众包转录的 Clara Barton Papers。

**Blackwell 那 6 份**：4 本日记（1836／1872–74／1900–02／1903–05）
＋ 2 份手稿（解剖学讲稿、《The Position of Women》）= **88,685 词**。

**两人合计约 146,000 词的第一人称材料，既没进语料，也没写进 `_EXCLUDED.txt`。**

### ★★★ 更正一处我自己先写轻了的：**它们不只是缺席，它们还在被计分**

我第一版写的是「没进语料」。**去核之后发现更糟：这 10 行都在 `source-ledger.jsonl` 里，
因此照常计进 `min_sources` 与 `primary_ratio`。**
Blackwell 那 81.1% 的一手占比里，**有 6 份是没有正文的**。

> **门在给没有正文的源发学分。**

★ **为什么至今没发现**：`check_staged_but_not_ingested` 与 `check_corpus_presence`
**两件都没接进任何一道门**，只在 `check_checkers` 元普查里跑——
**而元普查是审计工具，不是门。判据建好而不接线，等于没有。**

★★ ~~更要紧的是：`check_corpus_presence` 对 Blackwell 报 ✓ 95/95，
它按目录在不在算，目录在、正文不在时看不出来。~~
**——这句话也是我错的，当天一并撤回。**
去实测：那 95 行的 `local_path` **逐条解析得到真实文件，0 行落空**，
其中 6 行指向 `references/holdout/…`，**文件确实都在**。
`check_corpus_presence` 解析的是 `local_path` 指到的**文件**，不是目录。
★★★ **它报 ✓ 是对的。我在撤回自己一个错误的同一段里，
顺手冤枉了一件正常工作的判据**——这比原来那个错更该记住。

已把 `check_staged_but_not_ingested` 接进 `run_corpus_text_checks`
（研究门与发布门都跑，只报不拦）。实测：
- Blackwell：`台账 95 / 工作区 89，**没进工作区 6 份，其中一手 6 份**`
- Carver：`台账 41 / 工作区 38，没进 3 份，**其中一手 0**`（分档未知，不是一手）

★ 对照组说明这不是「都这样」：Barton 另有 2 份**是有意排除的且写了理由**
（`lb-unbound-1882-1893` 是日内瓦国际委员会通函、另一份是 Hubbell 的信底——
**都不是她写的，排除是对的**）。**所以这个流程会记排除理由；那 10 份是漏的，不是判的。**

### 为什么这一条要紧

- **Barton #117 已判「三轮用尽、拒发」**（真 delta −0.0273，门 +0.07）。
- **Blackwell #118 产物「已做完」，卡在待裁定 ⑤。**

**日记正是「他怎么想」最集中的地方**——而这两人恰好都是靠书信与报告撑起来的语料。
★★ 我**不能断言**补上就能翻盘（延后/拒发的直接原因是判分，不是语料量），
**但「判决建在缺了主角日记的语料上」这件事，本身就该由你知道。**

### 你说／我做

| 你说 | 我做 |
|---|---|
| **A. 两人各补语料后重跑**（我的建议） | ingest 那 10 份 → 重跑研究门 → **重新出题重判**。★ 但这要动「三轮用尽」那条约束：**是否给他们新的三轮，只能你定** |
| B. 只补语料，不重判 | 语料补齐、台账写明，**判决维持原样**并标注「所依据的语料后来补过」 |
| C. 先只补 Blackwell | 她还没定案（卡在 ⑤），补完再判**不需要动三轮约束**；Barton 已拒发，动他要开口子 |
| D. 记录，暂不动 | 写进台账，等 600 人跑完统一重蒸（任务 #29）时一并处理 |

★ **为什么必须由你定**：A 与 C 都要**碰「每人评审上限 3 轮」这条冻结约束**，
而我此刻正是那条约束的受益者/受害者——**被约束的人不该自己决定要不要解除它。**

## ⑰ C. L. Coffin 的卒年（1926）很可能抄的是另一个人的（2026-08-05 记）

队列表里「C. L. Coffin(1926)」的 1926 —— **与 Charles Albert Coffin 的卒年完全相同**，
而后者正是 **GE 首任总裁、汤姆森—休斯顿总裁、Elihu Thomson（#129）的合伙人**。

实测：
- Charles Albert Coffin：生 **1844-12-31** 缅因州 Fairfield，卒 **1926-07-14** 纽约 Locust Valley
  （Find a Grave 墓志 + 维基 GE 条目，两处一致）。
- 英文维基 `Charles_L._Coffin` 条目写的也是 **1844–1926**，
  **且全文一次都没提到 Charles A. Coffin 或 GE**——两个人四个数字全同，**疑为条目串人**。

**对本次排期的影响：没有。** 他的承重一手是 **1890 年美国专利 428,459**，
按 pre-1929 已出版即公有领域，**与卒年无关**，不需要可得性探测。

**但两件事必须记下来：**
1. **队列的生卒年这一栏至少有这一条不可信**，而 `next_person.py` 正是拿它决定要不要探测。
   → 排期时凡卒年卡在 1930 附近的，**先核这个数本身的出处**，别直接用。
2. **抓源的同名风险极高**：当年电气刊物里的「Coffin」**大量指 Charles A. Coffin**，
   而我刚做完的 Thomson #129 语料里满是汤姆森—休斯顿的材料。
   同名门已跑：`resolution=single`，`selected_subject_uid=coffin-charles-l-welding-detroit`，
   Charles Albert Coffin **已单列并显式排除**。


## ⑱ A/B 位次一边倒：Adams #131 抽到 14/16（2026-08-05 记）

派发第 1 轮时肉眼看见的，**不是任何一道门报出来的**。

### 已实测的数

`assign()` 里 A/B 由 `sha256(case_id) % 2` 决定——**与内容无关、可复现**，
这是为了「各轮之间 A/B 逐条一致」而故意设计的。代价此前没量过：

| 前缀 | A 侧是候选 | |
|---|---|---|
| **`ca`（Adams #131）** | **14/16** | **位次与系统相关度 88%** |
| `et`（Thomson #129） | 8/16 | 正好平衡 |
| `gwc`（Carver #127） | 9/16 | 接近平衡 |

**哈希本身是公平的**（2000 个合成前缀：均值 **8.059**，
偏离 ≥4 的占 **7.55%**，16 次公平抛硬币的理论值 **7.68%**）——
**Adams 只是手气差，不是代码有 bug。**

★★ 但「公平」不等于「够用」：**每 13 个人物就有 1 个落到 ≥12/16**，
600 人算下来约 **46 次**。那时评委任何一点位置偏好都会**直接灌进 delta**。
**这与已裁定的「长度混杂」（⑭）是同一类混杂，只是通道从篇幅换成了位次。**

### 已经做了的（不需要你定）

- `assign(..., balanced=True)`：按 sha256 排序取前一半，**强制 8/8**，仍然只依赖 case_id、可复现。
  自测已证：同一组 Adams 真实题号，取模给 14/16，balanced 给 8/16，**两者 A/B 确实不同**。
- 派发前**新增位次混杂报告**（≥75% 时告警）。★ 这一条本身就是教训：
  **判据不说话就等于不存在**——14/16 是我用眼睛看出来的。

### 要你定的只有一件

**从下一个人物起，`balanced=True` 是否成为默认？**

| 选项 | 后果 |
|---|---|
| **A. 从下一人起改默认**（我的建议） | 位次混杂**从此不再出现**。★ 代价：Adams 及以前的人物**用的是另一套 A/B**，跨人物比较时要记得这条分界线 |
| B. 维持取模，只报不拦 | 与 ⑭ 同样处置。**约每 13 人一次的位次混杂会继续发生**，每次都得在报数时声明 |
| C. 连 Adams 一起改 | **不可行，我不会做**——会让已派发轮次的 A/B 变号，与「各轮 A/B 必须逐条一致」直接冲突，且本轮四席正在判 |

★ **为什么由你定**：这是**中途改测量工具**那条约束的边界问题。
「下一个人物」算不算「中途」——**改默认会在人物之间划一道方法学分界线**，
而我是那条分界线两边所有 delta 的报数人，**不该自己决定分界线画在哪。**

### 本轮报数时必须带上的话

**Adams #131 第 1 轮的 delta 有位次混杂（A 侧 14/16 是候选）**，
与已知的长度混杂（均长比 0.61、候选更短 13/16）**并存**。
两条都不使 delta 作废，但**都必须与 delta 一同出现**。
