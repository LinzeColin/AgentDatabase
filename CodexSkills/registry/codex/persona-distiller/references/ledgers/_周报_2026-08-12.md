# 周报 2026-08-12 —— 账期 **08-06 → 08-12**（七天）

> **上一节点：第 21 轮结算，2026-08-06**（`_额度台账.md`）。
> 本报覆盖那之后到今天。**写完要把节点推到 2026-08-12。**

---

## 一、一句话

**本账期没有新产物入库（registry 仍是 102），产出全在两件事上：
① 把 105 个人按可得性筛出队列；② 判据从 66 件长到 92 件，并第一次系统性地
「查判据自己有没有被查」。**

★ 这句话不好听，但它是实况。上一份周报的判语「这一轮的钱花在了工具和纪律上，
没花在产出上」**在本账期同样成立**——而这次多了一层：
**工具的钱有一部分是花在修工具自己的缺陷上。**

---

## 二、数字（全部现算，不凭记忆）

| | |
|---|---|
| 提交数 | **699**（08-06 151／08-07 15／08-10 226／08-11 244／08-12 63） |
| registry 产物 | **102**，账期内**净增 0** |
| 新增延后 | **105 人** |
| 判据 | 66 → **92 件**（新增 26） |
| 测试 | **73 个**，全过 |
| VERSION | v0.0.0.154 |

### 105 个延后的分布

| 日 | 人数 | 性质 |
|---|---|---|
| 08-06 | 16 | 逐个探测 |
| 08-07 | 1 | 逐个探测 |
| **08-10** | **84** | **批量按出版年筛**——横跨十个族 |
| 08-11 | 4 | 逐个探测 |

08-10 那批的族分布：财务合规师 19、建造采购师 16、客户营销师 12、创业经营师 12、
艺术设计师 8、思想教育师 5、政治法律师 4、农林牧渔师 4、材料建工师 3、软件开发师 1。

**主因是「结构性不可能」**：生年太晚，全部作品落在 `≤1930` 的 PD 分界之后——
**不是材料不好找，是按现行规则永远找不到。**

⇒ 这直接连到待裁定 **㉜′**：`≤1930` 这道门本身要不要保留。
  **本账期它一次就筛掉了 84 个人。**

---

## 三、本账期做完的人物

| 编号 | 人物 | 结果 |
|---|---|---|
| #161 | Pacioli | 延后（新类别⑦：方法证据全汇到一部作品） |
| #162 | Paton | 延后（新类别⑧：盲判装置不成立），**零分数产生** |
| #163 | Littleton | 延后（新类别⑨：成名作全在 PD 分界之后） |
| #164 | Heinrich | 延后（新类别⑩：代表作差一年，2027-01-01 到期） |
| #166 | Cicero | 两轮 delta 都不作数（rubric 与答案同方，**待裁定 ㉟**） |
| #168 | Grotius | 拒发（两轮判完，已结案） |
| #169 | Blackstone | 延后（新类别：可引面与可证面不重合） |
| #170 | Holmes | 拒发（两轮四读数全负） |
| **#171** | **Kelsen** | **除 ㊳ 外全部做完**——研究门过、11 条断言、十份产物、16 道用例、引文 24/24 核过 |
| **#172** | **Brandeis** | **同名判定 + 工作区 + 抓源清单已就绪**，卡在 PD 口径待裁 |

**十个人物，零入库。** 四个新的延后类别是本账期的实质发现：
**「做不了」也有很多种，而每一种都值得单独记，否则下一个人会重走一遍。**

---

## 四、判据侧（本账期真正的产出）

66 → 92 件。**最后一天（08-12）的三件是同一族**：

1. **`check_selftest_reach`** —— 判据的自测走没走到它自己的判定函数。
   实测 89 件里 **36 件有函数从没被自测进入，其中判定类 18 件**。
   ★ 最贵的一件是 `check_holdout_overlap`：11 条断言全绿，验的全是配料，
   **而 `check()` 一次没被进入过——待裁定 ㊲ 的全部依据就是它的输出。**

2. **`check_paper_lanes`** —— `min_lanes` 能靠 `dimensions` 字段凑满。
   全库 39 个工作区、**9 个有纸面道**，其中 **2 个是已发布产物**（⇒ ㊴）。

3. **`check_doc_command_shapes`** —— 文档里的命令与脚本参数形状对不对得上。
   起因：HANDOFF 写着 `--identity <identity.json>`，而它要的是族号——
   **接手方按文档敲的第一条建工作区命令就会失败**，而这条错
   **不是读文档发现的，是真跑 #172 时撞出来的**。

### 修的四处「绿章盖在不知道上」

| 位置 | 原来 | 现在 |
|---|---|---|
| `quality_check` 花体乱码 | 解析失败 → 印 **`✓ 没有花体乱码`** | 「未核（不是通过）」 |
| `quality_check` 引文层 | 失败时既没扫到也没记下 | 读不了的文件单列 |
| `quality_check` 引文层 | 没东西可扫时也是「已扫 0／问题 0」 | 明说「本项没有起作用」 |
| `check_corpus_feasibility` | `meta.json` 读不出 → **静默退到最松的 quick** | 仍按 quick 算，**但必须报出来** |

★ 第四条最实：同一批 12 份材料，**meta 写 `deep` 报「走不完全程」、meta 坏掉报「可行」**
——一个损坏的文件把判决从失败翻成通过。全库 36/36 个工作区都有 meta.json，**11 个是 deep**。

---

## 五、★★ 本账期最值得记的一条方法

**从一个已确认的缺陷形状去 grep 同族，比逐件补自测快一倍。**

全库 57 处「只 `pass`/`continue`/赋常量」的 `except`，逐处读完 → **真正要修的 4 处**，
其余是解析坏行时跳过（有意）。**别按数量恐慌：57 里 53 处是对的。**

同样的做法用在「射程漏扫」上则是**确认干净**——仓里早就用 `rglob` 或显式列两种布局，
`check_verdict_attribution` 甚至有一条专门的反对照证明「只扫一层会漏掉 bob」。

---

## 六、我自己犯的错（判据一次都没抓到）

| | 错在哪 | 差点报出 | 真相 |
|---|---|---|---|
| 1 | 把 `title` 的**问题率**当合格率 | 「从 91.9% 崩到 9.4%」 | 90.6%，**与记录一致** |
| 2 | 用 `max` 扫多个键挑字段 | 未声明 **1160**，近记录两倍 | **624**（把「已声明的 789」算进去了） |
| 3 | 修兜底分支时 `import sys` 没插进去 | —— | **NameError 只在兜底那条路上炸**，正常路径全绿、73 测试照过 |

**1 与 2 都差点报成「既有记录严重失实」，而记录一直是对的。**
⇒ 已写进必读：报「率」先写清分子与方向；**不许用 `max`/`any` 挑字段**；
**复核既有记录时先假定记录是对的**。

---

## 七、账期末的待裁定：**六条**

| | 事 | 我的倾向 |
|---|---|---|
| ㊳ | Kelsen holdout 被我看见 5%（含完整目录） | 不必作废 |
| ㊲ | Carver #127 那 3 段要不要重估 | 不必 |
| ㊲ | 七个**未发布**人物的 holdout 要换掉 | 影响排期，不涉及已发布分数 |
| ㊴ | Livermore #100 / Steinhardt #98 的纸面道 | Livermore 的问题更实 |
| **㊵** | **已判分、未发布的产物能不能改**（收编四类、几十个工作区） | **C：分类处理** |
| — | Brandeis 的 PD 口径（政府文告 vs ≤1930） | 裁定前按 ≤1930 抓 |

★ **㊵ 是本账期的整理成果**：原本散在四条「已量清未修」里，逐条复核后发现
**卡的是同一件事**。你只需裁一次。

---

## 八、移交状态（#87）

分支 `handoff/2026-08-13` 已用本账期全部提交重建并在干净目录整链复验：
**12729 个文件（无语料）｜34 MB（闸 200 MB）｜pre-push 放行｜
92/92 判据自测过｜漂移门绿｜文档命令形状绿｜73 测试过。**

推送由**人**按下（脚本不推送），**推前必须重跑一次脚本**——每多一个提交那个分支就旧一次。

---

## 九、★★ 同日续报（本节写于周报之后，同一天）

上面那份写完之后，08-12 当天还做完了一件成规模的事，**记在这里而不是等下一期**
——下一期的账期从 08-12 起算，它会掉进缝里。

### `check_selftest_reach.KNOWN`：**18 → 0**

首扫 18 件判据「验了配料、没验判决」，**同日逐件补完清零**。
做法固定：**tempdir/临时 git 仓造夹具跑真判定函数 → 变异验证
（每组先跑 M0 正例确认装置成立）→ 真实工作区冒烟 → 同步镜像**。
十三件共 **71 个变异体**，一一对应。

### 补的过程里挖出的真缺陷（不是补自测顺手，是补自测才看见）

| 判据 | 缺陷 | 后果 |
|---|---|---|
| `check_refusal_overflow` | 载荷有两种形状，**扁平那种一条不剩地被跳过** | 去重后 31 份载荷里 **21 份从没被扫过**，而屏幕印「✓ 无拒答溢出：**每一处**…」 |
| `check_material_split` | 两个物料目录都不存在 → 印「不等于通过」，**然后印「结论: 通过」返回 0** | 未落物料的工作区能拿绿章（爆炸半径实测 36/36 俱在 ⇒ 修它零影响） |
| `check_ocr_language_death` | 读不了的文件 `except: continue`，**整条记录消失** | 「扫了 N 份」的 N 里根本没有它 |
| `check_unqualified_priority_claim` | 坏 JSON 行静默丢掉；读不了的文件**直接崩** | 「首创声明 0 处」与「一个单元都没扫到」长得一样 |
| `check_semantic_residue` | **自测抄了一份旧版判定逻辑**（少了「剥 Markdown 强调符」那一步） | 那个修法从来没被测过 |
| `check_version_bump_ships_product` | **`git log --since` 解析不了的日期不报错，悄悄退到「此刻」** | 报告照印「自 X 起」，数的不是那一段 |

### ★ 新增待裁定 ㊶

拒答溢出门修好解析之后全库首扫 **62 条**，**逐条读原文：读了 11 条、9 条是误杀**。
八类误杀已补进 `ACTIONABLE`（62 → 29/588 = 4.9%），**而抽读剩余仍见误杀**。
⇒ 停手，交上来：**A 重做**（我的倾向）／**B 撤掉**／**C 保留现状当候选名单**。
★ 它是 metrics-only、不参与任何 delta ⇒ **不触动已判分数**。

### 本节我自己犯的错（都是变异测试戳穿的，不是我想起来的）

| | 错在哪 | 怎么发现 |
|---|---|---|
| 1 | 变异体全 rc=1 而**一条 ✗ 都没有**——它们死在 `ModuleNotFoundError` | 把**未变异的原件**放进同一装置，它也是 rc=1 |
| 2 | 断言绿着而**走的不是要验的那条分支**（`unchecked` 有两个来源） | 改坏段级那条，自测一条没红 |
| 3 | 五条新断言插在 `if fails: return 1` **后面**——印了 ✗ 而 **rc 仍是 0** | 变异体全打 ✗ 而 rc=0 |
| 4 | 守卫用正则**猜** git 认不认日期，误伤 `30 years ago` | 自测自己就在用那个写法 |
| 5 | 把不稳定的量（3/1）写进断言，实测是 3/0 | 换机器/换秒数就会随机变红 |

### 补完之后顺手做的全库回扫——**看看这些「原先看不见的东西」到底有没有**

补自测的目的是让判据可信；可信之后就该拿它去扫一遍。结果：

| 扫什么 | 规模 | 结果 |
|---|---|---|
| 语料文件读不了的 | **1833 份** | **0 份** |
| 下划线台账（原先静默跳过，不进计数） | 1833 份中 | 27 份，现已标出 |
| 断言层坏 JSON 行 / 读不了 | 46 份 claims.jsonl / **1102 个单元** | **0 / 0** |
| 无限定的首创声明 | 同上 | **0 处** |
| holdout 物理泄漏 | **36 个工作区** | **0 个** |
| 物料与账本成员错配 | 同上 | **1 个** ↓ |

**唯一那一处：Rosenhain #138。**
`references/sources/` 下多出一个目录 `src-8b2f22981b97`，
里面是 `1912-jisi-v85-discussion…**normalized**.txt`——
**同一份源的归一化副本，却用了账本里不存在的新 id**
（原件在账本里是 `src-fb2c35a9a3d7`，`split=train`，指向 `raw/…/…20602.txt`）。

★ 我第一遍读错了，写成「账本完全不认识这份源」——
  grep 那个 **id** 得 0，而 grep **文件名**得 3。**先看原文再下判断。**

★ 另外「账本有而 `sources/` 无」的 3 个 id **正是 3 份 holdout**——那是对的，
  `references/sources/` 本来就不该有 holdout。

**影响面：Rosenhain #138 已记拒发、不在 registry**（真 delta +0.0018，噪声内），
**不涉及任何已发布分数**。⇒ 记档，不动。

⇒ 顺带定一条口径：**归一化／派生副本要么复用原 `source_id`，要么在账本里独立声明。**
  用一个账本不认识的新 id 放进 `references/sources/`，
  建模者从磁盘数会得到 16，而账本说 15。

---

### 研究道 Scope 同步：可动的**是 6 个不是 5 个**（记录少了一个）

台账原记「14 个不同步，9 个已判过分动不得，可动的只有 5 个」。
按 `results.jsonl` 的**真实行数**重分（不按记忆）：**8 个有判分行／已发布，6 个零判分行**。

差的那一个是 **Martens #134**。我第一遍也把他归进「动不得」——
因为拿 `martens` 去 `team-index.json` 里搜命中了 1 处，而那处是
**`martensite`（马氏体）**，Edgar Bain 的特征词。
⇒ [[regex-must-clear-the-corpus-language]] 那一族的原版：`lister ⊂ callister`。
**子串匹配不算「在名册里」。**

已同步 6 个（22 → 28）；仍不同步的 8 个**全部有判分行**，按 ㊵ 未裁定前不动。

★★ **搬手写判断，不是覆盖它。** 工具警告有 12 份道文件的手写行会被静默抹掉，
其中包括 Pacioli 06-timeline 那句「三份来源在卒年与任教城市上互相不一致，
**产物里不许悄悄取一个当确定值**」——那是研究结论，不是台账。
先把它们搬进 `Source-linked observations`，再让工具重出 Scope。

★★★ **搬完之后我自己把三个工作区改红了**：我加的注释里写着
反引号包住的英文段名，而 `check_lane_quotes_verbatim` 把反引号里的英文
**当成一条待核引文**——它当然核不到。改动前三个都是 rc=0，是我改红的。
去掉反引号后三道门（Scope 同步／holdout 提及／引文逐字）全绿。
⇒ [[prose-inside-templates-hits-metacharacters]] 的同族：**我写的散文踩了我自己的判据。**

---

### 证据塌缩：两条记录对不上，现算对齐了

任务表里同时挂着「证据塌缩定案 **17** 条」与「Koch **41** 条（不是 15）压在一对源上」。
跑 `check_claim_source_independence --workspace` 全库现算：

| 工作区 | 塌缩条 | 压在几对源上 | 判分行 | 能不能动 |
|---|---:|---:|---:|---|
| koch-107 | **15** | **1 对**（`src-9115214f10fd` ／ `src-94dc006b6b8a`） | 192 | **动不得** |
| nightingale-112 | **2** | 2 对 | 192 | **动不得** |
| **合计** | **17** | | | |

⇒ **17 是对的；「Koch 41」不成立**（现算 15）。两人**都已判过分**，
  与 ㊵ 同一条红线，**没有「不必裁定就能修」的子集**。

★ 途中我自己造了两次假数，都是取数写法的问题：
① 第一版用位置参数跑判据（它只吃 `--workspace`），每次都报错、stdout 为空，
   **全库合计得 0**——我差点写成「塌缩已清零」。
② 第二版按目录名取判分行，Nightingale 的工作区不叫 `wip-*`，
   于是判分行读成 0、被标成「可动」——**与今天 Martens 那次同形**。
   改成「路径里第一个以 `wip-` 开头的段」才对。
⇒ **两次都是「空/零」冒充答案。** 报数之前先问一句：这个 0 是真的没有，还是我没取到。

---

**⇒ 一句话：这一天真正学到的是「怎么验一个判据」，而不是「又补了几件判据」。**

---

**★ 节点推进：下一份周报从 2026-08-12 起算。**
