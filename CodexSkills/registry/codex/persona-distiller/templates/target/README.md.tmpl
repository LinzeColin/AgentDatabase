# {{TARGET_NAME}} 人物 Skill

- 构建器：Persona Distiller `v0.0.0.3`
- 目录/Skill 名：`{{SLUG}}`
- 构建主身份：{{IDENTITY_DISPLAY}}
- 主身份规范值：`{{IDENTITY_CANONICAL}}`
- 主场景：{{SCENARIO_DISPLAY}}
- 来源类型：`{{SUBJECT_ORIGIN}}`
- 研究档位：`{{PROFILE}}`
- 模型版本：`0.1.0-draft`
- 创建时间：{{CREATED_AT}}

每次实质调用必须重新选择身份或权重；场景由任务自动路由。用户也可以把选择和任务写在同一条消息中，例如：

```text
1｜用他的方式审查这个技术方案
1:60+5:40｜设计一套研究与教学计划
```

安装：

```bash
python3 install.py
```

默认安装到 `~/.codex/skills/{{SLUG}}`。研究工作区包含原始/受限材料；发布 ZIP 默认只带运行所需模型、审计摘要和安全更新模板。
