# Codex Task Pack - Teleiosis v0.0.0.3

Use the outer taskpack `scripts/publish_main.py`. Do not edit source, redesign the run contract, create a branch/PR, or require fixed repository/file SHA. The script fresh-clones latest main, semantically integrates the payload, runs native and v3 compatibility tests, updates Catalog, commits, fetch-checks main, retries on remote movement, and fast-forward pushes main.
