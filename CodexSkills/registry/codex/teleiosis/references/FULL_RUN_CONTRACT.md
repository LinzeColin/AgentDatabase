# 完整 Run 合同

一轮固定为 `T→C→S→C→P→C→A→C`，三轮一组，三组一次 Run。36 个引擎阶段共享同一 Candidate lineage；每阶段内部执行 Baseline/Discovery、Adversarial/Stress、Adjudicate/Stabilize 三个 pass。

每项能力必须给出状态与证据。错序、漏模块、缺能力、未知字段、缺 evidence、Candidate hash chain 断裂、隐藏集泄漏、内部 formal PASS、Workspace 污染和嵌套 Run 全部阻断。
