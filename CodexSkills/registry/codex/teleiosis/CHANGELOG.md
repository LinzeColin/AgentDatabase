# Changelog

## v0.0.0.1 / Genesis-locked self-evolution r31 — 2026-07-26

- Detected a release-document ambiguity only after the r30 archive passed package, install, upgrade and rollback validation: the Codex Task Pack named a different ZIP from README and installation guidance.
- Standardized every operator-facing instruction on `White-Box-Iteration-Skill-Teleiosis-v0.0.0.1-final.zip`; no runtime slug, version or installation semantics changed.
- Added regression coverage that extracts release archive names from README, installation guidance and the Codex Task Pack and requires one canonical value across all three.
- Kept formal autonomous promotion blocked when genuine external 2x6 independent SubAgents and a separate read-only verifier are unavailable.

## v0.0.0.1 / Genesis-locked self-evolution r30 — 2026-07-26

- Converted a self-hosting orchestration failure into a product-level diagnostic improvement: review evidence is now preflighted before any immutable packet is written.
- Aggregates every missing, unsafe or malformed evidence reference in one fail-closed error instead of exposing one stale path per rerun.
- Reuses the validated content bindings for all twelve packets, reducing repeated hashing and avoiding partial review-plan creation.
- Added regression coverage proving that multiple stale evidence paths are reported together and no review packet is emitted on failure.

## v0.0.0.1 / Genesis-locked self-evolution r29 — 2026-07-26

- Corrected the evaluation semantics for defective stable versions: a baseline hard-gate failure is now recorded as a baseline defect rather than treated as an evidence-integrity failure.
- Retained fail-closed behavior for ambiguous baseline identity: a baseline still must resolve to one exact tree hash, and every candidate hard-gate failure remains non-compensatory.
- Added regression coverage proving that a candidate can repair an unsafe or otherwise non-compliant baseline without weakening the frozen evaluation contract.
- Kept the external-review boundary unchanged: engineering evidence can mature the candidate, but formal autonomous promotion remains blocked without genuine 2×6 independent SubAgents and a distinct read-only verifier.

## v0.0.0.1 / Genesis-locked self-evolution r28 — 2026-07-26

- Merged the durable, non-recursive installer branch with the stricter white-box governance branch instead of choosing one and losing the other.
- Added an external archive SHA-256 trust anchor for release/deep installation while retaining private input freezing, transaction receipts, interruption recovery, atomic switch and content-bound rollback.
- Fail-closed on malformed run paths before filesystem traversal, and bounded evidence-command and optional Git-provenance scans.
- Kept the activation kernel below the frozen Baseline cost envelope; deterministic rules remain in Gate, Schema, scripts and tests.
- Expanded the regression suite to 139 tests and added current optional adapters for paired trajectory audit, lifecycle credit, process-use rubrics and privacy-preserving semantic-diff aggregation.
- Formal autonomous promotion remains blocked when genuine 2×6 independent SubAgents and a distinct read-only verifier are unavailable; engineering release evidence remains independently reproducible.

## v0.0.0.1 / Genesis-locked self-evolution r27 — 2026-07-26

- The self-hosted frozen evaluation rejected r26 because the activation kernel exceeded the Baseline cost tolerance even though safety, truthfulness, installation and latency passed.
- Kept the frozen evaluation unchanged and compressed `SKILL.md` from 5,479 to 4,753 characters by moving installation recovery detail and reference descriptions behind progressive disclosure.
- Reduced the estimated activation kernel from 1,369.75 to 1,188.25 four-character tokens, below the frozen Baseline estimate of 1,228.5, without deleting any Genesis hard gate or runtime capability.
- Preserved complete install, recovery and reference instructions in `delivery/INSTALL.md` and the referenced control-plane documents.

## v0.0.0.1 / Genesis-locked self-evolution r26 — 2026-07-26

- Froze the source ZIP to a private installer snapshot before extraction and rejected concurrent input mutation or oversized archives.
- Hardened internal controller paths: lock files use no-follow opening where available; transaction directories and receipts reject symbolic links and use private permissions.
- Closed interruption windows before the atomic switch: an interrupted predecessor rename can reconstruct a receipt from the pre-frozen old-tree hash, while abandoned incoming copies are removed only when their generated identity is provable.
- Serialized explicit rollback against installation/recovery and retained fail-closed behavior for ambiguous state.
- Added durable elapsed-time and output-size evidence to the top-level self-test result.
- Expanded the regression suite to 132 tests covering archive mutation, linked control paths and both new recovery branches.

## v0.0.0.1 / Genesis-locked self-evolution r25 — 2026-07-26

- Reproduced and removed recursive release-install validation: normal installation now uses strict validation plus a bounded non-recursive smoke; the release packager runs the full suite once.
- Added durable install transaction IDs, atomic external result files, `install-status`, hash-verified reconciliation and fail-closed recovery.
- Added process-scoped exclusion between installation and recovery, and rejected symlinked control paths.
- Upgraded new rollback receipts to schema 1.1 so the manifest is content-bound; retained explicit schema 1.0 compatibility.
- Added subprocess elapsed-time evidence and independent configurable fast/deep timeouts.
- Expanded the frontier registry through July 2026 with SkillAxe, MIND-Skill, SkillMaster, OpenClaw-Skill, MUSE-Autoskill, Formal Skill and SSL representation; mechanisms remain optional adapters rather than Genesis constraints.
- Removed complete regression execution from ordinary Skill activation to avoid repeated runtime and token waste; deep tests are tree-bound release evidence.


## v0.0.0.1 / Genesis-locked self-evolution r17 — 2026-07-26

- Renamed the Chinese display name to **白箱迭代Skill** while retaining the Teleiosis brand and `teleiosis` runtime slug.
- Locked the user-authored 27-requirement Genesis by byte-preserving source plus status-only locked copy and external SHA-256 anchor.
- Separated permanent principles from replaceable implementation: strategy lane and custom release-profile namespaces are open rather than fixed enums.
- Rebuilt target and optimizer self-evolution as one canonical Candidate with immutable Baseline, exact diffs, failed-candidate retention and rollback.
- Fixed zero-budget semantics: unused zero allowance is valid, positive use against zero is a violation, and an exact positive limit stops cleanly.
- Added run seals binding Genesis, research, evaluation, holdout pointers, review/release policy and external attestation contract before Candidate mutation.
- Added real competitor discovery/pull/static-inspection pipeline with exact commits, safe archive extraction, licence/provenance records and default no-exec supply-chain boundary.
- Added multidimensional evaluation, protected-task regression checks, exact Candidate tree binding and Pareto selection without compensating hard failures.
- Added external review-attestation adapter boundary: local claims cannot satisfy formal 2×6 review or the distinct thirteenth verifier.
- Hardened deterministic packaging, archive safety, external release receipts, atomic install, backup and rollback.
- Split fast structural verification from release-level deep verification to prevent recursive test amplification while preserving final-release rigor.
- Expanded the bundled regression suite to 94 tests before formal self-run evidence packaging.

## v0.0.0.1 / Genesis-locked rebuild — 2026-07-26

- Replaced the prior implementation constitution with the user-provided Genesis Baseline.
- Removed permanent version lock, fixed extension ceiling, one-architecture-reset ceiling, GitHub-only worldview, mandatory visual showcase and unanimous soft-opinion rule.
- Integrated Darwin, Luban, current primary methods, progressive disclosure, release/reheat and dual white-box governance.
