# 原始证据合同

每个本地门保存：命令、开始与结束时间、退出码、stdout、stderr、环境、Subject hash、输出文件和 SHA-256。`RAW_EVIDENCE_INDEX.json` 必须逐项引用真实文件并核验 hash；摘要只能帮助阅读，不能替代原始日志。

可保存的本地证据必须随 ZIP 提供。目标 Registry、凭证、生产控制台、原生竞品、真实用户和外部独立 Verifier 只能在授权环境中补齐，并保持未运行状态直到真实执行。
