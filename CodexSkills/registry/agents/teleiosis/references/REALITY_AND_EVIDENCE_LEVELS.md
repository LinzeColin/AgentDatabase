# 现实与证据等级

`SYNTHETIC`、`CONTROLLED_HUMAN`、`FIELD_OBSERVED`、`PRODUCTION_BLIND` 必须显式区分。模拟流量、Fixture、方法代理、同模型角色扮演和 LLM judge 不能升级为真实用户、市场采用、生产结果或独立终审。

所有“领先”结论必须带有效期、比较集合、模型、预算、数据、指标、区间、硬门、来源和未知边界。环境或竞品变化触发 `REHEAT_REQUIRED`。
