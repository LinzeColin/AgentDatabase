# Prompt Grammar v0.0.0.2

通用语义顺序：主体锚点 → 可见动作 → 一个主要相机行为 → 环境/空间 → 光线/材质 → 时间 → 反馈 → 声音 → 结束状态 → 不变量。

这不是固定输出模板。模型渲染器可改变：字段名、段落、时间码、Reference 标签和长度预算，但不得改变事实层。

禁止：抽象形容词堆积、同一短镜头多重冲突运镜、无结束状态、I2V 重述并重绘输入图、Reference 素材无角色、工业反馈无因果、参数与正文混写。
