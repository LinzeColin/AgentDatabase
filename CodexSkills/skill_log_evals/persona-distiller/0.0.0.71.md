# v0.0.0.71 —— 不再一个个修射程，改修这一类

日期：2026-08-04

## 触发

v0.0.0.70 修完第四次「判据绿了却指错了文件」之后，
翻账发现**前三次我也都只修了那一个实例**：

| # | 判据 | 表征 |
|---|---|---|
| 1 | `check_holdout_overlap` | **从来没被调用过** |
| 2 | `check_contract_drift` | 只存在于一侧的文件**静默跳过** |
| 3 | `check_corpus_presence` | 账本与文件是**两批不相干的东西**，相减得出「很健康」 |
| 4 | `check_corpus_presence` | root 传高一层，17 个工作区被 **collapse 成一行**，报绿 |

**四次的表征完全一样：一片绿。**

## 实测输出（第一版，错的那一版）

```
该扫 **15** 个工作区，实际扫了 **15** 个

✗ **少扫 11 个——判据即使全绿也盖不住它们**：
    _corpora/wip-fleming-111/workspaces/alexander-fleming/alexander-fleming
    _corpora/wip-godin/ws-godin/seth-godin
    _corpora/wip-jenner-104/ws-jenner/ws-jenner
    _corpora/wip-koch-107/workspaces/robert-koch
    _corpora/wip-lister-108/workspaces/joseph-lister
    _corpora/wip-nightingale-112/workspaces/florence-nightingale/florence-nightingale
    _corpora/wip-osler-110/workspaces/william-osler/william-osler
    _corpora/wip-pasteur-106/workspaces/louis-pasteur
    _corpora/wip-semmelweis-105/workspaces/ignaz-semmelweis
    _corpora/wip-steinhardt-98/ws-steinhardt/michael-steinhardt
    _corpora/wip-virchow-109/workspaces/rudolf-virchow/rudolf-virchow
```

**计数对得上（15 = 15），名字一个都不匹配。**
`roster()` 报账本所在的**工作区**，`scan()` 报**外层容器**——
**同一批东西，两套标识符空间。**

**这道判据自己犯了它要抓的错。** 写进文档，因为下一个改它的人会再犯。

## 修完后的自测输出

```
── ★ 正向：两种账本布局都要数进来 ──
  ✓ 名册 = {wip-a, wip-b}
── ★★ 反向对照 ①：**容器套一层，名册不许缩水** ──
  ✓ 名册 = 2 个（不是 1 个 `_corpora`）
── ★★ 反向对照 ②：**空目录不许算进名册**（否则永远对不上） ──
  ✓ 没有账本的目录不进名册
── ★ 反向对照 ③：**少扫要报失败，不许当「没问题」** ──
  ✓ 名册 2 个、实扫 1 个 → 报出 wip-b
── ★★ 反向对照 ④：**容器行盖住它底下的工作区，不算少扫** ──
  ✓ `wip-a` 盖住 `wip-a/workspaces/…`（名字不等但路径包含）
── ★★ 反向对照 ⑤：**前缀必须按路径段，不许按字符** ──
  ✓ `wip-a` **不**盖住 `wip-abc`（否则会假装盖住别人）
── 反向对照 ⑥：多扫也要报（射程超出名册同样是错） ──
  ✓ 实扫里有名册没有的 → 报出来

✓ 自测全过
```

`pytest 70 passed`；`✓ 无合同漂移`。

## 判不了什么

- **判不了不按工作区扫的判据**（如 `check_quoted_arithmetic` 按 payload 扫）。
- **判不了「扫到了但读错了」**——第 3 次那种射程是对的，错在口径。
  **这一道抓不住它，别指望。**
