# 先读这一页，再看这个工作区里的任何一个 delta

**这个人物没有入库。三轮用尽，记拒发。** 详见 `reports/FINAL-三轮用尽记拒发.md`。

## 你会在这里看到两个数。只有一个能用。

| | 三轮 | 能不能用 |
|---|---|---|
| **+0.4516 / +0.4122 / +0.4084**（候选胜 16/16 三轮不变） | 有 rubric | **不能当能力证据** |
| **−0.1522 / −0.0756 / −0.0859** | 无 rubric | **这个是准的** |

`evals/round*/seat_D_raw.json` 与 `seat_E_raw.json` 里那些 0.9x 的分，
产生条件是**评委手里拿着逐题 rubric，而那些 rubric 里预先写好了目标引文的原字符串**
（连三处 OCR 讹字都写了）。**拿着它打出 16/16 的两席自己说了这件事：**

> 席 D：「**我无法区分某一侧是真从语料检索到的，还是照着 rubric 复述的**……
> 那几个接近满分的分数**证明的是字符串对齐，不是检索能力**。」
>
> 席 E：「**我打的高分量的是与 key 的字符串吻合度，不是人物保真度。**」

`seat_F_raw.json` 与 `seat_G_raw.json` 是同一批答案、同一 A/B 归属、同一天，
**唯一变量是评委手里没有 rubric**。

## 为什么可以确定不是「无 rubric 那两席比较严」

因为改进的方向被测过。三轮里我改写了 9 道候选答案（A/B 归属逐题不变、基线一字未动）：

| | 有 rubric | 无 rubric |
|---|---|---|
| **改过的 9 题**（R1→R3） | +0.4217 → +0.3394　**−0.0822** | −0.2472 → −0.1289　**+0.1183** |
| 没改的 7 题 | +0.0071 | −0.0007 |

**没动的 7 题两把尺子都只动了不到 0.01。**
**同一次改进，一把尺子加分、另一把扣分。** 这不依赖任何一次绝对分。

## 这个工作区留着干什么

语料（56 源 / 一手 92.9% / 4 道）、9 条断言、16 道用例、两侧答案、
三轮六席次的原始分与笔记**全部保留**。
**rubric v2 做好之后可以直接重判，不必重抓源。**

v2 要改的那一条：**判据不许把「本库没收录 X」这类资料层状态指定为正确答案**
（本人物 10/16 道 rubric 犯了这条，而同一份指令第 3 条又要评委扣「出戏」）。
判据已落成：`scripts/check_persona_frame_break.py`。
