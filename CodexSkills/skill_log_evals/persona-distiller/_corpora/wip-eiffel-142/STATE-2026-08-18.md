# Eiffel #142 本轮状态：**语料已落盘，卡在一手规模 6 < 8**（2026-08-18）

## 已做完

- `namesake_gate` → `ready`（今早 10:17 已把 3 候选收窄到 1；我又建了一份是重复劳动）
- `init_target` 跑通，工作区 `workspaces/gustave-eiffel` 已建
- **10 份扫本已取回并入库**，逐份与今早那份独立记录**字节数完全相同**：

      1879 Douro 32,491 ／ 1888 Garabit-A 43,616 ／ Garabit-B 46,312
      1888 Grandes constructions 54,374 ／ 1889 Mémoire 324,654
      1900 Travaux-A 576,454 ／ Travaux-B 525,623
      1900 三百米塔 1,593,995 ／ 1902 Tour en 1900 790,986
      1910 Résistance de l'air 429,518

  ★ 1910 那部拿到的 `source_id = src-72329cd0626a`，而今早记录的 sha256 是 `72329cd0…`
    —— source_id 由 sha256 派生，**两处逐位相同**。这是第四次独立印证。
- `meta.json` profile：deep → **quick**，并写明选档理由（deep 45 份 / standard 24 份，
  材料本身就不够；quick 门 8）
- 两对重扫已 `--derived-from` 声明（Garabit A↔B、Travaux A↔B）

## ★★★ 卡点：诚实计数是 **6 部作品**，不是 8

`check_source_dedup` 实测（阈值 0.3）：**可用 10 份 → 去重后 6 部**（虚高 1.667×）。
除我已声明的两对外，还有**三对重叠**是我没看出来的：

    51.8%  1900 Travaux-A  ×  1900 三百米塔
    46.7%  1900 Travaux-B  ×  1900 三百米塔
    30.8%  1888 Garabit-B  ×  1889 Mémoire

前两条说得通：《三百米塔》是大部头，《Travaux scientifiques》是它的科学实验部分。
第三条 30.8% 刚过阈值：1889 备忘录重印了 1888 纪要的材料。

⇒ **quick 的 min_sources = 8，诚实计数 6 —— 差 2 部。**
**不许靠「不声明重复」把 10 当 10 用** —— 那正是今天在 Lincoln #174 身上查出的
`source-count-inflated-by-duplicates`（虚高 11.167×）同一个洞。

## ★★ 但**现在还不能记延后** —— 因为「找不到更多」这句我没证成

他 1907《Recherches expérimentales…》、1911《Examen des formules》、
1914《Nouvelles recherches…》三部还没定论。今天的实测是：

| 查询 | 结果 |
|---|---|
| `"Nouvelles recherches sur la resistance de l'air"` | **numFound 0**（前台手跑两次，HTTP 200 合法 JSON） |
| 另外 5 条（含 `creator:("Eiffel, Gustave") AND date:[1903 TO 1925]`） | **未跑成** —— `advancedsearch.php` 全部 ValueError |

★ 同一批代码在前台手跑通、在后台连跑必败；而 `/metadata/` 与 `/download/`
在后台一直正常。判定：**是 `advancedsearch.php` 对我今天的请求密度拒答**，
不是「查不到」。[[webfetch-truncates-and-reports-absence]]

**所以本轮的诚实结论是「未定」，不是「延后」** ——
「被 X 挡住」的标记要先真跑一次 X（[[a-blocked-by-x-label-needs-x-rerun]]）。

## 下一轮从这里接

1. 前台、逐条、间隔够长地跑那 5 条查询（尤其 1907／1911／1914 三部）
2. 找到 ≥2 部不同作品 ⇒ quick 成立，继续六路研究
3. 一部都找不到 ⇒ 记延后，类别「一手规模不够」，理由写实测的 **6 部 < 门 8**
4. 另有两件已知的开工后问题：
   - `research.authorship-unproven` ×3（Douro／Travaux-B／三百米塔文中查无署名证据）
   - **声口在法文语料上量不出来** —— `check_first_person_density` 只认英文锚点，
     今早实测它正确地返回 `null` 而不是 `0`。判决书里要写「未量」，
     不许当成「够」也不许当成「不够」。
