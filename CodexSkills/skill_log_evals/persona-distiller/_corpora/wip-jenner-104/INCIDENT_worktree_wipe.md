# 事故记录 · 工作树被清空（2026-08-02 约 15:53）

## 发生了什么

第 2 轮两席判分进行中，工作树 `/Users/linzezhang/Documents/Codex/AgentDatabase/
character-distillation-skill-reorganize-d57595` 被清空：`.git` 与全部内容消失。

清空之后，仍在运行的两个判分子代理把各自的结果写了出来，**因此重建了目录链**——
磁盘上只剩那两个文件。**不是我发起的删除**（本会话未执行过任何 `rm -rf`、
`git worktree remove`、`git reset --hard`）。

## 我的第一反应下错了两次判断，都记在这里

1. **「工作树被清空」这句我说早了。** 第一次勘察用的是 `find -maxdepth 4`，
   它停在 `_corpora` 就没往下看，我据此说「只剩一条空目录链」。
   **实际 `round2/` 里有两个文件。** 后来重跑 `find -type f` 才看清。
2. 反过来，看到 `round2/` 有文件后我一度以为「只丢了 `.py`」。
   **也不对**——重数是磁盘总共只剩 2 个文件。

**两次都是「查了，但查的不是我以为的那个东西」。** 与同一天的
「146 KB 的语料」「查 `十二个` 漏掉 `这十二例`」是同一形态，**今天第四次**。

## 损失盘点（实查，非估计）

| | |
|---|---|
| 已提交内容 | **一条未丢。** 分支 `claude/character-distillation-skill-reorganize-d57595` 完好在主仓对象库，tip `e5c26e7a`，9,986 个文件 |
| 第 2 轮两席判分 | **在盘上，已抢救到工作树之外**（各 34 组 + `_overall`） |
| 第 2 轮候选答案与盲判载荷 | **丢失**——它们在 `e5c26e7a` 之后生成，从未提交 |

## 处置

1. 把两份判分复制到 scratchpad（**先保住不可再生的东西**）。
2. 残壳目录**挪开而非删除**（`.emptied-<时刻>`）——受保护资产只增不减。
3. `git worktree prune` —— **只清陈旧登记，不碰对象与分支**。
   （与铁律里禁止的 `git gc --prune=now` 是两回事：后者销毁不可达对象，
   本仓有线程因此丢过 2467 个提交；`worktree prune` 只删过期的管理文件，
   而我的提交挂在具名分支上，本就可达。）
4. `git worktree add <原路径> <原分支>` 恢复，实测 9,986 文件、HEAD 复位。
5. 放回两份判分，用 `assemble_ej_results.py` 算出第 2 轮真 delta。

**A/B 映射由 `sha256(case_id) % 2` 决定，与丢失的载荷无关**——
所以第 2 轮的 delta 可算且有效。

## 对后续的影响

第 2 轮**被判过的那份答案文本已不可复原**。若做第 3 轮，须由
`gen_ej_answers.py`（git 里是第 1 轮状态）重新施加第 2 轮的修正，
**重建件与被判件不会逐字相同**。这一点必须写在第 3 轮的记录里，
**不许当作同一份东西**。
