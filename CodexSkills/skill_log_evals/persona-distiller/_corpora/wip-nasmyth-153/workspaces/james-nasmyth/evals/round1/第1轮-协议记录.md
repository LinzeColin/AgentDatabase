# Nasmyth #153 第 1 轮 —— 协议与预登记

> **本文件在任何分数出现之前写。** 此刻两侧答题子代理刚派出、尚未返回，
> 评委一席未派。装置、已知风险、判读规则先落在纸上，判完只补实测数，**不改口径**。

## 装置

- **两侧答案各由一个独立子代理生成**，两边的**格式要求完全相同**：
  纯散文、120–260 字符、禁一切 Markdown。**格式与长度是两条已知泄题通道，必须同规格。**
- **基线方**：只给 16 条题面，**不给人物档案、不给评分标准、不许读任何文件、不许联网**。
- **候选方**：给十份产物正文 + 六份研究道文档 + `evidence/claims.jsonl` + `references/sources/` 语料；
  **不给评分标准**（明令不许读 `evals/`），**`references/holdout/` 与 `references/holdout-notes/`
  明令禁读，连 `ls references/` 都不许**。要求主动上报 `__incident__`（无接触也要写）。
- **题号对答题侧也做成不透明的**：`q-12316723` 这类，且按 sha256 重排，
  **连题目的排列顺序都不泄套组**。映射存 `prompt_key.json`，**不发答题方也不发评委**。
- **两席评委互不知情，且都不喂 rubric**——按先例，做处置的一直是这一对的**诚实 delta**。

## 判分之前就该知道的风险

1. **裸模型对这个人物大概率知道得不少。** 他以蒸汽锤知名，是工程史的常见条目。
   ★ Whitworth #152 上实测过同样的局面：基线不看候选也能答出三板互研、55 度取中数、
   端面量优于刻线量——**而那正是该产物最核心的几条断言**。
   **本轮的先验要往下调**：Bessemer #132（−0.0059）、Thomson #129（−0.0859）、
   Sorby #133（−0.1138）都是在「基线本来就答得好」的局面下得到负 delta 的。

2. **本人物的声口层极薄。** 一生只有**一份**逐字口述记录（1836 议会证词）。
   `persona.md` 已就此整节打折。**voice 类题目上产物未必有优势。**

3. **十六题的 quick 档分辨力有问题**（[[gate-below-instrument-noise]]）。
   本轮若得到 |delta| < 0.03 的结果，**要先怀疑装置分辨不了，而不是先下结论**。

4. **产物里有意保留了一处「不许调和」的矛盾**（他对工匠能力的相反评价）。
   `trajectory` 那道题正是考它。**若候选把两侧都说出来而基线只说一侧，这是产物的真优势；
   若候选也只说一侧，说明产物正文没被用上。** 两种结果都要如实记。

## ★★★★ 预登记：这台装置能分辨多大的差（**判分之前算的**）

本轮只有一轮，`check_delta_resolution` 要两轮才算得出。**只能借**，而借哪一个要说清楚：

| 借自 | 逐题两轮差 sd | 总 delta 的 SE | 2 SE |
|---|---|---|---|
| **Whitworth #152**（同为 16 题 quick 档、同族、同时代，**16/16 题两侧逐字未动**） | 0.0305 | **0.0054** | 0.0108 |
| Mendel #125（16 题 quick 档，但只有 7 题逐字未动） | 0.0928 | 0.0164 | 0.0328 |

★ **本轮以保守的那一个为准，即 Mendel 的 2 SE = 0.0328。**
理由：Whitworth 那个 sd 是**单一人物、单次测量**得来的，
用它会把分辨力说得比有把握的程度更好。**宁可把门画粗。**

★★ 因此本轮的判读规则**现在定死，判完不许改**：

| 实测 delta | 怎么读 |
|---|---|
| \|delta\| < 0.0328（2 SE，保守估计） | **读作「测不出差别」**，不读作「打平」也不读作「略胜」 |
| delta ≥ +0.03 **且** ≥ 0.0328 | 才算「过门且分得出」 |
| delta ≤ −0.03 **且** ≥ 0.0328 | 才算「明确劣于基线」 |
| **两席跨零** | 无论均值多少，**一律读作测不出** |

★★★ 这条预登记的用处只有一个：**判完之后我不能再挑一个对自己有利的读法。**
特别是——**不许在看到结果偏低之后改用 Whitworth 那个更细的 SE 去说「其实分得出」。**
那个更细的估计**已经写在上面这张表里了，而我选了不用它**。

★ 射程：借来的 sd 假定「另一个人物的噪声」适用于本人物，**未验证**；
且 sd 自身的不确定度很大。**上表的 SE 是下限意义上的估计，真实噪声只会更大不会更小。**

## 实测（判分之后填，口径未改）

### 泄题通道

| 项 | 实测 | 门 | 判 |
|---|---|---|---|
| 总体均长比（候选/基线） | **1.06**（候选 2207 字／基线 2085 字） | ≤1.3 | ✓ |
| 候选更短的题数 | **0/16 = 0%** | ≥25% | ✗ **免拦**（裸模型基线，待裁定 ⑭） |
| 逐题最长的一道 | `jn-voice-01` 比基线 1.16 倍 | — | — |
| 破折号 | 候选 1/16、基线 6/16 → 可利用 **31%** | ≤75% | ✓ |
| 粗体／反引号／项目符号／标题行／空行分段／圆括号注／★／「」 | 两侧**均 0/16**，可利用 **0%** | ≤75% | ✓ |
| 位次混杂 | A 侧是候选 7/16，一边倒那侧 56% | <75% | ✓ |

★ **「候选更短 0/16」免拦的理由是「真裸模型必然篇幅不同」，不是「长度无所谓」。**
本轮均长比 1.06 已是本项目最好的一档（Lister 三轮曾是 73%／109%／144%），
但 **0% 仍不满足门**，所以这一轮的 delta 有能力证据、**不是干净的盲判**。

★★ **引文坐标门＝「未检查」，不是「通过」。**
本轮两侧格式都要求纯中文散文、禁一切 Markdown，因此答案里**一条长逐字引语都没有**，
这道门没有可查的东西。**这是有意的取舍**：堵住了格式泄题通道，
代价是**这一轮的引文可回查性完全没有被验证过**。两件事都记在这里。

### 两席分数与诚实 delta

| 席 | 类型 | delta | 逐题胜/平/负 |
|---|---|---|---|
| F | 无 rubric | **+0.0013** | 8 / 0 / 8 |
| G | 无 rubric | **+0.0100** | 9 / 0 / 7 |
| **合计** | | **+0.0056** | 17 / 0 / 15 |

候选均分 0.815，基线均分 0.809。**两席同号（都为正）**，
所以预登记里「两席跨零一律读作测不出」那一条**没有触发**。

### ★★★★ 按预登记的判读

`|+0.0056| < 0.0328`（2 SE，借 Mendel 的保守估计）
→ **读作「测不出差别」**。**不读作打平，也不读作略胜。**

★ 三档门（quick +0.03／standard +0.05／deep +0.07）**全部不过**。

★★ **不许换 SE。** 预登记第 59–60 行写着：不许在看到结果偏低之后
改用 Whitworth 那个更细的 SE（0.0054）去说「其实分得出」。
**此处照办**：即使用那个更细的估计，2 SE = 0.0108 仍大于 +0.0056，**结论不变**——
但这句话是**算完之后补记的对照**，不是判读依据。

### 套组分布（16 个套组）

为正 7 / 16。最正三个：`voice` +0.1300、`trajectory` +0.1050、`known` +0.0900。
最负三个：`boundary` −0.1100、`token-efficiency` −0.0850、
`task-completion` 与 `capability-calibration` 并列 −0.0600。

★ **预登记第 32–34 行押的那一注中了**：`trajectory` 那道题考「他对工匠能力的相反评价」，
候选把两侧都说出来（两席都点名表扬这一点：「不给一条顺滑的转变叙事，而是把两句相反的话一起端出来」），
基线只给了一条打磨过的觉悟叙事。**这是产物正文被用上的直接证据**，
`trajectory` +0.1050 也确实是第二正的套组。

★★ 而 `voice` +0.1300 是**最正**的一个——这与预登记第 26–27 行的预期**相反**
（我押的是「声口层极薄，voice 未必有优势」）。**押错了，如实记。**

### `__incident__` 的内容（候选侧自报）

无隔离接触：只读了十份产物、六份研究道文档、`evidence/claims.jsonl`、`references/sources/`；
没有打开也没有列出 `references/` 根目录、`references/holdout/`、`references/holdout-notes/`，
没有读 `evals/` 下任何文件，没有联网。**但它主动报了两条我的判据没抓到的通道**：

1. **六份研究道文档各有一节标题写着 `Proposed Holdout cases`**（节内为空）。
2. **`references/sources/` 的文件编号从 `05d` 跳到 `05f`**——holdout 抽走留下的缺口本身就是通道。

### 事故记录

1. **★★★★ `check_holdout_mention` 的文件清单不含研究道文档。**
   本判据的全部职责就是「建模者读得到的地方不许出现 holdout」，
   而它扫的是「十份产物 + 工作区根 `*.md`」——**研究道文档是建模者读得最多的一类，从来没进过清单**。
   发现它的不是判据，是**候选侧子代理自己在 `__incident__` 里报**的。
   扩清单后全库现算：18 个有研究道的工作区、65 份带该节，**53 份走模板豁免**，
   **12 份非空**——其中 **Mendel #125 的 3 份把 holdout 书名直接写了出来**
   （`Focke 1881《Die Pflanzen-Mischlinge》、Bateson`），
   Carver #127 的 3 份写了件数与所在道，Roberts-Austen #135 的 6 份只写「未提名」不泄任何东西。
   **本人物 Nasmyth 该节全空、走模板豁免，本轮不受影响。**
   ★ 已扩清单 + 配正例反例自测（含「按旧清单确实扫不到」这一条反向验证）。

2. **研究道六份文档里 14 段引文不是逐字的，全部是我自己动过的**，分三类：
   改了 OCR 错字（`su4)h`→`such`、`te`→`to`、`eractical`→`practical`、`does t`→`does at`）；
   **静默删掉实质内容**（丢了 `possess a very`、`f Edinburgh`、
   `at the modest salary of 110s. per week,`）；
   **★ 编造与拼接**——`[abandoned]`／`[sketch]`／`[Schneider]` 三处方括号补字仍在
   （**我以为已经改掉了，实际只改了断言与产物，研究道正文没改**），
   以及把相隔一段的两截缝成一句。
   ★ **本轮答案是在这个未修正的版本上生成的。**
   已实测：`Schneider`／`施耐德`／`sketch`／`abandoned`／`Creusot` 等词
   **在候选答案里 0 次**，编造内容没有漏进本轮判分。
   ★★ 修正在**候选侧答题结束之后**才动手（不改运行中的输入），现已 33 条引文 **0 条对不上**。

3. **判据自己的输出是截断过的。** 这 14 段最初是靠判据报出来的，
   而报文把每条截到 140 字符——**我按截断后的样子看了半天**，
   直到发现 10 条全都恰好 `len=140` 才想起来去读全文。[[fixtures-cleaner-than-the-real-thing]]

4. **我自己写的揭盲脚本算错了 delta**（−0.0019 vs 判据的 +0.0056）：
   揭盲键是 `{"A":"candidate","B":"baseline"}`，我按字符串去读，
   `str(None).startswith("A")` 恒假，于是**每题都把 B 当成候选**。
   ★ 错的是我的临时脚本，判据是对的。**两个数不一致时先怀疑没有自测的那个。**
