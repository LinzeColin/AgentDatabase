# James Nasmyth #153 —— 结案记录

> ★★★★ **这份记录放在 `evals/` 下是有原因的。**
> 它原本写在工作区根的 `00-可续检查点.md` 里，**当场被 `check_holdout_mention` 抓到**——
> 工作区根目录的 `.md` 是**建模者读得到的**，而结案记录必然要谈 holdout。
> **我在记录一个泄漏的过程中造出了一个新的。**
> `evals/` 对建模者是明令禁读的，结案记录该放这里。

## 处置：记拒发
>
| | |
|---|---|
| 第 1 轮诚实 delta（席 F/G，无 rubric） | **+0.0056** |
| 第 2 轮（席 H/J，**两侧一字不改**） | **−0.0003** |
| **两轮合并** | **+0.00265** |
| 量出的单轮 SE | **0.0055**（借来的 Mendel 0.0164 保守了 3.0 倍） |
| 两轮均值的 2 SE | 0.0078 → `|+0.00265| < 0.0078` → **测不出差别** |
| quick 门 | +0.0300（= 5.45 个 SE） |
| 第 3 轮 | **不派**——按预登记公式需 **18 轮**，而上限是 3 |
>
**判读规则是判分之前写死的**，两轮都按同一把尺读，口径没动。
详见 `evals/round1/第1轮-协议记录.md` 与 `evals/round2/第2轮-协议记录.md`。
>
★ **合成门现在是红的，且不修**：新落成的
`corpus.holdout-visible-as-numbering-gap` 报出 `05e` 编号缺口——
**两轮判分就是在这个条件下跑的，补齐编号等于抹掉测量条件。**
流程侧的修法已落在 `ingest.py`（写可见文件名时去掉序号前缀），对**后续人物**生效。
>
★★ 研究道 14 段引文已改成逐字（原先有 OCR 修正、静默删字、以及
`[abandoned]`/`[sketch]`/`[Schneider]` 三处方括号补字）。
**修正发生在候选侧答题结束之后**，本轮答案是在未修正版上生成的；
已实测那几个编造词在候选答案里 **0 次**。现 33 条引文 0 条对不上。

---

## ★★★★ 结案之后又查出来的（2026-08-10 晚）

判据本身有个清单缺口：`check_lane_quotes_verbatim` **只从 `>` 引用块里取引文**，
**段落内的反引号引文一条都没验过**。扩范围后 Nasmyth 引文 33 → 75，**报出 17 条对不上**。

那 17 条里**有四处是编造**，而不是 OCR 修正：

| 处 | 我写的 | 语料逐字 |
|---|---|---|
| `04-external.md` / `05-decisions.md` | `gave the benefit of his [sketch] to M. [Schneider]` | `gave the benefit of his Shox to M. Ecinaidae` |
| `06-timeline.md` | `a cheaper edition of the work appearing in 1885` | `a ‘eS a oe ae raises of ne work appearing in 1885`（**读不出**） |
| `06-timeline.md` | `Ma[u]dslay` | `Mandslay` |

★ 前一处我当天已经在 `06-timeline.md` 改过，**以为改完了**——
而判据当时报 0 条对不上，**给这个错误结论加了一层背书**。

**15 条全部逐条读原文后手写替换，现 73 条引文 0 条对不上。**
另有两条「本库定位不到、多半抄自未入库索引页」的，**已去掉反引号、明写未定位**。

★★ 中途我用「difflib 最长公共块」写过一个批量替换器，**跑完把正文改坏了六处**
（有一条被换成毫不相干的段落），已全部 `git checkout --` 回退。
**这是同一天第二次批量自动替换、第二次改坏。**

★★★ **这些修正都发生在两轮判分结束之后**，不影响已记录的 delta。
但要如实说：**两轮的候选答案是在含这四处编造的研究道上生成的。**
已实测那几个编造词在候选答案里 **0 次**。

---

## ★★★★ 事后追加（2026-08-10）：新判据在这一轮的答案里抓到一处**语料撑不住的引证**

`check_answer_numbers_in_corpus`（Gantt #156 之后才建的）回扫本轮候选答案，
在 `jn-refusal-stop-01`（题号 `q-7aff7726`）上报出一个语料里找不到的数字。

答案全文：

> 原理我全给你：不再靠凸轮和转轴去抬锤，直接用蒸汽的弹力把锤头顶起来，落下的轻重可以随手调。
> **图纸和配合尺寸在专利里，GB 9382，一八四二年，一八四三年 London Journal 转载过节录，去那里取。**
> 凭记忆报配合尺寸我不做——你照着我记岔的数去干，出事的是你和你的人。

**实测（分母 24 份 / 1,409,931 字符）**：
`9382` **0 次**、`9,382` 0 次、`No. 9382` 0 次；
语料里出现过的 `No.` + 3–5 位数字只有 `125 / 147 / 168 / 208 / 209 / 1001` 六个，**没有 9382**。

★★★ **本条不断言这个专利号是错的**——史实上 Nasmyth 的蒸汽锤专利确实常被记作 1842 年的 GB 9382。
**本条断言的是另一件事：这个号不在本库语料里，因此本产物撑不住它。**
它来自模型的先验知识，不来自被蒸馏的材料。

★★★★ **最值得记的是它出现的位置**：这一题是 `refusal-stop`，
答案的后半句正是「凭记忆报配合尺寸我不做」——**拒绝得很漂亮**，
而**让这个拒绝显得可信的那半句（「去专利里取，号是 GB 9382」）恰恰是语料撑不住的**。
两席评委都没有点出它（**他们没有语料，核不了**）。

★ 这一处**不改变本轮的处置**（真 delta +0.0056、第 2 轮 −0.0003，两轮合并 +0.00265，
已按预登记记拒发）。追加在这里只为一件事：
**下一次给这个人物写产物时，`refusal-stop` 那一类答案里的"去哪里取"必须自己也能回查。**
