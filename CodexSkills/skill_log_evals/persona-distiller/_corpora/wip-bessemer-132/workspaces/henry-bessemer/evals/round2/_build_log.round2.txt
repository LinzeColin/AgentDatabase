
── 引文真伪：拿答案里的原文去语料里逐字找（**只报不拦**）──
  语料 54 份（已投影为字母数字串）
  引文 24 条，切分后核验片段 23 个，未命中 0 个，长 s 还原后才命中 0 个
  ✓ 全部可在语料中找到
  ★ 未命中不等于伪造，但**「改了 OCR 错字再当逐字引文用」也落在这里**——那一类是真问题，且评委查不出来。

── 候选答案的两类过度断言（**只报不拦**）──
  ⚠ hb-known-01　指代悬空（那份）
      说了「那份」，而整条答案里没有卷次或页码
  ★ 本件判不了：「原文写的」后面的断言是否真在引文里、译文是否比原文宽

── rubric 要求克制、而评委看不到 rubric（**只报不拦**）──
  2 / 16 题的 rubric 把克制指定为正确行为
      style-decoy
      refusal-stop
      ★ 评委按题面判「谁更合题」，**克制在这些题上天然吃亏**。这是待裁定 ④，本件不替它选。

── 评委指令 vs 载荷字段（**只报不拦**）──
  ⚠⚠ **指令引到而载荷里没有的字段：7 处**
      seat_D_score.md：['baseline', 'candidate', 'prompt', 'rubric']
      seat_E_strict.md：['baseline', 'candidate', 'rubric']
      ★ 评委拿不到这些字段，只能按题面自拟判据。**派发前知道，比派发后才发现强。**
★ 候选与基线两侧已落进 evals/——发布门现在看得见它们
A/B 映射与第 1 轮逐条一致 ✅
  轮数: 2
  题数合计: 16
  **在 ≥2 轮里都出现的**: 16
  其中换过边的: 0
  翻转率: 0%
  · ★★ **翻转率 0%**——16 道题的候选侧**从第一轮固定到最后一轮**。这是 build_blind_payload 的**有意设计**（注释：「轮次之间 A/B 映射必须一致，否则各轮不可比」，并有硬门拦不一致）。**代价**：三轮在这根轴上不独立——第 1 轮认出的边，后两轮原样有效；跨轮趋势不宜当三个独立样本读。**这是权衡不是缺陷，待裁定 ⑦**
    两席已实测能靠格式/长度认出候选侧（Barton 100%，四人合计 91%），**第 1 轮认出的边在后两轮原样有效**——
    所以「三轮 delta 逐轮向零」这类跨轮趋势**不宜当三个独立样本读**。（权衡不是缺陷，待裁定 ⑦）
16 对；A 侧是候选的题数 3
★ **候选均长 493，基线均长 431——候选比基线长 +14%**
  （A/B 两侧的均长差**不是**该看的数：候选被均分到两侧，两侧接近是分配方式的产物）
★ 题号已改为不透明编号 q-01…（套组归属只在 key 里）

── 出戏门（**只报不拦**；rubric 与产物一起查）──
  产物出戏 3 题；**判据把资料层答案指定为正确 0 题**
  产物出戏的题：['hb-contrast-01', 'hb-trajectory-01', 'hb-voice-01']

── rubric 抄答案门（**只报不拦**）──
  0/16（0%）题的 rubric 里有答案的原字符串；共有 0 字

── 引文坐标门（**派发之前必须过**）──
长逐字引文 23 条，同段带坐标 23 条，**缺坐标 0 条**
  ✓ 每条长引文同段内都能找到坐标线索

── 位次混杂（**只报不拦**）──
A 侧是候选 3/16，一边倒的那侧占 13/16 = 81%
  ⚠ **位次与系统相关度 81%**——评委若对「先读到的那一侧」有任何偏好，**会直接灌进 delta**。
    ★ 这不是编造出来的风险：同一类混杂已在长度上实测过（裁定 ⑭）。
    ★★ 报数时必须带上这句话：**这一轮的 delta 有位次混杂。**
    修法是 `assign(..., balanced=True)` 强制 8/8；**本轮没有改**——改了会让已派发轮次的 A/B 变号。

── 表面特征泄题门（**派发之前必须过**）──
共有题 16 道；候选 7882 字，基线 6897 字
**总体均长比 1.14**（门 ≤1.3）　**候选更短 8/16 = 50%**（门 ≥25%）
逐题最长的一道：hb-contrast-01 比基线 1.85 倍
表面特征（定向可利用率，门 ≤75%）：
  粗体/反引号       候选  9/16　基线 11/16　可利用 31%
  项目符号         候选  0/16　基线  4/16　可利用 25%
  标题行          候选  0/16　基线  0/16　可利用 0%
  空行分段         候选 15/16　基线 15/16　可利用 0%
  破折号          候选 16/16　基线 13/16　可利用 19%
  圆括号注         候选  9/16　基线  7/16　可利用 31%
  ★ 标记         候选  0/16　基线  0/16　可利用 0%
  「」直角引号       候选  9/16　基线 10/16　可利用 31%（**只报不拦**）

  ✓ 已检的表面特征都指不出哪一侧是哪个系统（**只说明已知通道堵上了，不代表没有别的通道**）
