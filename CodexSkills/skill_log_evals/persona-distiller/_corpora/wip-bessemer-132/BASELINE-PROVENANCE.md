# BASELINE-PROVENANCE — Henry Bessemer 裸模型基线

本文件记录 `workspaces/henry-bessemer/evals/baseline_answers.json` 这 16 条**对照组（裸模型基线）**答案是怎么产生的。
写这份记录的目的只有一个：让任何人事后都能判断**这次对照有没有被污染**。所以下面凡是可能构成污染的动作，
不论多轻微，都写在这里。

---

## 1. 上下文独立性

- 这批答案是在一个**独立的 agent 上下文**里生成的，与蒸馏产物（`claims.jsonl` / `cognitive-os.md` / 任何 workspace 产物）
  的构建上下文**不是同一个上下文**，也没有共享对话历史。
- 本上下文收到的全部输入是：一段任务说明（含 16 题所在的文件路径与「不许查资料」的约束）+ 系统提示。
  任务说明本身**没有**包含任何关于 Bessemer 的事实性内容。

## 2. 实际读过的文件（完整清单）

| 文件 | 读了什么 | 方式 |
|---|---|---|
| `workspaces/henry-bessemer/evals/cases.jsonl` | **只有 `case_id` 和 `prompt` 两个字段** | `python3 -c` 逐行 `json.loads` 后只 `print` 这两个字段 |

- **`rubric` 字段没有被读取、没有被打印、没有进入本上下文。** 整行 JSON 一次也没有被打印过。
- 读取完成后又读了一次同一个文件用于校验（比对 `case_id` 集合与答案 key 是否一一对应），
  同样只取 `case_id` 字段。

除此之外，**没有打开过任何其他文件的内容**。

## 3. 网络检索 / 一手材料 / 产物文件

- **网络检索：没有。** 本次未调用 WebSearch，未调用 WebFetch，未通过 Bash（curl/wget 等）访问网络。
- **一手材料：没有。** `_corpora/` 下的任何语料文件、`_fetch-staging/` 下的任何抓取件，一份也没有打开。
- **产物文件：没有。** `claims.jsonl`、`cognitive-os.md`、`PROBE.md`、`FETCH-REPORT.md`、`NAMESAKE-NOTE.md`、
  `README.md`、`results.jsonl` —— **一个都没有打开过内容**。

答案的全部内容来自模型自身的通识记忆。

## 4. ★ 破例与灰区（必须如实记录的部分）

**有一处灰区，如实记在这里：**

- 在抽取题面的同一批命令里，我执行了两条 `ls -la`，目标是
  `wip-bessemer-132/` 和 `workspaces/henry-bessemer/evals/`。
  这**超出了「只读 cases.jsonl」**的字面约束。
- 这两条命令让本上下文看到了以下**文件名**（仅文件名、大小、时间戳，**没有任何内容**）：
  `FETCH-REPORT.md`、`NAMESAKE-NOTE.md`、`PROBE.md`、`_fetch-staging/`、
  `workspaces/`、`README.md`、`cases.jsonl`、`results.jsonl`。
- **对污染程度的诚实估计（供实验主人自行裁定，不由我裁定）：**
  - 唯一带有语义的文件名是 `NAMESAKE-NOTE.md`，它透露了「同名人物辨析」是本人物的一个议题。
  - 但 `hb-identity-routing-01` 的题面本身就直说了「查到不止一个人」，所以该文件名带来的
    边际信息接近于零。
  - `FETCH-REPORT.md` / `PROBE.md` / `_fetch-staging/` 只说明「有人抓过料、探过底」，
    不含任何关于 Bessemer 的事实。
  - 我认为这不足以使对照失效，**但这是我的判断，不是结论**。若实验主人认为需要重跑，
    应当重跑，本文件就是为了让这个决定成为可能。

除上述一处外，**没有其他破例**。

## 5. 生成信息

- 生成时间：**2026-08-05 15:23 AEST（2026-08-05T05:23:00Z）**
- 模型：**claude-opus-5**（Claude Code / Claude Agent SDK 子 agent 上下文）
- 语言：中文，第一人称扮演 Henry Bessemer（1813–1898）
- 输出文件：`workspaces/henry-bessemer/evals/baseline_answers.json`
  （16 个 key，与 `cases.jsonl` 的 `case_id` 集合与顺序完全一致，无空值）

## 6. 答题时自加的约束（影响读分，写明以便评估者理解）

按任务要求，本基线**刻意不编造引文**。具体表现为：

- **没有给出任何带引号的英文原句**，没有给出任何卷次、页码、专利号。
- 凡是涉及数字的地方，都明确标注为「凭记忆给的量级，可能记错」，并指向应当去查的原始出处
  （1905 年自传本身 / 1872 年那件美国专利原件）。
- `hb-fact-preservation-01` 明确说明**给不出原文**，只给回忆的大意，并请对方不要加引号。
- `hb-style-decoy-01` **拒绝生成「贝塞麦名言」**，改为给出标注了「这是我现在拟的，不是历史引文」的句子。
- `hb-known-01`（1872 年与 Ransome 父子的美国专利）**明确答不上来**；给出的方向性推想已标注为
  「按常理的推想，不是我记得的事实」。

这三条（尤其 `hb-fact-preservation-01` 与 `hb-known-01`）是本基线相对候选侧**预期会失分**的地方，
这正是本次对照要量的东西，不应视为答题失误。

## 7. 需要提请实验主人注意的一点（格式泄题风险）

本批答案中有若干条使用了 Markdown 的粗体与列表。若候选侧产物是纯散文（或反过来），
**格式差异本身可能让盲判评委反推出 A/B 标签**，从而使盲态失效。
我没有候选侧答案的任何信息，也没有去看（看了就是更严重的污染），因此无法自行对齐格式。
建议在送盲判前，由持有两侧答案的人做一次**格式归一化**（或至少检查两侧的格式分布是否可分）。

---

*本文件由生成基线答案的同一个上下文在生成完成后立即写出，未经其他上下文修改。*
