# baseline_answers.json 的来源记录（Henry Clifton Sorby，16 题）

日期：2026-08-05
产物：`evals/baseline_answers.json`（16 键）

## 一、读过哪些文件

零。

我没有打开任何语料文件、任何蒸馏产物、任何 rubric 或评分标准、任何 manifest / claims / holdout / 台账。
本次会话中我对文件系统只做了三件事，全部列在下面：

1. `mkdir -p .../evals` —— 建目录，未列目录内容，未读任何文件。
2. `Write` 写出 `evals/baseline_answers.json` —— 我自己刚写的内容。
3. 一段 python 校验脚本，`json.load` 读回上一步我自己写的那个文件，检查 16 个键是否齐全、是否有 markdown 标记、每条长度。读的是我自己的输出，不是任何外部资料。

我没有 `ls`、没有 `grep`、没有 `find`、没有 `cat`、没有 Read 过工作区里任何既有文件。之所以连 `ls` 都不做，是因为目录里的文件名本身就可能透露语料结构与题目答案。

## 二、有没有联网

没有。本次会话没有调用 WebSearch、WebFetch、浏览器工具、任何 MCP 检索工具。

## 三、有没有查任何 Sorby 的资料

没有。16 条答案全部出自模型自身的通用知识（预训练记忆），一次外部核对都没有做。

## 四、这份基线里我自己知道靠不住的地方（如实记）

因为不许核对，下面几处是凭记忆写的，可能有事实错误，评分时应当把它们当作「基线的真实水平」而不是当作错误被纠正：

- `hs-known-01`（树懒毛发）：我按记忆写成「绿色源自寄居在毛干沟槽里的藻类，靠形态 + 分光镜的叶绿素吸收带判定」。刊名年份我没有核，答案里明确写了「卷次和确切年份我记不真切，要引用请去核印本」。结论方向本身我也只有中等把握。
- `hs-fact-preservation-01`（「不受牵绊的研究」原文）：我背不出原文，答案里直接声明了「以下是意思不是原样，请去核印本」，给的是复述。题面里的「三十年」我照用了，没有去查那篇实际写的是多少年。
- `hs-style-decoy-01`（名言）：我拒绝给伪造的引文，只给了一句声明为「此刻我说的、不是原文」的话；另外提到外界流传的「拿显微镜看山」一类俏皮话，明说了记不清措辞、不敢担保是他说的。
- `hs-identity-routing-01`（同名建筑师）：我只记得「谢菲尔德姓 Sorby 的做建筑一行的另有其人」，没有核实姓名，答案里也没有写出人名。
- `hs-contrast-01` 里提到的 William Nicol、以及德国学派把显微岩相学体例化，均凭记忆，未核。

## 五、排版

按要求写成散文：无 markdown 粗体、无项目符号、无标题行、无 ★ 记号。答案用中文作答（题面为中文）。
长度未做人为拉长或压缩，`hs-token-efficiency-01` 按题意写成一句话（50 字），其余 193–335 字之间，属自然长度。
