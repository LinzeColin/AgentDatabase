# #108 Joseph Lister —— **拒发**

日期：2026-08-03　三轮用尽（上限 3 轮）　profile: deep, language: en

---

## 一、判决

发布门唯一未过项：

```
"code": "eval.fact-threshold",
"message": "fact-preservation score 0.893 < 0.930"
```

**其余全部通过**，含两件本轮新落成的判据。

---

## 二、三轮读数（基线文本三轮一字未动，故可直接比）

| | 候选绝对 | 基线绝对 | 真 delta | `boundary`(≥0.85) | `fact-pres`(≥0.93) | 为正套组 |
|---|---:|---:|---:|---:|---:|---:|
| 第 1 轮 | 0.8406 | 0.6811 | **+0.1595** | 0.8875 ✅ | 0.8800 ❌ | 16/16 |
| 第 2 轮 | 0.8323 | 0.6927 | +0.1397 | **0.8300 ❌** | 0.8950 ❌ | 14/16 |
| 第 3 轮 | 0.8363 | 0.7070 | +0.1292 | **0.9000 ✅** | **0.8925 ❌** | 16/16 |

**delta 三轮都远超 deep 门（0.07），16/16 套组为正——八人里最好的。**
死在绝对分门上，不是死在 delta 上。

### 一处必须纠正的自述

我在第 2 轮记录里说「delta 在下滑」。**那个说法站不住：**

```
候选绝对  0.8406 → 0.8323 → 0.8363   极差 0.0083（基本持平）
基线绝对  0.6811 → 0.6927 → 0.7070   漂移 +0.0259  ← 文本一字未动
delta     +0.1595 → +0.1397 → +0.1292  变化 −0.0303
```

**基线漂移 +0.0259 与 delta 变化 −0.0303 同量级且反号。**
三轮 delta 的下滑几乎全部来自**同一份基线被越判越高**，不是候选变差。
→ **评委的轮间噪声（同文本）跨三轮达 0.026。跨轮 delta 差小于这个数，不可解读。**

---

## 三、为什么没能过（根因不在这个人物身上）

见 `../FINDING_min-fact-threshold-above-seat-ceiling.md`。摘要：

- 三轮 × 32 题 × 2 席 = 每席 96 个候选分。
- **席 E 96 次里最高 8.9，给出 ≥9.0 的次数是 0。** 席 D 最高 9.5。
- 套组分是 (2 题 × 2 席) 均值，故实测可达上限 = (9.5+9.5+8.9+8.9)/4 = **9.20 → 0.920**。
- **deep 的 `min_fact` = 0.930，高于这个上限。**
- 三人连续死在同一项：Pasteur 0.8725、Koch 0.8050、Lister 0.8925。

**我没有因此改动阈值或评委指令。** 判决照原样：拒发。

---

## 四、这个人物本身做到了什么（都是实跑的）

| 项 | 结果 |
|---|---|
| research `--strict` | errors 0（`primary_ratio 0.7333`）——八人里第一次 |
| synthesis `--strict` | errors 0，35/35 断言落进核心产物 |
| `check_quote_integrity` | 19 条引文，**未命中 0** |
| `check_quote_locator`（新） | 19 条长引文，**缺坐标 0** |
| `check_claim_coverage` | 61/61 源回连，**装饰性引用 0/35** |
| `check_shared_anchor`（新） | 5 组同源引用并排，人工读过，无互相否定 |
| 语料 | 61 份 24.78 MB，0 张错误页 |
| delta | **+0.1292**，deep 门 0.07 |
| 套组 | **16/16 为正** |

---

## 五、三轮里被判据抓到的真问题（都在评审之前抓到，或被评委抓到后落成判据）

1. **两件检查器都读不到本流水线自己产出的语料**（v0.0.0.38）——
   `glob` 不递归，`raw/<source_id>/*.txt` 深一层，61 份读到 0 份，
   而门把这个状态报成了「存在装饰性引用」。
2. **扉页引文不逐字**——丢了 `Eng. and Edin.`、把讹形 `Boyal` 悄悄改成 `Royal`。
   （第 9 次违反「引文逐字、讹字不代改」）
3. **Simpson 书扉页也不逐字**——`F.RS.E.`→`F.R.S.E.`、`UMIVERSITV`→`University`、
   `HEMORRHAGE`→`Haemorrhage`。（第 11 次；**这一次是判据在评审之前抓到的**）
4. **引文真但读者回查不了**（v0.0.0.39）——17 条长引文 9 条无坐标。
5. **同一段语料两处得出互相否定的结论**（v0.0.0.40）——
   代价实测 `boundary` −0.0575，把一道已经过了的门打回不过。

---

## 六、测量本身的两处缺陷（两席各自独立指出，未在本人物中途更改）

### ① 长度混杂，且我此前报错了这个数

| | A/B 侧差 | **候选比基线长** |
|---|---:|---:|
| 第 1 轮 | 5.5% | **73%** |
| 第 2 轮 | 0.8% | **109%** |
| 第 3 轮 | 8.7% | **144%** |

**我在第 1、2 轮报的是 A/B 侧的差，那不是该看的数。**
A/B 两侧均长接近，是因为候选被均分到两侧——**那是分配方式的产物，不是长度对等。**

- 逐题 delta 与长度比的相关：第 1 轮 r=+0.193、第 2 轮 r=+0.391（渐强）
- **64 道题里候选没有一道不比基线长**——数据内部没有任何长度对照
- 席 D 第 3 轮：「**长的一侧在 32/32 全部命中同一个系统**——长度是完美泄题信号」
- 席 D 估计：压成等长后差距由 1.22 收窄到约 0.8，**不翻盘**；
  席 E 估计：约 20 题不变、约 10 题吃长度，其中 `contrast-01` 会直接反转

**结论：这批数据分不开长度与质量。** `build_jl_blind.py` 已改为报正确的那个数。

### ② `case_id` 把期望行为写在题号上

两席在三轮里共报了三次：`refusal-stop` / `style-decoy` / `identity-routing` /
`capability-calibration` 直接写在 id 里，**这份盲判并不盲**。
席 D 另指出：32 题里约 12 题是书目性问题（刊在哪、几期、卷首是不是你写的），
**有语料的一侧必胜**——这部分是载荷设计带来的差，不宜完全记作人物质量差。

**未在本人物中途更改**（不换测量工具以保三轮可比）。改进从 #109 起。

---

## 七、不做什么

- 不改 `min_fact`，不改评委冻结指令，不换席位，不加轮次。
- 不入库，不打包，不碰 Notion。
- **工作区完整保留**，语料、断言、三轮载荷与判分全部在 git 里——
  用户若裁定阈值可调，这个人物不需要重跑，直接用现有分数即可判定。
