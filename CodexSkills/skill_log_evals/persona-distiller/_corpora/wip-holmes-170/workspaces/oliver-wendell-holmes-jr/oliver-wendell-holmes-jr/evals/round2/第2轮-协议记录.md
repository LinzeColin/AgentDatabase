# #170 Holmes 第 2 轮 —— 协议记录（**判分之前写**）

日期：2026-08-11。第 1 轮结果：真 delta **−0.0269**，三档门全不过。

## 一、这一轮改了什么、没改什么

| | 第 1 轮 | 第 2 轮 |
|---|---|---|
| **产物正文** | v1 | **改了三份**（见下），断言 15 → 17 条 |
| 用例 16 题 | 同 | **同（一字未改）** |
| 题号 | seed `…round1-prompts` | seed `…round2-prompts`（**重新洗过**） |
| 答题两侧的指令 | v1 | **与 v1 逐字相同**，只改路径与题号，另加「禁 `ls`」 |
| 评委席位指令 | v1 | **与 v1 逐字相同**（按人物冻结） |
| 基线 | 裸模型 | **同样是裸模型，全部输入只有 16 条题面** |
| 判据 | v0.0.0.154 | **同** |

★ **只有产物正文变了。** 这是 [[product-edits-can-move-honest-delta]] 唯一允许的杠杆。

## 二、产物改了哪三处，依据是第 1 轮评委的原话

1. **`work.md` 新增「进入一门自己不熟的题目，次序是这样的」五步**，
   并把原来的落笔纪律另起一节隔开。
   > 依据：席 K「五步里只有『找这条规则从哪来』是真正的研究动作」；
   > 席 L「几乎没回答『不熟悉』时怎么把这门弄明白」。
   语料依据是他自陈的成书方法（`We must alternately consult history and existing theories of legislation`、
   `I shall use the history of our law so far as it is necessary … but no further`）
   与他对「因为一向如此」的表态（`It is revolting to have no better reason for a rule of law
   than that so it was laid down in the time of Henry IV`）。**新增断言 clm-0b1c2d3e4f60。**

2. **`cognitive-os.md` 新增「这把尺子被用起来的样子」**，给出两个具体换算。
   > 依据：席 K「B 举的是宣布标准的场合，不是用它去评判某条陈述的场合，等于第二步没有落地」。
   语料依据：`The duty to keep a contract at common law means a prediction that you must pay damages
   if you do not keep it — and nothing else`（1920 年论文集）＋
   `It does not attempt to see men as God sees them`（1882 年专著）。**新增断言 clm-f9a0b1c2d3e5。**

3. **`boundaries.md` 补一句**：说「举不出」时要给一个对方能自己去查的落点。
   > 依据：两席都指出候选在 boundary／contrast 两题上「可自查的落点更少」。

★★ 这两条断言**是语料里本来就有、而我第 1 轮没挖出来的**，不是为了凑分新编的。
合成门重跑：**0 错 0 警告**，mental-model 2、heuristic 5。

## 三、★★★ 我在本轮开工时自己造了一次污染，已撤销并记档

第一次派发两侧答题代理时，我在**两侧的指令里都加了三条「答的时候特别注意」**，
内容是从第 1 轮评委意见里提炼的（研究动作 vs 写作纪律、举真用过的场合、给可自查落点）。

**两分钟内撤掉、重发。** 撤的理由有两条，第二条更要紧：

1. 基线的定义是「全部输入只有 16 条题面」。加了三条提示，它就不再是裸模型基线。
2. ★★ **更要紧的是：那样一来我测的就不是「改产物」的效果。**
   候选本该从改过的产物里自己得到这一层；把它写进指令，等于绕过被测对象直接喂答案。
   即使两侧对称地加，也只是把 delta 变成「两侧同时被我教了一遍之后的差」。

**撤销是干净的**：两个代理都在读文件阶段被停掉，两侧的落盘目录复核为空，无半成品。
重发的指令与第 1 轮逐字相同（只改路径与题号，另加「禁 `ls`」）。

这一条与 [[i-create-the-leak-channels-myself]] 同族——**第四次**，而且这次是在答题指令里。

## 四、这一轮的隔离比第 1 轮硬在哪

- **答案落盘目录移出轮次目录**，且两侧**各自一个目录**：
  `answers-round2/candidate/` 与 `answers-round2/baseline/`。
  第 1 轮候选方 `ls` 过轮次目录、看到了 `prompt_key.json` 的文件名（自报未打开）。
  **嘱咐管不住 `ls`，目录布局管得住。**
- 两侧指令都加了「严禁 `ls` 任何目录」。
- 本文件与基线运行记录都放在工作区 `evals/round2/`，**不放进评委能 `ls` 到的 `round2/`**。

## 五、判读规则（与第 1 轮相同，此处重申，不改）

- 主判据：两席 delta 均值；**两席跨零不作数**；quick 门 +0.07。
- 分辨力：16 题一轮噪声实测 ~0.0055；|delta| 落在该量级即判「测不出」。
- 好得离谱＝缺陷报告：delta > +0.40 先查泄题。

## 六、★ 开工前先把账算清楚（写在看到分数之前）

第 1 轮差 **0.097** 才够 quick 门，而**改产物能推动的历史最大摆动是 +0.065**
（Rosenhain #138：−0.0632 → +0.0018）。
**即使这一轮拿到史上最大的一次改进，仍然到不了 +0.07。**

→ 所以本轮的现实目标不是过门，是**量出「补上具体应用之后能挽回多少」**。
这个数本身有用：它是「产物缺应用实例」这条缺陷的代价的第一次实测。
**不许跑完再把目标改写成别的。**
