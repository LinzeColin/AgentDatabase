# 研究道里点名了 holdout —— 判读（2026-08-10 全库重扫发现）

## 事实

`check_holdout_mention` 加上「泛提 vs 点名」分档之后全库重扫，本工作区**有 1 处点名**：

`references/research/04-external.md` L21：

```
- src-8c8bf79b3942 / （scovil-later-activities-1920，**保留集，未在研究阶段引用**） **Scovil 两篇**，1913 / …
```

**这一处最重**：它同时写出了 **`source_id` 与文件名**（`scovil-later-activities-1920`），还标了年份。读到这一行的人不必再查任何东西，就知道那道题考什么。

## 为什么不改

本工作区**已经判过分**（`evals/results.jsonl`
有内容）。**判过分的产物不追改**——
与 Adams #131 的 `work.md`、Virchow #109 的两处同一处置：
改了会让「当时建模者读到的是什么」变得不可考。

**它的意义是记录，不是修补。**

## ★ 全库的真实分布（这个数此前是错的）

台账曾记「研究道 holdout 提及**已清到只剩 1 处**」。2026-08-10 重扫：

| | 行数 |
|---|---:|
| 提到 `holdout`／`留出集` 的行 | **37**（跨 7 个工作区） |
| 其中**泛泛提及**（不说是哪一份，无害） | **32** |
| **点名了具体作品的** | **5** |

点名的 5 处：Adams `work.md` 1、**Nightingale `04-external.md` 1**、
**Osler `05-decisions.md` 1**、Virchow 研究道 2。

★ 而 Barton 有 **16 行字面提及、点名 0** —— **旧的不分档计数会把 Barton 显示成最严重的**，
而它其实是干净的。**分档之前，这个排序是反的。**

## 给新工作区的一条

**要说「本路不引 holdout」可以，但不要写它是哪一份**
（书名、年份+题材、文件名、`source_id`、卷次页码，任何一种都算点名）。
