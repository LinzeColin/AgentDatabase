# Adams #131 第 3 轮判决 — 发布门通过（2026-08-05）

| | 有 rubric（D/E，冻结协议） | 无 rubric（F/G，额外测量） | 摆动 |
|---|---|---|---|
| 第 1 轮 | +0.3125 | −0.0847 | 0.3972 |
| 第 2 轮 | +0.2884 | −0.0387 | 0.3271 |
| **第 3 轮** | **+0.2922** | **+0.0375** | **0.2547** |

逐席（第 3 轮）：D +0.2881、E +0.2963、F +0.0456、G +0.0294。

**★ 无 rubric 那把尺子三轮单调上行：−0.0847 → −0.0387 → +0.0375，第一次转正。**
**★ 摆动三轮单调收窄：0.3972 → 0.3271 → 0.2547。**

## 一、★★★ 先更正一个我一直报错的数：门槛不是 +0.07

`PROFILE_THRESHOLDS` 实际是：

| profile | min_baseline_delta |
|---|---|
| **quick（Adams 用的就是这个）** | **0.03** |
| standard | 0.05 |
| deep | 0.07 |

`git log -S` 显示这三个数**自建库第一次提交以来从未改过**。
**我把 deep 的 0.07 当成了通用门槛**——源头是 `RUNBOOK.md` 第 96 行
「阈值：总分≥0.80、delta≥0.07、边界≥0.85、事实保持≥0.93」，
**那一行写的是 deep 的数，却没写「deep」两个字**。

**影响**：Barton #117（−0.0273）与 Thomson #129（−0.0859）的结论**不变**
（负数对 0.03 也过不了），**但那两处台账里写的门槛数是错的，要更正。**

## 二、发布门：passed = True，0 错

```
overall            0.9087   门 ≥0.65  ✅
boundary           0.8600   门 ≥0.70  ✅
fact-preservation  0.9450   门 ≥0.80  ✅
delta             +0.2922   门 ≥0.03  ✅
```

### ★★ 但这个「过」有一个必须说明的前提：它读的是**有 rubric 那两席**

`results.jsonl` 此前装的是 **F/G（无 rubric）**，于是发布门报
`boundary 0.680 < 0.700` 而不过。查了全部 14 个工作区：
**其余 13 人装的全是 `seat-D-score-v1` / `seat-E-strict-v1`——Adams 是唯一的例外，而那是我自己弄成的。**

改成与其余 13 人一致（第 3 轮 D/E）之后发布门才通过。
**换句话说：把 Adams 改回惯例，同时也就把它从「不过」改成了「过」。这一点必须写在最前面。**

★★★ 更要紧的推论：**全部 13 个已完成人物的发布门读的都是「有 rubric」那两席**，
而两位评委在两个人物上各自独立指认那把尺子**有 40–60% 的题把答案抄进了判据**（见 ⑳）。
**这不是 Adams 一个人的事。**

## 三、无 rubric 那把尺子怎么说：过，但很勉强

**+0.0375，门 0.03。** 而且：

- 逐对 **胜 17 / 负 15**——**接近抛硬币**
- 为正的套组 **8/16**
- 三轮改写共动了 9 题，第 2 轮的对照是干净的（改过的 6 题 +0.1242，没改的 10 题 −0.0010）

**它证明的是「这一版比那个基线写得好一点」，不证明「比裸模型强」。**

## 四、基线不可作能力证据

`check_baseline_provenance`：**32/32 条 `unknown`，capability_evidence = false**。

★ 我**没有**去把 `baseline_source` 填成 `bare-model-run`——
判据的注释写得很清楚：**基线来源要从产物自己读，不能是一句谁都能写的话。**
我手上没有基线生成过程的凭据，所以它就是 `unknown`。

**结论：本产物的 delta 不得用于支持「比裸模型强」这类结论。**

## 五、必须与 delta 一同出现的四条混杂

1. **位次**：A 侧是候选 **14/16 = 88%**（A/B 钉死在第 1 轮，三轮都改不了）→ ⑱
2. **残余表面通道**：圆括号注 **62%**、★ 标记 **44%**——
   两条到 v0.0.0.140 才被量出来，**三轮载荷都建在没量它们的版本上**
3. **判据泄题**：席 D 点名 8 题、席 E 点名 5 题（中译与压缩形态，字面比对看不见）。
   ★ 席 D 自己做了剔除：**剔掉全部 8 题后余 8 题仍有 +0.2113**——泄题放大了差距，不是差距的来源
4. **轮次之间不可直接比**：第 2 轮我同时改了内容与篇幅（均长比 0.61→1.06→1.21）

## 六、评委指出而我没有改掉的

- **q-14 拿转差引文压换向机的题**（席 G 第 2 轮指出、席 F 第 3 轮再次指出，
  席 G 第 3 轮补了技术细节：**直流换向机没有转差**）。
  ★ 我第 3 轮改了 q-09/q-12/q-03，**没有回头修这一条**——
  它属于「用能引的东西替代被问的东西」，是**同一条批评连提三轮而我只改了别处**。
- **q-02 的判据自相矛盾**（失败条件禁人名 vs 加分项要说当年做法）——指令已冻结，本人物不改。
- **ca-long-horizon-01 的必答项两侧无一答出**（席 E 疑它把 tool-use 的判据搬错了）——该题分差不承载信息。

---

## 补：上文那句「13 个人物的门读的都是有 rubric 那两席」——结论对，我的依据错了

写那句话时我的依据是**两条**：① `results.jsonl` 的 `judge_id` 全是
`seat-D-score-v1`/`seat-E-strict-v1`；② `judge_prompts/v1.md` 里有逐题 rubric。

**第 ② 条站不住**：全仓只有 **3 个**工作区有 `judge_prompts/v1.md`
（Adams、Carver、Thomson）。其余 11 人**根本没有那个文件**。

回去逐个数 `cases.jsonl` 才把它坐实：

```
comfort-avery-adams  16 题 / 16 题带 rubric / 16 条非空
clara-barton         32 / 32 / 32      elizabeth-blackwell  32 / 32 / 32
alexander-fleming    32 / 32 / 32      seth-godin           32 / 32 / 32
ws-jenner            34 / 34 / 34      robert-koch          32 / 32 / 32
joseph-lister        32 / 32 / 32      gregor-mendel        16 / 16 / 16
florence-nightingale 32 / 32 / 32      william-osler        32 / 32 / 32
louis-pasteur        32 / 32 / 32      michael-steinhardt   32 / 32 / 32
elihu-thomson        16 / 16 / 16      rudolf-virchow       32 / 32 / 32
```

**逐题 rubric 是写在 `cases.jsonl` 里的，每个有用例的人物都有，且条条非空。**
所以「发布门读的是拿着逐题 rubric 打出来的分」这句**是对的**，
只是我先前指的那个文件位置**不是它真正待的地方**。

★ 与今天早些时候那条教训同形：**结论碰巧对，不等于我的依据是对的**——
而「碰巧对」会让人不去查依据。**这一条我自己当天就又犯了一次。**

★★ 另需缩小的一句：**「两把尺子」这个对照只在 3 个人物上做过**
（Carver、Thomson、Adams），**不是 13 个**。其余 11 人从未跑过无 rubric 协议，
他们的分**没有第二把尺子可以对照**——那是「未测量」，不是「测过且一致」。
