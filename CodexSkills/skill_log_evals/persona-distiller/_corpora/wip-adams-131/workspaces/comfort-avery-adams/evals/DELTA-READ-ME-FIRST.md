> ★★★★ **2026-08-10 从工作区根搬到 `evals/` 下。**
> 它原本在根目录，而**根目录的 `.md` 是建模者读得到的**——
> 一份记录 holdout 隔离失效的事故报告放在那里，本身就是新的泄漏。
> `evals/` 对建模者是明令禁读的。（Nasmyth #153 的结案记录同日同因搬过。）

# 先读这一页，再引用本产物的 delta

**发布门通过（passed=True，0 错）。记录的 delta 是 `+0.2922`。**
**那个数不能当「比裸模型强」的证据用。** 下面是四件已实测的事。

## 一、同一批答案，只换评委手里有没有 rubric，差 0.2547

| | 候选 / 基线 | delta |
|---|---|---|
| **有 rubric**（冻结协议 D/E，**发布门读的就是这个**） | 0.909 / 0.617 | **+0.2922** |
| **无 rubric**（同一批答案、同一 A/B、同一天） | 0.807 / 0.769 | **+0.0375** |

无 rubric 那一侧**逐对只有 胜 17 / 负 15**——接近抛硬币；为正的套组 8/16。

★ 三轮的走向值得看：无 rubric **−0.0847 → −0.0387 → +0.0375**，摆动 **0.3972 → 0.3271 → 0.2547**。
**两把尺子在靠拢，但还没有合到一起。**

## 二、发布门读的是「有 rubric」那两席，而那把尺子被两位评委各自指认过

席 D 点名 **8 题**、席 E 点名 **5 题**的评分标准把答案抄了进去，
形态是**中译与压缩**（`取决于制造与使用两侧的许多细节` ← `depends upon avery great many
details not only of manufacture but`），**字面比对的判据看不见，实测报 0/16**。

★ 但**不是全部差距都来自泄题**：席 D 自己做了剔除——**剔掉全部 8 道受污染题，余 8 题仍有 +0.2113**。

★★ 而且这**不是本产物特有的**：全部 13 个已完成人物的 `results.jsonl` 装的都是这两席。

## 三、基线不可作能力证据

`check_baseline_provenance`：**32/32 条 `unknown`，`capability_evidence = false`**。

我**没有**把 `baseline_source` 填成 `bare-model-run`——判据的注释写明
「基线来源要从产物自己读，不能是一句谁都能写的话」，而我手上没有那份凭据。

**所以：本产物的 delta 只说明它比这个对照写得好，不说明它比裸模型强。**

## 四、四条混杂，必须与 delta 一同出现

1. **位次**：A 侧是候选 **14/16 = 88%**（A/B 钉死在第 1 轮，三轮都改不了）→ 待裁定 ⑱
2. **残余表面通道**：圆括号注 **62%**、★ 标记 **44%**——**到 v0.0.0.140 才被量出来**，三轮载荷都建在没量它们的版本上
3. **长度**：三轮均长比 0.61 → 1.06 → 1.21，**第 2 轮同时改了内容与篇幅，两轮之间不可直接比**
4. **holdout**：第 1 轮判分时隔离是失效的（3 份 holdout 同时在 train 目录里），
   **第 2 轮起才是真正的 holdout 测试**；第 1 轮的 `known` 分数按判据口径不可信

## 五、评委连提三轮而没有改掉的一条

`ca-tool-use-01` 拿一句讲**转差**的引文去压一道**换向机**的题。
席 G 第 2 轮指出、席 F 第 3 轮再次指出、席 G 第 3 轮补了技术细节：**直流换向机没有转差**。
**第 3 轮我改了别的三题，没有回头修这一条。**

## 唯一在两把尺子下都站得住的

`fact-preservation`：有 rubric +0.71、无 rubric **+0.4350**，三轮都是最高的那一项。
**「答案能不能被核」这一条上，本产物确实优于该对照。其余不要引。**
