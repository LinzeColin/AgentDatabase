# Adams #131 当前状态：判分做完、发布门过、**strict 打包过不去**（2026-08-05）

## 做完的

| | |
|---|---|
| 三轮判分（四席 × 两协议） | ✅ 12 席次全部完成 |
| 发布门 `--phase release` | ✅ **passed = True，0 错，2 警** |
| 有 rubric（冻结协议 D/E） | delta **+0.2922**，overall 0.9087 / boundary 0.8600 / fact 0.9450 |
| 无 rubric（额外测量 F/G） | delta **+0.0375**（三轮 −0.0847 → −0.0387 → +0.0375） |
| quick 门槛 | delta ≥0.03 / overall ≥0.65 / boundary ≥0.70 / fact ≥0.80 —— **四项全过** |

## 卡住的：`--strict` 下两条 warning 也拦，两条都是真的

### ① `eval.baseline-not-capability-evidence`（**我不打算绕过**）

`results.jsonl` 的 baseline 行缺 `baseline_source`，判据读成 `unknown`，
于是 `capability_evidence = false`。

**已入库的 Carver #127 那 32 行写的是 `bare-model-run`，所以这个字段是「写进去」的。**
**我没有把它写进去，理由是我拿不出凭据：**

- 工作区里**没有任何基线生成过程的记载**（`evals/README.md` 不提，
  `baseline.v1.json` 只有 case_id→答案，无元数据）
- 我在构建载荷时确实每次都传了 `--baseline-source bare-model-run`，
  **但那是我在命令行上说的一句话，不是凭据**
- 判据自己的注释写得很直白：**「基线来源要从产物自己读，不能是一句谁都能写的话。」**

★ **重做一份有记录的基线也不行**——那会改基线，撞「中途不得改基线」。

### ② `corpus.unexamined-band`（**已按它要求的做了，但拦仍在**）

判据报 **27/69 份**语料「字符够而词数不够，没有任何判据看过内容」，
并写着「若某条道只靠这类文件撑着，**请人看一眼**」。

**我看了。** 逐份抽读：**全部是真的 Adams 讨论席发言**，只是短
（87–124 词），例如
`C. A. Adams: The information contained in this paper is very valuable, but still somewhat empirical.`
**是覆盖缺口，不是缺陷**——与判据自己的定性一致。

★ 但**我数出来是 31 份，判据说 27 份**。两个数不一致 → 有一个错了。
差别在正文起点的切法（我按 `=`×80 分隔线切，判据用别的口径）。
**没有去追平这个差**，因为它不改变结论；但记在这里，别当成同一个数引用。

★★ 要真正消掉这条，得**放宽语种判据的词数下限**——那是改测量工具，
**中途不许改**。

## 所以这一步不做什么

- **不用 `--skip-quality`**
- **不手填 `baseline_source`**
- **不改语种判据的下限**

## 需要用户裁定的一件

**基线出处这条：是接受「命令行参数 + 提交记录」作为凭据，还是必须有产物级记载？**

- 若接受 → Adams 现在就能打包入库（其余各项已全过）
- 若不接受 → **已入库的 Carver 那 32 行 `bare-model-run` 也同样没有产物级凭据**，
  该一并复核

★ **为什么由你定**：这决定的不是 Adams 一个人，是**「基线出处」这个字段到底算什么**；
而且往回看会牵动已入库的产物。
