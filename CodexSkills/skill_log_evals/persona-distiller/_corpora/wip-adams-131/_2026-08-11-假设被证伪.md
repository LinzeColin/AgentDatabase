# 2026-08-11：我猜「Adams 卡的是基线出处」，**猜错了**

## 假设

今天在 Shewhart #165 上查出：`assemble_judge_results` **从不写 `baseline_source`**，
发布门于是永远读到 `unknown`，**用户 2026-08-05 在 ⑭ 的裁定
（裸模型基线的长度两条只报不拦）至今不可达**。
据此猜 Adams #131 卡在「strict 打包的基线出处」（待裁 ㉒）是同一个根因。

## 实测：**不是**

`quality_check --phase release` 今天实跑，4 条错：

```
✗ corpus.holdout-work-named-in-artifacts     work.md 里直接说出了 holdout 是哪一份
✗ corpus.holdout-mentioned-in-artifacts      work.md 里出现「holdout」一词
✗ corpus.holdout-visible-as-numbering-gap    编号缺口暴露在文件名序列里（3 处）
✗ content.quote-no-locator                   69 条逐字引文，27 条缺坐标
```

`baseline_provenance` 确实是 `{"unknown": 32}`、`capability_evidence: false`，
**但那是 warning，不在这 4 条错里**。

★★ **而且我的修复对他无效**：他的基线**根本不是裸模型跑出来的**，
没有「裸模型运行」可以声明，也就没有运行记录可以出示。
我加的那个参数只让「真跑过裸模型的人」把事实传下去，**造不出一次不存在的运行**。

## ★ 自报：**今天新加的门给这个本来就卡着的工作区又加了一道拦**

`content.quote-no-locator` 是今天才把产物接进去的（此前只扫答案与断言，不扫产物）。
Adams 69 条引文里 27 条缺坐标——**这一条以前不会报，今天开始会报**。

**这不是把门放松回去的理由**：缺坐标是真的，读者确实回查不了。
但记录必须写明：**他今天多出来的那道拦是我加的，不是他退步了。**

## 前三条：与 ㉒／㉒加料 是同一件事，不是新问题

任务台账 #79 早已记着：「研究道 holdout 提及已清到只剩 1 处（Adams 的 `work.md`，
**有意不改**——它是被判过分的产物）」；#78 记着「holdout 隔离在三条独立通道上都不成立」。

→ 今天的实跑只是把那个已知受阻项**复现**了一遍，**没有新增信息**。

## 结论

- 假设**被证伪**；Shewhart 那条修复**解不掉 Adams**。
- Adams 仍受阻于 ㉒／㉒加料（holdout 隔离）——**那确实只能由用户裁定**，
  因为改 `work.md` 等于改一份**已经被判过分的产物**。
- 新增那条（引文坐标 27/69）**可以自己做，与裁定无关**，记为待办。
