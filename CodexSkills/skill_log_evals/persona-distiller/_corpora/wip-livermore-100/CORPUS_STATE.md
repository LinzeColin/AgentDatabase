# #100 Livermore 语料状态（2026-08-04 二次复核，**已找回**）

## 结论先写：**536/536 全部按校验和找回并逐份复核**

位置：`~/Downloads/蒸馏/_corpora/livermore-100/verified-by-checksum/`（17 MB），
**按账本的 `local_path` 布局存放**，每一份复制后都重新算过 sha256 与账本比对。

## 此前两版记录都不完整

**第一版**（v0.0.0.46 手工审计）：「账本 536 条、语料 0 份」——如实，但读成了「丢了」。

**第二版**（本日早些时候）：查 D 档只找到 `corpus_newspapers_541.tar.gz`，
逐份对校验和**只有 143/536 相同**，于是记为「可核语料只剩 143/536，
重蒸前要重新抓源」。

**那个压缩包确实是另一次抓取。** 但真正入库的那一批**在本次会话的 scratchpad 里**
（`pd-work/core_livermore/`，930 份），**536 条全部对得上**。

## ★ 这件事差一步就成了不可逆的损失

C 档（会话 scratchpad）的规矩是「每轮清」。
我在清之前逐个目录核了一遍，才发现 `pd-work/` 下有
`vesalius-corpus` / `galen-corpus` / `harvey-corpus` / `core_livermore` 四个目录——
**四个「语料缺失」的工作区，语料全在那里**：

| 工作区 | 账本 | scratchpad 里对上 |
|---|---:|---:|
| Vesalius #102 | 47 | **47** |
| Harvey #103 | 46 | **46** |
| Galen #101 | 60 | **55** |
| Livermore #100 | 536 | **536** |

**2026-07-29 那次清理前抢救出 Godin 194 份，是同一个形状。**
这一次是第二次。**「每轮清 C 档」这条规矩必须配一条「清前逐目录核对校验和」**，
否则它自己就是不可逆损失的来源。

## 体积裁定未变

536 份进 D 档不进 git——**7.9 MB 一个人物、600 人 4.7 GB 那笔账没变**。
Vesalius（47）、Harvey（46）、Galen（55）体量小，已还原进 git 的工作区。
