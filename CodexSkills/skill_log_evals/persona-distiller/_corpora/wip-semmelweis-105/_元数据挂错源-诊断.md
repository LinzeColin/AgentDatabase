# Semmelweis #105 台账元数据挂错源 —— 诊断（2026-08-11）

> 原记「23 条年份待逐份读题名页」。**诊断之后，问题的形状与那句话不一样。**

## 一、坏的不是文件，是台账

读了三份文件自己印的题名页：

| 文件 | 题名页（照录） | 台账 `locator` | 判 |
|---|---|---|---|
| `x_meigs_obstetrics_science_art_1849.txt` | `OBSTETRICS: THE SCIENCE AND THE ART. BY CHAELES D. MEIGS^ M. D.` | `obstetricmemoirs01simp`（**Simpson**） | **文件名对，台账错** |
| `x_heymer_beitraege_puerperalfieber_wuerzburg_1847.txt` | `Beiträge zum Puerperalfieber … in Wiirzburg … von Carl Heymer` | `paper-doi-…-185507000`（1855 期刊论文） | **文件名对，台账错** |
| `p_buhl_klinik_geburtskunde_1864_v1.txt` | 前 14 行是扫描封面的 OCR 碎片，未定案 | `pathfindersinmed00robiiala` | 待读正文 |

→ **语料本身是好的。坏的是 `published_at` + `locator` 这一整块，它被挂到了别的源上。**

## 二、射程（★ 分标识符类型算，混着算会误报三倍）

| 类 | 行数 | 说明 |
|---|---:|---|
| 描述性标识符且对得上 | 24 | 正常 |
| **描述性标识符且对不上** | **11** | ★ 真可疑，见下 |
| 不透明标识符（Wellcome 条码 `b\d{7,}`／Google `bub_gb_`／IA 编号／DOI／BSB） | 17 | **本法判不了，记为未判** |
| 无 URL locator | 8 | 未判 |
| 合计 | 60 | |

★★ **我第一版把 32 行报成「挂错」，是误报。** 根因两条，都记下来：
1. 拿词重叠去判**不透明标识符**——Wellcome 条码按设计就不含题名词。
   `b_wellcome_b24875788_1905.txt → b24875788` **本来是对上的**，被判成不一致。
2. 分词法把 `semmelweisgesamm00semm` 当成一个词，与文件名的 `gesammelte` 比不出前缀，
   于是 `w_gesammelte_werke_ia_1905.txt` 这种**真对上的**也被报成可疑。改成子串比对后消失。

## 三、11 条可疑的，以及标识符**看起来该属于谁**

★★★ **右列是启发式猜测，不是结论。** 每一条都要**读那份文件的题名页**才能定案。

| 文件（文件名说它是） | 台账 `published_at` | 台账 `locator` | 这个 locator 看起来该属于 |
|---|---:|---|---|
| `p_buhl_klinik_geburtskunde_1864_v1` | 1912 | `pathfindersinmed00robiiala` | `b_pathfinders_in_medicine_1912` |
| `p_new_england_quarterly_1843_holmes` | 1842 | `lavieluvredephil0013vari` | 配不回本工作区任何文件 |
| `x_ferguson_das_kindbettfieber_1840` | 1862 | `daspuerperalfieb0000unse` | （疑）Döderlein 那份 |
| `w_zwei_offene_briefe_spaeth_scanzoni_1861` | 1863 | `transactions-of-the-obstetrical-society-of-london_1863_4` | `p_medico_chirurgical_transactions_1849_routh` |
| `w_gesammelte_werke_v1_1905` | 1905 | `bub_gb_oVDSUzGAU1cC` | 配不回（Google Books 不透明） |
| `w_gesammelte_werke_v2_1905` | 1905 | `semmelweisgesam00semmgoog` | `b_semmelweis_leben_wirken_g_1905` |
| **`x_virchow_gesammelte_abhandlungen_1862`** | **1905** | `semmelweisgesam01semmgoog` | `b_semmelweis_leben_wirken_g_1905` |
| `x_scanzoni_compendium_geburtshilfe_1861` | 1849 | `essaysonpuerpera00chur`（Churchill） | `x_holmes_medical_essays_1842_1882` |
| `x_kopseel_das_puerperalfieber_1865` | 1892 | `dasscheidensekre00dd` | `x_doederlein_scheidensekret_puerperalfieber_1892` |
| `x_eisenmann_wundfieber_bsb_1837` | 1837 | `bub_gb_dHI_AAAAcAAJ` | 配不回（Google Books 不透明） |
| `x_hugenberger_puerperalfieber_petersburg_1862` | 1867 | `epidemischepuerp0000wilh` | `x_heymer_beitraege_puerperalfieber_wuerzburg_1847` |

★ **最刺眼的一条**：`x_virchow_gesammelte_abhandlungen_1862` 的 `published_at` 是 **1905**、
locator 指向 **Semmelweis 全集**——**一份 Virchow 的文件挂着 Semmelweis 的书目**。

## 四、下一步该怎么做（不是逐份读 23 张题名页）

1. **只读这 11 份的题名页**（不是 23 份，也不是 60 份）——文件名已被验证是可信的一侧。
2. 每读一份，**同时改 `published_at` 与 `locator` 两个字段**——它们是一起挂错的，分开改会留下半错。
3. 不透明标识符那 17 行**记为未判**，不要因为「没报错」当成通过
   （[[empty-default-swallows-unknown]]）。
4. ★ 改完之后 `check_filename_year_vs_ledger` 的 23 条应当大幅下降；**若没有下降，说明诊断错了**，
   回头重看，**不要去调判据**。
