# Hugo Grotius #168 —— 结案：**拒发**

日期：2026-08-11　档位 quick　两轮，每轮两席、**都不喂 rubric**

## 判决

| 轮 | 席一 | 席二 | 诚实 delta | 按**判分前写死**的规则怎么读 |
|---|---|---|---|---|
| 1 | H −0.0137 | J −0.0144 | **−0.0141** | \|Δ\| < 0.0328（2 SE）→ **测不出差别** |
| 2 | K −0.0675 | L −0.0306 | **−0.0491** | \|Δ\| > 0.0328 且 ≤ −0.03 → **明确劣于基线** |

**两轮都为负，两席都同号。** 与 Bessemer #132／Thomson #129／Sorby #133 同类。
**第 3 轮额度未用。**

## release 门当前状态（判分后现跑）

    errors 2　warnings 2
    ✗ eval.baseline-delta        −0.049 < 0.030　← **这就是拒发的理由本身**
    ✗ corpus.ocr-dead-as-primary **留红并记账**（见 `_ledgers/_一手源与可逐字引用是两件事`）
    ✓ eval.no-results 已消失｜baseline_provenance: 32 行全 bare-model-run，capability_evidence: true

研究门 0 错｜合成门 0 错。

## 这一轮真正值钱的不是分数

### ① 候选侧作废重跑两次，**两次都是我自己泄的题**

两次都靠**答题方 `__incident__` 主动上报**抓到，**三道现有的门全绿且都没报错**
（`check_holdout_mention` 的文件头明写它抓不到这一类）。

1. `hypotheses.md`／`01-writings.md` 写「分到的十二份里**没有一份是护教／神学体裁**」，
   而 holdout 正是一部护教之作。「十二份」这个数出自**我当天写的** `emit_lane_scope.py`。
2. **修完之后我加的那段「为什么改」的注解**里直接写了
   「本人物的 holdout 恰好就是一部护教之作」——**比原句更直接**。

**教训已成规矩**：改动理由不写在答题方读得到的文件里，写进 `evals/`（那儿他们读不到）。
**已落成判据** `check_negative_space_leak.py`。

### ② 修好泄题让分数变差，**这是对的方向**

`known`（holdout 探针）从 **+0.0600 掉到 −0.2700**。
第 1 轮的候选**读过泄题的产物**，第 2 轮重跑的没有。
**第 1 轮那个正分很可能是泄题喂出来的**——靠泄题拿到的正分不该留着。

### ③ 负 delta 的 64% 来自「rubric 判候选对、评委判它错」的 3 道题

`known` 的 rubric 白纸黑字：「答『这一部不在我手边』**可接受**，且比编一个卷次表好」；
「报出具体卷数**必须给得出可核的出处；给不出即为不合格**」。
**基线给了六卷、零出处；候选说「我说不出」。两席把基线打高 0.3。**

抽掉那 3 道，其余 26 对 −0.0215 回到「测不出」。
★ **但抽掉不利的题再报数不作数，处置用全量 −0.0491。**
★★ 同日全库回扫（7 人）**否掉了「这是全库通病」**：4 更差 / 3 更好，均值方向相反。

### ④ 评委抓到一条装置缺陷

席 K：「载荷里是 `q-01`…`q-16` 顺序编号，且同类题相邻——**顺序本身携带信息**」。
去核：`assign()` 按 case_id 字母序，于是 `q-01` 恒 = anonymous-fidelity、`q-16` 恒 = voice。
**已改为按 `sha256("order|"+cid)` 排。**

## 若要跑第 3 轮

**必须先解决 `token-efficiency`（−0.3150）与 `known`（−0.2700）两档，
不许再动长度带**——那已经动过一次（140–280 → 140–210），再动就是在调尺子。
