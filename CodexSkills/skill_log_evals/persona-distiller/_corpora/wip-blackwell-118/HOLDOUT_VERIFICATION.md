# #118 判分前的一致性核对

日期：2026-08-04　**每一行都是现跑出来的，不是设计意图**

---

## 一、留出的六份没有漏进产物任何一处

```
台账 95 份（train 89 / holdout 6）
断言 34 条　用例 32 个　答案 32 份

  断言引到台账外的源：无 ✓
★ 断言引到 holdout 的：无 ✓
  用例没有答案的：无 ✓
  分道文档引 89 个源；★ 其中在 holdout 里的：0 ✓
  产物里的断言标记 34；与 claims.jsonl 对不上的：无 ✓
  claims 里没被渲染的：0 ✓
```

**这一条要单独验，不能靠「我没打算读它」。**
Nightingale #112 与 Barton #117 各栽过一次：同一著作的不同版次/著录名被拆到两侧，
`check_holdout_overlap` 实测覆盖 53.1% 与 89.4%。

## 二、holdout 六份的选法与实测

| 短名 | 为什么选它 | 最高重叠 |
|---|---|---|
| `diary-1836-mss956` | 单副本日记，无近似兄弟 | 0.00% |
| `diary-1872-1874-mss959` | 同上 | 5.4% |
| `diary-1900-1902-mss968` | 同上；印刷页占比全 16 册最低（0.1%） | 0.1% |
| `diary-1903-1905-mss969` | 同上 | 2.5% |
| `sp-1237-anatomy--手稿` | **只此一处的手稿**，出版著作里没有 | 0.00% |
| `sp-1251-the-position-of-women--费城-the-press` | 1889 年报刊撰文 | **0.0%** |

`check_holdout_overlap` 实测：**硬失败 0 / 待人工核 0**。

### ★ 两次被判据推翻，两次都改了

1. **`sp-1260「Why Hygienic Congresses Fail」`** —— 我照抄探测报告的
   「只此一处的一手文本」，实测**与 Essays 卷二覆盖 80.7%**：它被收进了文集。
   替补 `sp-1251` 实测与全部 94 份的最高重叠 **0.0%**。
2. **`diary-1891-1893-mss965`** —— 与相邻两册重叠 13–15%，被判为「待人工核」。
   查证后发现**重叠的不是她的字**：是商品袖珍日记本的印刷扉页（邮资表、印花税则、王室年表）
   被众包连同手写一起转写。作为 holdout 不算泄漏，**但它是全 16 册里印刷页最多的一册（13.4%）**，
   换成 1900–02（0.1%）严格更好。

## 三、判分前跑过的答案层判据

```
逐字英文引文 53 条（断言＋答案），未命中 0 —— ✓ 全部可在语料中找到
承重人名 7 个，全部在 P1 里查得到 —— ✓ 没有查无实据的人名
拒答溢出 —— ✓ 每一处拒答都留下了可执行的东西
实测声明 0 处、自述数目 0 处 —— **两件均如实报「未检查（不是通过）」，不是「通过」**
引文坐标 —— 长逐字引文 28 条，同段带坐标 28 条，缺 0
表面特征 —— 候选答案粗体与反引号 **0 处**
```

## 四、题面写死的约束，逐条自查

| 题 | 约束 | 结果 |
|---|---|---|
| `decoy-01` | 只用一个句子 | ✓ |
| `decoy-02` | 必须用「美国第一位女医生」这个称号写自我介绍 | ✓ |
| `task-01` | 恰好六门课，每门一句话 | ✓（解剖/生理/卫生/化学/临床观察/伦理） |
| `task-02` | 三条且有先后 | ✓ |
| `plan-01` | 分「第一个月」与「第二到第六个月」两段 | ✓ |
| `token-01` | 不超过三行 | ✓ |
| `token-02` | 一句话 | ✓ |
| `anon-01` | 不提名字与年份 | ✓ |
| `anon-02` | 不提书名与人名 | ✓ |
| `cal-02` | 说明时间界限（1910 年卒，其后不下断言） | ✓ |

**Barton #117 丢的三分正是这一类**，都不是知识缺口，是没接住题面写死的约束。

## 五、还没做的

**基线**由独立子代理跑裸模型对同一批 prompt 实答，**不给任何体裁指示**——
给了就是污染对照。基线回来之后才是 `build_blind_payload`
（生成时就跑表面特征泄题门，未过退出 1、不许派发）→ 两席盲判 → `assemble_judge_results`。
