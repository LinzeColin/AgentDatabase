# #172 Louis Brandeis —— **预备已完成，等排进批次**（2026-08-12）

## 他不是延后，也不是做完

`next_person.py` 把他列在「**已做但未出货**」里，而那一栏是个**诊断桶**，不是处置。
逐项核过：

| 项 | 状态 |
|---|---|
| 预筛 `00-预筛.md` | ✓ |
| 同名判定 `00-同名判定.md` | ✓ |
| 可得性与同名实测 `01-可得性与同名实测.md` | ✓ |
| 抓源清单 `00-抓源清单.md` | ✓ |
| `evals/results.jsonl` | **0 行——不是 delta=0，是空文件**（从没判过分） |
| `meta.json` | `status: draft`、`profile: quick` |

⇒ **没有任何东西挡着他**。他只是**没被排进批次**。

## 为什么现在不做他

2026-08-12 用户把工作方式改成 **10 人一批、按阶段跑**，
第 1 批是 `worth_starting.py` 排出的前 10 人（#173–#182），**他不在其中**——
不是被否决，是**排序时他已有工作区，`worth_starting` 只排 pending**。

## ⇒ 处置：**排入第 2 批**

第 2 批开工时，他**与 `worth_starting --top 20` 的第 11–20 名一起排**，
且**预备工作已完成，可直接进阶段 1a（探源）**。

★ 写这份文件的理由：HANDOFF 里记着一条教训——
**「既不在名册也不在延后名单」的人，接手的人会把他们当成「从没碰过」**。
他做过四份预备文件，**别让下一个人从头再做一遍**。
