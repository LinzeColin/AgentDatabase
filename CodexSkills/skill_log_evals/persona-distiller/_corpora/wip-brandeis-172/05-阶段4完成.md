# Brandeis #172 阶段 4 完成 —— 2026-08-13

**状态：阶段 1a–4 全部走完，停在阶段 5（判分）。**
判分需要 2 席独立子代理，只能由用户授权，本会话的 harness 不许派子代理。

---

## 一、这一轮真正的发现：**14 条第一人称候选里 9 条不是他**

机械抽 → 逐条读原文前 700 字，真值：

| 「我」实际是谁 | 条数 | 出处 |
|---|---|---|
| 尤蒂卡审计官 **Fred G. Reusswig** | 3 | 《Other people's money》里一整段长引语 |
| ICC 听证证人 **Henry R. Towne** | 3 | 《Scientific management and railroads》 |
| **Mr. Justice North** 的判词 | 1 | 《The Right to Privacy》的脚注里 |
| Sydney G. **Fisher** | 1 | 《Notes》 |
| 一名工厂主的转述 | 1 | 1918 Schweinler 辩状 |
| **Sir W. MacCormac** 在上议院委员会作证 | 1 | `decisions` 道那一卷（另计） |

★★ **`flag_borrowed_voice` 当天只标出 1 条，漏 8。** 三种机制它此前不认识，已补：
**⑤未闭合引语**（Reusswig／North）、**⑥听证转录的说话人标记**（Towne）、
**⑦引证抬头**（MacCormac —— 「Brandeis Brief」体**整段不打引号**，
靠一行「姓名，职衔：」把下面归给别人）。补完后命中 9/9、误伤本人 0/5。

## 二、第二个发现：**一部署他名字的书，前七分之一不是他写的**

写断言层时差一点把三条事实记成他的，而它们出自《Business—a profession》的卷前导言：

    src-f262a6c0fb76   0–62,094    13.57%   Ernest Poole 导言
    src-3d16531d4151   0–64,956    14.11%   同上
    src-cc33bc7e060b   0–73,433    13.71%   1925 版还多一套注，署名 Felix Frankfurter

揭穿它的是一句第三人称转述——**一本他自己的书里不会有人用第三人称转述他**。

⇒ 落成 `detect_front_matter.py`（按书眉的**周期性**定正文起点，测不出报 null 不猜）。
`gen_claims.py` 每次跑都断言：取自这三份的偏移必须大于各自的正文起点。

## 三、产物与用例

| | |
|---|---|
| 断言 | **22 条**（fact 14／work-method 4／boundary 1／value 1／mental-model 1／hypothesis 1） |
| 事实密度门 | **人物事实 14 ≥ 7**（`ceil(34/5)`）、**可复用做法 4 ≥ 3**，rc=0 |
| 十份产物 | 正文**从 claims.jsonl 现渲染**，22/22 条断言被引用，32 处 `<!-- claim: -->` 标记 |
| 盲判用例 | **32 题 / 16 类**，其中 **6 题建在 4 份密封源上** |

### 判据（本轮实跑）

    check_fact_density 0 ｜ check_holdout_mention 0 ｜ check_quote_speaker 0
    check_verbatim_quotes 0 ｜ check_claim_coverage 0（装饰性引用 0/22）
    check_quote_integrity 0（93 片段 0 未命中；★ 另有 34 处短串本就没查）
    check_lane_quotes_verbatim 0 ｜ check_negative_space_leak 0
    check_case_self_sufficiency 0（32 题无断链指代）
    check_cases_match_generator 0（10 个生成器重跑逐字未变）

## 四、这一轮自己造出来又自己抓到的两个坑

1. **偏移手抄**：第一版把引文偏移从别的脚本抄过来，**六条全错**——
   那个脚本的归一化是 `[-¬]\s*\n\s*`，这里是 `dehyphen()`，同一句偏移不同。
   断言当场拦下、没写出文件。已改成偏移由脚本自己算。
2. **泄题词**：我自己在 `boundaries.md` 里写了 `holdout` 这个词，被门抓到。
   已改写，并把泄题词自查写进产物生成器（这是第四次犯同一个错）。
3. **按体裁描述缺口**：`04-external.md` 写「传记、讣告、评论一份都没有」——
   缺口的另一侧就是密封集。已改成「这一问本道答不了」。

## 五、门的位置（判分之前必须知道）

    一手 29 ／二手 9 ／需人判 0 ／一手占比 0.7632
    train 逐道 {writings 21, external 9, expression 2, decisions 1, conversations 1}
    deep rc=1 ｜ standard rc=1 ｜ **quick rc=0**

⇒ **按 quick 档判分**：门 = delta ≥ +0.03。
★ 16 题的 quick 档分辨力只有 1.83 SE（`se_case = 0.0656`）——
**这一档本来就分不清 +0.03 与 0**，本轮做了 32 题正是为此（2.59 SE）。

## 六、下一步只能由人做的事

**阶段 5 判分需要 2 席独立子代理**，按项目规矩恒 2 席、按人物冻结指令、每人上限 3 轮。
本会话不许派子代理 ⇒ **停在这里，等授权**。
