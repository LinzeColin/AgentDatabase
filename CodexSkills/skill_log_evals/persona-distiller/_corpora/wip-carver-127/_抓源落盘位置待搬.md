# 抓源落盘位置：**我给的路径是错的，等抓源跑完要搬一次**

日期：2026-08-05　状态：**待办**（抓源代理仍在跑，**跑完再搬，不要动它正在写的目录**）

---

## 约定是什么（实测三个人物）

```
wip-<人>/raw/                         ← **暂存区**：抓源落这里
wip-<人>/workspaces/<slug>/raw/       ← **工作区自己的 raw**：由 `ingest.py` 写
```

| 人物 | 外层 raw（暂存） | 工作区内 raw |
|---|---|---|
| barton-117 | `wip-barton-117/raw` | `workspaces/clara-barton/clara-barton/raw` |
| blackwell-118 | `wip-blackwell-118/raw` | `workspaces/elizabeth-blackwell/raw` |
| nightingale-112 | `wip-nightingale-112/raw` | `workspaces/florence-nightingale/florence-nightingale/raw` |

**两层各有各的用处**：`check_staged_but_not_ingested` 比的就是这两层的差
（「抓到了但没进工作区的」）。**只有一层就比不了**——
该判据今天已经把 Carver 报成「`wip-carver-127`：没有外层 `raw/`，**没走过抓源台账那一步**」。

## 我错在哪

**我在抓源指令里写的落盘位置是 `workspaces/george-washington-carver/raw/`**——
那是**工作区自己的 raw**，不是暂存区。

★ 这是今天第三处**指令前提写错**：
① 告诉 lane 2 检索「不要找通信集」（而 lane 2 里装得最多的恰恰是通信集）；
② 告诉抓源方手写 `_ids.txt` 并给了错的分隔符与列序（它其实由 `build_source_ledger.py` 生成，9 列制表符）；
③ 本条：落盘位置给成了工作区内层。

**三次都是「凭印象写约定，没先去仓里核」。**

## 跑完之后怎么收（**顺序不能反**）

```bash
# 1. 把抓源产物搬到暂存区
mv wip-carver-127/workspaces/george-washington-carver/raw \
   wip-carver-127/raw

# 2. 用共享件写台账（**--no-copy**：不复制正文，只写台账）
python3 scripts/build_source_ledger.py --rows <_fetch_rows.json> \
        --src wip-carver-127/raw --raw wip-carver-127/raw --no-copy

# 3. ingest 才是把语料落进工作区的那一步
python3 scripts/ingest.py <workspace> wip-carver-127/raw/...
```

★★ **第 2 步必须 `--no-copy`**：`build_source_ledger` 会写 `<short>/<short>.txt`，
而 `ingest.py` 又写 `src-XXXX/<原名>.txt`——**两个都跑就是双份**。
Liebig #124 的 `raw/` 因此存了两遍、**42 MB**。

★ `_fetch_rows.json` 里的 `file` 是绝对路径，**搬完要一起改**，否则 `build_source_ledger` 找不到源文件。
