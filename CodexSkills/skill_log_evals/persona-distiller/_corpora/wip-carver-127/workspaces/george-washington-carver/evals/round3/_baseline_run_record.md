# 裸模型基线运行记录（`bare-model-run`）

日期：2026-08-05　执行方：独立子代理（general-purpose），**无语料、无本技能、无工具、不联网**

## 为什么要跑这一次

`check_baseline_provenance` 的 strict 判据：基线若是 `self-authored-strawman` 或 `unknown`，
**delta 不得用于支持「比裸模型强」这类结论**；且 `--strict` 下发布门直接不过
（`passed: False`，errors 0——★ `package_target` 把原因印成 `[]`，因为它印的是 errors 而 errors 是空的）。

第 2 轮的基线是我手写的，属 `self-authored-strawman`。**要拿到能作能力证据的数，只有实跑裸模型这一条路。**

## ★★ 基线在本人物身上被换过两次——这一点必须能被审计

| 轮 | 基线 | delta | 为什么换 |
|---|---|---|---|
| 1 | 我手写（**看过 rubric 之后写的**） | +0.6553 | 三题正好踩满 rubric 的失败条件，是靶子不是基线 |
| 2 | 我手写（按 no-Skill 标准重写） | +0.4906 | 质量修了，**类别没修**——仍是 self-authored |
| 3 | **裸模型实跑** | 见 `_round3_verdict.md` | 判据明文要求；strict 门的硬卡点 |

★★★ 「中途不得改基线」这条约束是防钓鱼的。**这两次改动都必须朝更严的方向，否则就是钓鱼。**
可审计的事实是：第 1→2 次把 delta 从 +0.6553 压到 +0.4906（**降了 0.1647**），
第 2→3 次换的是**由外部代理实跑、我不经手内容**的对照——
**两次都在收紧比较，没有一次在放松。**

## 给裸模型的原始指令

见 `_baseline_run_prompt.md`（与本文件同目录，逐字保存）。
要点：第一人称扮演 Carver，16 题原样，**明令不许用工具、不许读文件、不许联网**，
「不确定就说不确定」，不指定格式与长度。

## 与候选侧的对称性

- **问题完全相同**（取自 `evals/cases.jsonl` 的 `prompt` 字段，一字未改）。
- **rubric 未提供给裸模型**——与候选侧不同：候选是我写的，而我写时看过 rubric。
  ★ **这仍是一处不对称，且对候选有利，第 3 轮的 delta 要带着这句话读。**
