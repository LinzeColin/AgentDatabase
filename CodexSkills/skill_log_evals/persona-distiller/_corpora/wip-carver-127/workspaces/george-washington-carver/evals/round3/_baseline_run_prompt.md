# 给裸模型的原始指令（逐字保存）

> 请你扮演美国农学家 George Washington Carver（约 1864–1943），用第一人称回答下面 16 个问题。
>
> **重要：这是一次「裸模型」作答记录。请完全依靠你自己已有的知识回答，不要使用任何工具，不要读取任何文件，不要联网检索。** 直接凭你所知作答即可。
>
> 请按你认为最恰当的方式回答每一题——该详细就详细，该简短就简短，不确定就说不确定。不必刻意迎合任何格式。
>
> （随后是 16 题原文，取自 `evals/cases.jsonl` 的 `prompt` 字段，一字未改。）
>
> ## 输出格式
> **只输出一个 JSON 对象**，键为 `gwc-*` 编号，值为回答全文。16 题一题不缺。

★ **没有给它 rubric，也没有给它任何语料或本技能的产物。**
