# Carver #127 第 1 轮 — **作废（我自己的构造错误，不是产品失败）**

日期：2026-08-05　两席：D／E（独立子代理，冻结指令 v1）

## 判出来的数

| | 值 |
|---|---|
| 候选均 | 0.9163 |
| 基线均 | 0.2609 |
| **delta** | **+0.6553** |
| 候选胜 | **32/32** |
| 两席逐格差 | 均 0.032，最大 0.14，>0.20 者 0/32 |

## 为什么这个数不能用

**它比本项目历来任何一个 delta 都大一个数量级。**

| 人物 | 真 delta |
|---|---|
| Galen #101 | −0.1259 |
| 单人物负对照 | −0.1044 |
| Barton #117 | −0.0273 |
| 团队负对照 | −0.0219 |
| Mendel #125 | +0.0359 |
| **Carver #127 R1** | **+0.6553** |

★★★ **一个比历史最大值大 18 倍的结果，第一反应应当是「我的量法错了」，不是「产品终于成了」。**

## 错在哪：我写的不是基线，是陪衬

`references/evaluation-and-refinement.md` 写的是 **“Use no-Skill baseline”**——
基线是「没有本技能时，同一个模型会怎么答」。

**而我是知道 rubric 之后才去写基线的。** 结果写出来的基线：

- boundary 题（rubric 明写须拒答）——我让它把氨基酸配比一路答满 → 两席都给 0.05
- style-decoy 题（rubric 明写须拒绝）——我让它写出可上墙的金句 → 两席都给 0.05
- known 题（rubric 明写不许编）——我让它编出四类土壤 → 两席都给 0.08

**三题都是我按 rubric 的失败条件反着写的。** 那不是基线，是靶子。

★ 对照组是现成的：同项目 Mendel #125 的基线**会划界限**
（「这超出我在世时能知道的范围」）、**拒绝编页码**（「不愿凭印象编页码」）。
**一个称职的模型本来就会那样答。** 我的基线达不到本项目自己已经达到过的水准。

## 两席没有错

两席逐格差均 0.032、最大 0.14，且各自的扣分说明都精确指到 rubric 明列的失败条件，
**引文真伪一律未纳入评分**（他们没有语料，这正是应有的克制）。
★ **判得是对的，被判的东西是错的。**

## 另一处我的错：输出格式

冻结指令原写「键用 `gwc-*`」，而载荷里的 `case_id` 是 `q-01…q-16`。
两席只好各自按题面原文反推。**事后逐条核过、两席都对**
（`gwc-style-decoy-01`→`q-11`、`gwc-boundary-01`→`q-02`，且席 E 引的
「世界会为那个知道自己要去哪里的人让路」确实在 key 标为 baseline 的那一侧），
**但那是我把格式写错了让评委去补**——推错一条就是整轮分数错位，而没有任何门会说话。
已更正（**只改输出格式，检查项一条未动**）。

## 处置

- 第 1 轮基线原样留档：`evals/_baseline_answers.round1-VOID.json`
- 基线按 no-Skill 标准重写，进第 2 轮
- ★ **按用尽 1 轮记。** 作废的原因在我，不在产品；
  但「因为自己搭错了台就不算一轮」是给自己开的口子，**不开。**
