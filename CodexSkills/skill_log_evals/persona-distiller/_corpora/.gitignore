# ★ 语料规则**不在这里**，在上一层：`persona-distiller/.gitignore`。
#
# 2026-08-14 搬上去的，原因写在那份文件的抬头：规则留在本目录时**够不到
# `_ledgers/_corpora/`**（那棵树同名，装着 Livermore #100 的 536 份报纸正文），
# 于是 pre-push 钩子拦下 2,250 MB。两棵树的共同祖先只有 `persona-distiller/`。
#
# **不要在本文件里再写语料规则** —— 两把尺子迟早对不上。
# [[i-built-a-second-ruler-while-the-authoritative-one-sat-in-scripts]]
