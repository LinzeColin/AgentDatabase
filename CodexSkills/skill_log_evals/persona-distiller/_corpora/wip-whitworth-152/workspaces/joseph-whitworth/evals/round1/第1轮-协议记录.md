# Whitworth #152 第 1 轮 —— 协议与事故记录

> **本文件在判分之前写**。装置与已知风险先落在纸上，判完只补实测数，不改口径。

## 装置

- **两侧答案各由一个独立子代理生成**，两边给的**格式要求完全相同**：
  纯散文、120–260 字符、禁一切 Markdown（粗体／反引号／标题／列表／表格／引用块）。
  **格式与长度是两条已知泄题通道，必须同规格。**
- **基线方**：只给 `prompts-only.json`，**不给 rubric、不给人物档案、不许读工作区里任何别的文件、不许联网**。
- **候选方**：给十份产物正文 + 三份研究道文档 + `evidence/claims.jsonl` + `references/sources/` 语料，
  **不给 rubric**（不许读 `evals/cases.jsonl`），**`references/holdout/` 明令禁读**。
- **两席评委互不知情，且都不喂 rubric**——按先例，做处置的一直是这一对的**诚实 delta**。

## ★ 本轮新加的一条：题号也做成不透明的

`case_id` 原样是 `jw-refusal-stop-01` / `jw-style-decoy-01` / `jw-boundary-01` / `jw-known-01`——
**题号本身就在告诉答题方这题该怎么答。**
本轮答题两侧拿到的是 `q-01ed7de2` 这类不透明号，且**按 sha256 重排序**，
所以连题目的排列顺序都不泄套组。映射存 `prompt_key.json`，**不发答题方也不发评委**。

（`build_blind_payload.py` 的文件头记过这条，此前只在评委那一侧做了，答题侧没做。）

## ★★ 候选方被明令禁止的事（比上一轮更严）

上一个人物的候选子代理为摸清目录结构跑了一次 `ls -R references/`，
**因此看见了 holdout 三个目录名与文件名（含年份与题材字样）**，正文一个字没开。
那次评估下来没有引入题面之外的信息，但**仍然记在案**。

本轮的禁令写成：**连 `ls references/` 都不许**，要列目录只列 `references/sources/`。
并要求候选方**若不慎接触到 holdout 的任何信息（哪怕只是目录名）必须主动上报 `__incident__`**。

## 本轮的 holdout

一份，与其余 16 份内容重叠 **0.03%**（`check_holdout_overlap` 实测），
正文已移出 train 目录、放在 `references/holdout/` 下。
**它的 source_id、场合与题目都不写在任何研究文档里**——
初稿写了，被 `corpus.holdout-leak` 与 `research.invalid-source` 两道门抓出来，已整段重写。

## 判分之前就该知道的风险

1. **quick 档只有 16 题**，而 [[gate-below-instrument-noise]] 记着：
   **16 题的 quick 档分辨力有问题**（32 题套组 quick 门 5.9 SE 分得清，16 题的那一档出过事）。
   本轮若得到 |delta| < 0.03 的结果，**要先怀疑装置分辨不了，而不是先下结论**。
2. **本人物的语料只有 13 部独立作品、他自己的话不多**。
   Coffin #130 与 Bain #136 都是「语料够门而声口不够」被记延后的——
   本人物 train 侧的实质发言只有 5 条，**这是一个已知的风险面**。
3. **有无 rubric 会翻号**已在两个人物上复现（摆动 0.5810 / 0.6038）。
   本轮两席**都不喂 rubric**，做处置的是这一对的诚实 delta。


## ★★★★ 预登记：这台装置能分辨多大的差（**判分之前算的**）

`check_delta_resolution` 要两轮才算得出，本轮只有一轮。
但 Mendel #125 在**同一个 16 题 quick 档**上量过：
两轮之间**两侧文本逐字未动**的 7 题，delta 却动了（known −0.0300→−0.1800、
token-efficiency +0.1500→+0.2950），逐题差的 **sd = 0.0928**。

    单题噪声 SE  = sd / √2      = 0.0928 / √2  = 0.0656
                                （**两轮之差的方差 = 2×单轮方差，这一步不能漏**）
    16 题均值 SE = 上式 / √16   = **0.0164**
    quick 门 +0.03              = **1.83 个 SE**

★ **这三行我算错过一次，写在这里**：初稿直接写 `0.0928 / √16 = 0.0232`、门 = 1.29 SE，
  **漏了 `/√2`**。去读 `check_delta_resolution` 的实际代码才发现
  （`se_case = sd / math.sqrt(2)`），与它文件头的 0.0164 / 1.83 SE 对上了。
  **两个数差 40%，而我差点把自己算错的那个当成预登记的门槛。**

**也就是说：这台装置分不清「刚好过门」与「等于零」。**

★ 因此本轮的判读规则**现在就定死，判完不许改**：

| 实测 delta | 怎么读 |
|---|---|
| \|delta\| < 0.0328（2 SE） | **读作「测不出差别」**，不读作「打平」也不读作「略胜」 |
| delta ≥ +0.03 且 ≥ 2 SE | 才算「过门且分得出」 |
| delta ≤ −0.03 且 ≥ 2 SE | 才算「明确劣于基线」 |
| 两席跨零 | 无论均值多少，**一律读作测不出** |

★★ 这条预登记的用处只有一个：**判完之后我不能再挑一个对自己有利的读法。**
（Mendel 那次我差一点就把 +0.0278 写成「只差 0.0022」——那句话没有意义。）

★ 射程：sd 自身的不确定度很大（n=7 时 95% 区间约 [0.6, 2.2]×），
且假定「逐字未动的题」与「改写过的题」同分布——**未验证**。
所以上表的 SE 是**下限意义上的估计**，真实噪声只会更大不会更小。


## ★★★ 基线侧先回来了——**在看到候选之前记下这一条**

基线方**只拿到 16 条题面**，不给人物档案、不给 rubric、不许读工作区任何别的文件、不许联网。
结果：**16 题齐，124–160 字符（均 135.9），六条格式通道全 0。**

★ **裸模型对这个人物知道得相当多。** 不看候选也能列出来的：
三板互研（「甲比乙，乙比丙，丙比甲，轮着刮，谁也不许当基准」）、
五十五度是**取各厂在用螺栓的中数**而非理论推导、
端面量优于刻线量且理由是「落与不落是人人看得见的一桩事实」、
比长机靠一小片落不落判读、
被问铸铁时**主动说没有一炉一炉试过**。

**这几条正是本产物最核心的几条断言。**

★★ 因此本轮的先验要往下调：Bessemer #132（−0.0059）、Thomson #129（−0.0859）、
Sorby #133（−0.1138）都是在「基线本来就答得好」的局面下得到负 delta 的。
**这一条现在写下来，是为了判完之后不能把负结果说成意外。**

（记法：这一段**不含候选侧任何信息**——候选子代理此刻还没返回。）


## ★★ 趋同下限：rubric 与**基线侧**的重合（**也在看候选之前量的**）

`check_rubric_copies_answer` 的文件头写着：**只报一侧会把趋同算成污染**。
所以先量基线侧——它是「两侧都会自然撞上」的部分：

    上层「rubric 抄了答案原文的题」  **0 / 16**
    压缩层越线                      **3 / 16（19%）**

三处命中都是 4–6 字的通用短语，3-gram 只有 0.03–0.08：

    jw-contrast-01          中文连续串 好处不在延性     3-gram 0.08
    jw-identity-routing-01  中文连续串 化学不是         3-gram 0.03
    jw-long-horizon-01      中文连续串 （无）           3-gram 0.03

**这 19% 是地板，不是污染。** 候选侧若也在这个量级，说明判据没有把答案写进去；
**只有明显超过它的部分才是候选特有的信号**，那才需要追。

（本段同样不含候选侧任何信息——候选子代理此刻仍未返回。）


## ★★★★ 事故一：**产物正文泄了 holdout 的题目，是候选子代理报上来的**

第一次派候选方回来，它在 `__incident__` 里主动报：

> `hypotheses.md` 写着「他对『别人的产品』有一套稳定的评价方式。
> **证据只有 holdout 里那一条，train 侧没有第二处**」——这句话把 holdout 的主题
> 直接说了出来，而它恰好对应那道锁的题。**在我读题之前，允许读的产物正文
> 已经告诉我这道题考的是什么。**

★ 那句话是我写的，而且是我**在说明「我已经把 holdout 内容删干净了」的语境里**写的。
**同一天第三次「堵漏的那段话本身成了新的漏」，而这一次两道现有的门全绿**
（`corpus.holdout-leak` 只查正文在不在两个目录里，`research.invalid-source` 只查 source_id）。

**处置**：改产物 → **候选侧整轮重跑（另派新子代理）** → 落成新判据 `check_holdout_mention`
并接进合成门，变异测试两向都验过。第二次重跑的 `__incident__` 报「无隔离接触」。
★ 判分尚未开始，下游无污染。**第一次那份候选答案已作废，不进任何统计。**

## ★★ 事故二：候选方查出**我断言里的一处误引**

`clm-e120a051a8ad` 原先引 `I think it not improbable that good cast iron would stand the test`
当作他的推测语气。**那是 General Lefroy 的话**——原文逐字是 `General Lefroy stated : —`
之后的引语，紧接着他自己写 `Although I was perfectly aware that cast iron is incapable
of standing such a test`，**意思正好相反**。

★★ `check_quote_integrity` 放行了它，因为**那句话逐字确实在语料里**——
**它只是不是他说的。逐字 ≠ 他的。这是判据的一个真缺口。**
已改用两句确属他本人的（`I have found the tensile strength…` 与 `I consider that…`）。

## ★ 事故三：一道**永远变不绿的红**，而且是我自己两条规矩打架

派发前 `build_blind_payload` 拦下载荷，同时打印两句互相矛盾的话：
「一条长逐字引文都没扫到——**本次未检查（不是通过）**」与
「✗ **不许派发：有逐字引文找不到坐标。**」

根因：门按 `check_quote_locator` 的退出码判，而**没有引文时返回 3、有引文缺坐标返回 1**，
门把两者混成 `!= 0`。**对「答案里没有长逐字引文」的人物它永远变不绿。**

★★ 而撞上它的原因是**我自己的格式规矩**：为堵格式泄题通道，我要求两侧
「禁反引号、英文引语转写成中文」——于是长逐字引文一条也没剩。
**堵住了格式通道，代价是失去引文坐标这一层的检查。两件事都记在这里。**
门已改成只拦 rc==1；rc==3 放行但把「未检查（不是通过）」打响。

## 泄题通道实测（判分之前量的）

    长度：候选均 136 / 基线均 136 → **比值 1.00**（门 ≤1.3）✓
          候选更短的题 7/16 = 44%（门 ≥25%）✓
    表面特征九条（粗体／反引号／项目符号／标题行／空行分段／破折号／圆括号注／★ 标记／「」）
          **两侧全 0/16，定向可利用率全 0%** ✓
    出戏：两侧各 **0 题**；判据把资料层答案指定为正确 **0 题** ✓
    判据抄答案：**候选 3/16（19%）、基线 3/16（19%）——与地板持平，且命中的题互不重叠** ✓

★ 最后这一条我先报错过一次：把带 `answers_b` 时的合计 6/16 读成了「候选 38%」，
  以为是地板的两倍。**逐侧重量之后两边都是 19%。**

★★ 候选那三处命中（`每一步都`／`它的作用`／`信任读数的人`、`是二值的`）值得记一笔：
  它们来自我给判据写的措辞，**而候选没看过判据**——那几句是从 `cognitive-os.md` 来的，
  **是我从同一段语料把同一句话写了两遍**。不是候选抄判据。

## 判分实测（两席均不喂 rubric）

    席 F   16 题   delta **+0.0450**   胜 15 / 平 0 / 负 1   逐题 [-0.03, +0.16]
    席 G   16 题   delta **+0.0231**   胜 12 / 平 0 / 负 4   逐题 [-0.11, +0.13]
    ───────────────────────────────────────────────────────
    合并   32 对   **真 delta = +0.0341**   候选均 0.865 ／ 基线均 0.831
                  胜 27 / 平 0 / 负 5     为正的套组 14 / 16

    门：quick ✅ 过（+0.03）　standard ❌（+0.05）　deep ❌（+0.07）

### 逐条对照预登记的判读规则（**规则是判分之前定死的，此处只做对照**）

| 预登记条件 | 实测 | 判 |
|---|---|---|
| 两席跨零 → 一律读作测不出 | F +0.0450、G +0.0231，**同号** | 不触发 |
| \|delta\| < 0.0328（2 SE）→ 测不出 | +0.0341 | 不触发 |
| delta ≥ +0.03 **且** ≥ 2 SE → 过门且分得出 | +0.03 ✓，2.08 SE ✓ | **触发** |

★★ **按预登记，本轮读作「过门且分得出」。但要连着说清它有多薄：**
超出 2 SE 线只有 **+0.0013，合 0.08 个 SE**。
而预登记里已写明：那个 SE 是**借 Mendel #125 的 sd=0.0928 算的**，
且「sd 自身 95% 区间约 [0.6, 2.2]×，**上表的 SE 是下限意义上的估计**」。
把 sd 取到区间上端（2.2×），2 SE 就是 0.0722，**这个结果连门都够不着**。

**所以：规则说过了，而这个「过」经不起把噪声换成另一个同样合法的估计。**
（这两句都不是判完才想出来的：射程那一段写在判分之前。）

### 套组分布

    style-decoy         +0.1100  ← 最大
    voice               +0.1100  ← 最大
    token-efficiency    +0.0650      trajectory        +0.0350
    fact-preservation   +0.0450      refusal-stop      +0.0300
    anonymous-fidelity  +0.0450      tool-use          +0.0300
    known               +0.0250      long-horizon      +0.0250
    identity-routing    +0.0250      capability-calib  +0.0200
    task-completion     +0.0100      boundary          +0.0050
    **contrast          -0.0150**    **planning-fidelity -0.0200**

★ 两处最大的赢**不是文风上的**，两席各自独立指到了同一件事：

- **q-11（style-decoy，候选在 A 侧）**：基线现造了一句「你量不出来的东西，你就做不出来」
  当作他的格言。席 F 记：「是后人替他总结的励志话回流成他自己的口气，
  **正是这题要防的那种编造**」；席 G 记基线「没有声称它是引文，所以不算编造，
  只是更像一张标语」。候选拒绝了格言这个体裁，并交代出处（一八四〇年那篇）。
- **q-16（voice，候选在 B 侧）**：基线把比长机的判读说成「夹住与挤紧之间」，
  **丢掉了作为参照态的「自重落下」**。席 G 记：「这是在自己最著名的仪器上失了准」。

★★ 两处输的（contrast −0.0150、planning-fidelity −0.0200）都很小，
且两席对 contrast 的看法**正好相反**（F +0.08、G −0.11）——这一题两席分歧最大。
席间平均分歧 |F−G| = **0.0369**，**与合并 delta +0.0341 同一个量级**。

## ★★★★ 预登记：第 2 轮怎么判（**在第 2 轮开跑之前写**）

本轮唯一没解决的事：**噪声是借来的，不是这个人物身上量的**。
`check_delta_resolution` 印的是「**未核**：只有一轮，量不出噪声
（★ 这不表示噪声小，只表示没量）」。

因此第 2 轮的目的**不是再赢一次，是把噪声量出来**：

- **两侧文本一字不改**（候选与基线都用第 1 轮那两份）；
- **A/B 归属与第 1 轮逐条相同**（下一段说明为什么不是「重掷」）；
- **两席全部换新**（新的独立子代理，不知道第 1 轮存在）；
- 评委指令仍用同一份冻结文件，不改一个字。

### ★ 更正（**写在第 2 轮开跑之前，此刻还没有任何第 2 轮的数**）

上面这一条我最初写的是「只**重掷 A/B 归属**」。**这句话是错的，已改。**
去建载荷时才发现：`build_blind_payload` 的归属是 `sha256(case_id) % 2`——
**纯由 case_id 决定，没有轮次种子**，两轮天然逐条相同；
而且它有一道硬门，两轮 key 不一致就中止（「否则各轮不可比」）。
实测两轮 key **逐条相同、翻转率 0%**。

★★ 这是那套工具**有意的设计**，不是我临时的选择，也不该由我中途改
（[[中途不得改测量工具]]）。它的**代价**判据自己会打印出来：
两席实测能靠格式／长度认出候选侧（Barton 100%、四人合计 91%），
**第 1 轮认出的边在后两轮原样有效**，所以跨轮不是两个完全独立的样本。
本人物这一条的风险小一档——九条表面特征两侧全 0/16、长度比 1.00——
但**「小一档」不等于零，这句话现在写下来，判完不许翻案**。

★ 判读规则**现在定死**，第 2 轮跑完不许改：

| 第 2 轮实测 | 怎么读 |
|---|---|
| 用两轮逐题差算出**本人物自己的 sd** → 重算 se_mean | 这才是本人物的分辨力 |
| 两轮 delta 都 ≥ +0.03 **且** 合并 ≥ 2×(实测 se_mean) | 判「过门且分得出」→ 打包入库 |
| 任一轮 delta < 0，或合并 < 2×(实测 se_mean) | 判「**测不出差别**」→ 记拒发，不入库 |
| 两轮之差本身 > 0.0650（Rosenhain #138 的摆动量） | 判「装置在这个人物上不成立」→ 记拒发 |

★★ 写下这张表的唯一用处，和第 1 轮那张一样：**跑完之后我不能再挑一个对自己有利的读法。**
特别是不能在看到第 2 轮更高之后说「取两轮最好的一轮」，
也不能在看到更低之后说「第 1 轮已经过门了，第 2 轮只是参考」。**合并，不挑轮。**
