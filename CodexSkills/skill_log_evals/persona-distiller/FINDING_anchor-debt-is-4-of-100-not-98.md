# 锚点债是 4/100，不是 98/100——而那 98 是我量错的

日期：2026-08-04　为 #15 定范围而做的全量实测

---

## 一、结论

**100 个已入库产物全部有锚点。** 真正不齐的只有 **4 个**：

| 人物 | distilled_with | 断言 | 锚点 | 缺 |
|---|---|---:|---:|---:|
| `evan-r-chesler` | v0.0.0.6 | 60 | 23 | **37** |
| `barbara-liskov` | v0.0.0.6 | 67 | 64 | 3 |
| `shigeo-shingo` | v0.0.0.6 | 60 | 58 | 2 |
| `ali-erdemir` | v0.0.0.6 | 61 | 59 | 2 |

**四个都在 v0.0.0.6**（2026-07-26 十二族重组的批量重打包批次）。
**v0.0.0.7 之后的 8 个人一个都不缺。**

最近三人是逐条对齐的：

```
  seth-godin          v0.0.0.16   断言  31 / 锚点  31  ✓
  jesse-livermore     v0.0.0.19   断言  30 / 锚点  30  ✓
  michael-steinhardt  v0.0.0.15   断言  39 / 锚点  39  ✓
```

## 二、★★ 而我第一次量出来的是「98/100 零锚点」

```
扫了 **100** 个已入库产物
  锚点 > 0 的：**2**
  **锚点为 0 的：98**
  zip 里没有 claims.jsonl 的：98
```

**核心产物在嵌套 zip 里**（`runtime/<人>-persona-skill-v0.0.0.1.zip`），
而我只读了外层——**从没打开过它。**

外层那 15 个文件是交付壳：README、install 脚本、audit/*.json、
checksums、manifest。**真东西在 `runtime/` 那个 zip 里面。**

## 三、这是今天第三次同一形状的错

| 何时 | 错法 |
|---|---|
| v0.0.0.71 | `roster()` 报工作区、`scan()` 报容器——**两套标识符空间相减** |
| #15 范围统计 | `charles-becht-iv` 的 `iv` 是 `l-iv-ermore` 的子串 |
| **本次** | **只读外层 zip，核心产物在嵌套 zip 里** |

**共同点：我拿到了一个看起来完整的数，而它答的是另一个问题。**

三次里有两次**结论是「一片绿／一片红」这种极端值**——
**极端值应该是先去核工具、而不是先去信它的信号。**

## 四、对 #15 的影响

**「锚点」这一项基本不是债**：4/100，且集中在一个已知的批量重打包批次。
**#15 的重量全在另外两项**（装饰性引用、holdout），
而那两项 **97/100 量不出来**（见
[`FINDING_tech-debt-is-unmeasurable-for-97-of-100.md`](FINDING_tech-debt-is-unmeasurable-for-97-of-100.md)）。

## 五、那 4 个要不要修，仍是用户的事

修它们要重新打包已入库产物——**动的是受保护资产**。
`evan-r-chesler` 缺 37 条是唯一一个量级明显的，
另外三个各缺 2–3 条。

**我不自行动已入库产物。**
