# 判分积压的卡点**记错了** —— 不是缺评委，是**12/13 位连两侧答案都没有**（2026-08-18）

## ★★★ 首先：本文最初的版本把**数**改错了，已撤回

本文第一版写「三个数都不对」，把 #115 的 **10 人 / 321 题** 改成 **13 人 / 417 题**。
**那是错的，#115 的两个数一直是对的。**

**根因**：我自己从「有题 且 无 `results.jsonl`」现造了一份清单，只扣掉 4 位已结案，
**从没 join `_延后名单.json`** —— 而 Blackwell #118、Churchill #191、Michelangelo #185
三位**在延后名单里**（186 项，按 `name` 字段精确命中，不是子串）。

    13 − 3 = **10 位**　　417 − 32×3 = **321 题**　　与 #115 原文逐字相符。

**而仓里的权威工具 `next_person.py` 本来就做了这个 join。**
[[i-built-a-second-ruler-while-the-authoritative-one-sat-in-scripts]]

★ 我是怎么发现自己错的：`next_person.py` 的「已做但未出货」列 **11 位**，我列 13 位，
**两份对不上**。没有糊过去，逐位查差异 —— 才看见延后名单这条 join。
**两个数打架时，先找口径，别选一个信。**

★★ 中途还踩了一次子串匹配：`"Brandeis" in json.dumps(延后名单)` 返回「是」，
而按 `name` 精确匹配是「否」（他在别人的 `reason` 里被提到）。**又一次。**
[[filename-matching-is-brittle]]

## 站得住的那一半：**卡点确实不是评委**，而且比我原先说的更强

| | 台账写的 | 实测（本日，已修正口径） |
|---|---|---|
| 人数 | 10 | **10 ✓ 原记录正确** |
| 题数 | 321 | **321 ✓ 原记录正确** |
| 卡点 | 缺「两席独立评委」 | **✗ 缺阶段 4 的两侧答案** |

逐位实测（题数 / 两侧答案文件数 / 是否在延后名单）：

| 人物 | 题 | 答案 | 延后 |
|---|---:|---:|---|
| Otto von Bismarck | 32 | **0** | 否 |
| Louis Brandeis | 32 | **0** | 否 |
| John Dewey | 32 | **0** | 否 |
| Friedrich Frobel | 32 | **0** | 否 |
| Thomas Jefferson | 32 | **0** | 否 |
| Immanuel Kant | 33 | **0** | 否 |
| Abraham Lincoln | 32 | **0** | 否 |
| Niccolo Machiavelli | 32 | **0** | 否 |
| Johann Pestalozzi | 32 | **0** | 否 |
| Jean-Jacques Rousseau | 32 | **0** | 否 |
| — Elizabeth Blackwell | 32 | 7 | **是（延后）** |
| — Winston Churchill | 32 | 0 | **是（延后）** |
| — Michelangelo | 32 | 0 | **是（延后）** |

> **在册待判 10 位 / 321 题；其中有两侧答案的 —— 0 位。**
> 唯一有答案的 Blackwell，恰好在延后名单里。
> 这 10 位的盲判载荷生成数同样是 **0**（仓里仅有的 3 份载荷属已结案的 Cicero 与 Paton）。

## ★★★ 这意味着什么

**「找到两席评委」这件事，即使今天办成，也推进不了任何一个在册待判的人 ——
因为 10 位里没有一位有可判的两侧答案。**

真正的下一步是**阶段 4：为这 10 位各生成候选侧与对照侧 32 条答案**
（候选＝跑蒸出来的人物，对照＝一次真实的裸模型作答）。

## ★★ 我在查这件事时犯的错（当场被自己的下一步抓住）

第一遍我按**文件名**找答案，报「6/13 位有答案文件」：
`tj-candidates.json`、`ob-candidates.json`、`ff-candidates.json`、`al-candidates.json`、
`namesake-candidates.json` —— 名字里有 `candidates` 就被我算成「候选答案」。

打开一看：**这些是「同名候选」，每份只有 2 项。**
改按**内容**判（≥16 项、值全是字符串的字典 / ≥16 条含 A|B|answer|text 的数组），
真实数字是 **1/13**。

**「文件名里有那个词」不是「文件里是那个东西」。**
[[filename-matching-is-brittle]]｜[[the-field-was-filled-with-the-filename]]

## ★★★ 顺带定案：**外部 API 推不动阶段 4**

既然真正的下一步是「生成两侧答案」，那就要问：手上这个 DeepSeek 通道能不能用来跑？
**不能，而且理由是量出来的。**

- **候选侧**必须跑蒸出来的那个人物产物 —— 换模型就不是这个产物了，不必论。
- **对照侧**（`bare-model-run`）看似「谁跑都行」，但查了出处记录：
  全库 `*provenance*` 类文件**只有 3 份、全在 Bessemer #132**，
  其中 `BASELINE-PROVENANCE.md:61` 写着 **模型：`claude-opus-5`**。
  ⇒ 已有的对照侧是**同一个模型家族**跑的。
  换成 DeepSeek，delta 量的就从「蒸馏增益」变成「**蒸馏增益 ＋ 模型差异**」，
  而且与已入库的 9 人**不可比**。

★★ 附带一个真缺口：**对照侧用的是哪个模型，只有 1 位记了。**
其余人物的产物里没有这个字段，散文侧也没写。
⇒ 那些 delta 说的是「比某个没记名字的裸模型强 X」。
[[evidence-must-carry-what-it-measured]]

★ 我查这件事时又踩了一次**无条件 echo**（今天第二次）：先打印
「（无输出＝连这份最详细的出处记录也没写模型）」，而上面 grep **明明有输出**。
**印在结论位置的话，必须由数据决定，不能由脚本无条件打出来。**

## ★★★ 卡点的最后一层：阶段 4 **本会话做不了**

把「缺两侧答案」再往下追一层 —— 谁能生成它？

对照侧的标准写在 Bessemer #132 的 `BASELINE-PROVENANCE.md` 里，**极严**：

- 「在一个**独立的 agent 上下文**里生成，与蒸馏产物的构建上下文不是同一个上下文」
- 「`_corpora/` 下任何语料、`claims.jsonl` / `cognitive-os.md` / `results.jsonl` —— **一个都没有打开过内容**」
- 连**两条 `ls -la` 只看到文件名**都被记成「灰区」，白纸黑字写进出处。

**而本会话已经读过这 10 位的语料正文、`cases.jsonl` 连同 `rubric`、盲判载荷、
判分产物。对他们全部是最大污染态。** 候选侧同理（它也要一个只装人物产物、
没看过题面与 rubric 的上下文）。

⇒ **阶段 4 需要干净上下文，而本会话既污染、又不许派子代理**
（用户：「取消并发和 subagent，确保依旧是最低 token 去进行」）。
**这一步必须在新会话里做**，不是本会话能推进的。

★ 这不是「找不到办法」，是**把「卡在两席独立评委」这句话追到了真正的那一层**：

    记录写的     → 缺两席独立评委
    第一层实测   → 缺两侧答案（10 位全无）
    第二层实测   → 生成两侧答案要**干净上下文**；本会话污染且无子代理
    ⇒ 可执行动作 → **开一个新会话专做阶段 4**，每人一个独立上下文，按 Bessemer 那份
                    provenance 的格式逐项记录（含灰区），并在其中写明**模型是 claude-opus-5**。

## 处置

1. **只改 #115 的卡点那一句**（人数、题数**照旧**——它们本来就对）。
   不要让下一个人照着错的卡点去投工夫。
2. **不动门、不动席位**（用户 2026-08-12 起的常设约束）。
3. 阶段 4 要不要开工、按什么顺序开，属于排产，**下一轮按 `worth_starting` 的黄/红灯排**。

## 可复算

★ **本节的命令必须算出上文那三个数**（13 / 417 / 1）。第一版没排除已结案的 4 位，
算出的是 17 / 483 / 3 —— **可复算命令算的不是文中那个数**，这正是本仓一直在挑的毛病。
已修正：`CLOSED` 显式列出，两个口径都印。

```bash
cd CodexSkills/skill_log_evals/persona-distiller/_corpora
python3 - <<'EOF'
import json, pathlib
CLOSED = {"wip-blackstone-169", "wip-cicero-166", "wip-kelsen-171", "wip-paton-162"}
rows = []
for ws in sorted(pathlib.Path(".").glob("wip-*")):
    cj = list(ws.rglob("cases.jsonl"))
    if not cj:
        continue
    n = len([l for l in cj[0].read_text(encoding="utf-8").splitlines() if l.strip()])
    res = list(ws.rglob("results.jsonl"))
    if not (n and not (res and res[0].read_text(encoding="utf-8").strip())):
        continue
    ans = 0
    for p in ws.rglob("*.json"):                    # ★ 按**内容**判，不按文件名
        if p.stat().st_size > 5_000_000:
            continue
        try:
            d = json.loads(p.read_text(encoding="utf-8"))
        except Exception:
            continue
        if isinstance(d, dict) and len(d) >= 16 and all(isinstance(v, str) for v in d.values()):
            ans += 1
    rows.append((ws.name, n, ans))
for label, sel in (("全部未判分", rows),
                   ("**扣掉已结案后的真积压**", [r for r in rows if r[0] not in CLOSED])):
    print("%-22s %2d 位｜%3d 题｜有两侧答案 %d 位"
          % (label, len(sel), sum(n for _, n, _ in sel), sum(1 for _, _, a in sel if a)))
EOF
# 期望输出：
#   全部未判分              17 位｜483 题｜有两侧答案 3 位
#   **扣掉已结案后的真积压**  13 位｜417 题｜有两侧答案 1 位
```

[[a-blocked-by-x-label-needs-x-rerun]]｜[[counts-need-their-cutoff-stated]]
