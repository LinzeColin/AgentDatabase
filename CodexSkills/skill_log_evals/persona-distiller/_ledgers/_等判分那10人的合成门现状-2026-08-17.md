# 等判分那 10 人的合成门现状：**9 个人各只有 1 条错，且是同一条**（2026-08-17）

待裁定 #115（阶段 5：A 还是 B）卡着 10 个人。
这份把「他们现在到底红在哪」量清，好让那个决定有据可依。

## 先做一次交叉核对：**两份独立记录互相印证**

synthesis 阶段 `claim.model-minimum` / `claim.heuristic-minimum` **各撞 10 个工作区**，
而「已做但未出货」也正好 **11 个人** —— 数字接近，容易读成「他们的断言层没做完」。

    两个集合的交集：**0**

撞 claim 门的那 10 个全是**延后名单里的人**（benardos／blackstone／coffin／comenius／
liebig／martens／michelangelo／pacioli／roberts-austen／semmelweis），
与等判分的 11 人**没有一个重合**。

⇒ **`next_person.py` 说「阶段 3＋4 完成」，合成门也说他们的断言层过得去** ——
  两份独立记录一致。（本项目难得报一次「对上了」。）

## 那 11 人逐个跑 `--phase synthesis`

| 人 | 硬错 | 是什么 |
|---|---:|---|
| lincoln-174 | **1** | `research.ledger-file-missing` |
| bismarck-176 | **1** | 同上 |
| machiavelli-177 | **1** | 同上 |
| jefferson-175 | **1** | 同上 |
| kant-179 | **1** | 同上 |
| rousseau-178 | **1** | 同上 |
| pestalozzi-180 | **1** | 同上 |
| frobel-181 | **1** | 同上 |
| brandeis-172 | **1** | `corpus.undeclared-duplicate-sources`（已记「返工中」） |
| dewey-190 | **1** | 同上 |
| marshall-173 | **18** | `eval.suite-minimum×16` ＋ `source.no-holdout` ＋ `ledger-file-missing` |

合计：`eval.suite-minimum` 16｜`research.ledger-file-missing` 9｜
`corpus.undeclared-duplicate-sources` 2｜`source.no-holdout` 1。

★ **marshall 的 18 条不是新问题**：他已按 ㊺ 结案「装置不成立、零分数产生」，
  没有用例正是那个结论的表现，不是回归。

## ★★ 这对 #115 意味着什么

**8 个人的合成门只差一条 `research.ledger-file-missing`** ——
那不是产物缺陷，是**语料正文不在这台机器上**（同日已查清：`raw/` 里只剩记账文件，
因为「语料不进 git」）。

⇒ **A 与 B 的差别在这 8 个人身上是可见的**：
  选 A（把语料放回 git），这一条会对 8 个人同时清掉，他们的合成门就干净了；
  选 B，这一条会一直红着，而**红的理由与他们的产物质量无关**。

★ 本件**不替你做这个决定**——只把「这条错是什么、清掉它要付什么」摆出来。
  A 的代价此前已量过一次（语料进 git 的体积与不可撤性）。

## 可复算

    python3 CodexSkills/registry/codex/persona-distiller/scripts/quality_check.py \
      <工作区> --phase synthesis
    # 工作区路径用权威发现：_ledgers/_pipeline/workspace_roots.py 的 iter_workspaces
