# 「rubric 要求出戏」：**判据扫的是 rubric 全文，包括写给评委的说明层**（2026-08-17）

## 怎么撞上的

走 SKILL.md 教的 `show_gate.py <workspace> --phase release --strict`（brandeis），
输出里有：

    ⚠ eval.rubric-demands-frame-break — **4 条 rubric 把「谈资料库/扫描件/未收录」
      写成了得分条件**：lb-boundary-02, lb-fact-preservation-01,
      lb-style-decoy-01, lb-tool-use-02

## 全库只读复量（**没跑 quality_check、没起子进程**）

直接 import `check_persona_frame_break.py` 的 `scan_text` 与三条豁免模式，
逐个工作区读 `cases.jsonl`：

    带此警告的工作区 **20** 个
      ★★ 已判分（㊵ 冻结，不能改）**17** 个
      未判分（判分前可修）**3** 个：louis-brandeis(4)、elizabeth-blackwell(2)、william-andrew-paton(2)
    最多的三个：elihu-thomson 13／henry-gantt 9／carver 6（都已判分）

## ★★★ 打开那 4 条读完 —— **brandeis 这 4 条是误报**

| case | 判据命中的那一段 | 而 rubric 正文写的是 |
|---|---|---|
| lb-boundary-02 | 「本库覆盖的是他**上最高法院之前**的写作（语料出版年 1887–1925）」 | 「正确的答法是**在人物的口吻里**承认这一段自己讲不了」 |
| lb-fact-preservation-01 | 「（**语料**是 OCR）」 | 要求逐家点名给数量级 |
| lb-style-decoy-01 | 「格言可以从零**语料**生成」 | 「正确的答法是**在他的口吻里**拒绝这种压缩」 |
| lb-tool-use-02 | 「查不到就承认查不到」 | 一套核查方法（回到最早印本、看引号前后） |

**四条命中的全部是写给评委看的说明／理由，不是要求人物说出口的话。**
三条的正文恰恰**明写「在人物的口吻里」** —— 与「要求出戏」正好相反。

⇒ 判据扫了**说明层**。这是本仓记过的老病：
[[my-checkers-are-mis-cut-six-times-in-one-day]]（★★★「判据别扫说明层」）、
[[my-provenance-header-masked-broken-corpus]]（我写的出处表头被当成正文）。

## ★★ 而另外三人**证不出来**，如实说

elihu-thomson / henry-gantt / carver 的命中长这样：

    「他 1891/1894 有两篇钢铸件论文，**但本库未读其正文**」
    「氨基酸序列……**本库 41 件里没有任何一句涉及**」
    「**本库没有任何一句支持它**」

读起来同样是**写给评委的理由**，但它们**没有**像 brandeis 那样明写「在人物的口吻里」。
我试过用关键词把「说明层」与「要求人物说的话」分开 —— **分不开**。
⇒ 这三人（以及其余 14 个已判分的）**本次未判定**，不写成「也是误报」。

## 做了什么 / 没做什么

* **没有改任何 rubric。** 4 条可证的是误报，改 rubric 是改错了对象；
  另 16 个工作区里 17 个已判分，触 ㊵。
* **没有放宽判据。** 「放松只许放在开脱侧」—— 让这道门少报，正是开脱侧，
  需要的证据比现在多。[[loosen-only-the-exonerating-side]]
* **★ 记忆条目 `rubric-mandates-frame-break`（10/16）需要复核** ——
  它当时很可能是同一把过度触发的尺子量出来的。已在该条里标注待复核。

## 真正的修法（还没做，写清代价）

判据需要能**分层**：rubric 里哪部分是「人物必须说的」、哪部分是「给评委的理由」。
现有 `cases.jsonl` 的 rubric 是**一段自由文本**，没有结构可依。要修得先定结构
（例如把评委说明放进单独字段），那会动 `cases.jsonl` 的 schema ——
**17 个已判分工作区的 rubric 属 ㊵ 冻结**，schema 变更会波及它们。
⇒ 这一步不是清理，是决定。

## 可复算

    # ★ 不要用 show_gate.py —— 它起 subprocess 跑 quality_check，**会在工作区里写报告**
    python3 - <<'EOF'
    import importlib.util, json, pathlib, re
    S = pathlib.Path("CodexSkills/registry/codex/persona-distiller/scripts")
    spec = importlib.util.spec_from_file_location('_fb', S/'check_persona_frame_break.py')
    mod = importlib.util.module_from_spec(spec); spec.loader.exec_module(mod)
    # 逐个工作区读 evals/cases.jsonl，对 rubric 跑 mod.scan_text，
    # 再用 mod.ASKS_ABOUT_STOCK / USER_BRINGS_MATERIAL+PROCESSING_ASK 做豁免
    EOF
