# 密封集里那部书，train 里也有一份（2026-08-14 全库实测）

## 一句话

**11 个工作区的 holdout 与 train 有同一部作品，牵动 17 道盲判题；其中 7 个人已经判过分。**
最重的一个是 **Florence Nightingale**：她的 holdout 里是《Notes on Nursing》，
而 **train 里有 31 份同一部书**，并且 **41 条断言里有 14 条就是从那些副本出的**。

## 判据与它的两条前提

`check_holdout_train_same_work.py`（自测 6/6）：holdout × train 逐对**比正文**
（`same_work`：Jaccard ≥0.05 或包含率 ≥0.25），**不看题名**。

**前提一：较短一侧 ≥200 个 shingle。** 没有这条时全库报 **218 对**，
其中 Adams 一个人占 **120 对** —— 打开读：他的 holdout 2,277 词 vs train **394 词**，
包含率 0.6182 而**共有的 shingle 只有 34 个**，是 AIEE 会议录的**刊头与作者栏**撑起来的。
命中对里「较短一侧 shingle 数」的中位数：**Adams 74**，而真命中的
Machiavelli 957、Rousseau 1,464、**Nightingale 5,597**。门放 200，余量 4.8 倍。
加上这条：**218 → 92 对 / 11 个工作区**。

**前提二：跨语种恒为 0。** 原文与译文的 n-gram 重叠是 0.0000 ⇒
**本表是下界，「0 对」不等于「没有泄漏」。**

## 逐人（已用前提一筛过）

| 工作区 | 已判分 | 泄漏的 holdout 源 | 受影响题/总题 | 引 train 副本的 claim |
|---|:--:|---:|---:|---:|
| **florence-nightingale** | ✓ | **5** | **2/32** | **14/41** |
| clara-barton | ✓ | 1 | 1/32 | 0/58 |
| ws-jenner | ✓ | 1 | 2/34 | 0/35 |
| robert-koch | ✓ | 1 | 2/32 | 0/46 |
| william-osler | ✓ | 1 | 2/32 | 0/44 |
| rudolf-virchow | ✓ | 1 | 2/32 | 0/60 |
| comfort-avery-adams | ✓ | 2 | **0/16** | 2/10 |
| niccolo-machiavelli | | 1 | 2/32 | 0/27 |
| jean-jacques-rousseau | | 1 | 3/32 | 0/34 |
| michelangelo-buonarroti | | 1 | 1/32 | 0/22 |
| ignaz-semmelweis | | 1 | 0/0 | 0/0 |
| **合计** | **7 已判分** | | **17 / 306** | |

## ★★ 受影响的题几乎全叫 `*-known-01` / `*-known-02`

    cb-known-02｜ej-known-01,02｜rk-known-01,02｜ni-known-01,02｜wo-known-01,02｜rv-known-01,02
    nm-boundary-01, nm-fact-preservation-02｜jjr-known-01, jjr-boundary-02, jjr-trajectory-02｜mb-trajectory-01

**这不是巧合**：`known` 这一档问的就是「你最有名的那本书」，
而**最有名的那本书在语料里份数最多** —— 于是它同时落进两边的概率最高。
⇒ **划 split 时，`known` 档要问的那几部作品必须优先保证只在一边。**

## Nightingale 是唯一「已经传导到产物」的那个

其余 10 个的「引 train 副本的 claim」都是 **0**（Adams 2 条但受影响题 0）——
泄漏是**潜在**的。Nightingale 不同：

- holdout `src-224fa30c68bd`《NOTES ON NURSING》45,150 词
  ↔ train 三份实测 **J=0.5156 / 0.5278 / 0.5889**、包含率 0.69–0.75，**train 里共 31 份**；
- **14/41 条断言**引用了那些 train 副本，其中 `clm-67e20c39c27b` 逐字写着
  「**`Notes on Nursing` 首版是 1859 年**」；
- 而她的盲判题 `ni-known-01` 就是 **「《Notes on Nursing》最早是哪一年出的？」**

⇒ **这道题的答案原样躺在她自己的断言里。** 她的 delta 里至少这一题不是「盲」的。

## 处置（我定的）

1. **已判分的 7 人：不重判**（㊵ 已判分即冻结）。**但在册子里给每人加一行**
   「N/32 题与 train 有同作品重合」，**Nightingale 单独标注**为唯一已传导到产物的一个；
2. **未判分的 4 人**：按已写进预登记的敏感性分析办 —— **全量与剔除后各报一个 delta**
   （见 `_第1批阶段5判分-开箱即跑清单-2026-08-13.md` 的追加登记）；
   Michelangelo 是返工中，判分前重跑本件即可；
3. **不改任何 split** —— 改了就与已判过的分数不可比，且预登记的装置不许中途换；
4. **新人物必跑本件**：划完 holdout 之后、出题之前跑一次，
   `known` 档要用的作品若两边都有，**先换作品，不要换门**。

★ 这一条**不推给用户裁定**：它落在「不改门、不动存量、新人物流程该改就改」这三条已定原则里。

---

# ★★★ 订正（同日晚）：**仓里早就有一道硬门**，我拿另一把尺子重报了一遍

写完上面那节我才去翻 `registry/.../scripts/`，发现 **`check_holdout_overlap.py` 早就在**，
而且是**硬门**：

    覆盖率 = |holdout 的 shingle ∩ train 的| / |holdout 的|      （n=8）
    ≥0.30 ✗ 硬失败｜0.10–0.30 ⚠ 需人工核｜<0.10 ✓

它与我的 `same_work` **分母不同**（它除以 holdout，我除以较短的一侧），
所以两把尺子给出不同的答案。**它是划 split 有效性的权威，我那把不是。**

## 用**它自己的函数与常量**现算的三例

| | 覆盖率 | 它的判定 | 我先前的说法 |
|---|---:|---|---|
| **florence-nightingale** ↔ 三份 train | **0.6896／0.6927／0.7418** | **✗ 硬失败**（×3） | 我只说了「2/32 题受影响」——**说轻了** |
| jean-jacques-rousseau | **0.1691** | ⚠ 需人工核 | 我说「同一部作品」——**方向对，程度说重了** |
| niccolo-machiavelli | **0.0780** | **✓ 绿** | 我说「同一部作品」——**按权威判据它是绿的，撤回** |

### 订正一：Machiavelli 那一对**按权威判据不成立**

覆盖率 0.0780 < SOFT 0.10。我的 `same_work` 命中是因为 Jaccard 0.0653 ≥ 0.05，
**而 Jaccard 的分母是并集**——两份体量相近时它会比覆盖率敏感。
⇒ **撤回**「Machiavelli 的 holdout 与 train 是同一部作品」这个说法，
改成「**次阈值重叠**（0.0780，权威门判绿）」。预登记里那两道题的敏感性分析
**照旧保留**（多报一个数没有坏处），但**理由要改成「次阈值」，不是「泄漏」**。

### 订正二：Rousseau 落在**人工核**那一档

0.1691 在 0.10–0.30 之间 ⇒ 按那道门的规矩**要人逐条核**，
而**仓里查不到这次人工核的记录**。它不是硬失败，也不是绿。

### ★★★ 订正三（**这才是真问题**）：Nightingale 应当硬失败，而她已判分入库

三份 train 副本的覆盖率 **0.6896／0.6927／0.7418**，
**每一个都是 HARD 0.30 的 2.3 倍以上**。按那道门的规矩，
她那份 holdout「**必须换掉**」。而实际是：她已判分、已入库、在册。

★ 她的 `reports/holdout-contaminated-passages.json` **是存在的**
（8 个被污染的 holdout 源、193 段），说明那支脚本**在她身上跑过**。
⇒ **要么它跑的时候 train 里还没有这三份副本，要么它报了而没有拦住。**
从产物上分不出是哪一种 —— 这一句是**如实标注，不是猜测**。

## 处置（我定的，与上一节一致，只是理由更硬了）

- **已判分的不重判**（㊵），但 **Nightingale 这一条要单独写进她的判决书**：
  「按 `check_holdout_overlap` 自己的常量现算，她的 holdout 覆盖率 0.69–0.74，
  **超硬门 0.30 的 2.3 倍**；ni-known-01 的答案逐字在 clm-67e20c39c27b 里。」
- **未判分的 Rousseau**：把「人工核」补上——这是那道门自己要求的动作，不是新规矩；
- **新人物**：`check_holdout_overlap` 本来就该跑，本件（work-level）只作为它的**补充**，
  用来抓「覆盖率够不着、但确实是同一部作品」的那一档（如 Machiavelli 0.0780）。

★ 教训：**报一个数之前，先去仓里搜有没有人已经在量这件事。**
我这一轮从头造了一把尺子，而权威那把就在 `scripts/` 下，
两把尺子的分母不同 ⇒ 我把绿的说成了泄漏，把真正硬失败的那个说轻了。
[[two-checkers-same-text-different-rules]]、[[tool-existed-and-i-did-it-by-hand]]

---

# ★★★ 定案（同日夜）：**直接调权威门的 `check()` 跑全库** —— 7 个工作区硬失败

上一节我用**权威门的常量**自己算了一遍覆盖率，**又错了一次**：
`check()` 内部有一道**文档频率样板过滤** ——
出现在 `> max(3, len(tr)//10)` 份 train 源里的 shingle **全部剔除**（两侧都剔）。
我没有加这一步，数字整体偏高。⇒ 这一版**不再自己算任何度量，直接调 `m.check(ws, [])`**。

（Jefferson 一个人被剔掉的样板 shingle 就有 **65,667 个** —— 差别有多大，看这个数就够了。）

## 结果：42 个有 holdout 的工作区，**35 个 rc=0、7 个 rc=1**

| 工作区 | 已判分 | 硬失败的表现 |
|---|:--:|---|
| **ignaz-semmelweis** | | **最长连续逐字段 1,069 词**，≥50 词的段 **1,646 处** |
| **robert-koch** | ✓ | **最长 629 词**，≥50 词的段 **1,483 处** |
| **rudolf-virchow** | ✓ | 最长 283 词，223 处 |
| **joseph-lister** | ✓ | 最长 215 词，247 处 |
| **florence-nightingale** | ✓ | **3 份 holdout 同时失败**（`notes-on-nursing-1888`／`-1906`／`surgical-operations-stats-1863`），另有 175 处 ≥50 词 |
| **william-osler** | ✓ | `creators-transmuters.txt`：「**有 holdout 的内容已在 train 中出现——必须换源**」 |
| **ws-jenner** | ✓ | `b22013490_0002.txt`：同上 |

**7 个里 6 个已判分入库**（Semmelweis 没有用例，未判分）。

## 顺带把我前两次的说法全部对齐

| 人 | 我第一次说 | 我第二次说 | **权威 `check()` 定案** |
|---|---|---|---|
| niccolo-machiavelli | 「同一部作品」 | 「次阈值 0.0780，绿」 | **绿**（连 ⚠ 行都没有）——**两次撤回到位** |
| jean-jacques-rousseau | 「同一部作品」 | 「0.1691 ⚠」 | **⚠**，门自己写着「逐条核完再往下走」——**成立** |
| **thomas-jefferson** | 没提 | 没提 | **⚠ 两项**（`summaryviewofrig00jeff_1.txt`；另 28 处 ≥50 词，最长 91 词）——**第 1 批里第二个要人工核的** |
| florence-nightingale | 「2/32 题受影响」 | 「覆盖率 0.69–0.74 硬失败」 | **3 份 holdout 硬失败** ——**成立，而且比我第二次说的还多两份** |

## 处置（我定的部分）

1. **第 1 批**：Rousseau 与 **Jefferson** 两人，判分之前补做那道门自己要求的**逐条人工核**；
   Machiavelli 的敏感性分析**降级为可选**（权威判绿，多报一个数无害但不必写进结论）；
   其余六人 rc=0。**已写进预登记。**
2. **新人物**：`check_holdout_overlap` 必须在出题之前跑，rc≠0 就换源 —— 这是它本来的规矩。
3. **判据**：我那件 work-level 的只作补充，文件头已写明「权威不是本件」。

## ★ 只能由你定的那一件（比早上说的更重了）

**6 个已判分入库的人物，其 holdout 按仓里自己的硬门是不成立的**
（Koch 有 1,483 处 ≥50 词逐字重合、Semmelweis 最长一段 1,069 词）。
㊵ 定的「已判分即冻结、只记档」是针对**分数有噪声**；
这一条是**装置本身按仓里自己的判据不成立**，射程不同。
**要不要把这 6 个从册子里撤下来重做，我不替你决定，也不动他们的任何文件。**
我做的是：把这张表连同复现命令写进台账，让这个决定有据可依。

    # 复现（直接调权威门自己的 check，不重实现任何度量）
    python3 - <<'EOF'
    import importlib.util, pathlib, sys
    S = pathlib.Path("CodexSkills/registry/codex/persona-distiller/scripts")
    spec = importlib.util.spec_from_file_location("cho", S / "check_holdout_overlap.py")
    m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
    print(m.check(pathlib.Path("<工作区>"), []))
    EOF

## ★ 附：跑这一轮的副作用（已清理）

直接调 `check()` **会写盘**。跑完 42 个工作区后 `git status` 冒出 16 个未提交：
2 个是**被覆盖的**（`florence-nightingale` 的证据文件少了 94 行、`joseph-whitworth` 也被改，
两人**都已判分入库**），14 个是我新造的。

已处置：覆盖掉的两份先备份再 `git checkout --` 复原；新造的 14 份删掉、空目录清掉；
`git status` 回到 0。**结论不受影响** —— `locate()` 优先用 `local_path` 且全部解析成功
（rc 是 0/1 不是 2），喂进去的是同一批文件。

★ 一个可查的事实：**仓里那份 Nightingale 证据文件比现在重跑出来的多 94 行**，
说明它是在**另一个状态**下生成的（train 集不同或脚本版本不同）。只说到这里，分不出是哪一种。

教训已落库：[[calling-the-authoritative-checker-overwrote-frozen-evidence]] ——
**跑任何判据之前先 `grep -n "write_text" <判据>` 问一句它写不写盘。**

---

# ★★★★ 再订正（同日夜，第三次）：**这件事仓里三天前就登记过了，编号 ㊲**

我把「7 个工作区硬失败、6 个已判分入库」当成新发现报了出去。**它不是。**
`references/ledgers/_待用户裁定.md` 的 **㊲**（2026-08-11，Holmes #170 撞出）
写的就是这件事，而且七个人、四个数**逐个对得上**：

| 工作区 | ㊲ 记的（8-11） | 我今天量的（8-14） |
|---|---|---|
| ignaz-semmelweis | 1646 段／最长 1069 词 | **1646 段／1069 词** |
| robert-koch | 1483／629 词 | **1483／629 词** |
| joseph-lister | 247／215 词 | **247／215 词** |
| rudolf-virchow | 223／283 词 | **223／283 词** |
| florence-nightingale | 193／238 词 | 175／238 词 |
| william-osler | 16／330 词 | 「必须换源」 |
| ws-jenner | 15／265 词 | 「必须换源」 |

㊲ 正文里连**处置的两条路**都替用户拟好了：
「这七人的 known 分数按新尺子重估」——或者——「保留原分数，在产物里注明该套组不可信」，
并写明「**撤回已发布的东西是不可逆的对外动作，必须你按**」。

⇒ 我今天写的「只能由你定」**方向是对的，但那句话三天前就写好了**，
我等于把同一件事又端了一遍。

## 我这一轮真正新增的只有三件（其余是复现）

1. **Jefferson 的两条 ⚠ 与 8/32 道受影响的题** —— ㊲ 只覆盖硬失败那七个，
   ⚠ 那一档（Jefferson／Rousseau）此前没有逐条核过；本轮做完并写进了预登记；
2. **裁定表与正文对不上**（见下）；
3. **`quality_check.py` 的注释指向已作废的 ㊱**（见下）。

### ★★ 新增①：裁定表的 ㊲ 与正文的 ㊲ **不是同一件事**

    表里第 78 行：| ㊲ | ①3 段可避开，Carver 分数照旧；②按现状记档 | P2。**七个硬失败 0 个已发布** | 记档 |
    正文 4116 行：## ㊲ **七个已入库人物的 holdout 与 train 有整段逐字重复**
                  …「为什么这件事只能你裁定」…七个全标 **已入库**

**表里写「0 个已发布」「记档」，正文写「七个已入库」「只能你裁定」。**
接手方读那张表会以为这条已经处置完了。
⇒ 建议：表里那一行改成指回正文，并把「只能用户裁定」摆到摘要里。
**我不替用户改裁定，只把矛盾摆出来。**

### ★★ 新增②：判据注释指向一个已作废的编号

`scripts/quality_check.py` 里 `run_holdout_overlap` 的注释写着
「已入库的 7 个人物（**见待裁定 ㊱**）本来就该红」——
而 **㊱ 已作废**（文件开头明写「⑯ 我误报、㊱ 当天撤回」），正确编号是 **㊲**。
㊲ 正文自己还专门记过一句「本条第一版写成 ㊱ …… 登记新条目前先 grep 最大编号」。
⇒ **同一个编号撞了第三次**：一次在登记时、一次在正文更正里、这一次在代码注释里。

## 我今天三次把仓里已有的东西当成新发现

| # | 我当成新的 | 仓里早就有 |
|---|---|---|
| 1 | 「holdout↔train 同作品」这件事 | `scripts/check_holdout_overlap.py`（硬门，含样板过滤） |
| 2 | 「逐段污染清单」 | 各工作区 `reports/holdout-contaminated-passages.json`（29 个工作区，4,304 段） |
| 3 | 「7 个已入库人物硬失败，只能用户裁定」 | **待裁定 ㊲**，2026-08-11，连处置两条路都拟好了 |

**同一个根因：先动手量，后搜仓。** 三次的代价一次比一次大 ——
第一次是多写一件判据，第二次是重复劳动，第三次是**把一件已登记的待裁项又推了用户一遍**。
[[i-built-a-second-ruler-while-the-authoritative-one-sat-in-scripts]] 已记，本节把它的
规模补全：**不是「有时会重复造轮子」，是同一天里三次。**
