# 两处开工前必须先弄清的事 —— 2026-08-13

排 Brandeis #172 进批次之前核他的「预备已完成」这句话，顺手查出两件，**都不是他一个人的事**。

---

## 一、★★ 9 个工作区的路径**套了两层**，所有工具都读错一层

    正常：wip-<人>-<n>/workspaces/<slug>/{raw,evidence,evals,…}
    这 9 个：wip-<人>-<n>/workspaces/<slug>/**<slug>**/{raw,evidence,evals,…}
                                            ↑ 多出来的一层

| 工作区 | slug |
|---|---|
| wip-barton-117 | clara-barton |
| wip-blackstone-169 | william-blackstone |
| **wip-brandeis-172** | louis-brandeis |
| wip-fleming-111 | alexander-fleming |
| wip-holmes-170 | oliver-wendell-holmes-jr |
| wip-nightingale-112 | florence-nightingale |
| wip-osler-110 | william-osler |
| wip-sorby-133 | henry-clifton-sorby |
| wip-virchow-109 | rudolf-virchow |

### 后果（已实测到一处）

移交包的回读自验证按正常布局取 `workspaces/<slug>/evidence/source-ledger.jsonl`，
对这 9 个取到的是空目录 ⇒ **它们全部被报成「没有台账或 manifest —— 本条未检查」**。
那一栏我原以为全是「老工作区没有 manifest」，**其中至少 9 个另有原因**。

★ 同理，`classify_primary` / `assign_lanes` / `emit_source_ledger` 这些
一律接 `--raw <ws>/raw` 的工具，对这 9 个都要多写一层，否则报「文件不在」。

### 处置

**本轮不动。** 理由：这 9 个里 7 个已记拒发/延后（Barton／Blackstone／Fleming／Holmes／
Nightingale／Osler／Sorby／Virchow 多为已结案），移动目录会让台账里的 `local_path` 全部失效，
而**收益只是让一个「未检查」变成「已检查」**。
⇒ 只在这里记档；**真要动，先改台账 `local_path` 再移目录，两步一起做**。

★ 但 **Brandeis #172 是要开工的**，他必须先修——否则阶段 1a 第一条命令就报文件不在。

---

## 二、★★★ Brandeis #172 的同名门有**两份互相矛盾的判定**，且**晚的那份是 blocked**

    namesake_gate.json          status=**ready**    候选 **1**  selected=brandeis-louis-1856
                                内部时间 2026-08-11T16:20:10Z｜mtime 08-12 02:20:10
    namesake-resolved.json      人做的裁定，选定 brandeis-louis-1856，附完整依据
                                （1856–1941、Muller v. Oregon 的「Brandeis Brief」、
                                 Other People's Money…）｜mtime 08-12 **02:20:10**

    namesake_gate.BLOCKED.json  status=**blocked**  候选 **14**  selected=None
                                resolution=**multiple**
                                内部时间 2026-08-11T16:21:55Z｜mtime 08-12 **02:21:55（晚 105 秒）**

那 14 个候选：`brandeis-louis-1856`／**`brandeis-louis-1902`**（另一个 Louis Brandeis）／
alfred-1854／madeline-1897／irma-1905／antonietta-1848／eugen-1846／antoine-1868／
friedrich-1835-missionary／friedrich-1835-composer／janadolf-1818／adele-1885／
**`brandeis-university-institution`**（布兰代斯大学）／`wehle-louisbrandeis-1880`。

### 两种读法都说得通，**artifacts 分不出来**

| 读法 | 说得通的地方 | 说不通的地方 |
|---|---|---|
| A：先窄搜裁定（ready），**再广搜留档**（BLOCKED 只是快照） | `namesake-resolved.json` 里有完整的人工裁定，不像废弃物 | **广搜为什么在裁定之后才做？** |
| B：**广搜推翻了窄搜的裁定**，他现在是 blocked | 时间戳支持（晚 105 秒） | 那为什么没人把 `namesake_gate.json` 改成 blocked？ |

**我判不了，也不猜。**

### ★ 这一条与 `00-状态-预备已完成等排批次.md` 直接冲突

那份文件写着「**⇒ 没有任何东西挡着他**」，而它逐项核过的四项里**不含同名门的 status**。

### 处置：**开工前先弄清这一件，再动手**

具体做法（不需要重跑广搜）：
1. 读 `namesake-candidates.json`（15.8KB，与 BLOCKED 同一时刻写的）里那 14 个候选的证据；
2. 对照 `namesake-resolved.json` 的裁定依据，看它是否已经覆盖了那 14 个
   （尤其 **`brandeis-louis-1902`** 与 **`brandeis-university-institution`**——
   前者是另一个同名自然人，后者是**以他命名的大学**，
   按 `creator:` 检索必然大量命中，与 Michelangelo 的画册同型）；
3. 覆盖了 ⇒ 把 `namesake_gate.BLOCKED.json` 改名为 `namesake_gate.superseded-<日期>.json`
   并在里面写明被谁取代；**没覆盖 ⇒ 补裁定，再改 gate 的 status**。

**在这之前不要跑他的探源** —— 同名门是检索路径上的东西，
路径一旦跑起来，14 个候选里没排掉的会直接进语料
（[[test-the-guard-against-this-persons-namesake]]）。
