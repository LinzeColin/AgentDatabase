# 研究门尾部四个码查完 —— **12 种硬错全部有解释**（2026-08-17）

前面已结案：`holdout-unverifiable`／`ledger-file-missing`（语料不进 git）、
`source-unclaimed`（18 已记档 + 2 本机补不了）、`lane-completion`（判据对）、
`attribution-basis`、`namesake-unseparable`（**查出并修了 1 个真缺陷**）。
剩下四个从没打开过的，一次量完。

## `source.minimum` + `corpus.structurally-infeasible` —— benardos-128

    usable train sources **0** < profile minimum 8
    **这批材料在结构上走不完全程**：可用 0 份 < 真实下限 9
    （8 份 train + 至少 1 份 holdout）—— **再写多少文字都过不去**

★ 台账记的是「一手 **6/20** = 30.0%，门 0.40」，而门说 usable **0**。
  两个数差太远，去打开账本 —— **`evidence/source-ledger.jsonl` 是空的（0 条）**，
  `raw/` 里只有 `.gitkeep`。**不是文件丢了，是从没入账。**
  与记录的「通道受限」逐字一致：抓不到 ⇒ 没入账 ⇒ 可用 0 份。
  那「6/20」是**探测期**的数，不是账本里的数。

★ 门这句话值得留着：「**再写多少文字都过不去**，现在停手比做完十份产物再撞上便宜」——
  这是把「结构上不可能」和「还没做够」分开说，正是本项目要的形状。

## `structure.missing` —— liebig-124

    missing required file: evidence/claims.jsonl

他记的是延后（一手占比 0.6094 < 0.65），**根本没走到断言层**。缺 claims 是预期。

## `corpus.undeclared-duplicate-sources` —— brandeis-172（25 对）、dewey-190（21 对）

两人都在「已做但未出货」的 11 个里，等阶段 5。
`_dedup-remaining的可动面-2026-08-14.md` 已记着他们：**dewey 7｜brandeis 4，返工中**，
并且写明「624 对与这 83 个簇**不是一回事**」。

★★ **同一件事三个数，全是单位／阈值差** —— 现算验过：

| | 台账 | 现算 | 门 |
|---|---|---|---|
| 单位 | **簇** | 簇 → 两两配对 | **对** |
| 阈值 | 0.55 | 0.55 | **0.3** |
| brandeis | 4 | 4 簇（4,2,3,2）= **11 对** | **25 对** |
| dewey | 7 | 7 簇（2,2,2,2,3,3,2）= **11 对** | **21 对** |

**阈值越低配到的对越多 —— 这不是回归，是两把尺。**
[[changing-the-sampling-unit-changes-the-ruler]]｜[[counts-need-their-cutoff-stated]]

★ 顺带记我自己第六次凭猜读键：查簇数时先猜了 `clusters`／`groups`，**两个都不存在**，
  真键是中文的 **`重复簇`**。读结构比猜键快。

## 结论

研究门 **12 种硬错全部有解释**，其中：

* **1 个是真缺陷，已修**（`namesake-unseparable`：判据把目标本人的全名当成同名者）；
* 其余 11 种要么是「语料不在本机」的后果，要么是**已有处置记录**的人身上的
  「还没做完」，要么是判据**判得对**。

⇒ 这一轮全库普查到此查完，**除已修的那一件外，零条需要立刻动手**。
