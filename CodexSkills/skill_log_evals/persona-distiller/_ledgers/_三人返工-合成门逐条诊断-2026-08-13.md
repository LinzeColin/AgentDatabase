# Brandeis #172／Michelangelo #185／Dewey #190 —— 合成门逐条诊断与今晚的进度

2026-08-13 深夜。这三人白天被我登记成「等着判分」，晚上真跑合成门才发现不过。
本文件是**给返工的人用的**：哪些已经修了、哪些还剩、每一类要怎么修。

## 今晚的实测进度（★ 每一步都真跑过合成门，不是估的）

| 人 | 起点 | status/category | 同名 criteria | 归属判据+2 | 署名跨行/撇号 | **现在** |
|---|---:|---:|---:|---:|---:|---:|
| Brandeis #172 | 22 | 14 | 12 | 10 | — | **10** |
| Michelangelo #185 | 46 | 40 | — | — | — | **40** |
| Dewey #190 | 36 | 31 | — | — | 30 | **30** |

★ 后两人没再降，是因为他们卡的不是那两类：Michelangelo 的 40 条里
`structure.missing` 10 ＋ 支撑/独立 16 ＋ `source-unclaimed` 6 是主体；
Dewey 的 2 条 `authorship-unproven` 是**另外两份源**（见下）。

★ Michelangelo 的 `claim.insufficient-support`／`non-independent` 从 7/7 **升到** 8/8：
不是修坏了，是 `status` 归位之后**原先被跳过的断言进入了支撑检查**——暴露出更多真问题。

## 已修（机械类，零发明）

**`status` 与 `category` 被混成了一套。** 合法 `status` 只有
`fact / pattern / hypothesis / unknown / superseded`（`ledger.CLAIM_STATUSES`），
而我的生成器写的是 `"status": cat` ⇒ `work-method`／`boundary`／`value`／`mental-model`
这些**类别名跑进了 status**，每人 6–8 条 `ledger.invalid`。

映射**读自全库 570 条已有断言的实际用法**，零例外：

    fact → fact          boundary → fact
    heuristic → pattern  mental-model → pattern  work-method → pattern
    value → pattern      epistemic → pattern     blind-spot → hypothesis

另有每人 1 条 `category: "hypothesis"` —— **`hypothesis` 是 status 不是 category**
（合法类别里同义的是 `blind-spot`，且库里 `blind-spot` 全部配 status `hypothesis`）。已改。

★ **数据与生成器一起修**，并重跑生成器验证**逐字重现**（Brandeis／Dewey／Churchill 三份都 ✓）。
★ **Michelangelo 的 `gen_claims.py` 不在仓里** —— 他的 `claims.jsonl` 是唯一真源，
  没有可重跑的生成器。这是「每人一份的临时脚本不进仓」那条老病，单独记。
★ Churchill #191 的同一处也一并修了（他虽然语料没过门，但 claims 的取值错是同一个 bug）。

## 还剩什么（按类，附怎么修）

### 一、Dewey 的 `meta.json` 是**存根**（13 条错的根）

他的 meta.json 只有 5 个键：`aliases / name / normalized_name / slug / ★`，
而 Kant／Brandeis／Michelangelo 各有 31 个。**缺 26 个**，直接后果：

- `profile` 缺 ⇒ 门回退到**最严档**，于是报
  `source.lane-coverage: 3 lanes < profile minimum 6` 与 `research.lane-completion` 同理。
  ★ 他实测是 **quick**（train 33／lanes 3／一手 100%），与同批三人一致。
- `runtime_invocation_versioning` 缺 ⇒ `runtime.versioning-enabled`。
- `identity-catalog.json`／`route-manifest.json` 等 10 个结构件缺 ⇒ `structure.missing` 10
  ＋ `route.invalid` ＋ `identity.catalog-invalid`。

**⇒ 他的工作区从来没被正式初始化过。** 补的办法**不是手填**：
用建器（builder）按流程重新初始化一次工作区，把已有的
`evidence/`、十份产物、`evals/` 挂进去。

★★ **今晚有意没补**：其中 `namesake_gate`／`identity_selection`／`attribution_basis`／
`consent_authority`／`subject_origin` 是**逐人判断**，别人的值抄不过来；
手填等于编造元数据。**宁可留着红，不许填一个看起来对的值。**

### 二、断言层的支撑不够（三人都有，是真活）

    claim.insufficient-support   每条断言要 ≥2 个支撑源
    claim.non-independent        要 ≥2 个互相独立的证据簇
    claim.model-minimum          mental models ≥2
    claim.heuristic-minimum      heuristics ≥3

现状：Brandeis 各 4／4，mental-model 1、heuristic **0**；
Michelangelo 各 8／8，mental-model 1、heuristic **0**；
Dewey 各 6／6，mental-model 1、heuristic **0**。

**三人的 heuristic 都是 0。** 这是阶段 3 的活——要回语料里找「他反复用的判断规则」，
不是改字段能解决的。**别用改门或改分类的办法凑。**

### 三、研究层遗留

- Brandeis：`research.authorship-unproven` 2（`cu31924030130557` 文中查无归属证据；
  `jstor-2276262` 文中署名 OCR 成 `Louis D. Bbandbis.`）、
  `research.namesake-unseparable` 2（**Louis Brandeis (1902–1945)**、**Louis Brandeis Wehle**
  未进 `namesake-criteria.json` 的 `excluded_names`）。
- Michelangelo：`research.source-unclaimed` 6、`research.attribution-basis` 3；
  ★ 另有 **holdout 2 个源／14 段与训练源 `src-fc3cd1b895bb` 重合**
  （见 `reports/holdout-contaminated-passages.json`，意大利语十四行诗原文 60–65 词/段）。
- Dewey：`research.authorship-unproven` 2。

## 判分之前必须过的顺序

    语料门（阶段 2）→ 研究门 → **合成门** → 判分

**「产物齐」不等于「过门」。** 这三人卡在合成门，
而我白天的预检（`check_scoring_ready.py`）**一次都没问过合成门**——判据已修。

## 2026-08-13 深夜追加：Brandeis 已修的两类（判据缺陷，不是数据问题）

**① 听证记录的发言轮次判据认不出。** `cu31924030130557`（1914 年州际贸易委员会听证）
里有 `STATEMENT OF MR. lOUIS D. BRANDEIS` 与 **151 段互不相同的 `Mr. Brandeis.` 发言**
（全场 72 个说话人标签，他排第 7），而 `TURN` 要冒号、`TURN_CAPS` 要全大写标签，两个都不匹配。
已新增 `TURN_TITLE_DOT`（称谓白名单收紧）。

**② 被 OCR 打坏的本人署名成了指控他的反证。** `jstor-2276262` 只有一行
`By Louis D. Bbandbis.`（BRANDEIS 的 R→B、E→B）。反证的豁免用**姓氏逐字命中**、
证据那条路用 **OCR 容错**，两把尺子不对称 ⇒ 同一行既是证据又是反证，反证优先。已改为过同一把尺子。

★★ 改完**全库回扫 1708 份一手**（新旧两版逐份对比）：**恰好 5 份改变**，
Brandeis ×3、Carver ×1、Gantt ×1，四份非 Brandeis 的都逐份核过说话人标签确是本人。
中途抓到自己新正则的一个假阳：Jefferson 的《Annual register 1800》里
`Mr. JEFFERSON. 91` 是**书眉**（称谓＋姓＋页码），已加页码守卫 ＋ 逐字反例。

**③ 同名 criteria 从无到有。** 他此前**根本没有** `namesake-criteria.json`。
两条分不开的同名者（侄子 `Louis Brandeis Wehle`、大屠杀遇难者 `Louis Brandeis (1902–1945)`）
都来自 Wikidata 全量 SPARQL 候选；markers 用 `Dembitz`（台账 38 行里 29 行带它）。
★ `Wehle` 在他台账里 **0 命中** ⇒ 预防性，不是在修错。

## Dewey 剩下的 2 条 `authorship-unproven` 是哪两份

    src-746dc8089f3c  b28102253.txt
    src-9fdb7da7d9d3  jstor-1640600.txt

**已逐份打开读了。两份的归属证据都印在纸面上，是判据够不着——但成因不同：**

### ① `b28102253.txt`《Psychology》1886 —— **署名跨行** ✅ 已修（2026-08-13 深夜）

扉页逐字是：

    PSYCHOLOGY
    BY
    JOHN DJEW'EY, Pn.D.
    ASSISTANT PROFESSOR OF PHILOSOPHY IN MICHIGAN UNITE RSITT

`DJEW'EY` = DEWEY 插了一个 `J`（全文 `Dewey` 只出现 2 次，`JOHN` 1 次，都在这里）。
`ocr_byline_evidence` **逐行扫**：`BY` 那行没有名字，`JOHN DJEW'EY, Pn.D.` 那行
既不以 `By` 打头、又不是纯大写（`Pn.D.` 有小写 n）⇒ 两行各自都不成立。

★ 判据**有**「并短行重试」，但按设计**只救说话人标记**，不救署名——
那是为挡住「版权页上下两行的名字被并到一起、`版权归 Richard Roe` 被误放行」定的（见其注释）。

**✅ 修法（没有放宽那条并短行）**：并行**只做一件事**——某行 strip 之后
**恰好等于**署名前缀（`BY`/`Von`/`Par`/…）时，才与下一非空行拼一次。
版权行不会「恰好只有一个 by」，所以碰不到那条反例；拼出来的候选仍要过三条设防。
另修**词内撇号被当成分隔符**（`DJEW'EY` → `DJEW`+`EY`），并**对称**归一目标姓名
（否则 `O'Brien` 会被弄坏，两条对称性自测钉着）。

★★ 全库回扫这一改动多带出 **4 份**，逐份读过全是真的：
Kant ×2（`Ouvrages traduits de l'allemand / D'EMMANUEL KANT / Par J. TISSOT`）、
Koch（`… mit heisser Luft. / Von / Dr. Robert Koch und Dr. Gustav Wolffhügel.`）、
**Mendel**（`Versuche über Pflanzen-Hybriden. / Von / Gregor Mendel.` ——
**他 1865 年那篇论文的署名，此前一直看不见**）。四份都是同一个跨行形态。

### ② `jstor-1640600.txt`《Introductory Address》1915 —— **签名落在合订件中段**（未修）

正文末尾有他的签名：

    …the good will and mutual confidence our profession exacts of us,
    we shall go forward and overcome them.

    John Dewey

另有两处旁证：目录行 `Introduotory Address: Peofessoe John Dewey 147`、
正文 `Professor John Dewey, of Columbia University, called the meeting to order
and delivered an introductory address`。

★ 但这是**期刊合订扫描件**：签名后面紧接着下一篇（`ORGANIZATION OF THE AMERICAN ASSO-…`），
所以签名不在文件末尾 ⇒ 末尾签名那一档够不着。
★★ 实测位置：签名 `John Dewey` 在文件的 **86.0%** 处，而 `A-standalone` 那一档
要求**落在文件前 30%**（那条 30% 是为挡索引/参考文献列表定的）。
⇒ 要修得先想清楚「怎么在不放进索引条目的前提下把窗口放开」，**今晚没动**。

### 两份都**不是**「查无证据」

实测：`check_text` 对两份都是 **ok=False、反证 0 条** ——
**纯欠检**，不是「文中有别人的署名」。
⇒ **不许改台账的 `attribution` 让它过门**，那是为过门而改数据；
   要么补判据（先解决上面各自那条防线），要么在判决书里写明「归属靠 IA creator 栏 + 扉页实读，
   判据的三条路都够不着」。

## 2026-08-13 深夜追加二：Michelangelo 的 `structure.missing` 10 查清了

**他缺 14 件脚手架**（对照 Kant／Brandeis 逐件比过）：

| 类 | 件数 | 是什么 |
|---|---:|---|
| **逐字模板** | **10** | `identity-catalog.json`（★ 全库 30 份**同一个哈希**，是 12 族分类的共享目录）、`scripts/runtime_router.py`、`scripts/runtime_recorder.py`、`install.sh/.py/.ps1`、`runtime/invocations.jsonl`（0 字节）、`corrections/corrections.jsonl`（0 字节）、`corrections/ACTIVE.md`（28 字节 `None.`）、`evals/results.jsonl`（0 字节） |
| 换名即可 | 3 | `agents/openai.yaml`（换名后 **0 行不同**）、`README.md`（只差身份族两行）、`team-card.json`（只差 `identity_family_id` 与 `subject_uid`，两者都在他自己的 meta 里） |
| **逐人内容** | **1** | `route-manifest.json` —— 110 行里 **57 行不同**，编码的是**这个人能以什么深度服务哪些身份族**（`"thinker-educator": "deep"` vs `"screening"`）。**机械生成不了。** |

### 为什么今晚没补

**① 该跑工具，不该手写。** 写 `route-manifest.json` 的是 `init_target.py`（第 204 行）。

**② 但它是「建工作区」的，会重写 `meta.json`** ⇒ **不许在他的活工作区上跑**
（他有十份产物、22 条断言、32 题用例）。正确做法是**跑到临时目录，再只把缺的拷回去**。

**③ 而它现在跑不起来**：`--namesake-gate` 必须是 schema-1.0 且 `candidate_count ≤ 1`
（`count > 1` 直接 `BLOCKED_NAMESAKE_SELECTION`）。而他 meta 里记的
`namesake_gate.candidate_count = 2`（他本人 ＋ 侄孙 il Giovane 1568–1646）。
⇒ **要先把同名裁定写成 resolved 的形状**（`resolution: single` ＋ `selected_subject_uid`，
   参照 `wip-brandeis-172/namesake-resolved.json`），素材他 meta 里都有（`chosen_candidate`、
   `chosen_subject_uid: michelangelo-buonarroti-1475-1564`）。

### 接手的人按这个顺序做

    1. 把 wip-michelangelo-185/ 下的同名裁定写成 schema-1.0 的 namesake_gate.json
       （resolution=single，selected_subject_uid 与 candidates[0] 对上）
    2. init_target.py --name "Michelangelo Buonarroti" --identity 艺术设计师 \
         --namesake-gate <上一步> --profile quick --language it --time-scope 1475-1564 \
         --subject-origin historical --workspace <**临时目录**>
    3. 只把缺的 14 件拷进他的工作区，**不要覆盖 meta.json / 十份产物 / evidence / evals**
    4. 重跑合成门，看 structure.missing 10 ＋ route.invalid ＋ identity.catalog-invalid 是否清零

★ **半套脚手架不如不套**：只拷 10 件模板会让工作区**看起来**初始化过，
而 `route-manifest.json` 仍缺、`route.invalid` 仍红——**看起来更完整、实际没更接近过门**。

## 2026-08-13 深夜追加三：Michelangelo 的 6 份 `source-unclaimed` 逐份读完了

判据说这 6 份「声称他所著，**既无 A-* 署名证据，也未在 `attribution_basis` 里被逐份点名**」。
打开读之后**分三类，而且不是同一件事**：

| 源 | 年 | 分栏 | 扉页/题名逐字 | 归属实况 |
|---|---|---|---|---|
| `src-7bd7f20fa044` | 1834 | holdout | `LETTERA DI MICHELANGIOLO BONARROTI PER GIUSTIFICARSI CONTRO LE CALUNNIE DEGLI EMULI E DE'NEMICI SUOI SUL PROPOSITO DEL SEPOLCRO DI PAPA GIULIO II` | **他自己的信**（题名即署名） |
| `src-28683b5f4292` | 1821 | train | `Rime di Michelagnolo Buonarroti **il vecchio**, col commento…`（Biagioli 在巴黎印） | 他的诗 ＋ 别人的注 |
| `src-d3a820576752` | 1623 | train | `RIME DI MICHELAGNOLO BVONARROTI. **Raccolte da Michelagno[lo]**` | ★★ **侄孙 il Giovane（1568–1646）辑校的那一版** |
| `src-4b92a7f08408` | 1913 | train | `MICHELANGELO — A RECORD OF HIS LIFE AS TOLD IN **HIS OWN LETTERS AND PAPERS**` | 他的书信，英文编本 |
| `src-a774edeb44f1` | 1860 | train | `MICHEL-ANGE POÈTE — **PREMIÈRE TRADUCTION COMPLÈTE** DE SES POÉSIES`（A. Lannau-Rolland） | **法译本** |
| `src-fc3cd1b895bb` | 1896 | train | `Die Gedichte des Michelangelo Buonarroti **übersetzt** … von Walter Robert-tornow` | **德译本** |

### 三条要紧的

**① 两份是译本 —— 正文是译者的措辞，不是他的话。**
（1860 法译、1896 德译）这与 `expression` 道那份唯一的源
（`Selected poems …, **with translations from various sources**`, 1885）**同一形态**。
⇒ 一个以「声口」为卖点的人物档案，**建模侧有三份是译文**。这不是判据的错，是选材要重看。

**② `src-d3a820576752`（1623）是同名侄孙辑校的版本。**
il Giovane 就是 HANDOFF 里记过的那个同名者。学界公认他**改动过原诗**。
⇒ 这份的「他所著」成立与否，取决于用它做什么：做**诗的内容**可疑，做**流传史**没问题。

**③ `src-fc3cd1b895bb` 就是污染 holdout 的那一份**（2 个源／14 段与训练源重合，
见 `reports/holdout-contaminated-passages.json`）。**它同时是译本、又是污染源。**

### 该怎么补（不是改 `attribution` 让它过门）

判据要的是 `attribution_basis` **逐份点名**并给出依据。这 6 份的依据我已逐字读出来放在上表，
**填的时候照抄那一列即可，不必重读**。另外三条缺字段（`citation` / `disputed_policy` /
`disputed_works`）里，`disputed_works` 他是**有实料**的。

★★ **订正（8-14 凌晨补齐时发现）**：上一句原本写「已改判 **6** 份（Lester 1845 的书 ＋
Fisher 的 5 本蚀刻摹本册）」——**那是漏计，只数了第 ① 轮。** 真源
`raw/_primary-decisions.json` 里判为「二手」的是 **15** 条：
**13** 条是三轮从自动判定的 P1 里改判下来的（①1＋①5＋②4＋③3，见 04-探源 六之二节），
另 **2** 条来自「需人判 4 条」里判二手的 Holroyd 与 Hildebrandt。两个数各自都对，**射程不同**。
★ 另注意：这 15 条的改判落在 `_primary-decisions.json` ＋ 研究道（`external` S1），
**台账 `attribution` 字段没有跟着改**（Fisher 那 5 本至今仍标 `HIS-OWN`）——
同一件事存了三处而只改了两处，要不要统一是**选材裁定，留给人**。

### 已做（8-14 凌晨）：三个字段补齐，46 → **31**

`disputed_works` **从 `_primary-decisions.json` 现取**（15 条，逐条带原始理由，不重打）；
`citation` 用主干编本在台账里的真 `locator`（`archive.org item leletteredimiche00mich`）；
`disputed_policy` 写明规则**并写明射程**：只覆盖「著录挂名 ≠ 他所著」这一类，
**没有**普查学界伪托之争，已知落在那类里的只有 1623 年侄孙辑校本一例，单独记明。

上表那 6 份也已逐份点进 `covered_sources`。★ **第一次补是白补的**：我写的是 `source_id`，
而 `check_source_attribution` 认的是源的 **`locator` / `original_name`**（判据文档第 42–43 行写着），
补上 `archive.org item <ia-id>` 之后 `research.source-unclaimed` 才从 **6 → 0**。

    46（登记时）→ 40（前一轮）→ 37（补完四字段）→ **31**（补完逐份点名）

剩 31 条：`structure.missing` 10 ＋ `claim.insufficient-support` 8 ＋
`claim.non-independent` 8 ＋ `route.invalid` 1 ＋ `identity.catalog-invalid` 1 ＋
`claim.model-minimum` 1 ＋ `claim.heuristic-minimum` 1 ＋ `claim.insufficient-contexts` 1。
**其中 structure/route 那 11 条只能由建器产出**（route-manifest.json 等），手补就是伪造脚手架。

★★ **但「译本算不算他的」这一条我没有裁**：它会改变他的一手占比与声口口径，
**属于选材裁定，不是判据能定的**。

### 追加：他的建模语料里译本占多少（**实测 23%，我先前说 49% 是错的**）

train 47 份逐份扫「翻译」字样：

| 判法 | 份数 |
|---|---:|
| **题名里明写** | **7**（`première traduction complète` / `now for the first time translated` / `with translations` …） |
| **扉页里明写**（已剔样板文字） | **4**（Carden 1913 `TRANSLATED AND EDITED BY ROBERT W. CARDEN` 的两个印本、Lester、Holroyd） |
| 合计 | **11 / 47 = 23%** |

★★ **我第一次算出 23/47 = 49%，那是错的。** 13 处扉页命中里 **10 处是样板文字**：

- **8 处是 Google 图书的数字化告示**：
  「If you are conducting research on **machine translation**, optical character recognition …」
  ——**每一份 Google 扫描件都有这句**，与这本书是不是译本毫无关系；
- 1 处是出版社书目（Biagioli 在巴黎印的自家书单里有 `Traduction en français des Fables de Phèdre`）；
- 1 处是版权套话（`Tous droits de reproduction, de **traduction** et d'adaptation réservés`）。

★ 而我当时**顺手写的「广告页」判别器一处都没抓到**——它找的是 `8vo`／价格，
而 Google 那句里没有。**是逐条打开读命中才发现的。**

⇒ **23% 仍是个要紧的数**（其中两份还是同一部书的两个印本，独立作品约 10）：
一个以「声口」为卖点的人物档案，建模侧近四分之一是**译者的措辞**。
**这条只报不裁**——「译本算不算他的」会改一手占比与声口口径，属选材裁定。

---

## 2026-08-14 凌晨：Brandeis #172 补第二源 —— 一条成了，三条如实记「找过、没有」

三人现测（都是**真跑 `--phase synthesis` 数 errors**，不是估的）：

| 人 | 登记时 | 今晚开工前 | 现在 |
|---|---:|---:|---:|
| Brandeis #172 | 22 | 10 | **8** |
| Michelangelo #185 | 46 | 40 | **31** |
| Dewey #190 | 36 | 30 | **30** |

### 成了的那条：`clm-0d81a3f287cb`（work-method）

原来只有 1905《Life insurance》一个源，而它自己的 `evidence_clusters` 就写明
「论文本 ＋ 演讲本是同一场演说的两个印本，**算一处证据**」——**判据是对的，断言也是诚实的**。

在他 38 份语料里搜到真正独立的第二处：**1889《The Law of Ponds》**
（`src-7c7b306a7231`，Brandeis 独著、P1、train，隔 16 年、另一个领域）：

> `The test of perceptible current is not a legal test ; it is both unpractical and unscientific.
> Perceptible in what degree ? it may be asked. To the eye ; and to what eye ?`

**他驳一个判准的方式不是举反例，是问它怎么量。** 与 1905 年「先定可量的判准、再排除规模这种代理指标」
是同一个形状。三句引文**逐字核过在不在各自文件里**，并交叉核过**不在对方文件里**（否则就是同一处）。

★ **没有硬塞**：1889 那段是水法，与本条原来的「评价一家机构」框架不同域。
处置不是把它塞进旧框架，而是**把断言收到证据真正支撑的那一层**——「判准必须可量，量不了的作废」，
并在正文里写明「用它评价一家机构」只是 1905 那一处的用法，1889 那处作证不了。
另加两条 falsifier（若 1889 那段实为转述他人 ⇒ 第二处作废退回单源）。

⇒ `insufficient-support` 与 `non-independent` 各减 1，**10 → 8**。

### 三条如实记「找过、没有」

| 断言 | 探针（正则） | 结果 |
|---|---|---|
| `clm-65191a9e194b` 披露要求 | `will not suffice｜does not suffice｜is not enough` | 命中全在 **Goldmark 的疲劳研究**（`OTHER`，不是他写的）与他 1916 年辩护状里**转引 Crichton 等人**的段落 ⇒ **不算他的第二处** |
| `clm-6003eb08ad39` 排除干扰 | `isolate｜compulsion｜free it from` | 24 处全在同一本疲劳研究（`OTHER`）⇒ **不算** |
| `clm-e52f68f18b67` 换算口径 | `is equal to \d+%｜equivalent to \d+%` | 3 处命中**逐字同句**，分别在 `Business — a Profession` 的 1914／1914／1925 三个版次 ⇒ **同一篇的重印，不是三处证据**（[[two-source-ids-is-not-two-evidences]]） |

**没有为了过门而把重印本当第二源。** 这三条维持单源、维持红。

### 剩下的 8 条：3＋3 是上面这三条，另 2 条要新写断言

`claim.model-minimum`（mental models 1 < 2）与 `claim.heuristic-minimum`（heuristics 0 < 3）
**要从语料里新蒸馏出断言**，不是补字段能解决的 —— 每条都要 2 处独立证据，
按上面的实测命中率，**这是一个完整工作段的量，不是收尾能塞进去的**。

★ 语料状况（做这件事的人先看）：三人的正文**都不在 git 里**（`.gitignore` 挡着），
但**都在本机磁盘上**：Brandeis 38 份 19.8 MB、Dewey 39 份 20.0 MB、Michelangelo 56 份 23.6 MB。
换一台机器要先 `fetch_ia.py --ids-file <工作区>/raw/_ids-rebuild.txt` 抓回来。

## 2026-08-14 凌晨续：Dewey #190 的两条逐条查完 —— 一条不该修，一条按㉕已决

### ① `runtime.versioning-enabled` —— **查出来它不是一个字段的事**

判据要 `meta.get('runtime_invocation_versioning') is not False` ⇒ 报错。
全库实测：**37 个是 `false`，6 个是 `null`**（burbank-183／churchill-191／dewey-190／
ford-188／leonardo-184／plato-186，**没有一个是已判分的**）。看着像「填一个 `false` 就好」。

**动手前先验「原样读写能不能逐字重现」，守卫当场拦住了。** Dewey 的 `meta.json` 不是任何一种
`json.dumps` 出来的 —— 它是一份**手写的 6 行占位**，文件里自己写着：

> `"★": "最小 meta.json：让 next_person 认出「这个人已经开过工」。名字取自 _蒸馏队列.json 与 IA creator 字段里已有的写法，不做推断。"`

它只有 `name` / `normalized_name` / `slug` / `aliases` / `★` 五个键。
**给它加一个 `runtime_invocation_versioning: false`，是在没有地基的房子上砌一块砖** ——
和本文档前面记过的 Michelangelo 脚手架同一个坑：「看起来更完整、实际没更接近过门」。
⇒ **不填。** Dewey 的这一条与 `route.invalid`／`identity.catalog-invalid`／10 条 `structure.missing`
是**同一个根因**：工作区从没被建器初始化过。**要修就整个重跑建器，不是补字段。**

★ 另外 5 个 `null` 只记档不动（churchill/ford 已记延后；burbank/leonardo/plato 在制）。

### ② `research.authorship-unproven`（`src-9fdb7da7d9d3` / jstor-1640600.txt）—— 打开读了

判据的话是「文中查无归属证据（署名／编者注／逐字稿轮次三者皆无）」。**这句不准确**，
证据是有的，但它的**形状**与**位置**都不在判据的射程里：

| 处 | 位置 | 是什么 |
|---|---:|---|
| `Introduotory Address: Peofessoe John Dewey 147` | **7.0%** | **目录行**（OCR：Professor→Peofessoe）。在前 30% 窗口内，**但不该算署名** —— 整期期刊的目录列着所有作者，认它就等于「谁被列进目录谁就写了全本」 |
| `…we shall go forward and overcome them. John Dewey` | **86.0%** | **落款署名**。这是真证据，**而它在前 30% 窗口之外** |
| `Professor John Dewey, of Columbia University, called the meeting to order…` | 88.1% | 第三人称叙述，不是署名 |

**落款处正好是这份文件的分界**（两条独立证据）：

| | 占全文 | 第一人称 |
|---|---:|---|
| JSTOR 样板 | 6.5% | — |
| 他的致辞（到落款为止） | **79.1%** | we 19／our 18／I 18 |
| 落款之后（会议纪要＋目录里 Lovejoy 那篇） | **14.0%** | **we 0／our 0／I 0** |

抽查 15／40／70／84% 全是第一人称致辞（`I am a great believer in…`），书眉 `SCIENCE 149`
与目录页码 147 对得上。

**处置：不放宽判据。** 理由是先例㉕（Martens／Roberts-Austen）已裁：**切段各自要署名，维持现状**。
在混装载体里，落款只证明**它前面那一段**是他的，而台账这一行是**文件级**的 ——
认了落款就等于把 14% 别人的文字也判成他的。
⇒ 这一条从「一条说不清的红」变成**已决类别**：混装载体，按㊵/㉕ 记档，红着不改。

★ 留给做这件事的人：真要收干净，是把这一行**按落款切成两条源**（他的 79.1% ＋ 他人的 14.0%），
那属于阶段 2 的重新分栏，不是判据的事。

## 2026-08-14 凌晨再续：Dewey #190 的「材料不够」是假的 —— 真因是 meta 缺 `profile`

写在前面：**我差点把他判成结构性无望。** 他的错误里有
`source.lane-coverage: 3 lanes < profile minimum 6` 与 `research.lane-completion` 同款，
而 `min_lanes 6` 正是第 2 批十人九出局的第一死因。按那个读法，Dewey 该直接记延后。

**去查档位，翻号了。**

| 人 | `meta.profile` | 台账源数 | 有源的道 |
|---|---|---:|---:|
| Brandeis #172 | **quick** | 38 | 5 |
| Michelangelo #185 | **quick** | 56 | 4 |
| Dewey #190 | **（没有这个字段）** | 39 | **3** |

Dewey 的 `meta.json` 是手写占位（只有 5 个键），**没有 `profile`**，于是
`quality_check` 按 **standard** 判他：`min_lanes 6`／`min_models 3`／`min_heuristics 5`。
他两个同伴都在 **quick**：`min_lanes 3`／`min_models 2`／`min_heuristics 3`。
**同一批返工的三个人，一个被拿另一把尺子量着。**

### 压线 3=3，所以把三条道都打开读了

`min_lanes` 在 quick 是 3，他实测**正好 3** —— 踩坑库里「读数正好压在门槛上」那条说过，
压线那一项往往是唯一没打开读过的（Leonardo 一读就翻号）。逐源读：

| 道 | 源 | 是不是真的 |
|---|---|---|
| writings（4） | 《学校与社会》1900、《民主主义与教育》1916、《人性与行为》1922、《我们怎样思维》1909 | **四部各自独立的代表作** ✓ |
| conversations（1） | 《Letters from China and Japan》1920 | **只有一份，且与妻子共同署名** —— 已写在 START-HERE，是已知脆弱点 |
| expression（2） | ① `jstor-1640600` 学会成立大会开幕致辞（1915-01，登在 *Science*）② `jstor-40216730`「**会长年会致辞，1915-12-31**」 | **两篇不同的演说**：相隔 11 个月、不同场合；8-gram Jaccard **0.0324**（155/≈4800，基本只有样板重合）✓ |

★ 顺带查清：②那份**开头 12% 处就有正规署名** `ADDRESS OF THE PRESIDENT PROFESSOR JOHN DEWEY, Ph.D., LL.D.`，
所以前一节那条 `research.authorship-unproven` **只涉及①那份混装期刊**，不是这一道整体有问题。

### 处置：定 `profile = quick`，理由按㉞ 写明

㉞ 已裁「**按可得性选档 + 退档写明理由**」。Dewey 39 条源够 standard 的 24，
**但 standard 要 6 条道而他只有 3** ⇒ **退档到 quick**，与两个同伴同一把尺子。
**这不是挑一个好过的档**：定 quick 之后 `min_lanes 3` 与他实测 3 **正好齐平、没有富余**，
而且上面已逐源读过、不是空心道。

⇒ 定 quick 之后他的门变成：`models 1 < 2`（差 1）、`heuristics 0 < 3`（差 3），
`lane-coverage` 与 `lane-completion` **两条消失**。

### 所以 Dewey 的 30 条实际是这样分的

| 类 | 条数 | 怎么关 |
|---|---:|---|
| 工作区从没被建器初始化（`structure.missing` 10 ＋ `route.invalid` ＋ `identity.catalog-invalid` ＋ `runtime.versioning-enabled`） | **13** | **跑一次建器**（`init_target.py`，要一份 ready 的同名护栏 ＋ `--profile quick`）。★ 8 个内容产物（persona/work/cognitive-os/decision-policy/strategy/capabilities/boundaries/divergence-map ＋ facts ＋ hypotheses）**全都在**，缺的只是脚手架 |
| 档位缺失造成的 | **2** | 同上，建器会写 `profile` |
| 断言支撑/独立性 | 12 | 要真找到独立的第二处（Brandeis 那节的做法与命中率） |
| 断言条数下限 | 2 | quick 下差 models 1 ＋ heuristics 3 |
| 混装载体（已决，按㉕ 红着不改） | 1 | — |

**⇒ 30 条里 15 条是一次建器运行的事，而不是十五件事。**

## 2026-08-14 凌晨收束：脚手架照 Dewey 的治法给两人都补了 —— 104 → 42

| 人 | 登记时 | 今晚开工前 | 现在 | 这一轮做了什么 |
|---|---:|---:|---:|---|
| Brandeis #172 | 22 | 10 | **8** | 一条断言补到双源双簇；另三条如实记「找过、没有」 |
| Michelangelo #185 | 46 | 40 | **19** | `attribution_basis` 四字段＋逐份点名（→31）；补建器脚手架（→19） |
| Dewey #190 | 36 | 30 | **15** | 补建器脚手架＋定档 quick（→15） |
| **合计** | **104** | 80 | **42** | |

### 补脚手架的做法（两人一样，可照抄）

```bash
# 1) 写一份 schema 1.0 的同名护栏（**候选只能 1 个**，多于 1 个 init_target 直接 BLOCKED）
#    ★ selected_subject_uid 用**可读 slug 式**（全库 30 个存量都是这个式样）；
#      已有 meta 的人就直接用它的 chosen_subject_uid，不另造。
# 2) 跑进**临时目录**，绝不对着现有工作区跑（--force 也会拒，因为现有目录不是建器产出的）
python3 .../scripts/init_target.py --name "<名>" --identity <族> \
        --namesake-gate <gate.json> --profile quick --subject-origin <public|historical> \
        --workspace /tmp/<x>
# 3) **只拷贝本工作区没有的文件，一个已存在的都不覆盖**（两人各拷入 52 个、跳过 20/21 个）
# 4) 复跑 quality_check --phase synthesis 核实
```

★ 拷贝前后都核过：**语料 `.txt` 一个都没进仓**（`.gitignore` 按设计挡着）。

### 一个我自己造出来又自己抓回来的坑

第一次给 Dewey 写护栏时，`selected_subject_uid` 我用 `subject_uid()` 现算了一个
`person-6752688d800cdfb0`。跑完才去查全库：**43 个有 uid 的工作区里 30 个是可读 slug**
（`otto-von-bismarck-1815-1898`、`bessemer-henry-1813-1898-steelmaker`…），12 个没有，
`person-<16hex>` **只有我刚建的这一个**。

根因：`init_target.py` 的 `chosen_subject_uid` 是**把护栏的 `selected_subject_uid` 直接抄过来**，
**约定由护栏作者定，不是由那个函数定**。改护栏重跑，只有 `meta.json` 含这个值，
52 个脚手架不受影响，合成门也没变（仍 15）。

### 剩下的 42 条是什么

| 类 | 条数 | 说明 |
|---|---:|---|
| `claim.insufficient-support` ＋ `claim.non-independent` | **32** | 同一批断言各计两次 ⇒ 实为 **16 条断言**缺独立第二处。Brandeis 那节记了做法与命中率：**重印本不算第二处** |
| `claim.model-minimum` / `heuristic-minimum` | 6 | quick 档：models 要 2、heuristics 要 3。三人分别差 1/1、1/1、1/3 |
| `claim.insufficient-contexts` | 1 | Michelangelo |
| `research.authorship-unproven` | 1 | Dewey 的混装期刊，**按㉕ 已决，红着不改** |
| 合计 | **40** | （另 2 条为四舍：Brandeis 8 ＝ 3+3+1+1） |

**都要读语料才能关，没有一条是补字段能解决的。** 三人的正文都在本机磁盘、不在 git，
换机器先 `fetch_ia.py --ids-file <工作区>/raw/_ids-rebuild.txt`。

## 2026-08-14 凌晨末：给 Michelangelo 找第二个源，找到一个**会骗过判据的假的**

`clm-504ac43c44d3`（blind-spot，1 源／1 context／1 簇，三样都不够）引的是英译本一句
`But ye have never understood me in the past, and ye do not understand me now. May God forgive you !`
（`src-8539ad71569a`，1913 英文编本）。

在 56 份语料里搜到意大利文本 `src-6094206729a1`（1875 Milanesi 书信集）：
`Ma voi non m'avete mai conosciuto, e non mi conosciete. Idio ve lo perdoni!`
——另一部书、另一年份、另一种语言，**判重门给出 8-gram Jaccard 0.0000**。

**打开读，是同一封信**：前一句（「你本该说：米开朗基罗记得他写过什么…」）、
中间那个比喻（「给一匹已经竭尽全力的马加鞭没有用」）、本句、后一句（「他给了我恩典让我扛着…」）
**四句在两边同一个顺序**。加上它，`insufficient-support` 与 `non-independent` 两条当场消失，
而证据仍然**只有他说过的那一句话**。

⇒ **没有加。这条维持单源、维持红。** 已落成踩坑库
`original-and-translation-are-one-utterance-with-zero-overlap`（第 164 条）。

★ 同一把尺子在这份语料上的三个读数：同一部书信集的两次数字化 **0.1892**（OCR 差异压低的，
逐字同文）、同一封信的原文 vs 译文 **0.0000**、真正不同的两部作品也很低 ——
**它分不开这三种**。「独立的第二处」只能在「同一次话语」这一层判。

## 2026-08-14：Michelangelo 那 16 条不是 16 件事 —— 是延后类别⑦

把他 8 条 `insufficient-support` 逐条摊开看引的是哪部源：

| 断言 | 类 | 引的源 |
|---|---|---|
| `clm-57132310d198` 估工期与花费 | work-method | `Le lettere`（1875） |
| `clm-584dece9bffe` 不内行就请人估价 | work-method | `Le lettere` |
| `clm-a928f1063ac7` 远程付款要回执写名 | work-method | `Le lettere` |
| `clm-2a3b37136bc9` 钱的小数说到底 | mental-model | `Le lettere` |
| `clm-7cee3cb6b511` 对家人要品行不要成就 | value | `La lettere`（**同一部书的另一次数字化**） |
| `clm-504ac43c44d3` 直说对方一直误解他 | blind-spot | `A record…`（**同一批信的 1913 英译选本**） |
| `clm-f7f67f29f5af` 同场地多工种先腾干净 | work-method | `A record…`（同上） |
| `clm-f7a2ee35980a` 下施工指令给判据 | work-method | `Le rime`（诗集，**这一条确实是另一部书**） |

**8 条里 7 条落在同一部书信集上。** 而台账里那部书信集占了 **5 个 source_id**
（四个 1875 数字化 ＋ 1913 英译选本，逐对 Jaccard 0.15–0.24 与 0.0000，
用一句原文当探针四份全中——见踩坑库第 164 条）。

### 所以「补第二个源」这条路对他是走不通的

- 台账的粒度是**一本书一个 `source_id`**。同一部书信集里**十封不同的信**，仍然只算 **1 个源**。
- 而拿那 5 个 id 里的另一个去凑，判据会当场变绿，**证据却还是同一部书** ——
  这不是修复，是把判据骗过去。今晚差点这么做，被探针拦下。

⇒ **这就是 Pacioli #161 记的延后类别⑦「方法证据全部汇到一部作品」**，
只是他这里更险：**语料里就摆着 5 个 id，凑起来毫不费力。**

### 只有两条真出路（都不是判据能定的）

1. **阶段 2 重新分源**：把书信集按**单封信**切成独立的 source（每封信是一次独立的话语），
   这样「同一方法出现在 1509 年和 1523 年的两封信里」才数得出两处。
   —— 这是重做他的阶段 2，不是补字段。
2. **记延后类别⑦**，与 Pacioli #161 同处置。

**本轮不替人做这个选择**：它改的是他全部 19 条里的 16 条，且会连带改动一手占比与分道口径。

## 2026-08-14：Dewey 的第二个源 —— 找到了一处，**而它在密封集里**

先用新工具 `measure_distinct_works.py` 把他的重复组量出来（**免得挑到同一本书**）：

    train 33 份 → 独立作品 15–23（塌缩 10–18）；旧 `_dedup.json` 报塌缩 9
    Ethics ×3（Jaccard 0.8843–0.9377）、Democracy and Education ×3（0.7787–0.8201）、
    Psychology ×2（0.8471）、Creative Intelligence ×2（0.8208）

⇒ `clm-dee42888b380`（定义靠排除，引自《How We Think》1909 `src-e35552b6cd32`）的候选里，
**1910 年那版《How we think》是同一本书**，先排掉。

用「定义式排除」的探针（`the term is restricted`／`by X we do not mean`／
`the word X is restricted|confined|limited`／`exclude from the term|definition` …）在其余作品里搜：

**训练侧 0 命中。**

★ **按规矩先验探针看不看得见已知正例**（「0 命中」先问命令能不能看见它）。
不加任何过滤重跑，命中 **3 份**：

| 源 | 年 | split | 说明 |
|---|---|---|---|
| `src-e35552b6cd32` | 1909 | train | **本条自己的源** |
| `src-43a5ae8b4b7d` | 1910 | train | **同一本书的另一印本** |
| `src-34a627f2709a` | 1930 | **holdout** | 《The Quest For Certainty》 |

⇒ **探针没瞎**，而唯一的第二处落在**密封集**里。

### 处置：不用，维持单源维持红

密封集是判分用的，**拿它去补建模侧的断言就是泄题**。
这条从「找不到第二处」变成**「第二处存在但取不得」** —— 后者是个更硬的结论：
**再找也没有用，除非重新分 split（那要重做阶段 2 且会作废他现有的盲判题）。**

★ 方法上多一条：找第二个源时要**同时排除两样**——
① 同一部作品的其它印本（用 `measure_distinct_works.py` 先量重复组）；
② `split == holdout` 的源。我这次是先按 train 过滤才没踩到②，**那是运气不是设计**。
