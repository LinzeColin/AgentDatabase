# 自优化回路端到端实跑：**通的，而且门是诚实的**

Owner 的评分写着「自优化迭代…没有实质性收益」，而 2026-08-17 实测
`telemetry/team-outcomes.json` **0 条记录** —— 于是有两种可能：
**回路坏了**，还是**没人跑过**。此前从未区分过。

## 实跑（在 scratchpad，不碰仓）

    ① route_team_moe.py  --task <软件题>            → rc=0
    ② score_team_delta.py --result <判分输入>        → rc=0，overall_delta=70.45
    ③ record_team_outcome.py --route-plan --delta-score --task-slice software
                                                    → rc=0

写出的遥测结构完整：

    sample_count=1｜runs=1｜experts=10 人
    expected_calibration_error=0.5796｜observed_task_slices=['software']
    **eligible_for_c=False**

## ★ 决定性的一步：把遥测喂回路由

                    头名               telemetry_prior   telemetry
    无遥测          linus-torvalds     0.0               {'reason': 'not C'}
    **带遥测**      linus-torvalds     0.0               {'reason': 'not C'}

**先验正确地没有生效** —— 1 条样本远不够 C 策略的准入
（`route_team_moe` 第 94 行：**≥60 outcomes、ECE≤0.12、任务切片覆盖≥0.75**）。

## 结论

**回路是通的，拒绝也是对的。** 「0 条记录」不是代码问题，是**没人跑过**；
要让 C 策略真正上线，缺的是 **≥60 条真实结果**，不是修 bug。

★ 射程：本次第 ② 步的数值是**我编的合法占位**，只用来验回路通不通，
  **不构成任何绩效证据**，遥测文件留在 scratchpad、未进仓。
★ 这条与「calibration_status 已写进合同顶层」配套：合同现在会说
  「本次排序未经校准、先验全 0」，而本件证明**那不是坏了，是还没喂数据**。
