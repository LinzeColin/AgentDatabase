# `check_ocr_language_death` 的阈值依据**当天被新语料证伪**（2026-08-18）

## 判据自己怎么立的这个数

`scripts/check_ocr_language_death.py` 文档里写得很清楚，而且写得对：

> 227 份真实语料的分布：最大的四个间隙
>   间隙 0.122：**0.117 → 0.239   ← 空带，其下 18 份、其上 209 份**
>   间隙 0.014：0.255 → 0.268 ／ 间隙 0.013：0.094 → 0.108 ／ 间隙 0.012：0.268 → 0.280
> **0.117 与 0.239 之间一份都没有**，而次大的间隙只有 0.014。
> 阈值取 `0.15` —— **落在这条空带里面，不是我挑的一个数。**

这是本仓少见的、**从数据里读出来**的阈值，写法值得保留。

## ★★ 今天一个人的语料就把那条空带填了

Bismarck #176 语料复原后跑它：**70 份可判，19 份低于 0.15**。逐份看落点：

    落在「空带 0.117–0.239」**里面**：**15 份**
      0.118 0.118 0.119 0.120 0.120 0.121 0.122 0.128
      0.129 0.132 0.136 0.137 0.140 0.141 0.144
    真正在空带**以下**（<0.117）：**4 份**
      0.095 0.098 0.111 0.114

**15/19 落在那个「一份都没有」的区间里。**

## 这意味着什么（两种读法，我没有证据分辨）

- **A**：原来那 227 份里 Fraktur 太少，**空带是取样的产物**，
  德文哥特体本来就连续分布在 0.11–0.15 —— 那么阈值仍可能是对的，
  但「它落在空带里所以不是我挑的」这条**理由已经过期**。
- **B**：这 15 份确实是坏的，只是坏得没那么彻底 —— 那么阈值是对的，
  只是这批语料把分布的下半段填密了。

两种读法下**门都不该动**，但**理由必须换**：
不能再说「阈值落在空带里」，因为空带不空了。
同族：[[a-stale-reason-does-not-void-the-conclusion]]（理由过期 ≠ 结论作废）、
[[a-reading-exactly-at-the-threshold-is-load-bearing]]。

## 我没有做的事，以及为什么

- **没有改阈值。** 改门是 Owner 的事；而且此刻改门会直接让 Bismarck 的
  `corpus.ocr-dead-as-primary` 变绿 —— **为了让自己手上这个人过门去动判据，
  正是本仓一直在挑的毛病**（同 08-18「没有抬那道 500 文件门」）。
- **没有把这 15 份降级。** 降级会让它们的逐字引文一并失效，
  而是否真的坏到不能引，需要**打开读**，不是看一个比值就定。

## 给 Owner 的两个选项（都要你定）

① **重算空带**：拿现在全库（不止那 227 份）重跑分布，看最大间隙落在哪，
   阈值随之更新，并把新的分布写进判据文档。代价：一次全库扫描。
② **维持 0.15，只改理由**：文档里把「落在空带里」改成
   「原 227 份样本上的空带阈值；2026-08-18 起该带已被德文 Fraktur 填充，
   阈值保留但依据降为经验值」。代价：0，但从此这个数不再有实测支持。

★ 在你定之前，Bismarck #176 的这条红**照红**，不绕、不豁免。
