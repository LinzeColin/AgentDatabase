# 已判分的 21 个跑发布门：**0 个干净** —— 但其中**已入库的只有 2 个**（2026-08-17）

㊵ 说「已判分即冻结」，指的是**不动**它们；只读地跑一道门不改任何字节
（跑完 `git status -- _corpora` = 0 行）。于是补上这个从没做过的测量：
**已交付的产物今天还过得了发布门吗。**

## 数

    已判分（results.jsonl 非空）的工作区：**21** 个
    逐个 `quality_check.py <ws> --phase release`
    **0 错的：0 个｜有错的：21 个**

单看这句会读成「21 个产物全坏了」。**分母切错了。**

## ★ 决定性的切法：**在不在那 102 人的名册里**

`persona-distiller-group/team-index.json` 的 `products` 有 102 条，
逐条取 `subject_slug` 与工作区目录名比对：

| | 个数 | 该怎么读 |
|---|---:|---|
| **已入库**（`subject_slug` 在名册里） | **2** | shewhart-165（4 条）、carver-127（3 条） |
| 不在名册（拒发／延后） | **19** | 红是**记录在案的预期**，不是回归 |

19 个里最红的两个正是记过拒发的：thomson-129 **219 条**、rosenhain-138 **90 条**
（各自 192／68 条是 `eval.result-invalid-system`）。

★ 顺带记一次我自己的错：查名册时我先猜了 `personas`／`members`／`roster` **三个键，全不存在**，
  真键是 `products` —— **今天第五次凭猜读键**。第一版靠「整份 JSON 里搜子串」
  碰巧也得到 2/19，**结论对而路径错**。

## 那 2 个已入库产物的 7 条，逐条定性

### shewhart-165（4 条）—— **四条全是「语料不在本机」的后果**

    research.ledger-file-missing   13 条账本记录指着不存在的文件
    content.coverage-unresolved    「过半来源回连不上缓存正文」
    content.quote-unresolved       「**一份语料都没读到**，未核成不是通过」
    content.names-unresolved       「**一份语料都没读到**」

三条自己就印着「**未核成（不是通过）**」。**这不是产物坏，是我没带 `--cache` 跑。**
带上真语料重跑才是对它的测量；语料不进 git，本机没有。

### carver-127（3 条）—— 1 条语料缺失 ＋ **2 条都已有记录**

    research.ledger-file-missing            41 条 → 同上，语料不在本机
    corpus.holdout-mentioned-in-artifacts   5 处 → **仓里已经查过两次**
    content.quote-no-locator                92 条长引文、56 条带坐标、**36 条缺**

★★ 那「5 处」**不是新发现**：`_holdout提及全库现算-2026-08-12.md` 的起因
  **逐字就是**「Carver #127（已发布产物）报 5 处，而台账记着只剩 1 处」，
  当时定案是**口径差不是回退**；**今天**又有一份
  `_holdout提及全库复核-12个工作区47处-全部已冻结-2026-08-17.md` 复核完，
  结论是「12 个工作区 47 处，**全部已冻结，没有东西可清，也没有新缺陷**」。
  逐行打开看，那 5 处全是我自己**关于 holdout 机制的说明文字**，
  含「本道**无**不入训练的材料」与「★ **此处刻意不写**它们的 source_id」。
  **我又发现了一遍。**[[three-times-in-one-day-i-rediscovered-what-the-repo-already-had]]

★ `quote-no-locator` 在 `_一手源与可逐字引用是两件事-2026-08-11.md` 里
  记的是「已修（见同日 `check_quote_locator` 三处改动）」，而 Carver 今天仍报 36 条。
  他属 ㊵ **已判分即冻结**，**记档不改**；要不要重估留给重蒸那一轮。

## 结论

「21/21 全红」的正确读法是：

* **19 个从来就没入库** —— 它们的红是拒发／延后的记录本身；
* **2 个已入库的共 7 条**，其中 **4 条是我没带语料跑出来的「未核」**，
  另 3 条**全部已有记录**（2 条已两次定案、1 条属冻结件）。

⇒ **这一轮没有在已交付产物上查出新缺陷。**
  真要回答「已交付的产物今天还好吗」，**必须带 `--cache <真语料>` 重跑** ——
  在没有语料的机器上，这道门的一半问题只会答「未核成」。
