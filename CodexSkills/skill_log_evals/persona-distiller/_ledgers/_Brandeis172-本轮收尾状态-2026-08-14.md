# Brandeis #172 本轮收尾：**硬错 8 → 5**，余下三条为什么没做完

**2026-08-14 傍晚**｜本轮到此交接，状态逐条现算。

## 做掉了什么（3 条）

| | 起点 | 现在 | 怎么做掉的 |
|---|---:|---:|---|
| `claim.insufficient-support` | 3 | **2** | `clm-e52f68f18b67` 补上 1905《Life insurance》 |
| `claim.non-independent` | 3 | **2** | 同上 |
| `claim.model-minimum` | 1 | **0** | 新增 `clm-6c5f657166fe`（受托人范畴） |
| `claim.heuristic-minimum` | `1<3` | `2<3` | 新增 `clm-ab9f7197235f`（先说不算数，再说缺什么） |
| **合计** | **8** | **5** | |

三条新证据**全部逐字核过**：位置（归一化文本百分比）、节题、前后双引号数都记进了
`evidence_clusters`；OCR 讹形一律照录不改并写明。

## 余下 5 条，逐条说清为什么没做完

### ① `heuristic-minimum 2 < 3` —— 差 1 条，**没有硬凑**

补一条 heuristic 必须自带两处独立证据。本轮已把 heuristics 从 1 提到 2，
第 3 条**没有找到两处可证的**。**不许为了过门写一条只有一处支撑的。**

### ②③ `clm-65191a9e194b` 与 `clm-6003eb08ad39` 各缺第二处 —— **搜过 6 轮，确实没有**

搜索面每次都印了分母：**train 有正文 34 份 → 16 部独立作品**。

| 断言 | 用过的判据 | 结果 |
|---|---|---|
| `clm-65191a9e194b`（披露三步） | `not sufficient` 族／`disclos\|publicity`／`It is not enough` 族 | 前两组逐条读完全是别的题材；第三组**被工具 rc=3 拒跑**（正对照没过） |
| `clm-6003eb08ad39`（排除扭曲判断的处境） | `conditions under which` 族／`temptation\|bias\|self-interest` 族 | 173 处 / 8 部逐条读完，**没有一部在做同一件事** |

★★ **最像的候选被合著挡掉**：1890《The Right to Privacy》署名是
`Samuel D. Warren, Louis D. Brandeis.`，台账却记 `P1／HIS-OWN` 无分工声明
（判据看不见是因为署名在 **99.9%** 处而窗口只看前 4000 字）。
**两人署名的文章不能用来证明「他的」方法。** 另记
`_ledgers/_署名在文末的合著判据看不见-2026-08-14.md`。

★★ **第二像的候选查证后弃用**：1918 那句 `The war alone, in my opinion, is not sufficient
to justify…` 句式完全吻合，**但它在 `the Secretary of War wrote on December 18, 1917:`
的引号之内** —— 是**陆军部长**的话。已写进新 heuristic 的 `alternative_explanations` 里留证。

## 下一位接着做的两条路（**都要人读语料**）

1. **继续找第二处**：换方向搜「他布置制度的段落」（谁来定规则、谁被排除在决定之外），
   而不是搜措辞；用 `find_second_evidence.py --claim <id> --pattern <正则>`，
   **它会先跑正对照，抓不到已知正例就 rc=3 拒跑**——这条纪律本轮救过我一次。
2. **改断言而不是改证据**：把两条 work-method **收窄到确实两处可证的那一层**，
   或合并成一条。⇒ 会动 `work-method` 条数，**先确认不会把别的门顶红**。

★ 另有 2 条 warning（不是硬错）：新加的两条断言 `claim.orphan`
——**尚未渲染进任何 core artifact**（既有的 `clm-1e536611e3dd` 同样如此）。
补渲染时要连 `persona.md` 的 `<!-- claim:… -->` 标记一起加。

## ★ 一条顺带订正

`START-HERE.md` 里返工三人写的是 **8／19／13**（8-14 凌晨的数）。
**8-14 傍晚现测：Brandeis 5／Michelangelo 19／Dewey 2，合计 26**（原 40）。
Dewey 那 13 → 2 不是本轮做的，是此前别处做掉的；本轮只把数更新并注明出处。
