# #174 Lincoln 过研究阶段总门 —— `passed: True / errors: 0`

    check_lane_quotes_verbatim   rc=0（10 条引文 0 对不上）
    check_holdout_mention        rc=0
    check_holdout_overlap        rc=0（硬失败 0／待人工核 0／无内容重合）
    check_attribution_basis      rc=0
    quality_check --phase research  **rc=0 / passed=True / errors=0 / warnings=2**

## 归属依据（`meta.json.attribution_basis`）——**只写能指出来的**

- **authority**：主干是 Nicolay–Hay 编本，依据取自**语料自身的题名页逐字**：
  `Complete Works of Abraham Lincoln  Edited by John G. Nicolay and John Hay …`
  两人是他任内的私人秘书。★ **这是编者权威，不是逐篇署名证据**，两者的区别写进了 policy。
- **disputed_policy** 分两类，都不靠沉默过关：
  ① **6 份无正文署名的单行本**（葛底斯堡那份正文第一句就是 `FOURSCORE AND SEVEN YEARS AGO`，
    全篇不出现他的名字；3 份题名含 Lincoln、3 份不含）⇒ 只计份数，不单独支撑断言
  ② **Bixby 信**：`grep Bixby` 实测出现在 **20 份**语料里，各编本均署为林肯，
    而**我手上没有能出示的学术来源**去核它的归属 ⇒ 进 `disputed_works`，
    **在有可出示依据之前不得用作声口或事实样本**
- ★ 这一项**不是**「没有争议篇目」，**是查过并列了一条**。

## ★★ 我的声明与台账状态曾经不一致

写完「Bixby 信不得使用」之后，台账里含它的源**仍是 P1/train、毫无标记**。
逐份扫过之后：**21/70（30%）含 Bixby 信**
（writings 13、conversations 5、expression 2、decisions 1），已全部加上
`contains_disputed_work` 与处置说明。

⇒ **写下一条禁令，不等于它在数据里生效。** 声明与状态要分别落地并对账。

## 顺带查清的两条警告

| 警告 | 实况 |
|---|---|
| `corpus.unexamined-band` 2 份 | 两份都是葛底斯堡的小册子（340／321 词），落在「字符够而词数不够」的缝里 |
| `source.filename-year-mismatch` 1 条 | **4 条命中里 3 条是把馆藏编号当年份**：`cu31924…` 的 1924、`india.history.resource.111513` 的 1513、`jstor-20086068` 的 2008。真正的一条是 `earlyspeechesofa1858linc`（内容年 1858 / 版次年 1924），两个数都对 |

## ★ 分道的一个已知限制

`src-af9d9ae573da` 的全题名是
`Gettysburg address … ; [and] **Lincoln's letter to Mrs. Bixby**` ——
**一个文件里装着两件作品**（一篇演说 + 一封信）。
`assign_lanes` 按模式优先级取第一个命中，`letter` 排在 `address` 前面，
于是整份进了 `conversations`。

⇒ **一份文件只能进一条道**（道数要能被门直接用），
  但**「这一份里其实有两件东西」这件事本身要记下来**，否则下游会以为
  conversations 道里多了一篇演说、expression 道里少了一篇。
