# #92「全库未声明 625」复核 —— **数对得上；四人占 96%，且都已判过分**

日期：2026-08-12。原记：「全库未声明 **625**（原记 912）｜★ 抽样 11 对里 10 对**本来就不该声明**——不是欠账」。

## 一、数：**624，与记录一致**

全库 36 个工作区跑 `check_source_dedup.py --json`，只取 `**未声明的重复对**` 这一个键：

```
**未声明的重复对：624**
已声明的重复对：789
只因样板越线（已扣除）：0
```

原记 625 —— **对得上**。

### ★★ 我第一版报的是 1160，错在自己身上

第一版我写了「扫所有含『未声明』或『重复』的键，取 `max`」——
于是把 **`已声明的重复对数`（789）** 也算了进去，得出 1160，
差点报成「实际是记录的近两倍」。

**这是今天第二次栽在同一处**（上一次是把 `title` 的问题率当成合格率）。
两次的形状一样：**用一个模糊的取数规则去凑一个数，而不是指名要哪一个字段。**
⇒ 已有的 [[eval-artifacts-have-five-schemas]] 说的是「按一种命名去统计会错」；
  这一条是它的更蠢的版本——**我连命名都没指定，让 `max` 替我选**。

## 二、分布：**四人占 96.0%**

| 工作区 | 未声明对 | 判分文件 | registry |
|---|---|---|---|
| barton-117 | **222** | 7 | 否 |
| osler-110 | **148** | 7 | 否 |
| virchow-109 | **118** | 7 | 否 |
| nightingale-112 | **113** | 9 | 否 |
| 合计 | **601 / 624 = 96.3%** | 全部**已判过分** | 全部**未发布** |
| 其余（jenner 13、carver 7、koch 1、lister 1…） | 23 | | |

⇒ **与 #102、#98 撞的是同一条红线**：这四人都已判过分，
  补 `derived_from` 声明等于改被评分过的输入。
  且四人**都不在 registry**——所以同样不是「重估已发布分数」，
  而是「**已判分、未发布的产物能不能改台账**」＝ **㊵**。

## 三、原记那半句「抽样 11 对里 10 对本来就不该声明」——**已复核：不成立**

2026-08-12 晚补做。**抽样只覆盖 Barton（222/624 = 35.6%）**——
另三人（osler/virchow/nightingale）我的工作区路径写错、`--json` 取不到，
**这一点必须先说**：下面的结论只对 Barton 那 222 对成立。

从 222 对里按重叠度分层抽 11 对：

| 重叠 | 甲 ⇄ 乙 | 判读 | 依据 |
|---:|---|---|---|
| 0.9707 | `webarton-life-1922-DUPSCAN3` ⇄ `webarton-life-v1-1922` | **同一部** | 同名 + DUPSCAN 后缀＝同书另一次扫描 |
| 0.9519 | `story-rc-1904-DUPSCAN` ⇄ `story-rc-1915` | **同一部** | 两份扉页都作 `A STORY OF THE RED CROSS`（**读过**） |
| 0.9502 | `story-rc-1904` ⇄ `story-rc-1915` | **同一部** | 同上，**读过扉页** |
| 0.8305 | `rc-peace-war-1906` ⇄ `sw-books-the-red-cross-printed-copy…` | 同一部（**按题名推**） | 未读原文 |
| 0.8282 | `rc-history-1898-DUPSCAN2` ⇄ `rc-peace-war-1910` | **同一部（改判）** | 见下 |
| 0.6846 | `story-rc-1904-DUPSCAN4` ⇄ `sw-books-a-story-of-the-red-cross-proof-sheet…` | 同一部（校样） | 按题名推 |
| 0.6823 | `rc-history-1898-NLM` ⇄ `sw-books-the-red-cross-drafts-1884-1898…` | 同一部（草稿↔印本） | 按题名推 |
| 0.4626 | `sw-speeches…manuscripts-and-pri` ⇄ `sw-speeches…philanthropy-congre` | 不同（两批讲稿） | 按题名推 |
| 0.4619 | `asia-minor-1896-DUPSCAN4` ⇄ `rc-peace-war-1910` | 不同（两部书） | 按题名推 |
| 0.3162 | `bacon-foster-1918` ⇄ `swothers-books-bacon-foster-corra-clara-barton…` | 同一部（同一作者同一书） | 按题名推 |
| 0.3016 | `epler-life-1915` ⇄ `sw-speeches…memorial-day-address` | 不同（传记 vs 讲词） | 按题名推 |

⇒ **11 对里 8 对是同一部作品、3 对不是。** 原记的「10 对不该声明」**方向反了**。

### ★★ 那一条改判，是读原文读出来的

`rc-history-1898-DUPSCAN2` ⇄ `rc-peace-war-1910`（0.8282）我**按文件名初判为「两部不同的书」**。
去翻扉页：`rc-history-1898-NLM` 与 `rc-peace-war-1910` **开头是同一幅肖像、同一行说明**
（`CLARA BARTON. From a portrait taken about 187x`）——
1898 年那部很可能就是 1910 年那部的前身（改题增订）。**改判为同一部。**

⇒ [[read-the-hits-before-reporting-the-rate]]：**按文件名判同异不算数。**
  表里凡标「按题名推」的都**没有读过原文**，那 6 条只是候选判读，不是结论。

### 这对 ㊵ 意味着什么

原记那半句若成立，624 对里大部分是噪声，㊵ 的规模会小很多。
**复核之后方向反过来**：Barton 这 222 对里多数是真重复
（同书的多次扫描、不同印次、校样、草稿↔印本）。
⇒ **㊵ 的规模没有缩小。** 但它仍然是「已判分、未发布的产物能不能改台账」这一个问题，
  **不是「已发布分数要不要重估」**——四人全部不在 registry。

### 补：另三人也抽了，四人合计 23 对样本

第一版只抽了 Barton（35.6%），是因为我把另三人的工作区路径写错了。
改用「含 `evidence/source-ledger.jsonl` 的那一层」定位，三人都取到了数。

| 人 | 未声明对 | 抽样里的形态 |
|---|---:|---|
| osler | 148 | `ppm-ed5-1902` ⇄ `ppm-ed7-1909`（同书第 5／第 7 版）、`chronic-chorea-1893` ⇄ `collected-reprints-s3`（论文与收它的合集） |
| virchow | 118 | `tr-cellpath-en-chance-1860` ⇄ `…-1860c`（同一英译的两份副本） |
| nightingale | 113 | `cook-life-vol2-1913` ⇄ `cook-life-vol2-GUT`（扫描件 vs Gutenberg 本）、`notes-on-nursing-labouring-1868` ⇄ `…-1898`（同书不同版） |

## 四、★★ 分档：**≥0.50 那半边可以整批处置，<0.50 才要逐对读**

23 对样本里有一条清楚的规律：**重叠 ≥0.50 的，无一例外是同一部作品**
（多次扫描 / 不同版次 / 论文与收录它的合集 / 同一译本的两份副本）；
**<0.50 才混杂**（Barton 的 `asia-minor-1896` ⇄ `rc-peace-war-1910`、
Osler 的 `chorea-1894` ⇄ `chronic-chorea-1893` 都是不同的作品，只是同一作者反复用同一批话）。

全库 624 对按重叠度分档（现算）：

| 重叠度 | 对数 | 占比 | 判读 |
|---|---:|---:|---|
| ≥0.90 | 45 | 7.2% | **几乎必是同一部** |
| 0.70–0.90 | 131 | 21.0% | **几乎必是同一部** |
| 0.50–0.70 | 128 | 20.5% | **几乎必是同一部** |
| 0.40–0.50 | 173 | 27.7% | 混杂，须逐对读 |
| 0.30–0.40 | 147 | 23.6% | 混杂，须逐对读 |
| **≥0.50 合计** | **304** | **48.7%** | 样本 11/11 全是同一部 |
| **<0.50 合计** | **320** | **51.3%** | 样本约一半不是 |

⇒ **给 ㊵ 的可操作建议**：真要清，先清 ≥0.50 那 **304 对**——
  它们几乎不需要逐对判断，`derived_from` 该指哪一份从版次／扫描来源就看得出来。
  剩下 320 对**必须逐对读原文**，成本完全不同，**不该混在一个数里报**。

★ 仍未做：23 对之外的 601 对**一对没读**。上面的分档是**从 23 对样本外推的**，
  不是逐对判定——[[samples-cannot-support-universal-claims]]。
  「≥0.50 必是同一部」这句话的证据强度是 **11/11**，不是 304/304。
它若成立，624 里真正该声明的只有个位数——**那这条根本不是欠账，而是判据偏松**。

⇒ 要么逐对读、要么承认没核。**本轮承认没核**，写在这里免得下一个人当成已定案。
  [[self-report-is-not-evidence]]

## 四、处置

- 数：**624，与记录一致，不必改**；
- 分布与红线：**并入 ㊵**（同 #102 的 9 个、#98 的存量）；
- 「10/11 不该声明」那半句：**待复核**，本轮未做。
