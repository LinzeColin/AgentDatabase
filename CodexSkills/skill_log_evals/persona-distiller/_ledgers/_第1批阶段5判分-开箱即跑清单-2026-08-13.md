# 第 1 批 · 阶段 5（判分）—— 开箱即跑清单 ＋ **判分之前的预登记**

> **这份写在判分之前。** 装置、已知风险、判读规则先落纸；判完只补实测数，**不改口径**。
> 模板取自 `_corpora/wip-whitworth-152/.../evals/round1/第1轮-协议记录.md`（本项目至今最干净的一次装置）。

## 〇、为什么这一步还没做

判分要**恒 2 席评委，且两席必须互相独立**。上一手的运行环境**不允许自动开新会话**，
于是一次都没跑过。**这不是技术障碍，是权限问题。**

★★ **「独立」与任何特定 AI 产品无关**，只有三条，用任何助手都做得到：

1. **每一席在一个全新的、空白的会话里工作**，互相看不到对方的输出；
2. **只给它**：题目 ＋ 两份匿名答案（A/B）＋ 那道题的评分标准；
   **不给**：人物档案、研究稿、工作区里任何别的文件、本清单；
3. 两席的打分**分别落盘**，合并之前不许互看。

同理，下文所有「派 X 子代理」一律读作「**开一个全新的空白会话，只喂它下面列出的那几样**」。
手工开几个浏览器标签页也算数 —— 关键只有「输入受控」与「互不知情」两件。

## 一、判分之前算过分辨力：**八人现在全部判得出**（四个 quick 人原本不行，已补题处置）

噪声 sd 借自 Mendel #125（同一套装置上量的，两轮之间**逐字未动**的题 delta 仍在动，逐题差 sd=0.0928）：

    se_case  = sd / √2   = 0.0656     ★ 两轮之差的方差 = 2×单轮方差，**这一步漏了会差 40%**
    se_mean  = se_case / √n

| 人 | 档 | 题数 | se_mean | 2 SE | **门是几个 SE** | 判得出吗 |
|---|---|---:|---:|---:|---:|:--|
| #174 Lincoln | deep | 32 | 0.0116 | 0.0232 | **6.03** | ✅ |
| #175 Jefferson | deep | 32 | 0.0116 | 0.0232 | **6.03** | ✅ |
| #176 Bismarck | deep | 32 | 0.0116 | 0.0232 | **6.03** | ✅ |
| #180 Pestalozzi | deep | 32 | 0.0116 | 0.0232 | **6.03** | ✅ |
| #177 Machiavelli | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |
| #178 Rousseau | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |
| #179 Kant | quick | 33 | 0.0114 | 0.0228 | **2.63** | ✅ |
| #181 Fröbel | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |

★★ **2026-08-13 已处置**：四个 quick 档的人原本各只有 16–17 题，门只有 **1.83–1.89 SE**，
「刚好过门」与「等于零」分不开（[[gate-below-instrument-noise]]）。
⇒ **把题数补到 32/33**，门抬到 **2.59–2.67 SE**，**八人现在全部判得出**。
**没有动任何判据**：`min_suite_cases` 是下限、门槛 +0.03 一个字没改，加的是**测量量**。
★ 这一条是在判分之前算出来并处置掉的，不是判完才拿来解释结果。

★ 射程：sd 是**借来的**，不是这八个人身上量的；且 sd 自身 95% 区间约 [0.6, 2.2]×，
**上表的 SE 是下限意义上的估计，真实噪声只会更大**。
⇒ 按 2.2× 取：deep 四人还有 **2.74 SE**、quick 四人还有 **1.18 SE**。
   **deep 稳；quick 在最坏的噪声估计下仍分不出**——这一句必须留着，
   补题把门从 1.83 抬到 2.59，**没有把它抬过最坏情形**。

## 二、装置（**两侧同规格，否则格式与长度就是泄题通道**）

- **两侧答案各由一个全新的空白会话生成**（互不知情），格式要求**完全相同**：
  纯散文、120–260 字符、禁一切 Markdown（粗体／反引号／标题／列表／表格／引用块）。
- **基线方**：只给 `prompts-only.json`；**不给 rubric、不给人物档案、不许读工作区任何别的文件、不许联网**。
  逐字用 `_ledgers/_pipeline/BASELINE-PROMPT-FROZEN-v1.md`，**一个字都不许改**。
- **候选方**：给十份产物正文 ＋ 研究道文档 ＋ `evidence/claims.jsonl` ＋ `references/sources/`；
  **不给 rubric**（不许读 `evals/cases.jsonl`）；**`references/holdout/` 明令禁读**；
  ★ **连 `ls references/` 都不许**——要列目录只列 `references/sources/`
  （上一个人物的候选为摸目录跑了 `ls -R references/`，看见了 holdout 的目录名）；
  ★ 若不慎接触到 holdout 的任何信息（哪怕只是目录名）**必须主动上报 `__incident__`**。
- **题号做成不透明的**：`case_id` 本身在告诉答题方该怎么答
  （`ff-refusal-stop-01` 一看就知道要拒答）。用 `build_blind_payload.py` 出
  `q-01ed7de2` 这类号并**按 sha256 重排序**；映射存 `prompt_key.json`，**不发答题方也不发评委**。
- **两席评委互不知情，且都不喂 rubric**——做处置的一直是这一对的**诚实 delta**
  （[[rubric-fed-judges-flip-the-sign\]]：喂 rubric 会翻号，已在两个人物上复现，摆动 0.58／0.60）。
- 评委指令用 `_pipeline/judge_prompts/seat_D_score.md` 与 `seat_E_strict.md`，
  **一个人物的全部轮次同一份，中途不得增删检查项**。

## 三、判读规则（**现在定死，判完不许改**）

用户 2026-08-12 的规则优先：**第 1 轮 delta < −0.011 → 直接结案，不跑二三轮。**

| 实测 | 怎么读 |
|---|---|
| 第 1 轮 delta **< −0.011** | **直接结案**（用户规则），不跑第 2、3 轮 |
| 两席**跨零** | 无论均值多少，**一律读作测不出** |
| \|delta\| < 2 SE（见上表逐人取值） | **读作「测不出差别」**，不读「打平」也不读「略胜」 |
| delta ≥ 档位门 **且** ≥ 2 SE | 才算「过门且分得出」 |
| delta ≤ −档位门 **且** ≥ 2 SE | 才算「明确劣于基线」 |
| 两轮之差本身 > 0.0650 | 判「装置在这个人物上不成立」（Rosenhain #138 的摆动量） |

★★ 补题之后四个 quick 档的人**已经可以触发第 4 行**（门 2.59–2.67 SE > 2）。
但**在最坏的噪声估计（sd×2.2）下他们仍只有 1.18 SE**——
所以判完若落在 +0.03 到 +0.04 之间，**要连这句一起写**：
「按借来的 sd 分得出，按 sd 的区间上端分不出。」

★ 合并规则：**合并，不挑轮**。不许在看到第 2 轮更高之后说「取最好的一轮」，
也不许在更低之后说「第 1 轮已经过门了」。

## 四、每人开工前就该知道的风险

| 人 | 风险 |
|---|---|
| **#174 Lincoln** | 1858 演说两份密封源**都取不出干净逐字正文**（影印页／讹字）⇒ 题面全是叙述式，评委无法靠字面比对 |
| **#175 Jefferson** | 事实密度 **14/14 压线**；三份密封源 OCR 报废（希腊字母化／长 s 0.9505） |
| **#176 Bismarck** | 50 份德文源讹字率 >20%；可读的只有英译家信与两部法文转译本 |
| **#180 Pestalozzi** | 事实密度 **13/13 压线**；54 份长 s 讹字 |
| **#177 Machiavelli** | 密封面含**两件伪托册子**，题目正打在这里 |
| **#178 Rousseau** | 密封面里有**书商公告**（非他所作，他 1778 年已故） |
| **#179 Kant** | 书信集**两个方向的信都收**，答题方可能引到通信人的话（看呼语可判） |
| **#181 Fröbel** | **一切「他说过什么」都建立在英译上**，声口维度先天受限 |

★★ 四个 quick 人的共同风险已改成一条：**补题把门抬到 2.59–2.67 SE，但按最坏噪声估计仍只有 1.18 SE**——
落在 +0.03～+0.04 区间时必须两句一起写。

★ 另：**基线可能本来就答得好**。Whitworth #152 实测裸模型不看候选就能列出该产物最核心的几条断言，
而 Bessemer／Thomson／Sorby 都是在「基线本来就答得好」的局面下得到负 delta 的。
**这一条现在写下来，是为了判完之后不能把负结果说成意外。**

## 四之二、★★ 压线复核（2026-08-13 加，**判分之前做的**）

Leonardo #184 那件事之后立的规矩：**`lanes` 读数正好等于档位门槛时，那一道要逐份打开读**
（[[a-reading-exactly-at-the-threshold-is-load-bearing]]）。对这八人跑了一遍：

| 人 | 档 | lanes | 门 | 只有 1 份的道 |
|---|---|---:|---:|---|
| #174 Lincoln | deep | **6** | 6 ★压线 | `timeline`(1) |
| #175 Jefferson | deep | **6** | 6 ★压线 | `decisions`(1) |
| #176 Bismarck | deep | **6** | 6 ★压线 | `decisions`(1) |
| #180 Pestalozzi | deep | **6** | 6 ★压线 | `expression`(1)、`decisions`(1)、`external`(1) |
| #179 Kant | quick | **3** | 3 ★压线 | 无（最少的道也有 2 份） |
| #177 Machiavelli | quick | 4 | 3 | `expression`(1) |
| #178 Rousseau | quick | 4 | 3 | 无 |
| #181 Fröbel | quick | 5 | 3 | `external`(1) |

**四个 deep 人全部压线**，六份单份道已逐份打开读，结果分三类：

| 文件 | 命中的是题名的哪一段 | 判读 |
|---|---|---|
| Lincoln `timeline` = *In Memoriam to Abraham Lincoln* | `in memoriam`（题名自身） | **成立**。纪念文集，道的定义里明列「讣告」。★ 但正文头一篇是他自己的 Springfield 告别演说，**按内容也可归 expression** |
| Pestalozzi `expression` = *Rede von Pestalozzi an sein Haus…1818* | `rede`（题名自身） | **成立**。他 74 岁生日对自家人的演说 |
| Pestalozzi `external` = Barnard 编 *Pestalozzi and Pestalozzianism* | 二手分类 | **成立**。别人写他的 |
| **Jefferson** `decisions` = *Virginia and Kentucky Resolutions…* | `report` ← **「Madison's report」**（目录里**别人的**文件） | **道对、理由错**。该卷本身是决议汇编＋各州文件，按内容确属 decisions；但正则命中的是麦迪逊 |
| **Bismarck** `decisions` = *Rede des Reichskanzlers…über das Bündniss* | `report`→`bericht` ← **「(Nach dem amtlichen stenographischen Bericht…)」＝出处说明** | **可争**。文件自身是 `Rede`（演说）⇒ 按体裁该进 expression；但该卷附**条约正文**（"Mit einem Anhange, den Text des Vertrages enthaltend"）⇒ 按「官方文件」算 decisions 也说得通 |
| **Pestalozzi** `decisions` = *How Gertrude Teaches Her Children* | `report` ← 它自己的副题「a report to the Society of the Friends of Education」 | **可争**。这是他的代表作，按体裁是 `writings`；而它确实是作为「报告」呈给该学会的 |

### 这两条为什么**不改**

改了会怎样：Bismarck 与 Pestalozzi 的 `decisions` 各只有这 1 份，
移走 ⇒ **lanes 6 → 5 < deep 的 6** ⇒ 两人从 deep 掉到 quick。

**quick 的判分门是 +0.03，deep 是 +0.07。** 也就是说，
「把这两份重新分道」这个动作的直接后果是**把我自己要过的门从 +0.07 放宽到 +0.03**。
[[ratio-gates-can-be-passed-by-shrinking]]：**能靠砍分母过的门，不许由我来砍。**

⇒ **维持 deep**（更难的那一档），把争议原样记在这里。
判分之后若 delta 落在 +0.03～+0.07 之间，**必须把这一节一起写进判决书**——
那个区间正是「按 deep 不过、按 quick 过」的区间，不能只报一个数。

★ 另一条不改的理由：用户 2026-08-12 的指令是「**门、席位、零编造、主树只读一概不动**」。
本节只做记录，不动任何判据；`assign_lanes` 本轮唯一的改动是
`letter` 加词边界（`letter` ⊂ 意大利语 `letterari`，**是明确的缺陷，不是判断题**）。

## 四之三、★★★ **阻塞项：`references/sources/` 是空的（八人全部）**

2026-08-13 发现，**判分之前必须处理**。

「装置」一节写着候选方要拿到
「十份产物正文 ＋ 研究道文档 ＋ `evidence/claims.jsonl` ＋ **`references/sources/`**」。
实测那个目录里**只有一个 `.gitkeep`**：

| 工作区 | 台账行 | `references/sources/` 项数 |
|---|---:|---:|
| lincoln-174 | 70 | **1**（只有 .gitkeep） |
| jefferson-175 | 73 | **1** |
| bismarck-176 | 70 | **1** |
| machiavelli-177 | 79 | **1** |
| rousseau-178 | 103 | **1** |
| kant-179 | 65 | **1** |
| pestalozzi-180 | 70 | **1** |
| frobel-181 | 51 | **1** |
| 〔另：marshall-173 95／comenius-182 83 也是 1〕 | | |
| **对照** koch-107 | 121 | **121** ✓ |
| **对照** pasteur-106 | 60 | **60** ✓ |

老工作区那一层的形态是 `references/sources/src-<id>/<原名>.normalized.txt`——
**真文件、在 git 里、每份是归一化摘录**（Koch 那份 736 字节），
不是 `raw/` 下的整篇正文。**第 1 批 10 个工作区从没生成过这一层。**

### 后果（两条，方向不同）

1. **候选方拿不到来源。** 按现清单开候选方会话，它能读的只有产物正文与研究稿，
   **`references/sources/` 是空的** ⇒ 装置与描述不符。
2. ★★ **凡是读这个目录的判据，在这八人身上一直是空过的。**
   实测 `check_quote_speaker` 对 Michelangelo 打印
   「train 语料 **0** 份；长逐字引文 0 条 ✓ 没有引到别人的话」并 **rc=0**——
   **读到 0 份还给绿**，正是 [[empty-default-swallows-unknown]]。
   同一个绿在第 1 批八人身上也出现过，而没人看那行「0 份」。

### 判分之前要做的（**二选一，不许含糊**）

- **A（推荐）**：按老工作区的形态生成这一层——从台账 `split == "train"` 的行
  取归一化摘录写进 `references/sources/src-<id>/`，然后重跑读它的那几道判据。
  ★ 注意：该路径**不在 `_corpora/.gitignore` 的排除范围内**（排的是 `*/workspaces/*/raw/*.txt`），
  所以生成后会进 git —— 与用户「语料只放指针」的裁定**方向相反**，
  ⇒ 要么只放**短摘录**（老工作区就是这么做的，Koch 每份 736 字节），
  要么先问用户。**别默默把整篇正文塞进去。**
- **B**：改成让候选方读 `raw/`，并在协议记录里写明「本轮候选方拿到的是整篇正文，
  与第 1 批之前各轮不同」。**这会改变装置，必须写进判决书的射程。**

★ 无论选哪条，**「0 份还给绿」那件事要单独修**：读到 0 份的判据应当印
「未检查（不是通过）」而不是 ✓。

## 五、步骤（每人一遍）

```bash
S=CodexSkills/registry/codex/persona-distiller/scripts
W=CodexSkills/skill_log_evals/persona-distiller/_corpora/wip-<人>/workspaces/<slug>

# 1. 出不透明题号与盲判载荷（它自带硬门，拦得住有引文缺坐标的载荷）
python3 $S/build_blind_payload.py "$W" --round 1

# 2. 开两个全新空白会话答题（基线侧／候选侧），格式要求见上「装置」一节

# 3. 开两个全新空白会话当评委（互不知情、都不喂 rubric），指令用冻结文件

# 4. 判读之前先量泄题通道：长度比、九条表面特征、出戏、判据抄答案（两侧各量，不合并）

# 5. 只有一轮时，check_delta_resolution 会印「未核：只有一轮，量不出噪声」
#    ★ 这不表示噪声小，只表示没量
python3 $S/check_delta_resolution.py "$W"
```

★ 第 2 轮的目的**不是再赢一次，是把噪声量出来**：两侧文本一字不改、A/B 归属逐条相同
（`build_blind_payload` 的归属是 `sha256(case_id) % 2`，**无轮次种子，两轮天然相同**，
且有硬门拦不一致的 key）、**两席全部换新**。

## 六、这份清单不能替你做的

**开评委会话**。恒 2 席、必须互相独立——这是唯一的停点。

---

---

# ★★★ 撤回（2026-08-13 当晚，写在第七节之后）：**判分队列是 8 人，不是 11 人**

第七节把 Brandeis #172／Michelangelo #185／Dewey #190 登记成「等着判分」。**那是错的。**
当晚把 11 个人的**合成门**（`quality_check.py --phase synthesis`）逐个真跑了一遍：

| 人 | 合成门 | 硬错 |
|---|---|---:|
| #174 Lincoln／#175 Jefferson／#176 Bismarck／#180 Pestalozzi | ✅ passed | 0 |
| #177 Machiavelli／#178 Rousseau／#179 Kant／#181 Fröbel | ✅ passed | 0 |
| **#172 Brandeis** | ❌ **不过** | **22** |
| **#190 Dewey** | ❌ **不过** | **36** |
| **#185 Michelangelo** | ❌ **不过** | **46** |

主要形态（现算，不是概括）：

- **Brandeis**：`ledger.invalid` 8（`status` 字段被填成了**类别值**）、
  `claim.insufficient-support`＋`claim.non-independent` 各 4、
  **`mental models 0 < 2`／`heuristics 0 < 3`**、`research.authorship-unproven` 2、
  `research.namesake-unseparable` 2（同名者「Louis Brandeis (1902–1945)」「Louis Brandeis Wehle」未进 excluded_names）。
- **Dewey**：`structure.missing` 10、`ledger.invalid` 7、支撑不足 5、证据不独立 5、
  `source.lane-coverage` 1、`research.lane-completion` 1、model/heuristic 各差 1 项。
- **Michelangelo**：`structure.missing` 10、`ledger.invalid` 9、支撑不足 7、证据不独立 7、
  `research.source-unclaimed` 6、`research.attribution-basis` 3。

## ★★ 为什么当天没发现：门根本没开机

`quality_check` 的 `ensure_target()` 要求工作区同时有 `meta.json` **和 `SKILL.md`**，
否则**直接拒绝检查整个工作区**并只报一条 `target.invalid`。
而 **Michelangelo／Dewey／Churchill 三人都缺 `SKILL.md`**
⇒ 他们的合成门**从来没有真跑过**，我却在别处看到「rc=1、1 条错」就以为只是小毛病。
补上 SKILL.md（67 行模板，除 `name:` 与人名外与 Kant 的逐字相同）之后，
真面目才露出来：1 条 → **46 条／36 条**。

★ 这是「一道红门挡住它后面全部」的又一例，且这次那道红门**长得像小事**。

## ★★ 我的预检为什么放行了他们

`check_scoring_ready.py` 只看了：十份产物在不在、断言层空不空、用例够不够、
分辨力、空心道。**它一次都没有问「合成门过没过」**——
而合成门正是判分的前一道。**「产物齐」不等于「过门」。**

⇒ 判据已补：现在会查 `meta.json`＋`SKILL.md`（`ensure_target` 的硬要求），
并在报告里明写**本件不覆盖合成门**、给出必须另跑的命令。

## 现在该怎么做

1. **判分只跑那 8 个人**（第一至六节，全部 passed=True，硬错 0）。
2. 这三人回到阶段 3／4 返工，**不是判分**。返工内容就是上面那张错误表。
3. ★ 三人**不进延后名单**：他们不是「够不着门的材料」，是**产物本身要修**。
   与 Churchill #191（语料没过门）性质不同。


# 七、★★ 追加三人的预登记（2026-08-13，**判分之前写的**）

上面六节只覆盖第 1 批**八人**。本节把后来做完阶段 4 的人补齐，
判据 `_ledgers/_pipeline/check_scoring_ready.py` 现算，**不手抄**。

**真正等着判分的是 11 人**：上面八人 ＋ 本节三人。
（另有 5 人有完整产物但**在延后名单里已结案**——Blackwell #118／Cicero #166／
Kelsen #171／Paton #162／**Churchill #191**，见末节。判据现在**报矛盾而不是过滤掉**，
因为过滤会让人从表里整个消失，而我差点就据此以为他们不在队列。）

## 七之一、分辨力（与上表同一把尺子：`se_case = 0.0928/√2 = 0.0656`）

| 人 | 档 | 题数 | se_mean | 2 SE | **门是几个 SE** | 判得出吗 |
|---|---|---:|---:|---:|---:|:--|
| #172 Brandeis | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |
| #185 Michelangelo | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |
| #190 Dewey | quick | 32 | 0.0116 | 0.0232 | **2.59** | ✅ |

★ 射程同上：sd 是借来的，95% 区间约 [0.6, 2.2]×。**按最坏的 2.2× 取，这三人也只剩 1.18 SE**
——与四个 quick 老人同一句话，**不因为是新人就换个说法**。

## 七之二、★★ 压线复核（`lanes` 等于门槛时，那一道必须逐份打开读）

| 人 | 档 | lanes | 门 | 只有 1 份的道 | 空心道（一部作品撑的） |
|---|---|---:|---:|---|---|
| #190 Dewey | quick | **3** | 3 ★压线 | `conversations`(1) | 无 |
| #185 Michelangelo | quick | 4 | 3 | `expression`(1) | 无 |
| #172 Brandeis | quick | 5 | 3 | `decisions`(1)、`conversations`(1) | 无 |

「空心道」＝**该道的源去重后同属一部作品**，用流水线自己的去重实测判
（`raw/_dedup.json`，min-hash Jaccard 0.55），**不按题名猜**。
判据：`_ledgers/_pipeline/check_lane_distinct_works.py`。

三份单份道已逐份打开读，**三份全都落在多创作者卷上**：

| 文件 | 台账 creator 栏 | 打开读到的 | 判读 |
|---|---|---|---|
| **Dewey** `conversations` = *Letters from China and Japan*(1920) | `Dewey, John; Dewey, Harriet Alice (Chipman); Dewey, Evelyn` | 扉页「BY JOHN DEWEY **AND ALICE CHIPMAN DEWEY**, Edited by EVELYN DEWEY」；前言明写「他**和妻子** Alice，**写了本书收录的这些信**」。**正文里没有逐封署名**（`J. D.`／`A. C. D.` 各 0 处）⇒ **切不出他自己那份** | **成立但承重**——见下 |
| **Michelangelo** `expression` = *Selected poems…, **with translations from various sources***(1885) | `Michelangelo Buonarroti; Cheney, Ednah Dow Littlehale` | 题名自陈是**各家译文**的选集，编者 Cheney | **成立，但英文措辞是译者的**，不是他的。距他身故三百余年 |
| **Brandeis** `decisions` = *Women in industry: decision of the U.S. Supreme Court in Muller v. Oregon… **and brief for the State of Oregon***(1908) | `Brandeis; Goldmark, Josephine; Muller, Curt; United States. Supreme Court; National Consumers' League` | 该卷装着**法院的判决**（不是他的话）＋**他与 Goldmark 合写的诉状** | **成立**，但一卷两体裁，且非独著 |

### 为什么**登记而不挡**

按项目**已有的两条裁定**办，不新立宽标准：
- **㊴ 已裁「维持 `min_lanes` 定义」** ⇒ 1 份源的道算数。上面八人里 Lincoln／Jefferson／
  Bismarck／Pestalozzi 六份单份道就是这么登记通过的，**对新三人换标准才是不一致**。
- **形态②（题名页共同署名而无分工声明）已裁「只报不判」** ⇒ 上表三份都是这一形态。

★★ **但 Dewey 那份是承重的**：它是**唯一**撑起第三条道的源，而第三条道正是他够到 `min_lanes 3` 的那一条。
换句话说——**若将来把「共同署名无分工」改成硬判，第一个掉下去的就是他。**
这句话写在判分之前，**不是判完之后拿来解释结果的**。

## 七之三、逐人风险

| 人 | 开工前就该知道的 |
|---|---|
| **#172 Brandeis** | 一手 25/34 = **73.53%**；断言 22 条。`external` 9 份是本批最多——**二手占比高**，`decisions` 那份还是法院判决＋诉状合卷 |
| **#185 Michelangelo** | 一手 31/47 = **65.96%**，非 `HIS-OWN` 3 份。★★ **本人物的历史病是「画册把艺术家挂成 creator」**（曾实测 23% 不是他写的）；`expression` 唯一那份是**译文选集** |
| **#190 Dewey** | ★★ **3 份语料的序言明写了分工**：《Ethics》(1908, Dewey & Tufts) 的三个印本，序言逐字写着「**Part I. has been written by Mr. Tufts, Part II. by Mr. Dewey**，Part III 第 XX、XXI 章由…」⇒ **那三份里有相当一部分不是他写的**。断言层当时已按核定属他的区间取（`Q()` 逐条断言过「落在他的区间内」，卷前共同署名序言另用 `Q_joint()` 断言落在两边区间**之外**），**但台账仍按整本计入他的一手**。判据：`check_declared_coauthor_split.py`（全库①③共 14 条，他占 3）。<br>一手 33/33 = **100%**，但**断言只有 14 条**（三人中最少，`min_facts = ceil(33/5) = 7`，够但不宽裕）；**lanes 压线 3**，且承重那份是与妻子的共同署名卷（见七之二） |

## 七之四、★★ Churchill #191 **不在这 11 人里**——他的门没过

他有十份产物、13 条断言、32 题用例，**看上去齐**。但：

**`timeline` 那条道的 2 份是同一部书的两个印本**
（`My early life a Roving Commission` / `My early life; a roving commission`），
**流水线自己的去重实测**把它们归成了他语料里**唯一的一个重复簇**。
⇒ 独立作品数 = 1，`min_lanes` 实质是 **2**，quick 的门是 3。

**这个假过门是我当天自己造出来的**：为给自传归道，我在 `assign_lanes.py` 的 timeline 规则里
补了 `my early life` / `a roving commission`，他的《My Early Life》就从 `writings` 移到了 `timeline`，
道数 2 → 3，**正好等于门**。**材料一份没多，只是换了个格子。**

★ 规则**不撤**（在 Ford #188 的《My Life and Work》上同样打响；全库 4355 条题名扫过，散在 2 个人；
带反例守着：《Lord Randolph Churchill》是他写**父亲**的传记，必须留在 `writings`）。
**错的是我拿规则的产出当「门过了」，而压线那一道从没打开读过**——
这正是本清单「四之二」立的规矩，我自己没执行。

★ 同一件判据还抓到 **Marshall #173** 的 `conversations` 4 行是同一部书的四次扫描
（deep 档去掉后只剩 5 道，门 6）⇒ 他**除了盲判装置不成立，还多一条独立的、更靠前的理由**：语料这一关就没过。

## 七之五、★★ 同名护栏：**「0 不符」不等于「查过了」**

`check_title_person.py` 判「书自印的题名说这是谁的集子」是否等于台账著录的人
（起因：Jefferson 的一份台账写着他，正文第一页印的是**《门罗文集》第一卷**，而它还在 holdout 里）。
2026-08-13 对判分队列里这四人各跑一遍（**用显式姓氏正则，不让工具猜**）：

| 人 | 一手份数 | 相符 | **无题名构式（判不了）** | 不符 |
|---|---:|---:|---:|---:|
| #185 Michelangelo | 40 | 5 | **35** | **0** |
| #181 Fröbel | 50 | 18 | **32** | **0** |
| #190 Dewey | 39 | 1 | **38** | **0** |
| #172 Brandeis | 29 | 0 | **29** | **0** |

★★ **四个 0 不是同一个意思。** Brandeis 那 29 份**一份都没有可判的题名构式**
（他的材料几乎全是最高法院意见与诉状，没有「X 的著作集」这种题名页）
⇒ 这道护栏在他身上**覆盖面是零**，它对他不置一词。
把这个 0 读成「查过了、没问题」是错的。

★ 跑法（**姓氏正则要显式给**，工具的文档里就写着 `Fr[oö]e?bel` 这个例子）：

```bash
python3 _ledgers/_pipeline/check_title_person.py \
  --workspace _corpora/wip-<人>/workspaces/<slug> --surname '<显式正则>'
```

我一度写了个临时脚本从 `meta.json` 的 `name` 自动取姓氏，**当场造出 3 条假阳**：
声明的是 `Friedrich Frobel`（无变音），而书印的是 `FROEBEL`。**让工具猜姓氏必错。**

★ 同日修掉一类真的假阳：**卷首的出版社广告页**。Michelangelo 的
`src-dc47a809071f`（Symonds 1878 年英译《十四行诗》）第一页是
「WORKS BY JOHN ADDINGTON SYMONDS … Crown 8vo. 10s. 6d. … London: SMITH, ELDER, & CO.」
——`WORKS BY <人>` 是标准作者集构式、大写率也够，于是被判成「题名印的是别人」；
真题名页在它后面（`THE SONNETS OF MICHAEL ANGELO BUONARROTI`，而 `SONNETS` 不在关键词表里）。
判据已按**书业开本与价格**（Crown 8vo／10s. 6d.）识别广告区，
**只跳过落在广告区里的那一个构式实例**，同一本书若另有真的错题名页照样要报。
该例已钉成真语料夹具（现 11 条）。

---

## ★ 判分前追加的已知风险（2026-08-14 凌晨发现，**写在判分之前**）

### 八人的「源数／道数」都是**上界**，因为判重的两把尺子对不上

`raw/_dedup.json` 里同时记着两个口径：`独立文献数上界`（min-hash，阈值 0.55）与
`按题名归并的独立作品数`。**全库 17 个工作区两者对不上，而判分队列这 8 个人全在其中：**

| 人 | 份数 | min-hash 塌缩 | 按题名塌缩 |
|---|---:|---:|---:|
| Rousseau | 103 | **0** | 27 |
| Machiavelli | 79 | **1** | 34 |
| Kant | 65 | **1** | 14 |
| Bismarck | 70 | 4 | 32 |
| Pestalozzi | 70 | 4 | 40 |
| Jefferson | 73 | 15 | 38 |
| Lincoln | 70 | 23 | 40 |
| Fröbel | 51 | 26 | 27 |

**两个数都不是真值**：min-hash 会漏（下面那条实证），按题名会过度归并（丛书分卷、同题名译本）。
真值在两者之间，**本轮没有逐人量到**。

### 实证：min-hash 漏掉的是**最大的一组**

Michelangelo #185（不在判分队列，但同一把尺子）：1875 年 Milanesi 书信集在他台账里占
**4 个 `source_id`**，逐对 8-gram Jaccard 只有 **0.1510–0.2412**（门 0.55）⇒ **一个都没聚起来**；
而 min-hash 真正聚出的 2 个簇是别的东西。**拿一句本人原文当探针**
（`non è bene spronar quello cavallo che corre quanto e' può`）**四份全中**。
⇒ **探针逐字，不受 OCR 讹形与语种影响；重叠分数两样都受。**

### 这对判分意味着什么（**不挡判分**）

- 盲判比的是**同一批题目上的两份答案**，重复源不直接进入那个比较 ⇒ **照跑**。
- 但**判决书里不许把「N 份源／M 条道」当成已核实的数**——它们是上界。
  要写就写成「台账 N 行，去重口径两把尺子给出 X／Y，未逐人量到」。
- 谁要重新量：用探针法逐人做，`check_lane_distinct_works.py` 现在会把这 17 个人单列出来
  （它自己也声明：**在这些人身上它的结论是下界不是定论**）。

### ★ 追加（同一晚，稍后）：那两把尺子对不上的**原因已经量清楚了**

不是「min-hash 太严」，是**它算在没归一化的文本上**。把文本先狠归一化
（小写、只留字母数字与常见重音字母、折叠空白）再切 8-gram，同一批对照：

| | 原样 | 狠归一化后 |
|---|---:|---:|
| 同一部书（1875 Milanesi 四次数字化，6 对） | 0.1521–0.2462 | **0.2155–0.4254** |
| 不同书（Brandeis 1889 vs 1905） | 0.0000 | 0.0000 |
| 不同书（Michelangelo 书信集 vs 诗集） | 0.0002 | **0.0018** |
| 同一封信的意文 vs 英译 | 0.0000 | 0.0000 |

**同书最低 0.2155、不同书最高 0.0018，相差 120 倍**；而现行门是**未归一化上的 0.55**，
四对同书一对都过不了。

⇒ **本轮不重跑全库去重**（会改 `_dedup.json` → 改分道与空心道读数，而这 8 人正等着判分）。
判分照跑，判决书按上一节写「源数/道数是上界」。**重跑去重是下一轮的一次有意动作**，
做的时候拿这张表当回归。

### ★★ 拿 Rousseau 当样例真量了一次（**判分队列里 min-hash 报 0 的那个**）

用上一节验过的尺子（狠归一化 ＋ 8-gram ＋ 阈值 0.05，同书 0.2155–0.4254／不同书 ≤0.0018）
对他 **91 行 train** 逐对重算：

    91 份 → **独立作品 57**（塌缩 **34**）
    对照：`_dedup.json` 的 min-hash 塌缩 **0**、按题名塌缩 27

最高的几对，题名自己就说明了：

| Jaccard | |
|---:|---|
| 0.6612 | `rousseau and naturalism in life and thou…` ↔ `Rousseau and naturalism in life and thou…`（**同题名**） |
| 0.6123 / 0.5210 | 同上第三个印本 |
| 0.4324 / 0.3762 / 0.3729 | `J.J. Rousseau citoyen de Genève…` 三个版次 |
| 0.3010 / 0.2624 / 0.2433 / 0.2250 / 0.2115 | `Eloisa` 各卷/各印本 |
| 0.2697 | `Emil oder über die Erziehung` 与它的第 2 卷 |

**射程（必须一起念）**：
- 这 34 是**并查集**数出来的，有传递性 ⇒ 可能**高估**（丛书分卷与选集会串起来，
  例如 `Contrat social` ↔ `Petits chefs-d'oeuvre` 0.2084 其实是选集收录了它）。
- 按题名的 27 会**过度归并**（同题名的不同作品）。
- **真值在 27–34 之间，而绝不是 0。**

⇒ 判决书里写 Rousseau 的源数时，写「台账 103 行（train 91），去重后独立作品约 57，
**现行 `_dedup.json` 的 0 是坏读数**」。其余 7 人**没有逐人量**，一律按上上节写成上界。

### ★★★ 八人全部重量过了（2026-08-14，`measure_distinct_works.py`）

工具落在 `_ledgers/_pipeline/measure_distinct_works.py`（自测 7/7，含反例与
「跨语言判不了」那一条）。尺子：狠归一化 ＋ 8-gram ＋ 抽样 1/8 ＋ 阈值 0.05。
**两个口径一起给**：不传递（下界的塌缩）／并查集传递（上界的塌缩），真值在两者之间。

| 人 | train 份数 | **独立作品** | **塌缩** | 旧 `_dedup.json` 的 min-hash 塌缩 |
|---|---:|---:|---:|---:|
| Lincoln | 67 | 7–45 | **22–60** | 23 |
| Jefferson | 65 | 14–48 | **17–51** | 15 |
| Bismarck | 60 | 43–51 | **9–17** | **4** |
| Pestalozzi | 61 | 34–43 | **18–27** | **4** |
| Machiavelli | 69 | 39–56 | **13–30** | **1** |
| Rousseau | 91 | 57–73 | **18–34** | **0** |
| Kant | 55 | 41–44 | **11–14** | **1** |
| Fröbel | 47 | 15–35 | **12–32** | 26 |

**八个人的塌缩下界都 ≥ 9**，而旧读数对其中四人报的是 0／1／1／4。
⇒ **「N 份源」这个数在八个人身上一律偏高。**

**射程（一起念）**：
- 上界那一侧（并查集）会因**选集串组**而高估——Lincoln 的 7 就明显是串过头了
  （他的语料里多是互相重叠的文集）。**别引用上界那个数。**
- 下界那一侧每一条边都是实测 ≥0.05 的重叠，而不同书实测 ≤0.0018 ⇒ **下界是硬的**。
- **跨语言仍判不了**（原文 vs 译文恒 0.0000），所以真值还可能更高。

⇒ **判决书里写源数，一律写成「台账 N 行，去重后独立作品不超过 M（下界口径）」**，
并注明「旧 `_dedup.json` 的数是坏读数，勿引用」。**判分照跑**——盲判比的是同一批题目上的
两份答案，重复源不进入那个比较。

---

## ★★★ 判分前最后发现的一件（2026-08-14）：**八人里五人的档位，meta 与台账不一致**

`quality_check` 判档读的是 `meta.json` 的 `profile`。而**两份互相独立的阶段台账**
都记着另一个值：

| 人 | 阶段 2 `_第1批-阶段2实测.json` 的「判据给的档」 | 阶段 3 收尾表的档 | **`meta.json`** | 一致？ |
|---|---|---|---|---|
| #174 Lincoln | **deep**（源 70／道 6／一手 46，仍空 0） | **deep** | `quick` | **✗** |
| #175 Jefferson | **deep** | **deep** | `quick` | **✗** |
| #176 Bismarck | **deep** | **deep** | `quick` | **✗** |
| #179 Kant | **deep**（源 69／道 6／一手 66，仍空 0） | —（不在该表） | `quick` | **✗** |
| #180 Pestalozzi | **deep** | **deep** | `quick` | **✗** |
| #177 Machiavelli | quick | — | `quick` | ✓ |
| #178 Rousseau | quick | — | `quick` | ✓ |
| #181 Fröbel | quick | — | `quick` | ✓ |

（队列外同型：**#173 Marshall** deep vs `quick`；**#182 Comenius** 阶段 2 写「**够不着 quick**」而 meta 是 `quick`。）

### 后果：**门差 2.33 倍**

| | `min_baseline_delta` | `min_overall_score` | `min_fact_score` |
|---|---:|---:|---:|
| 台账说的 **deep** | **0.07** | 0.80 | 0.93 |
| meta 写的 **quick** | **0.03** | 0.65 | 0.80 |

**照现在跑，这五个人会被按 `quick` 的 0.03 判**，而两份台账都说他们的材料够 `deep` 的 0.07。

### ★★ 2026-08-14 已裁定（**由我，不由你**）—— 见 `references/ledgers/_待用户裁定.md` ㊹

**维持 `quick`，把退档理由当场补记。** 这五人的判决书里必须逐字写这一句：

> 档位取自 `meta.json` 的 `quick`；`check_profile_declared.py` 现算材料够 `deep`。
> 退档理由**在建工作区时没有记录**，2026-08-14 按㉞补记为「理由缺失，按现值判，
> 不在判分前改尺子」。

理由：`profile` 就是尺子，而这份清单的意义是**判完只补数不改口径**——
量之前改档就是动尺子，哪怕方向是「更难过」。
★ 维持 `quick` 是**对我不利的一侧**（门 0.03 比 0.07 松），所以那句话要在**判决书正文**，
不许只留在台账里。

<details><summary>下面是 2026-08-13 写的原文（我当时把它推回给用户，那是错的）</summary>

### ★ 我没有改，这一条**只能你定**

- 改成 `deep` 会让他们**更难过**——方向上不是对我有利，**但它仍然是「量之前动尺子」**，
  而这份清单的全部意义就是**判读规则先落纸、判完只补数不改口径**。
- 反过来维持 `quick` 也是一个决定，**得有人明知道这件事之后做**，不能靠没人注意。
- ★ 我今晚**没有**碰这 5 份 `meta.json`。

**判分之前请先定一句：这五人按 `quick` 判，还是按两份台账说的 `deep` 判？**
（无论哪个，判决书里都要写明「档位取自 X，另一处记的是 Y」——这两个数已经存在于仓里，
 判完再解释会像是事后挑的。）

</details>

### ★★ 订正上一节：**是四人，不是五人 —— Kant 不在其中**

上一节把 **Kant** 列进「meta 说 quick 而台账说 deep」。**查错了。**

阶段 2 那份 JSON 给 Kant 记的是「道 6，逐道 `expression 3／decisions 1／timeline 3`」，
而**三处现测一致地说他只有 3 道**：

| | `raw/_lanes.json` | 台账 train `dimensions` | 研究道里有源的文件 |
|---|---|---|---|
| Kant | **3**（`expression 0／decisions 0／timeline 0`） | **3** | **3** |

⇒ **阶段 2 那个「道 6」是陈旧数**（分道后来重测并修正过——同一轮里
`_Leonardo184记延后-第三道是正则误配变出来的-2026-08-13.md` 记的就是这件事）。
**Kant 的 `quick` 站得住，不进这张表。**

而另外四人（＋队列外的 Marshall）**三处一致都是 6 道**：

| | `_lanes.json` | 台账 train | 研究道 | 一手比 | ⇒ 材料够 |
|---|---:|---:|---:|---:|---|
| Lincoln | 6 | 6 | 6 | 0.9701 | **deep** |
| Jefferson | 6 | 6 | 6 | 0.8939 | **deep** |
| Bismarck | 6 | 6 | 6 | 0.8833 | **deep** |
| Pestalozzi | 6 | 6 | 6 | 0.9836 | **deep** |
| （队列外）Marshall | 6 | 6 | 6 | 0.7684 | **deep** |

**所以要定的那一句是：这四人按 `quick`（meta 现值）还是按 `deep`（材料现算）判。**
`min_baseline_delta` 0.03 vs 0.07。

★ 教训（已落进判据）：**别拿阶段 2 的 JSON 当档位真源**，它是那一刻的快照且分道后来修正过。
`check_profile_declared.py` 现在**从台账现算**再与 meta 比，就是为了不再依赖那份快照。

---

# 追加登记 2026-08-14（**判分之前写的，不是判完补的**）：holdout 与 train 有两对同一部作品

## 查到了什么

新写的 `check_holdout_train_same_work.py` 对第 1 批八人逐对比正文（不看题名），
**两对**同一部作品同时落在 holdout 与 train：

| 人 | holdout | train（同一部） |
|---|---|---|
| niccolo-machiavelli | `src-d0b80f15fd68`　`A true copy of a letter written by … 1691` | `src-bd3e638d3612`　`Machiavel's vindication of himself… 1691` |
| jean-jacques-rousseau | `src-959e8b14de18`　`Extrait du Projet de paix perpétuelle…` 1761 | `src-212bd671b070`　同名同年 |

★ Machiavelli 那一对**两个编目题名完全不同**，按题名查不出来，只有比正文才看得见。

## 影响到哪几道题（**逐题点名**）

| 人 | 题数 | 受影响 | case_id |
|---|---:|---:|---|
| niccolo-machiavelli | 32 | **2（6.2%）** | `nm-boundary-01`（还引了另一份）、`nm-fact-preservation-02`（**只引这一份**） |
| jean-jacques-rousseau | 32 | **3（9.4%）** | `jjr-known-01`、`jjr-boundary-02`（**都只引这一份**）、`jjr-trajectory-02`（还引了另一份） |

## ★★ 但它**没有传导到产物**——这一条是决定性的

| | 结果 |
|---|---|
| 引用了 train 那一份的 **claim** | **0 条**（两人都是 0） |
| `persona.md`／`SKILL.md` 里出现该 source_id | **无** |

⇒ 泄漏是**潜在的**，不是**已发生的**：那部作品虽然在 train 池子里，
**一条断言都没从它出**，人物档案里也没有它。
所以这 5 道题**并没有通过产物这条路被泄题**。

★ 说不到的那一半也要说清：合成阶段是否**读过**整份 train 语料，
**从产物上看不出来**。所以下面的处置是「两个数都报」，不是「判定无影响」。

## 处置（**判分之前定的**）

1. **不改 split、不删题、不动装置** —— 预登记的东西不许中途换；
2. **判分时照常跑满 32 题**；
3. **报数时对这两人各报两个 delta**：
   - 全量 32 题的 delta；
   - **剔掉受影响那几题**之后的 delta（Machiavelli 30 题、Rousseau 29 题）；
   两个数**都写进判决书**。若两者方向一致 ⇒ 结论不受这一条影响；
   若方向不一致 ⇒ **以剔除后的那个为准**，并在判决书里写明差多少。
4. 其余六人本件实测 **0 对**，不适用本条。

★ 这是**敏感性分析**，不是事后挑数：判据、受影响题号、报数规则**全部在判分前写死在这里**。

## 射程（必须连着念）

`same_work` 靠 n-gram，**跨语种恒为 0**。八人里有五人的语料混着 2–5 种语言
（Machiavelli 达 5 种），**原文在 train、译本在 holdout 的那一类本件一声不吭**。
⇒ **「其余六人 0 对」是下界，不是「没有泄漏」。**
按题名做过一次粗筛，命中很多（Rousseau 的 `Émile`、Pestalozzi 的 `Lienhard und Gertrud`、
Fröbel 的 `The education of man` 两边都有），**但逐份比正文都不是同一份文本**
（多卷本的不同卷、不同印本）——**题名同不等于文本同**，所以那一批没有列进上表。

## ★ 订正（同日晚，仍在判分之前）：上面那两对要按**权威判据**重新定性

追加登记写完之后我才发现，仓里早有硬门 `check_holdout_overlap.py`
（覆盖率 = |holdout∩train| / |**holdout**|，n=8；≥0.30 ✗ / 0.10–0.30 ⚠ / <0.10 ✓）。
它与我用的 `same_work` **分母不同**，**它是权威**。用它自己的函数与常量现算：

| | 覆盖率 | 权威判定 |
|---|---:|---|
| jean-jacques-rousseau | **0.1691** | **⚠ 需人工核**（那道门自己要求的动作，仓里查不到做过的记录） |
| niccolo-machiavelli | **0.0780** | **✓ 绿** —— 低于 SOFT 0.10 |

⇒ **Machiavelli 那一对撤回「同一部作品」的定性**，改称「**次阈值重叠**」。
**敏感性分析照旧执行**（多报一个数没坏处），但判决书里的**理由要写「次阈值 0.0780，
权威门判绿」，不许写「泄漏」**。
⇒ **Rousseau 那一对**：判分之前**补做那道门要求的人工核**，结论写进判决书。

★ 上面「追加登记」一节里说的**其余六人 0 对**不受影响；
但要连着念本件的射程：**跨语种恒为 0**，两把尺子都一样看不见。

★★ 教训：**报数之前先搜仓里有没有人已经在量同一件事。**
我造了一把新尺子，而权威那把就在 `scripts/` 下；分母不同 ⇒
我把一个绿的说成了泄漏。[[two-checkers-same-text-different-rules]]

## ★★ 再订正（同日夜）：直接调权威 `check()` 之后，第 1 批的结论变成这样

我上一次仍是**自己算覆盖率**，漏了 `check()` 内部的**文档频率样板过滤**
（Jefferson 一人被剔掉 65,667 个样板 shingle）。这一次**直接调 `m.check(ws, [])`**：

| 第 1 批 | 权威判定 | 判分前要做的 |
|---|---|---|
| **thomas-jefferson** | **⚠**（`summaryviewofrig00jeff_1.txt`；另 28 处 ≥50 词，最长 91 词） | **补逐条人工核** ← 新增，上一版漏了他 |
| **jean-jacques-rousseau** | **⚠**（`extraitduprojet00sain.txt`） | **补逐条人工核** |
| niccolo-machiavelli | **绿**（连 ⚠ 行都没有） | 敏感性分析**降为可选**；结论里不许写「泄漏」 |
| lincoln／bismarck／kant／pestalozzi／frobel | 绿 | 无 |

⇒ **第 1 批要人工核的是两个人，不是一个**。这一节取代上面两节里与之冲突的说法。

## ★★ 那道门要的「逐条人工核」：**做完了**（2026-08-14 夜，仍在判分之前）

权威门对 ⚠ 的规矩是「逐条核完再往下走 —— **同场活动的不同报道可接受，转载不可**」。
三条 ⚠ 全部打开读过（用它自己的 `runs()`，注意返回是
`(词数, **train 侧起点**, **holdout 侧起点**)——我第一次把两个下标读反，
印出来的 H/T 两段对不上，**那一版的判读作废**）。

### ① `jean-jacques-rousseau / src-959e8b14de18`

holdout《Extrait du Projet de paix perpétuelle》1761（12,639 词）
↔ train **同题名、同年**（11,717 词）。连续逐字段 **232 处，最长 25 词，≥50 词 0 处**。
读到的共有文字就是这部小册子自己的正文：

> `jamais il en vient une, ou l'on aura le tems de fe préparer …`
> `par le fameux Décret de Claude , qui incorporoit tous les Sujets de Rome …`

**判读：同一部 1761 年小册子的两次扫描（OCR 差异把长段打碎，所以没有 ≥50 词的段）。
＝ 转载 ⇒ 不可接受。**

### ② `thomas-jefferson / src-4f30582e77e0`

holdout《A Summary View of the Rights of British America》1774（7,794 词）
↔ train《Memoir, Correspondence, and Miscellanies》（204,704 词）。
连续逐字段 **153 处，最长 27 词，≥50 词 0 处**。读到的是《Summary View》自己的正文：

> `Not the hundredth part of the inhabitants of that town had been concerned in the act complained of …`

**判读：文集把这篇收了进去。＝ 转载 ⇒ 不可接受。**

### ③ `thomas-jefferson / src-27db67073ecf`（**上一版漏了这一条**）

holdout《Jefferson's Germantown letters …》1906（40,263 词）
↔ train《Writings. Library ed.》1903（118,229 词）。
连续逐字段 **234 处，≥50 词 1 处，最长 65 词**：

> `navigation, and is as little or less than is claimed by any of them on their own coasts …`

**判读：同一批信件在两处印。＝ 转载 ⇒ 不可接受。**

★ 另有 `thomas-jefferson / src-252ce7838048`「**剔除样板后正文过短，测不了**」——
**未量，不是通过**。它牵动的题一并按下面的口径处理。

## ★★ 处置（我定的，取代前面几节里与之冲突的说法）

**不换源**（换了就与预登记不符，且这八人的题已经冻结），改成把受影响的题**标为非盲**：

| 人 | 受影响的题 | 占比 |
|---|---|---:|
| **thomas-jefferson** | `tj-trajectory-01`、`tj-contrast-01`（源①）＋ `tj-known-01`、`tj-boundary-02`、`tj-trajectory-02`、`tj-contrast-02`、`tj-fact-preservation-01`、`tj-style-decoy-02`（源③） | **8/32 = 25.0%** |
| **jean-jacques-rousseau** | `jjr-known-01`、`jjr-boundary-02`、`jjr-trajectory-02` | **3/32 = 9.4%** |
| niccolo-machiavelli | —（权威判绿） | 0 |

**判分照常跑满 32 题，但这两人的主 delta 用「剔除后的子集」**
（Jefferson **24 题**、Rousseau **29 题**），全量 32 题的 delta 作为附带数一并写进判决书。

★ 这与本文件上一节说的「全量为主、剔除为辅」**相反，以本节为准**：
上一节写的时候还没做人工核，只知道「有重合」；
**现在读完了，结论是「转载」——而那道门自己写着「转载不可」。**
把已知非盲的题算进主结果，就是拿它们去抬 delta。

★ **对已判分的六人不追溯**（㊵），他们的处置见
`_密封集与train同作品重合-2026-08-14.md` 末节 —— 那一条只能由人定。
