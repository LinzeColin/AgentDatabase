# Eiffel #142 解锁 —— 第 8 部一手已取到正文，从延后名单出列（2026-08-18）

## 一句话

他挂在延后名单上的理由是「可用一手 **7 部 < quick 门 8**，**差整整 1 部**，四条通道全不通」。
今天把四条通道逐条打开重探，**第 8 部拿到了正文**，`unblock_todo ②` 的解锁条件
（「只要再取到 1 部，quick 的 8 源即成立」）**满足**，按 `next_person.py:120` 写死的机制
（「补足来源后从 `_延后名单.json` 删掉即可恢复」）出列。

## 原记录里的两条「不通」，逐条被证伪

| 记录里写的 | 今天实测 |
|---|---|
| 「**Gallica SRU 403 Access Interdit**」 | **是 User-Agent 过滤，不是访问控制。** 裸 `curl` 复现 403；换成如实标明身份的 UA（`persona-distiller-probe/1.0 (…contact:…)`）**同一 URL 200 / 59 KB**。 |
| 「archive.org 无此条目（`title:("resistance de l air")` **numFound=0**）」 | **那是去掉重音的书名检索。** 改按 creator：`creator:(Eiffel) AND date:[1880-01-01 TO 1930-12-31]` → **numFound 11**，`unblock_todo ①` 点名的 1910《La résistance de l'air et l'aviation》**就在里面**。 |

★ **书名匹配是脆的** —— 这是同一个病在本仓的第 N 次：一个带重音的法文书名，
用无重音串去检索就是 0 命中，而人物本身有一整批作品在那里。

## 第 8 部：实测数字

| 项 | 值 |
|---|---|
| IA identifier | `EiffelLaRsistanceDeLairEtLaviation1910` |
| 文本文件 | `Eiffel_-_La_résistance_de_l'air_et_l'aviation,_1910_djvu.txt` |
| 大小 / 词数 | **429,518 字节 / 77,462 词** |
| sha256 | `72329cd0626aab28…` |
| 法文实词命中 | **564**（résistance／expériences／aérodynamique／vitesse／plaque／mètres／avion／Eiffel） |
| 权利 | 出版 **1910**，作者 Gustave Eiffel **1832–1923** ⇒ ≤1930 分界内，**公有领域** |
| `access-restricted-item` | **None**（不受限） |
| OCR 门 | `check_ocr_language_death` **通过**（虚词占比在 0.15 下限之上） |

★ **文件名不是标准的 `{id}_djvu.txt`** —— IA 保留了带重音的原文件名。
我先按标准名猜，得 **404**；读 `archive.org/metadata/{id}` 的文件清单才拿到真名。
**文件名匹配是脆的，同日第三次。**

★★ `check_ocr_legibility` 那次 rc=0 **不算通过** —— 它「读到的份数 0」，
是**零扫描面的绿**，且它只认德文花体（射程外）。只算一把尺量过。

## 8/8 指针当日复验

把 `_fetch-manifest.json` 原有 **7 个 identifier** ＋ 第 8 个，逐个打
`archive.org/metadata/{id}`：**8/8 返回完整文件清单、全部 `access-restricted-item: None`、
各含 ≥1 个 `.txt` 文本派生件。**

    gri_33125000583548        lesgrandesconstr00eiff    noticesurleviadu00eiff
    TA62428EIF                travauxscientifi00eiff    n-0106381-pdf-1-400
    latoureiffelen1900eiff    EiffelLaRsistanceDeLairEtLaviation1910  ← 新

★ 复验**只打 metadata，不整包下载**（铁律 7）。
★★ 循环里的 `HTTP 405` 是我用 `-I`（HEAD）打出来的 —— 该端点只收 GET，
**同一循环的 GET 全部成功**。别把 405 读成不可用。

## ★★★ 出列之后卡在哪：**下载授权是 Owner 保留的**

本机 `wip-eiffel-142` 目前 **0 份 `.txt`**（语料只放指针、不进 git）。
真开工要按上面 8 个 identifier 重取约 **3.4 MB ＋ 新那部 0.43 MB**。

**而「下载授权」是 Owner 2026-08-12 保留的三项之一**（另两项：推送、删受保护资产）。
⇒ **出列 = 让队列如实反映「他不再被挡住」；抓源 = 要 Owner 点头。**
这两件是分开的，不要合成一件。

## 出列做了什么

- 证据搬进本文件（原条目 4977 字节，逐字保留在 git 历史里）。
- 从 `_延后名单.json` 的 `deferred` 数组删掉该条 ⇒ 186 → 185。
- 机制出处：`_pipeline/next_person.py:120`
  「因证据不足延后的人物：跳过但不出队，**补足来源后从 `_延后名单.json` 删掉即可恢复**」。

## 可复算

```bash
UA="persona-distiller-probe/1.0 (public-domain corpus availability check; contact: <你的邮箱>)"
# ① 按 creator 检索（不要用去重音的书名）
curl -s -A "$UA" "https://archive.org/advancedsearch.php?q=creator%3A%28Eiffel%29+AND+date%3A%5B1880-01-01+TO+1930-12-31%5D&fl%5B%5D=identifier&fl%5B%5D=year&fl%5B%5D=title&rows=25&output=json"
# ② 取真文件名（**别猜 {id}_djvu.txt**）
curl -s -A "$UA" "https://archive.org/metadata/EiffelLaRsistanceDeLairEtLaviation1910"
# ③ Gallica 那条 403：带 UA 就 200（书目可读；正文层未从 texteBrut/IIIF 暴露）
curl -s -A "$UA" "https://gallica.bnf.fr/SRU?operation=searchRetrieve&version=1.2&query=dc.creator%20all%20%22Eiffel%20Gustave%22"
```

[[filename-matching-is-brittle]]｜[[refused-is-not-firewalled]]｜[[a-blocked-by-x-label-needs-x-rerun]]｜[[bibliographic-proxy-instead-of-the-measurement]]
