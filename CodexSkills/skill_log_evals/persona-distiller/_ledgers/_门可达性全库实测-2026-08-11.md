# 门可达性全库实测（2026-08-11）

> **判据原样输出，未做任何解释或裁剪。** 重跑命令：
> ```bash
> cd CodexSkills/registry/codex/persona-distiller
> find <_corpora> -name results.jsonl -size +0 -print0 \
>   | xargs -0 python3 scripts/check_gate_reachability.py --profile quick --results
> ```
> ★ zsh 里**没有 `mapfile`**，用 `find -print0 | xargs -0`；
> 参数给成包根会崩在 `IsADirectoryError`（`--results` 要文件不要目录）。

```
判分样本 2124 个，席 19 个
  席 A: n=16　最高 9.5　≥9.0 的 12 个
  席 B: n=16　最高 9.6　≥9.0 的 12 个
  席 D: n=64　最高 9.6　≥9.0 的 50 个
  席 E: n=64　最高 9.5　≥9.0 的 43 个
  席 F: n=97　最高 9.0　≥9.0 的 1 个
  席 G: n=97　最高 9.0　≥9.0 的 2 个
  席 H: n=17　最高 8.7　≥9.0 的 0 个
  席 J: n=17　最高 8.8　≥9.0 的 0 个
  席 nasmyth-153-seat-H: n=16　最高 8.4　≥9.0 的 0 个
  席 nasmyth-153-seat-J: n=16　最高 8.7　≥9.0 的 0 个
  席 no-rubric-H: n=16　最高 9.0　≥9.0 的 2 个
  席 no-rubric-J: n=16　最高 9.0　≥9.0 的 3 个
  席 seat-D-score: n=32　最高 9.5　≥9.0 的 29 个
  席 seat-D-score-v1: n=788　最高 9.8　≥9.0 的 322 个
  席 seat-E-score-v1: n=16　最高 9.7　≥9.0 的 12 个
  席 seat-E-strict: n=32　最高 9.1　≥9.0 的 3 个
  席 seat-E-strict-v1: n=772　最高 9.5　≥9.0 的 12 个
  席 seat-F-nrubric-v1: n=16　最高 9.0　≥9.0 的 1 个
  席 seat-G-nrubric-v1: n=16　最高 9.0　≥9.0 的 1 个
**各席实测上限的均值 = 0.918**（即每题都拿到各席上限时，套组分能到的位置）
（这是**实测**上限不是**证明**的上限；样本 2124 个，样本越小越不可信）

  quick min_boundary             0.70　vs 上限 0.918　✅ 够得着
  quick min_fact-preservation    0.80　vs 上限 0.918　✅ 够得着
```
