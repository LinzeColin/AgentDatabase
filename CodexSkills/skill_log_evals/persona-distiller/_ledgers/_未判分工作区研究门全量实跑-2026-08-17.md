# 32 个未判分工作区全跑了一遍研究门：**被检查的 25 个全红，另 7 个门没开机**

已判分的属 ㊵ 冻结、处置已定，跑了也不改什么。**真正该查的是未判分的那 32 个** ——
它们的处置还没定，错误还有意义。

    for 每个未判分工作区: quality_check.py <ws> --phase research
    32 个｜197 秒｜**rc=0 的：0 个**

## ★★ 订正二：标题原写「32/32 不通过」，分母是错的

其中 **7 个**（burbank-183、leonardo-184、plato-186、ford-188、churchill-191、
pinchot-192、howard-193）报的**唯一**错误码是 `target.invalid` ——
`ensure_target()` 拿不到 `SKILL.md` 就**拒检整个工作区**，
**一条研究道检查都没跑过**。逐个核过：它们的错误码集合就是 `['target.invalid']`，
再无第二条。[[a-refusal-to-check-prints-one-error]]

⇒ **真正跑完研究道检查的是 25 个**，25/25 不通过（0 个干净）。
  下面那张分布表的比例应按 **25** 读，不是 32：
  `ledger-file-missing` 22/25 = 88%，`holdout-overlap` 22/25 = 88%，
  `title-is-just-the-filename` 12/25 = 48%。

  「N 项有问题」的判据必须印「各档合计 = 真读到份数」——
  这里 7 个从没被读到，却混在同一个分母里。
  [[a-rights-check-said-zero-red-after-reading-nothing]]

★ 这 7 个**不是新发现**：`_被拒检的工作区-2026-08-14.md` 已经记过（当时 5 个），
  `_pipeline/check_scoring_ready.py:180` 一直在盯。今天做的只是把 5 补成 7
  （新增 plato、albert-howard，均属良性档），并确认 **Churchill 仍是唯一要处置的**。

★ 先验了写盘：跑完 `git status` 逐个查，**0 个工作区被写过**
  （报告写盘由 `--write-report` 把关）。这一步是必须的 ——
  本项目记过「调权威判据把冻结工作区的证据覆盖了」。

## 错误码分布 —— ★ 订正：上一版把「出现次数」当成了「工作区数」

第一版我用 `re.findall` 的**出现次数**当工作区数，于是把 `source-unclaimed`
报成了 22 个工作区（**真值 4 个**，它在少数工作区里重复出现多次）。
按工作区去重重算，两种口径并列：

| 错误码 | **工作区数** | 出现次数 |
|---|---:|---:|
| `corpus.holdout-overlap` | **22** | 22 |
| `research.ledger-file-missing` | **22** | 22 |
| `corpus.title-is-just-the-filename` | **12** | 12 |
| `target.invalid` | **7** | 7 |
| `research.attribution-basis` | **5** | 6 |
| `research.lane-completion` | **5** | 5 |
| `source.filename-year-mismatch` | **4** | 4 |
| `research.source-unclaimed` | **4** | **22** ← 上一版报错的那个 |
| `source.primary-ratio` | **3** | 3 |
| `source.lane-coverage` | **3** | 3 |

★ 教训还是那条：**报计数必须连口径一起给**。「22」既可以是 22 个工作区，
  也可以是 22 次出现 —— 这两个数今天在同一张表里同时存在。

### 三个高频码是不是同一批工作区

    ledger-file-missing (22) ∩ holdout-overlap (22) = **21**
    source-unclaimed (4) ⊂ ledger-file-missing —— 4 个全在里面
    只有 benardos-128 是「有 holdout 重合但账本不缺文件」

⇒ **一个病根，不是三个**：语料正文不在本机 ⇒ 账本指空、holdout 比对无从做起。
  benardos-128 是唯一的例外（它的 holdout 重合是真问题，与缺文件无关）。

## ★ `ledger-file-missing` 查到底：**不是账本坏了，是语料正文不在本机**

22 个工作区、**合计 1189 条**账本记录指向不存在的文件（rousseau 103、
blackwell 95、marshall 95、comenius 83、machiavelli 79…）。
判据在 v0.0.0.46 **有意**把它改成硬错，代码注释里记着实测例
（Galen 账本 60 条盘上 9 份、Vesalius 47→0、Livermore 536→0），
并明写「Lister/Virchow 零缺失，**不存在「合法缺失」这一类**」。

打开看到底缺什么：

    账本里的 local_path： raw/10066336bsb.txt、raw/10097325bsb.txt …
    raw/ 里实际有的：     .gitkeep、_copyright-scan.json、_dedup.json、
                          _fetch-manifest.json、_ids-deltaF.txt

⇒ **`raw/` 里剩下的全是记账文件，语料正文一份都不在。**
这正是「语料不进 git」的既定结果 —— 账本、抓取清单进仓，正文不进。

### 我在这上面连错两次假设，都是打开看才纠回来的

  ① 先猜「账本坏了」——查 brandeis：`local_path` 缺失 **0 条**，它根本不在那 22 个里；
  ② 再猜「文件被 scratchpad 回收清掉了」——查 rousseau：`raw/` **有 14 个文件**，
     看起来没被清；打开才发现**那 14 个全是 `_`/`.` 开头的记账文件**。
  ★ 两次都是「用一个能一眼看到的量（有没有文件 / 有几个文件）代替真正要问的量
    （**语料正文在不在**）」。

## 这对待裁定项 #115 的意义

#115 的选项 A 是「把语料摘录放回 git」。本次实测给它一个可复算的代价面：
**在本机这份工作树上，22 个未判分工作区的研究门无法通过**，
因为 `primary_ratio`、引文核查、覆盖率都要读正文，而正文不在。
不是工作做坏了，是**复验需要重新抓语料**。

★ 射程：本件只跑了 `--phase research`，未跑 synthesis/release；
  也只覆盖**未判分**的 32 个，冻结的 21 个没跑（按 ㊵ 不动）。
