# ㊲ 复算：**判据行为未变**（两条独立证据），**且「七个已入库人物」的「已入库」是错的**

> **读法**：§三是我写到一半时的结论（「口径我复现不了」），
> **它已被 §三之二推翻**——原始名单后来找到了。§三保留下来是为了留住推翻的过程，
> **结论以 §三之二 与 §四 为准。**

日期：2026-08-12。起因：我给 `check_holdout_overlap.check()` 补了自测（此前它一次也没被自测进入），
补完之后必须回答一句——**㊲ 的数字还作数吗？**

★ 我一度在 HANDOFF 里写了「重跑结论不变」。**那句是没跑就写的，已删。** 下面是真跑出来的。

---

## 一、判据行为**未变**——这一条有可出示的证据

全量回扫 25 个工作区之后：

```
git status --porcelain -- skill_log_evals/persona-distiller/_corpora
→ 0
```

仓里被跟踪的 `reports/holdout-contaminated-passages.json` 共 **14 份**，
回扫完 **0 份内容变化**（逐字节相同）。

⇒ **补自测没有改变判据的判决。** 这是「行为未变」的直接证据，不是自述。
  ★ 为什么可信：那 14 份文件正是 `check()` 在有受污染段时写出来的产物；
    若阈值、样板过滤、locate 任一处被我改动，它们必然变。

---

## 二、本次回扫的**全部**结果（25 个工作区）

| 工作区 | rc | 非样板的连续逐字段 |
|---|---|---|
| koch-107 | **1** | **1483 处** |
| semmelweis-105 | **1** | **1646 处** |
| lister-108 | **1** | **247 处** |
| blackwell-118 | 0 | 40 处 |
| carver-127 | 0 | 3 处 |
| whitworth-152 | 0 | 1 处 |
| adams-131 / bessemer-132 / cicero-166 / gantt-156 / grotius-168 / kelsen-171 / mendel-125 / nasmyth-153 / pasteur-106 / paton-162 / rosenhain-138 / shewhart-165 / thomson-129 | 0 | 无 |
| benardos-128 / coffin-130 / liebig-124 / martens-134 / pacioli-161 / roberts-austen-135 | **2** | 无（**未核，不是通过**） |

**6 个有连续逐字段；3 个硬失败；6 个「无法判定」。**

---

## 三、★★ 口径对不上：**这 6 个里只有 1 个在 registry 里**

台账原记「**7 个已入库人物**有整段重复」。用两种互不依赖的办法查：

| 人物 | `team-index.json` 全文出现次数 | `registry/codex/` 下有无目录 |
|---|---|---|
| Carver #127 | **7 次** | 无 |
| Koch #107 / Lister #108 / Semmelweis #105 / Blackwell #118 / Whitworth #152 | **0 次** | 无 |

⇒ 本次能测到的 6 个里，**只有 Carver #127 在 102 个已入库产物里，且它是 3 处**（除 Whitworth 的 1 处外最少）。
  **三个硬失败（Koch 1483、Semmelweis 1646、Lister 247）一个都不在 registry。**

### 为什么对不上——**分母**

```
_corpora/wip-* 工作区目录 ：54
其中账本还在本树的     ：25   ← 本次回扫的分母
账本已不在本树的       ：29
```

那 29 个是按「语料另存 Release／私有仓，仓里只放指针」的裁定移走的。
**「7 个已入库人物」很可能有几个落在那 29 个里，本次根本扫不到。**

### ~~本节结论~~ ★★ 以下这段**已被 §三之二 推翻**，保留以留住过程

★ 我当时的结论是：
  · 「判据行为未变」——**成立，有证据**（第一节）；
  · 「㊲ 的 7 人还是那 7 人」——**本次证不了也证不伪**，
    因为**我量的是 25/54，不是全部**。
  ⇒ [[counts-need-their-cutoff-stated]]：单给一个数等于替读者选了最宽松那档。

**⇒ 这一半错了。** 我停在了「本树扫不到 → 无法比较」，**而原始名单就落在同一个 `_ledgers/` 目录里**
（`_holdout连续段全库分布-2026-08-11.md`），去读一眼就全有了。
[[stopping-at-the-first-answer-that-holds-together]]：第一个说得通的答案，十分钟后就不成立了。

## 三之二、★★★ 补完之后：**原始名单找到了，两个问题都定了**

原始表在 `_holdout连续段全库分布-2026-08-11.md`。逐条对我今天的复算：

| 工作区 | 2026-08-11 记的段数 | 2026-08-12 复算 | |
|---|---|---|---|
| ignaz-semmelweis | 1646 | **1646** | 一致 |
| robert-koch | 1483 | **1483** | 一致 |
| joseph-lister | 247 | **247** | 一致 |
| elizabeth-blackwell | 40 | **40** | 一致 |
| george-washington-carver | 3 | **3** | 一致 |
| joseph-whitworth | 1 | **1** | 一致 |
| rudolf-virchow / florence-nightingale / william-osler / ws-jenner / holmes / barton / blackstone / fleming | — | **扫不到**（账本不在本树） | — |

⇒ **凡是本树扫得到的，段数逐个精确相同。** 这是「判据行为未变」的**第二条独立证据**
  （第一条是 14 份污染段报告逐字节不变）。

### ★★★ 「七个已入库人物」——**「七个」对，「已入库」错**

原表的 `✗ 硬失败` 恰好是 **7** 个：
semmelweis / koch / lister / virchow / nightingale / osler / ws-jenner。
**「7」这个数是对的，它数的是硬失败。**

而「已入库」是转述时加上去的。按 `team-index.json` 的 102 个产物逐个精确核
（`subject_slug` 与 `canonical_name` 两种匹配一致，且已自查 102/102 slug 非空）：

| | 硬失败 7 人 | ⚠ 有段 7 人 |
|---|---|---|
| **在 registry 里** | **0 人** | **1 人**（Carver #127，**3 段**） |

三个索引（`index.json`、`registry/index.json`、`registry/_global/registry-snapshot.v1.json`）
也全查过，**这六人在任何 registry 里都不出现**。

★ 旁证三条，方向一致：
  · #98 台账早已记「barton/virchow/nightingale/osler **四人全不在 registry**」；
  · Holmes #170 是**记拒发**，不入库；
  · Carver #127 任务条目写着「已入库」，与本次唯一命中吻合。

⇒ **更正**：㊲ 应表述为
  「**7 个人物的 holdout 与 train 有整段逐字重复（硬失败）**，
    其中**在 102 个已发布产物里的是 0 个**；
    已发布产物里受影响的只有 Carver #127 一人、**3 段**（属可逐段避开的 ⚠ 档）。」

★★ 这把 ㊲ 的性质换了一档：**它不再是「七个已发布产物的分数可能要作废」**，
  而是「**七个未发布人物的 holdout 装置不成立**」＋「一个已发布产物有 3 段可避开的重合」。
  前者不涉及已发布分数，后者的量级远小于原表述给人的印象。

### ★ 一个仍未排除的可能性

上面已经定了「7 数的是硬失败、不是已入库」。**仍未排除的是另一件**：
那 8 个「扫不到」的工作区（账本已随语料移出本树），
**它们今天的段数我没有复算过**——虽然 2026-08-11 的记录里有数，
而那次用的是同一版判据（本节第一、二条证据都指向行为未变）。

⇒ 若要把这 8 个也复算，需要先按 `locator` 取回它们的语料。
  **本轮没做**，因为结论已经不依赖它：那 8 个里在 registry 的也是 0 个
  （逐个精确核过），所以**已发布分数是否受影响这个问题已经答完**。
  ⇒ [[counts-need-their-cutoff-stated]]：口径写在这里，别让读者以为我扫了全部。

---

## 四、要做的（不需裁定）与要你裁的

**已做（不需裁定）**：
- `check_holdout_overlap` 的 `check()` 补了 ⑦a–⑦f 共 9 条 tempdir 对照，5 条变异逐一验过；
- `check_holdout_mention` 的 `scan()` 补了 ⑧a–⑧g 共 9 条，4 条变异验过；
- 新判据 `check_selftest_reach` 接进漂移门，**挡住新增的「验了配料、没验判决」**。

**★ 仍然只能你裁的——但 ㊲ 的问法比原来小了两档**：
**㊲ 的问法本身要改**（依据见 §三之二）：

  原问：「七个**已入库**人物的 holdout 与 train 整段重复，要不要重估已发布分数？」
  实况：**已发布产物里受影响的是 1 人 3 段（Carver #127，可逐段避开）**，
        七个硬失败**一个都没发布**。

⇒ 真正要你裁的缩成两条，且都比原来小：
  1. **Carver #127（已发布，3 段）**——3 段可逐段避开，出题时避开即可；
     **要不要因此重估他的 +0.3791**？我的判断是不必（3 段、且已落盘可避开），
     但他是已发布产物，**动他要你点头**。
  2. **那七个未发布人物**（Koch 1483、Semmelweis 1646 多到无法逐段避开）——
     **它们的 holdout 必须换掉才能继续做**。这不涉及已发布分数，
     但会影响「这七人还做不做、怎么做」的排期。

★ 本轮已把「判据靠不靠得住」这一层补硬（自测 + 变异 + 两条独立的行为未变证据），
  **剩下的是产物侧的决定，判据给不出。**
