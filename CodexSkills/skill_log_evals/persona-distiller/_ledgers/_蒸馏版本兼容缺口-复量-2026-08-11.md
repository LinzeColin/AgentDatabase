# 蒸馏版本兼容缺口复量：**达标 1 / 108**

日期：2026-08-11，移交前复量。判据：`check_distillation_freshness.py`（**退出 0，只报不拦**）。

## 现状

    当前蒸馏版本 **v0.0.0.154**｜兼容下限 **v0.0.0.144**（当前末位 − 10，夹到 ≥1）
    **达标 1 / 108｜低于下限 107｜归因不到 0**

低于下限的多数停在 **v0.0.0.6**（`git-first-commit:bulk-repackage`）或 v0.0.0.7——
**落后约 148 个版本**。Carver 停在 v0.0.0.132/135（是最接近的一档）。

## ★ 两个口径要分清（这次差点又混用）

    `team-index.json` 的 `products`：**102 条** ← 这是「人」
    freshness 数的：                **108 行** ← 这是「产物条目」

差额来自**一人多产物**：George Washington Carver 一人占 **5 行**（artifact-manifest ×5）。
**「102」与「108」都对，但答的不是同一个问题。**
★ 另：该判据**自己会截断逐条列表**（我抓到的只有 20 行），要全量得读它的汇总数。

## 这不是新缺陷，是已裁定的状态

判据自己写着「**按裁定不重蒸，进重蒸台账（任务 #29）**」——
即「600 人全部完成后再统一重蒸」。所以它**该是这个样子**，不是失修。

★ 但要写进移交，因为收件人第一次跑这道判据会看到 107 个红字：
**那是设计如此，不是他弄坏了什么。**

## ★★ 缺口为什么会一直扩大（记法）

兼容下限**随当前版本滚动**，所以每升一版，存量产物就整体往「不适配」推一格。
本项目已经据此定过：**判据落地不再升版**（本轮我加了 3 件判据、改了 3 件，**没有升版**，
版本仍是 v0.0.0.154）。**这条规矩正在起作用**——若不然今天就该是 v0.0.0.160 上下。
