# 专家团队的路由，对着「随机抽人」比了一次

**2026-08-15**｜起因：Owner 的评价 ——
「路由、覆盖、记录、自优化迭代、有效激活调用、命中率**都没有实质性收益和效果**」、
「纸面数据很强，实际能力非常差，接近 0 分甚至负收益」。

TaskPack `V0.0.0.1` 已原样落地、它自己的验收 PASS。
**但那验的是「链路跑得通」，不是「选对了人」。** 这份记录量的是后者。

## 一、先看一眼真实入口的输出

一句普通用户的话、全默认（不选人物、不指定人数）：

    任务：小团队项目管理 SaaS，按人头月付 还是 按项目数计费？
    → small_team，9 名人物专家

选出来的 9 人里有：**焊接裂纹与失效机理**专家（john-c-lippold）、
**压力设备与管道**专家（george-antaki）、**两个编程语言设计者**
（john-carmack、guido-van-rossum）。真正沾边的只有 lou-gerstner。

分数带：

    入选 9 人 base_score  0.1375 – 0.2060（σ 0.0228）
    排除 61 人 base_score  0.0000 – 0.1989
    ⇒ **入选最低 < 排除最高，两个区间重叠**

## 二、量法：对着「从同一个池子按同样人数随机抽」比

新判据 `_ledgers/_pipeline/measure_routing_discrimination.py`。
六个任务，相关身份族**按任务文本预先标注**（标签与理由逐条写在文件里，可被推翻），
随机基线**从真实可选池按其族偏斜抽** 400 次、固定种子。

| 任务 | 人数 | 路由命中 | 随机命中 | 差值 |
|---|---:|---:|---:|---:|
| pricing-saas | 9 | 22.2% | 29.9% | **−7.7 pp** |
| api-readability（正对照） | 10 | 40.0% | 34.3% | +5.7 pp |
| steel-weld-failure | 11 | 27.3% | 27.5% | **−0.3 pp** |
| clinical-triage | 9 | 0.0% | 0.0% | ±0.0 pp |
| land-lease-dispute | 9 | 22.2% | 6.2% | **+16.0 pp** |
| factory-layout | 9 | 44.4% | 34.1% | +10.4 pp |

    6 个任务：高于随机 3 个、低于 2 个、平均 +4.0 pp
    σ 8.5 pp｜SE 3.5 pp｜**离零 1.16 SE ⇒ 判不出与零的差别**
    要让这个效应量达到 2 SE，任务集需要约 **18 个任务**（现有 6 个）

⇒ **「路由有收益」这句话，用现有证据说不出来。** 也说不出「没有收益」——
两个方向都在噪声里。这与 Owner 的体感一致，而现在它是个可复算的数。

★ 我差点只报「平均 +4.0 pp」。报均值不报离零几个 SE，本项目已栽过两次
（+0.0291 门落在区间里、+0.00265 离零 0.48 SE）。SE 已写进工具，不靠人记得。
[[a-reading-exactly-at-the-threshold-is-load-bearing]]

## 三、分层：**相关族越常见，路由越接近随机**

    随机基线 <15%（相关族稀有）1 个 → 平均 **+16.0 pp**
    随机基线 ≥15%（相关族常见）4 个 → 平均 **+2.0 pp**

池子本身极偏（软件开发师 34 / 101、投资资本师 21、材料建工师 15）。
相关族一旦常见，**随便抽都能抽到**，路由那点信号就淹掉了。

## ★★ 四、根因：分项打分里，只有两项在真正分辨人

样本 57 个入选席位 / 6 个任务（**排除者不带 `score_breakdown`，取不到 —— 口径写在这里**）：

| 分项 | 极差 | σ |
|---|---:|---:|
| `values.domain_match` | **1.0000** | 0.3958 |
| `values.currentness`（人物年代新旧） | **0.6500** | 0.1263 |
| `values.packet_similarity` | 0.2887 | 0.0668 |
| `values.task_similarity` | 0.1195 | 0.0334 |
| `values.scenario_match` | 0.0754 | 0.0218 |
| `values.capability_match` | 0.0499 | 0.0152 |
| `values.user_value_match` | 0.0485 | 0.0165 |
| `values.boundary`／`values.evidence` | **0**（恒 1.0） | 0 |
| `telemetry_prior` | **0** | 0 |
| `penalties.*` 两项 | **0** | 0 |

### ★ 一处我先前判错、当场更正的

我第一次只看了**单个任务的 9 行**，那 9 行的 `domain_match` 全是 0，
于是写下「那个字面叫领域匹配的分项是死的」。**错了。**
换成 6 个任务 57 个席位，它的极差是 1.0000，**反而是分辨力最大的一项**。
[[samples-cannot-support-universal-claims]]｜[[measurement-errors-all-point-the-same-way]]

### 逐任务看，才知道它什么时候不工作

| 任务 | `domain_match>0` 的人 | dm 极差 | currentness 极差 | 路由−随机 |
|---|---|---:|---:|---:|
| **pricing-saas** | **0/9** | 0.000 | **0.650** | **−7.7 pp** |
| **factory-layout** | **0/9** | 0.000 | 0.200 | +10.4 pp |
| api-readability | 5/10 | 0.500 | 0.200 | +5.7 pp |
| steel-weld-failure | 7/11 | 0.500 | 0.200 | −0.3 pp |
| clinical-triage | 7/9 | 1.000 | 0.200 | ±0 |
| land-lease-dispute | 4/9 | 1.000 | 0.300 | **+16.0 pp** |

**要害在第一行**：SaaS 定价这一题，可选池里**没有一个人的 domain_match 大于 0**，
而同一题的 `currentness` 极差是全表最大的 0.650 ⇒
**领域信号一失效，排序就落到「谁更近代」上，结果比随机抽还差 7.7 个百分点。**

★ `clinical-triage` 是另一回事：`domain_match` 有 7/9 在发挥作用、极差 1.0，
而命中率仍是 0 —— 因为**名册里医疗护理师是 0 人**。
`audit_persona_fleet_for_team` 同时报出 `zero_roster_categories: ["医疗护理师"]`。
Galen／Vesalius／Harvey／Jenner／Nightingale 都蒸过，**一个都没进团队名册**。
**这是名册的洞，不是路由的错** —— 两件事不要混着算。

★ `boundary` 与 `evidence` 恒为 1.0、`telemetry_prior` 恒为 0：
它们在公式里，但**一点信息都不携带**。`telemetry_prior=0` 与
`strategy=B`、`strategy_fallback_reason: telemetry unavailable; C requires >=60 outcomes`
一致 —— **C（校准路由）从来没有被激活过。**

## 五、这份记录**没有**证明什么

1. **没有证明专家团队最终答案不如裸模型。** 本件只量路由这一层。
   端到端要盲测、要两个互相独立的会话，本会话 harness 不许派子代理。
   [[bibliographic-proxy-instead-of-the-measurement]]
2. **没有证明路由无用** —— 6 个任务判不出与零的差别，那是「判不出」，不是「等于零」。
3. 相关族是**我标的**。标签与理由逐条写在工具里，推翻标签就推翻结论。

## 六、按合同，我不动这些

任务包写明「不要重新设计 MoE、Prompt、团队人数、评分、目录或 API」。
所以上面这些**只报不改**：
不动打分公式、不动 `currentness` 权重、不动人数、不动 95/75 门。

**留给 Owner 的三件**（都能用上面的数直接判）：

1. `domain_match` 为 0 时，是否该**拒绝路由**（明说「池子里没有懂这件事的人」），
   而不是退到 `currentness` 排序 —— 现在退下去的结果是**低于随机**。
2. `boundary`／`evidence`／`telemetry_prior`／两个 `penalties` 五项恒定，
   是**留着待接线**还是**从公式里摘掉**（留着会让人以为它们在起作用）。
3. 医疗护理师 0 人：已蒸的五人为什么没进名册。

**版本号不动** —— 本件是判据落地，按 2026-08-04 裁定算工具改动。

---

## ★★★ 2026-08-16 补测：任务集 6 → 18，**结论变了但仍未过线**

上面那版自己算出「要达到 2 SE 需要约 18 个任务」。补了 12 条，
**12 条的标签与理由都在看任何路由结果之前写好**（先写标签、后跑路由，
顺序不能反，否则是拿结果凑标签）；12 族各有覆盖，且**故意保留两条注定命中低的**
（医疗护理师 0 人、客户营销师 1 人），不挑题。

| | 6 个任务 | **18 个任务** |
|---|---:|---:|
| 高于随机 / 低于随机 | 3 / 2 | **9 / 7** |
| 平均 | +4.0 pp | **+11.7 pp** |
| σ / SE | 8.5 / 3.5 | 27.6 / 6.5 |
| **离零** | 1.16 SE | **1.80 SE** |
| 还需多少任务才到 2 SE | ~18 | **~23** |
| 相关族稀有 | +16.0（n=1） | **+22.5（n=6）** |
| 相关族常见 | +2.0（n=4） | **+7.5（n=10）** |

★★ **效应估计从 +4.0 跳到 +11.7** —— 这说明 6 个任务那次的读数**就是噪声**，
它自己的 SE（1.16）早就说了。**幸好没把 +4.0 当结论发出去。**
同一件事的另一面：**+11.7 也还不能当结论**（1.80 SE < 2），
样本再变还会再跳。[[a-reading-exactly-at-the-threshold-is-load-bearing]]

★ 分层结论**方向没变、幅度变了**：相关族越稀有，路由的增益越明显
（+22.5 vs +7.5）。池子极偏（软件开发师 34/101）时，常见族「随便抽都抽得到」。

★ 单条最差：`typography-system` **−12.0 pp**（相关族=艺术设计师 1 人＋软件开发师 34 人，
随机基线 34.2%，路由 22.2%）。最好：`land-lease-dispute` 等六条稀有族题。

★ `clinic-staffing` 路由判成 **`single_expert`、1 人** —— **模式判定这一层是活的**，
不是所有任务都塞 9 个人。这一点先前 6 条全是 `small_team`，看不出来。

### 分项诊断（样本从 57 席位 → **141 席位 / 18 个任务**）

结论与 6 条时**一致**，且更稳：

    values.domain_match      极差 1.0000
    values.currentness       极差 0.6500
    values.task_similarity   极差 0.2314
    values.capability_match  极差 0.0924
    **恒定为 0 的五项**：boundary、evidence、telemetry_prior、两个 penalties

⇒ 「五个分项在公式里却不携带任何信息」这条**在 141 个席位上仍然成立**，
不是小样本的偶然。

---

## ★★★★ 2026-08-16 第三次补测（24 个任务）：**混着算的三次读数全是假的**

第二次补到 18 条时我写「效应从 +4.0 跳到 +11.7」。补到 24 条又跳回 **+6.1**。
三次读数 **+4.0 → +11.7 → +6.1**，而每次的 SE 都说「判不出」。
**弹跳本身就是答案**，但真因不是「样本不够」——是**尺子混了两种单位**。

### 真因：`single_expert` 的队伍只有 1 人，命中率只能是 0% 或 100%

24 题里有 5 题被路由判成 `single_expert`（n=1）。一条题就能给出
**+89.2 / +77.5 / −39.5 / −12.5** —— 混进平均后，σ 从 2.2 撑到 **26.7**。
把 n=1 与 n=9 放进同一个平均，是拿两把尺子当一把用。
[[changing-the-sampling-unit-changes-the-ruler]]

### 分开之后，结论从「判不出」变成**有界**

| 模式 | 题数 | 平均 | σ | **SE** | 离零 |
|---|---:|---:|---:|---:|---:|
| `single_expert`（n=1） | 5 | +22.9 pp | 57.0 | **25.5** | 0.90 SE |
| **`small_team` 及以上（n≥5）** | **19** | **+1.6 pp** | 9.5 | **2.2** | 0.75 SE |

**产品的常态模式（n≥5）那把尺子 SE 只有 2.2 pp** —— 它其实很准。
于是可以给出**上界**，而不只是「判不出」：

    路由在团队模式下的真实收益 ≈ **−2.8 ~ +6.0 个百分点**（±2 SE）

⇒ **即使往最好里算，它值不到 6 个百分点的族命中率；也不能排除是轻微负的。**
这比「判不出与零的差别」信息量大得多，也比「+11.7 pp」诚实得多。

### ★ 我在这一件上连错两次，两次都是往好里报

1. 6 条时报 +4.0（1.16 SE）—— 幸好带了 SE，没当结论。
2. 18 条时报 **+11.7**，还写进了提交信息 —— 那个数**几乎全是 n=1 的两条
   （+89.2 / +77.5）撑起来的**，而我当时没有按队伍规模分层。
   **补到 24 条、加上 n=1 的两条负例（−39.5 / −12.5）之后它就塌了。**

★★ 教训不是「样本要大」，是 **「先问这几个数是不是同一种测量」**。
19 条 n≥5 的题一直在那儿，它们的 SE 一直是 2.2 —— 答案早就够准了，
是我把它和 5 条二值读数拌在一起，才看上去「判不出」。
[[merging-two-signals-cancels-both]]｜[[length-confound-in-blind-eval]]

### 稀有/常见的分层，方向仍然成立

    相关族稀有（随机基线 <15%）7 题 → **+17.5 pp**
    相关族常见（随机基线 ≥15%）15 题 → **+1.5 pp**

★ 但注意这两组也混着 n=1（`brand-relaunch` +89.2 在稀有组、
`warehouse-automation-roi` −39.5 在常见组），**同一个混单位问题**。
按 n≥5 单独看，常见组更接近零 —— 与上面的 +1.6 pp 一致。

### 分项诊断（179 个入选席位 / 24 个任务）

与 57 席位、141 席位时**完全一致**：`domain_match` 极差 1.0、`currentness` 0.65，
而 `boundary`／`evidence`／`telemetry_prior`／两个 `penalties` **五项在 179 个席位上
仍然恒定** —— 这一条不是小样本的偶然，**三次不同样本量都是同一个结论**。
