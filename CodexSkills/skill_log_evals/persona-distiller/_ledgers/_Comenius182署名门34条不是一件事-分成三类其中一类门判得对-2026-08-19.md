# Comenius #182 的署名门 34 条不是一件事 —— 分成三类，其中一类门判得对

**日期**：2026-08-19　**状态**：语料 83 份已在，断言 0，研究门 39 → 待续

## 一、开工时我看错了路径

第一次量报「Comenius 没有台账、没有语料」——**错的**。
我看的是 `_corpora/wip-comenius-182/evidence/`，真的工作区在
`_corpora/wip-comenius-182/workspaces/john-amos-comenius/` **下一层**。
实况：**台账 83 行、raw 87 份**。[[i-treated-a-name-as-a-fact]] 的同一形状。

## 二、署名门 34 条的真实构成（83 份逐份跑 `check_authorship`）

| 桶 | 份数 | 该怎么处置 |
|---|---|---|
| ✓ 已过署名门 | **27** | — |
| ② **署名的是译者/改编者** | **5** | ★ **门判得对**——不该算他写的 |
| ③ 开头有他的姓名，模式没认出 | **21** | 可查：属格 `COMENII`／讹形 `A.COMENIL`／`Comenio` |
| ④ 开头无任何署名线索 | **30** | 需逐份看是不是他的作品 |

实例（都是实际读出来的，不是推的）：
· `10026103bsb.txt`《Martyrologium Bohemicum》——`deutſch uͤberſetzt von Japand
  Theophilus Elsner`，**署名是译者**；
· `10270188bsb.txt`——`ehemals Johann. Amos Comenius in Böhmicher Sprache beſchrieben,
  Nun aber von einem Liebhaber der Comeniſchen Schriften`，**后人改编**；
· `10448791bsb.txt`——`A.COMENIL`，是 `A. COMENII` 的 OCR 讹形。

★ 我一开始的假设是「拉丁属格 `Comenii` 匹配不上主格」。**实测否掉了**：
  前 4000 字符内主格 `Comenius` 出现 65 次、属格 `Comenii` 15 次——**多数是主格**，
  属格不是主因。[[exact-name-match-assumes-the-nominative]] 在这里只解释一小部分。

## 三、★ 卡点撞上一件已在待裁清单里的事：OCR 判据锚定德语

要把 ④ 那 30 份分成「OCR 毁掉读不出署名」与「确实没有署名」，需要一把 OCR 尺。
仓里三把（`check_ocr_legibility` / `check_ocr_language_death` / `check_ocr_homoglyphs`）：

· 对拉丁-希腊文的《Orbis Pictus》，`legibility` 直接返回 `verdict: "not-german"`；
· 对**整份毁掉的俄文版** Orbis Pictus，它返回 `verdict: "ok"`（good 0.0524）；
· `language_death.rate()` 把那份俄文报成 `'de'`。

本批语料的语种分布：**de 30 / en 21 / la 15 / pt 10 / fr 5 / it 1 / es 1**。
⇒ **这批语料上 OCR 判据不可用**，①/④ 分不开。
这正是待裁清单里「换语言就翻号」那一条，Comenius 是它的第二个实证语料。

## 四、下一轮从这里接

1. ③ 那 21 份：逐份确认是否他的作品，确认后**按实际署名形态**决定要不要动判据
   （★ 动判据前先跑负对照——加宽一次就会把译本也放进来，② 那 5 份是现成的反例）；
2. ④ 那 30 份：需要 OCR 尺，**卡在上面那条 Owner 裁定**；
3. 研究道只有 `01-writings` 有实质内容（7741 字节），其余五道是壳
   ⇒ `research.lane-completion 0 < 3`；
4. `research.attribution-basis` 未声明：Comenius 是印刷时代人物，题名页署名存在，
   应声明为 title-page byline——但要先把 ③/④ 分清再写，否则声明覆盖不了实况。

## 五、本轮已做

· `emit_corpus_pointer --restore` 取回 2 份缺失正文并校验通过（`ledger-file-missing` 清零）。
