# 全库 53 个工作区，**只有 3 个申报过并行见证** —— 判据一直在等一个没人做的申报

**2026-08-14**｜起因：Michelangelo #185 返工，要判「两处证据是不是同一部作品」。

## 判据早就有，缺的是申报

仓里有两道：`check_translation_witness.py` 与 `check_claim_source_independence.py`
（Pacioli #161 那轮落的，都在 `registry/codex/persona-distiller/scripts/`）。
**先搜仓再动手，没有另造第三把尺子。**

拿 Michelangelo 跑，两件都 rc=0，**而绿的原因本身就是发现**：

    check_claim_source_independence  →「**检查的断言: 0**」
        它只查有 ≥2 个 source_ids 的断言，那 8 条各只有 1 个。**不是判过了，是没东西可判。**
    check_translation_witness        →「申报的并行见证组 **0** 个」
        它自己写着「0 组不等于没有并行见证——本件不猜，只查申报」。

⇒ 缺的是 `meta.json` 里的 `attribution_basis.parallel_witnesses`。
**没有任何东西去做那个申报。** ㊸ 记的「40 条两处证据其实一部作品」，根因就在这里。
[[a-checker-nothing-calls-is-not-a-checker]]｜[[zero-hit-gates-must-prove-they-can-hit]]

## 落了 `emit_parallel_witnesses.py`（默认只量不写）

判定规则**照抄**权威判据的口径：8 词片、包含率 `|A∩B|/min(|A|,|B|)`、阈值 0.30、
**不做传递闭包**。它只把结果写成申报，判定仍由那两道判据做。

## ★★ 这个数我改了三次才对，**三次都往大里报**

| 版本 | 全库「会塌」 | Koch | 错在哪 |
|---:|---:|---:|---|
| 第一版 | **53** | **46** | 分母没按类过滤，把 `fact` 也算进去（门只对六类要求 ≥2 源） |
| 第二版 | — | **17** | 判定式写成「任意两份同源就塌」，而权威判据是「**全部**来源塌成一部」 |
| 第三版 | **19** | **15** | ✓ 与权威判据对上，但**扫描面只有 43 个工作区** |
| 第四版 | **20** | **15** | ✓ 扫描面补到 **53** 个 |

三处修正各自的实测锚点：

1. **分母要与门一致。** `quality_check.py` 只对
   `mental-model/heuristic/value/work-method/blind-spot/contradiction` 六类要求 ≥2 源。
   Koch 46 条断言里只有 **17** 条属于这六类。权威判据自己印着「检查的断言: 17」。
2. **判定式是「全部塌成一部」不是「任意两份同源」。** 一条引 3 份、其中 2 份同源
   而第 3 份独立的，**仍剩 2 部作品，判过**。Koch 正好有 5 条引 3 份，差的就是其中 2 条。
3. **扫描面按台账认，不按路径层数。** 第一版用 `*/workspaces/*/meta.json`，
   **57 个 meta.json 里漏了 14 个** —— 9 个工作区路径套了两层
   （`workspaces/<slug>/<slug>/`），还有 `ws-godin/seth-godin/`、
   `wip-galen-101/`（直接在顶层）。**Nightingale 整个没被扫到**，
   而台账定案里正有「Nightingale 2 条」。改成认「有 `evidence/source-ledger.jsonl` 的目录」。
   [[a-gates-scan-set-is-smaller-than-reality]]｜[[eval-artifacts-have-five-schemas]]

[[measurement-errors-all-point-the-same-way]] —— **第三次了。数字够吓人时我就停手了。**

## 定案（现算）

    工作区 **53** 个（已申报 **3**）｜团合计 **230** 组 / **579** 个 id
    分母 **397** 条（六类里引 ≥2 源的）→ **会塌 20 条**

逐人：

| 人 | 源 | 团 | 分母 | 会塌 |
|---|---:|---:|---:|---:|
| robert-koch | 121 | 16 | 17 | **15** |
| friedrich-frobel | 51 | 10 | 8 | **3** |
| florence-nightingale | 117 | 13 | 12 | **1** |
| jean-jacques-rousseau | 103 | 5 | 8 | **1** |
| 其余 49 人 | | | | 0 |

## 与台账定案的差（**两个口径都要说清，不许只报一个**）

台账 `#95` 记：**合计 17 条（Koch 15 + Nightingale 2）**，口径是**台账 `derived_from` 声明**。
本件 20 条，口径是**内容包含率**。逐项对：

- **Koch 15 —— 两个口径精确一致**
- **Nightingale：台账 2，本件 1** —— 声明的比量出来的多 1
- **Frobel 3 / Rousseau 1 —— 台账里没有**，是内容度量新查出的

⇒ **两个口径各自抓到对方漏的**。声明能抓跨语种同源（重叠恒 0），
内容能抓没人声明的。**引用时必须说清是哪一个。**
[[counts-need-their-cutoff-stated]]

## 没有写盘，为什么

㊵ 已裁「**已判分即冻结**」。给存量 50 个工作区写申报，会让现在绿的门变红 20 条 ——
那是**发现**，但动的是存量，而 ㊸ 已裁「存量不动」。

**已写的只有 Michelangelo 一个**（本轮返工对象，9 组 / 21 个 id，
正对照：申报前判据印「0 组」，申报后印「9 组，覆盖 21 份来源」；
负对照：meta.json 逐键比只多了两个键，合成门仍 19、研究门仍 passed）。

要给全库写，一条命令：

```bash
python3 CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline/emit_parallel_witnesses.py \
  --root CodexSkills/skill_log_evals/persona-distiller/_corpora --apply
```

**写之前先看上面那 20 条**——它们会从绿变红。
