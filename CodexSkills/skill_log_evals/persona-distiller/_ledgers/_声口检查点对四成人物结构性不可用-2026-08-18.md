# 「声口」这道检查点，**至少**对全库 39%（19/49）的人物结构性不可用（2026-08-18）

> ★ **「至少」是本文自己订正出来的**：见下面「19 这个数偏小」一节 ——
> 还有第 ③ 类（**本人作品的英译本**）会给出一个**量到译者**的数，
> 而那一类在语种扫描里被算成「英文」，没进这 19 里。

## 怎么撞上的

给 Eiffel #142（法文语料）跑 `check_first_person_density`：

    正文字符 374,811
    语种（按字符）：可读语种（英文）0／不可读 374,811 ——**可读占比 0%**
    **本判据不适用**：只认英文的第一人称动词锚点（`I have`／`I find`／`I prefer`…），
                      德文 `ich habe`、法文 `j'ai`、俄文 `я` **一个都不认**
    **实质第一人称句**：null      **密度（每万字）**：null

**判据是对的** —— 它返回 `null` 而不是 `0`，没有给出一个会被读成「声口薄」的数。
（这正是 2026-08-11 那次修好的：Mendel #125 德文 1578 万字符曾报「实质 0 句」，
Koch #107 德文 2 亿字符曾报 0.07/万字 —— **那不是声口薄，是判据不认识那门语言。**）

## 但由此得出的射程问题

`worth_starting` 把**声口**列为三个「开工后检查点」之一，并写明
「**不达标当场结案**」（另两个：一手源占比、min_lanes）。
而这道检查点**对非英文语料结构性给不出读数**。

**量一下有多少人**（口径：各工作区 `*fetch-manifest*.json` / `source-ledger.jsonl` /
`meta.json` 里 `"language"` 字段的**记录条数**，按工作区聚合；
★ 这是**元数据代理**，不是对语料正文重新判语种；★★ 数的是**记录条数不是字节数**）：

| | 数 |
|---|---:|
| 有语种字段的工作区 | **49** |
| 其中**英文占比 < 50%** | **19（39%）** |

    bismarck-176(de)   comenius-182(la)   frobel-181(de)     galen-101(grc 55/en 6)
    grotius-168(lat)   kant-179(de)       kelsen-171(de 11)  koch-107(de 122)
    liebig-124(de 40/en 25)               machiavelli-177(it)
    martens-134(de 19/en 7)               mendel-125(de 19/en 2)
    michelangelo-185(it)                  pasteur-106(fr 61)
    pestalozzi-180(de)                    rousseau-178(fr)
    semmelweis-105(de 61)                 vesalius-102(la 30/en 10/de 4)
    virchow-109(de 193/en 24/fr 11)

**其中多位已经出货**（Pasteur #106、Koch #107、Virchow #109、Semmelweis #105、
Vesalius #102、Galen #101…）—— 也就是说他们**是在没有声口读数的情况下走完全程的**，
而没有任何地方标出「这一项未量」。

## ★★★ 订正（同日，且**仓里早有更完整的先例**）：19 这个数**偏小**

写完上面之后我去查「那 19 位的产物里有没有真的写出一个声口读数」——
全库只命中 **1 处**：`wip-grotius-168/00-声口密度实测.md`（925 句 / 0.43 每万字符）。
**而那份文件不是假数，它比本条更完整** —— 它自己写着：

> 「**这是译者与编者在卷首序、译注里说话。**……它分不出『译文里的 I』和『译者说的 I』。
>  925 这个数**含译者**，未去除。」
> 「拉丁件的 `ego` 有 **39% 被 OCR 读成 `cgo`**，而本件按英文 `I` 计 —— 拉丁那 10 份
>  **大概率被系统性低估**。」
> 「**两头都不可用**：英译本量到译者，拉丁本量不到他。」

⇒ **声口测不准有三种，不是两种：**

| | 语料 | 判据行为 | 危险程度 |
|---|---|---|---|
| ① | 本人写的英文 | 给出有效读数 | 正常 |
| ② | 非英文原文 | **`null` ＋「本判据不适用」** | 低——它明说不适用 |
| ③ | **本人作品的英译本** | **给出一个像模像样的数，而那个数量的是译者** | **最高——看不出来** |

**而第 ③ 类在我上面的语种扫描里被算成「英文」** —— 所以 **19 这个数偏小**。
按同一份元数据看，至少 Cicero #166（en 15/la 5）、Harvey #103（en 33/la 14）、
Galen #101（grc 55/en 6）、Vesalius #102（la 30/en 10）都可能落在第 ③ 类里。
**本轮不把它们逐个判定** —— 要判得回答「哪些件是译本、译者是谁」，那是独立一轮的活。
★ 正确的问法不是「语料是不是英文」，而是「**这个人自己写的是不是英文**」。
[[measured-voice-in-the-wrong-register]]

## ★★★ 这不是新缺陷，是**射程缺口**

判据本身没错，也没有假绿（它明说不适用）。问题在于：

> **一道被当作「不达标当场结案」的检查点，对四成人物从来没有生效过；
> 而「未量」在下游被读成了「没问题」。**

对比「声口不够」这一档已判的三个人 —— Coffin #130（15 句 / 0.87）、
Bain #136、Sellers #154 —— **他们的语料都是英文**，读数成立，判定不受影响。
⇒ **本条不推翻任何既有判定**，它说的是另一侧：非英文那 19 位**根本没被这道门量过**。

## 处置（本轮只记，不动）

1. **不新造一把「能读法文/德文的声口尺」** —— 那会是为凑数造判据。
   要建，得先回答「非英文的第一人称锚点怎么定、怎么验」，那是独立一轮的活。
2. **Eiffel #142 抓源后**：声口一栏如实写「**未量：判据只认英文锚点**」，
   不写「够」也不写「不够」。
3. **既有 19 位**：属存量，按 ㊵「已判分即冻结」不动分数；
   但若将来重蒸，声口一栏同样只能写「未量」。
4. ~~**`worth_starting` 的说明**……**改文案属决定**，本轮不改。~~
   **★ 已改（同日，commit `2e9e24df3`）—— 我先前那句「属决定」判断错了。**
   加一句射程披露**不改任何阈值、不改任何判定**，与今天修的六处「未核 ≠ 通过」同类；
   而那句话恰好印在**决定要不要开工的人**眼前。现在它会连射程一起印。

## 可复算

```bash
cd CodexSkills/skill_log_evals/persona-distiller/_corpora
python3 - <<'EOF'
import json, pathlib, collections, re
LANG = re.compile(r'"language"\s*:\s*"([a-z]{2,3})"', re.I)
rows = []
for ws in sorted(pathlib.Path(".").glob("wip-*")):
    langs = collections.Counter()
    for p in (list(ws.rglob("*fetch-manifest*.json"))
              + list(ws.rglob("source-ledger.jsonl")) + list(ws.rglob("meta.json"))):
        if p.stat().st_size > 4_000_000:
            continue
        for m in LANG.findall(p.read_text(encoding="utf-8", errors="replace")):
            langs[m.lower()] += 1
    if langs:
        rows.append((ws.name, langs))
non_en = [w for w, c in rows
          if (c.get("en", 0) + c.get("eng", 0)) / sum(c.values()) < 0.5]
print("有语种字段 %d 个｜英文占比<50%% 的 %d 个" % (len(rows), len(non_en)))
EOF
# 期望：有语种字段 49 个｜英文占比<50% 的 19 个
```

[[zero-hit-gates-must-prove-they-can-hit]]｜[[measured-voice-in-the-wrong-register]]｜[[the-eval-covers-exactly-what-was-tuned]]
