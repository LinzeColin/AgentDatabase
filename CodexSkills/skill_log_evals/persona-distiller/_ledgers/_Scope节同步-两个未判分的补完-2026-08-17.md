# Scope 节同步：两个**未判分**的补完；其余 8 个是 ㊵ 冻结

**2026-08-17**｜任务 #121 挂着「19 个工作区的研究稿没有 Scope 节」。
**两个数和一个方向都是错的。**

## 三处更正

| 我原来记的 | 实测 |
|---|---|
| 19 个工作区 | **10 个** |
| 「**没有** Scope 节」 | 6/6 道文件**都有** Scope 节，问题是**过期** |
| 「全被 ㊵ 冻结挡住」 | **2 个未判分，可以补** |

★ 第三条我差点又错一次：第一版判据用宽 glob 数「有没有判分产物」，
把 **0 字节的 `results.jsonl`** 也数成了「已判分」，于是 10 个全被判成冻结。
正确判据是 **`results.jsonl` 行数 > 0**。
[[empty-default-swallows-unknown]]

    clara-barton              128 行 → ㊵ 冻结
    george-washington-carver   64 行 → ㊵ 冻结
    henry-gantt                64 行 → ㊵ 冻结
    oliver-wendell-holmes-jr   64 行 → ㊵ 冻结
    gregor-mendel              64 行 → ㊵ 冻结
    james-nasmyth              64 行 → ㊵ 冻结
    michael-steinhardt        128 行 → ㊵ 冻结
    elihu-thomson             384 行 → ㊵ 冻结
    **john-dewey                0 行 → 可补**
    **michelangelo-buonarroti   0 行 → 可补**

## 做了什么

**John Dewey #190**：直接重生成 6 份道文件。
逐字 diff 确认唯一被替换的是工具自己的旧样板行 —— **没有任何人工判断丢失**。

**Michelangelo #185**：**先搬后生成**。`--check` 明确警告两段手写判断会被覆盖：

- `02-conversations.md`：「本道**是这个人物最结实的一道**……
  探源时若照 Leonardo #184 的办法『同现即污染』一刀切，**这一道会整条消失** ——
  Milanesi 是编者，creator 里他挂在米开朗基罗前面。」
- `04-external.md`：「本道 16 份全部是二手（S1），其中 13 份是本轮改判进来的……」

两段都搬进 `## Source-linked observations` 成为 `O-0`，并注明搬迁理由。
搬完再 `--check`，警告消失；重生成后逐字 diff，非 Scope 节只多了工具的新样板行；
两段判断各出现 1 次，**没有丢也没有重复**。

## 这与今天上午修的是同一个病

`emit_lane_scope.py` 与 `emit_source_ledger.py` 同形：**纯重生成会覆盖人工内容**。
区别是前者**已经有警告**（`--check` 会逐条列出将被覆盖的手写行），
后者今天上午才补上护栏。

⇒ **警告是有用的**：正因为它逐条印了出来，我才知道要先搬。
但它仍然**只警告、不阻止** —— 直接跑 `emit_lane_scope.py <ws>` 不带 `--check`，
那两段判断就没了。**这是留给 Owner 的一条备选**（给它加 `--preserve`／
写盘前强制 diff），不在本轮射程内，因为它会改变一个 44 个工作区都在用的工具的行为。

## 结果

全库 54 个工作区复扫：**仍过期 8 个，全部是已判分的（㊵ 冻结，不动）**。
两个未判分的已同步，`--check` 均 rc=0。
