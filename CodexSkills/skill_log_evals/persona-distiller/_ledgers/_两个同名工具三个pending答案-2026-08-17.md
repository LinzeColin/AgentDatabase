# 两个同名的 `next_person.py`，同一份数据，**pending 给出 0 / 66 两个答案**（2026-08-17）

想推进本周蒸馏，先问「还有谁可做」。跑 `next_person.py` 得 `NEXT = null`、`pending = 0`。
但仓里有**两个同名文件**：

| 位置 | 行数 | done | **pending** | deferred |
|---|---:|---:|---:|---:|
| `_ledgers/_pipeline/next_person.py` | 245 | 40 | **0** | 186 |
| `registry/.../references/pipeline/checkers/next_person.py` | 611 | 39 | **66** | 132 |
| ★ 我自己现算（归一化姓名） | — | 40 | **13** | 184 |

三个数**都是同一天、同一份数据**。差全部来自**姓名 join 的口径**。

★ 611 行那个还自报两处问题：`语料区路径不存在`（它拼的是
`registry/skill_log_evals/...`，那个路径从来没有过）、`处置类缺 88 条`。

## 根因：队列条目**根本没有状态字段**

    _蒸馏队列.json 的一条：{name, family_zh, family_id, order, priority}

「已入库 / 已延后 / 未动」三档，全靠**拿姓名去另外两张表里对**推出来。
姓名写法一有出入，三档就全变。

## 逐对核完：**真正可开工 = 0**

我那 13 个「未动」拆开：

* **11 个是「已做但未出货」** —— 逐个查过工作区，cases 32/32/33/… 都在
  （brandeis / marshall / lincoln / bismarck / machiavelli / jefferson /
  kant / rousseau / pestalozzi / frobel / dewey），卡在待裁定 #115；
* **2 个是拼写不同**：

      队列 `Alfred Sloan`   ↔ 延后名单 `Alfred P. Sloan`
      队列 `William Paton`  ↔ 延后名单 `William Andrew Paton`

⇒ **245 行那个给出的 0 是对的**，但三个工具没有一个把「为什么是 0」说清楚。

## ★★★ 而同一次里查出一对**看着一样、实际是两个人**

    队列/延后  `Charles M. Eastman`   **建造采购师**｜1940-05-05 – 2020-11-09｜BIM 研究者
                                      （出处：Wikidata Q18630942 P569/P570）
    名册      `Charles Eastman`      **思想教育师**｜`subject_active_through` **1939**
                                      ｜入选理由写的是保留地医疗与原住民权利 ⇒ Ohiyesa

**「姓 + 名首字母相同」这个判别式对同名者恒为真。**
所以它只能用来**找疑似**，判定必须由人写下并附判据。
[[test-the-guard-against-this-persons-namesake]]｜[[namesakes-whose-works-are-also-public-domain]]

## 落地

1. **新建 `_ledgers/_姓名别名.json`** —— 唯一裁决处，4 对全部显式判过：
   3 对 `same`（各附族别/slug/编号判据）、**1 对 `different`**（Eastman，附三条区分依据）。
2. **新建 `_pipeline/check_roster_name_join.py`**（12 条断言，已接进 `run_checks`，
   总跑 **绿 42｜红 0｜2.6s**）：
   * 找出三张表两两之间的疑似对；
   * **每一对没在别名表里裁过 ⇒ rc=1**（不是「没问题」，是「没人裁过」）；
   * ★★ 再加一道：**判成同一人而两侧族别不同 ⇒ 报出来**（多半是同名者）；
   * 读不到任一张表 ⇒ **rc=4 未量**；
   * 印出按别名表消解后的三档，**并印分母**：已入库 40｜已延后 186｜未动 11。

### 变异对照

| 变异 | rc | 被抓住的 |
|---|---:|---|
| ① 抽掉 Sloan 那条 `same` | **1** | 「未裁决 1 对：Alfred P. Sloan ↔ Alfred Sloan」 |
| ② 把 Eastman 从 `different` 挪进 `same`（错误合并） | **1** | 「判成同一人、而族别对不上：建造采购师／思想教育师」 |

★ 变异② **第一版没被抓住**（判据只查「裁没裁」，不查「裁得对不对」，rc=0）——
  但它留下了可查的痕迹：**已入库 40 → 41**。族别判别就是照这条痕迹加的。

## 本件**没有**做的事

* **没有合并任何名字**（别名表只是记录裁决，判据不改数据）；
* **没有动 611 行那个 next_person.py** —— 两个同名工具该留哪个、要不要改名，
  牵涉随包分发的那一份，**不是清理，是决定**。本件先把「三个答案」这件事钉住。

## 可复算

    python3 CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline/check_roster_name_join.py
    python3 CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline/check_roster_name_join.py --self-test
