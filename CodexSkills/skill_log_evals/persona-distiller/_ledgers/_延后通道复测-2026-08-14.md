# 十三条「换通道能续」的延后，通道今天到底通不通（2026-08-14 实测）

延后名单里有 13 条的 `unblock_todo` 点名了具体机构或站点（非 ㉜、非 CCE）。
它们的共同措辞是「**换台机器／换通道能续**」——**而没有一条记着通道当时的状态码**。
今天逐个测了一遍。**只发 HEAD/GET，未绕过任何访问控制、未解验证码。**

## 一、首页/入口的状态码

| 通道 | 谁在等它 | 今天 |
|---|---|---|
| `jbc.bj.uj.edu.pl`（雅盖隆） | Comenius | **200** |
| `dbc.wroc.pl`（弗罗茨瓦夫） | Comenius | **200** |
| `kramerius.lib.cas.cz`（捷克科院） | Comenius | **200** |
| `ndk.cz`／`digitalniknihovna.cz` | Comenius | **200** |
| `archives.yale.edu` | Hopper | **200** |
| `aip.org`（玻尔图书馆） | Simons | **200** |
| `marxists.org` 俄文 | Vygotsky | **200** |
| `findingaids.hagley.org` | Sellers | **202** |
| **`babel.hathitrust.org`** | Borlaug／Timoshenko／Sellers／Nowlan 等 | **403（没变）** |

## ★★ 二、但「首页 200」几乎不说明任何事 —— 往下一层就塌了

| 再往里一层 | 结果 |
|---|---|
| Kramerius 的检索 API `/search/api/v5.0/search?q=Komenský` | **403 Forbidden** |
| MZK Kramerius v7 client API | 400 Bad Request |
| `dbc.wroc.pl` 的网页检索结果 | 200，**但结果是 JS 渲染的，HTML 里没有条目** |
| `dbc.wroc.pl/dlibra/results.json`、`/dlibra/rest/search` | **404** |
| `dbc.wroc.pl` 的 **OAI-PMH** `?verb=Identify` | **200（2,366 B）** ← 唯一真正的机读门 |
| `jbc.bj.uj.edu.pl` 的 OAI-PMH | **连不上（000）** |

⇒ **Comenius 那条「换通道」目前的真实状态是：OAI-PMH 门开着，但它不支持关键词检索，
要靠 `ListIdentifiers` 枚举整库再筛。** 这不是「跑一条命令就能续」，
但**也不再是「取不到」** —— 状态从「未知」变成了「有门、成本已知」。

## ★★★ 三、我在这一步上又栽了一次（当场记下）

我先用 `grep -c -i comenius` 数弗罗茨瓦夫那个结果页，得 **21**，
差一点写成「21 个命中」。**逐条打开看，21 次全在页面骨架里**：
`<title>Comenius - Dolnośląska Biblioteka Cyfrowa</title>`、`og:title`、
语言切换链接 `…?q=Comenius&language=en`……**一个真结果都没有。**

⇒ [[read-the-hits-before-reporting-the-rate]] 的第 N 次。
**在 JS 渲染的页面上用 grep 数命中，数到的是页面自己的名字。**

## 四、结论（逐条，不外推）

- **HathiTrust 仍 403** —— 那 4–5 条挂着它的待办**今天仍不可执行**，与 8 月初一致；
- **Comenius**：有 OAI 门，成本已知（枚举），**是这 13 条里唯一状态真正改善的**；
- 其余各条：**首页可达 ≠ 材料可取**，本轮**没有取到任何一份新语料**，
  也**没有改任何人的 disposition**；
- 三条挂在**付费墙**后的（Simons 的 Annals、Swensen／Druckenmiller 的 Charlie Rose 转录）
  —— **按纪律不碰**，本轮连试都没试。

★ 这份表的用处不是「解封了谁」，而是**把 13 条散文里的「换通道能续」换成了带状态码的事实**，
并把「首页 200」这个容易骗人的读数**和它下一层的真实结果并排放**。
