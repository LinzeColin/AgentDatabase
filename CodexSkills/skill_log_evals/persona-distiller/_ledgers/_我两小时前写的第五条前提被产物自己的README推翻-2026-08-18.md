# ★★★★ 我两小时前写的 ⑤ 那条前提，**被产物自己放的一份 README 推翻**（2026-08-18）

## 我写了什么

今天早些时候我复量 #123 ⑤，写下：

> 「**8 人 delta ≥ +0.07 却没出货**，卡点是 `fact-preservation 0.93` 而实测天花板 0.920。」

并把它当成「**8 个够好的人物被一道不可达的门挡住**」来汇报。

## 怎么翻的：顺着「这 8 位各自卡在哪」查下去

查 Adams 的延后理由时撞到一个矛盾：
`collect_honest_delta`（**「诚实 delta」的唯一口径**）给 Adams **+0.2922**，
而延后名单写「两轮合并 **+0.00265**」。**两个数差两个数量级。**

逐文件查 `wip-adams-131/workspaces/comfort-avery-adams/evals/`：

    results.jsonl                 64 行｜席 **seat-D-score-v1 / seat-E-strict-v1**   ← 工具读的是这份
    results.round1-FG.jsonl.bak   64 行｜席 **F / G**                                ← `.bak` ⇒ 工具**看不见**
    round1/ round2/ round3/       各含 seat_D/E/F/G 的 json 与 notes                 ← 也看不见
    **DELTA-READ-ME-FIRST.md**    ← 产物自己放的一页，标题就是「先读这一页，再引用本产物的 delta」

## 那一页写了什么（逐字，不是我的话）

> **发布门通过（passed=True，0 错）。记录的 delta 是 `+0.2922`。**
> **那个数不能当「比裸模型强」的证据用。**

| | 候选 / 基线 | delta |
|---|---|---|
| **有 rubric**（冻结协议 D/E，**发布门读的就是这个**） | 0.909 / 0.617 | **+0.2922** |
| **无 rubric**（同一批答案、同一 A/B、同一天） | 0.807 / 0.769 | **+0.0375** |

> 无 rubric 那一侧**逐对只有 胜 17 / 负 15** —— 接近抛硬币。
> 席 D 点名 **8 题**、席 E 点名 **5 题**的评分标准把答案抄了进去（形态是**中译与压缩**，
> 字面比对的判据看不见，**实测报 0/16**）。
> **★★ 而且这不是本产物特有的：全部 13 个已完成人物的 `results.jsonl` 装的都是这两席。**

## ★★★ 核那句「不是本产物特有的」：**8/8 全中**

全库 **24** 份非空 `evals/results.jsonl`，**席位恰好是 {seat-D-score-v1, seat-E-strict-v1} 的 14 份**。
把我 ⑤ 里那 8 位逐个对：

    adams-131 ✓｜nightingale-112 ✓｜lister-108 ✓｜fleming-111 ✓
    virchow-109 ✓｜pasteur-106 ✓｜koch-107 ✓｜osler-110 ✓        ← **8/8 都在这 14 份里**

⇒ **⑤ 的前提要改写：**

> 不是「8 个够好的人物被一道不可达的门挡住」，
> 而是「**8 个用「喂过 rubric 的两席」量出来的读数**，被一道不可达的门挡住」。
> **两句话要 Owner 做的决定完全不同。**

## ★★ 顺带暴露一个工具缺口：污染标记**覆盖不全**

`collect_honest_delta --exclude-tainted` 只剔掉 **3** 位（Livermore／Steinhardt／Godin）。
而**用同一对 D/E 席的有 14 份**，Adams 的 `+0.2922` **没有被标成污染** ——
尽管**它自己的工作区里就放着一页说明这个数不能用**。

⇒ 污染标记是**按人物白名单**列的，不是**按席位**判的。
  改成按席位判（「这份读数出自被喂过 rubric 的席」）才对得上事实。
  ★ **本轮不改** —— 改标记会移动所有既有 delta 的可用性判定，那是决定。

## 我这次做对的一件事：**读了产物自己放的那页**

`DELTA-READ-ME-FIRST.md` 就在 `evals/` 下，标题是「**先读这一页，再引用本产物的 delta**」。
它还写着自己为什么在那儿：

> 「2026-08-10 从工作区根搬到 `evals/` 下。根目录的 `.md` 是**建模者读得到的** ——
>  一份记录 holdout 隔离失效的事故报告放在那里，本身就是新的泄漏。」

**产物早就把话说全了，是我引用它的数时没打开它。**
[[read-the-artifact-as-its-actual-reader]]｜[[implausibly-good-result-is-a-defect-report]]｜
[[rubric-fed-judges-flip-the-sign]]｜[[my-verification-loop-never-opened-the-files]]

## 要 Owner 定的（⑤ 的选项因此变了）

原来的三条（调阈值／放松席位／给回 rubric）之外，**先要答这一条**：

> **那 8 位的 delta 要不要按「无 rubric」那一侧重算？**
> Adams 有现成的对照（+0.2922 → **+0.0375**）；其余 7 位**没有无 rubric 侧的数据**
> （他们的 `results.jsonl` 里只有 D/E）。要重算就得**重跑判分**，
> 而那要干净上下文的会话（同 #115）。

★ 在这之前，**「8 人 delta 够了」这句话不应再被当作论据使用**，包括被我自己。

## 可复算

```bash
cd CodexSkills
cat skill_log_evals/persona-distiller/_corpora/wip-adams-131/workspaces/comfort-avery-adams/evals/DELTA-READ-ME-FIRST.md
ls  skill_log_evals/persona-distiller/_corpora/wip-adams-131/workspaces/comfort-avery-adams/evals/

python3 - <<'PY'
import json, pathlib
DE={"seat-D-score-v1","seat-E-strict-v1"}
n=de=0
for p in pathlib.Path("skill_log_evals/persona-distiller/_corpora").rglob("evals/results.jsonl"):
    seats={json.loads(l)["judge_id"] for l in p.read_text(encoding="utf-8",errors="replace").splitlines()
           if l.strip() and "judge_id" in l}
    if seats: n+=1; de += (seats==DE)
print(n, de)
PY
# 期望：24 14
```

[[rubric-fed-judges-flip-the-sign]]｜[[read-the-artifact-as-its-actual-reader]]｜[[implausibly-good-result-is-a-defect-report]]｜[[my-verification-loop-never-opened-the-files]]｜[[a-verdict-whose-scope-exceeds-its-inputs]]
