# ★ 订正：**不是 1 处红，是 3 处** —— 而其中一处测的是**合同说「只用于兼容」的旧路由**（2026-08-18）

> ## ★★★ 本文件的标题与第 ③ 节**已被我自己的下一次实测推翻**（同日，稍晚）
>
> 「**测的是 A 层旧路由**」**不成立**：`scripts/route_team.py` 全文 **6 行**，
> `from route_team_moe import main` —— **两者是同一个路由**。
> 「MoE 把 Willison 排第 9、会入选」**也不成立**：那是 `base_score` 名次，
> 而选人走的是 `marginal_select()`，**不是取前 N**。
>
> **真因见 `_marginal_select把软件题选成每族一人-2026-08-18.md`**：
> `marginal_select` 重罚同族 ⇒ 软件题第 2–9 名每族各一人、**第 5 位是农场主**、
> 第 10 名才回到第二个软件人。
>
> ★ 本文件**其余部分仍然成立**：「不是 1 处红是 3 处」、①的超时定因、②的 Mulcahy 语言主因。


## 我错在哪

上一条台账与两条提交信息里我写了「failfast 定位到**唯一一处**」。
**那是 `-f` 的产物，不是全量结论** —— failfast 遇到第一处就停，
「唯一」这个词根本不在它的射程里。**我自己在命令行里提醒过这一点，然后照写不误。**

全量实跑（`unittest discover -s tests -v`，**不带 `-f`**，865 秒）：

    Ran **92** tests ｜ 通过 **89** ｜ **FAILED (failures=2, errors=1)**

[[samples-cannot-support-universal-claims]]｜[[a-red-gate-hides-every-gate-behind-it]]

## 三处各自是什么

### ① ERROR｜`test_package_has_single_top_level_product_version_and_unnumbered_runtime_reset`

    subprocess.TimeoutExpired: package_target.py … timed out after 60 seconds

**不是逻辑错，是打包脚本跑不完 60 秒**（`tests/helpers.py:16` 的 `run_script` 硬超时）。
★ 这一处**本轮未定因**：是打包真的变慢了、还是本机负载（同时跑着多个后台任务）——
**没量过就不写结论**。要定因得在干净机器上单跑一次 `package_target.py` 计时。

### ② FAIL｜`test_new_operator_deliveries_are_available_to_routing`（已详查）

缺 **Anne Mulcahy**（第 38 / 70）。真因是**卡片语言**：她与郭士纳**同族、同题、
`domain_match` 同为 1.000**，而她卡片中文字 **0%**、与中文题面 token 交集 **0** ⇒
`task_similarity` **0.0000**；郭士纳中文字 76%、交集 4 个 ⇒ 0.0848 ⇒ 第 1。
消融确认 08-17 那个 `设计`/`design` 修复**是对的**（Maeda 1→31、Godin 2→37、郭士纳 3→1），
而 **Mulcahy 两种状态下都是第 38**。详见
`_那道红了很久的验收测试-真因是卡片语言不是域-2026-08-18.md`。

### ③ FAIL｜`test_new_software_deliveries_are_available_to_routing` ← **本条新查**

断言五人须全部出现：Liskov／Chip Huyen／Kent Beck／Fowler／**Simon Willison**。
实测**只缺 Simon Willison**（其余四人都在并集里）。他**在册、readiness=ready、软件开发师**。

**★★★ 关键：这个测试调的是 `scripts/route_team.py`（4 处，第 64/131/149/227 行），
不是 `route_team_moe.py`。**

而 `references/moe-routing-contract.md` 对 A 层写着：

> 「A 保持旧类别和场景匹配，**只用于兼容**。」

同一道英文软件题，两个路由给出的结果差得很远：

| | `route_team.py`（A 层旧路由，测试用的这个） | `route_team_moe.py`（现行 MoE） |
|---|---|---|
| Simon Willison | **不在 14 人里** | **第 9 名**（size=14 ⇒ **会入选**） |
| 选出的人 | Kent Beck、Harry Bhadeshia（材料）、Martin Fischer（建造）、**Joel Salatin（农林牧渔师）**、Theodore V. Wells Jr.（法律）、John Maeda（艺术设计）… | Kent Beck 1、Chip Huyen 2、Willison 9 |

**一道软件工程评审题，旧路由的 14 人里坐着农场主、冶金学家和律师。**

⇒ **这道红测的不是现行路由**。它红，说明的是「A 层旧路由选不动人」，
  而合同已把 A 层降为兼容层。**照着它去改现行路由是改错对象。**

★ 但**不能就此说「这道测试没用」**：`route_team.py` 仍在包里、仍可被调用，
  测试盯着它并不荒谬。**要不要把它改成测 MoE，是 Owner 的决定** ——
  改了就等于宣布 A 层不再受验收保护。**本轮不改。**

★★ 顺带一个反方向的读数，防止把 MoE 说成全面更好：同一道英文题上，
  MoE 给 **Fowler `task_similarity` 0.0000（第 35）**、**Liskov 0.0000（第 27）** ——
  他们能进并集是靠**另外两道题**。**MoE 在这道题上同样看不见这两位。**

## 可复算

```bash
cd CodexSkills/registry/codex/persona-distiller
python3 -m unittest discover -s tests -v 2>&1 | grep -E "^(FAIL|ERROR):|^Ran |^FAILED"
# 期望：Ran 92 tests｜FAILED (failures=2, errors=1)

cd ../persona-distiller-group
T='Design a software engineering review covering TDD, refactoring, evolutionary architecture, Python SQLite CLI, coding-agent prompt injection, AI/ML evaluation monitoring feedback loops, distributed systems API type design and technical teaching'
python3 scripts/route_team.py --task "$T" --size 14 | python3 -c "
import json,sys; d=json.load(sys.stdin)
sel=[r['canonical_name'] for r in d['selected_roles'] if r['role_type']=='persona-solver']
print('route_team.py:', 'Simon Willison' in sel, sel)"
# 期望：False（他不在），且名单里有 Joel Salatin
```

[[samples-cannot-support-universal-claims]]｜[[two-verdicts-decide-by-coverage-not-timestamp]]｜[[measure-a-change-at-the-layer-it-acts-on]]｜[[a-red-gate-hides-every-gate-behind-it]]
