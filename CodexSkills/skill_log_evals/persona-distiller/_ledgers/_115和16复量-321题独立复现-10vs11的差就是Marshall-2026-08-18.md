# #115／#16 复量：**321 题 0 答案**独立复现；「10 人还是 11 人」的差就是 Marshall（2026-08-18）

## #115：数字全对，且这次是从**另一条路**数出来的

上一次是顺着台账数的。这次改从 `next_person.py` 的 `★ 已做但未出货` 名单出发
（**11 人**），逐个工作区数 `evals/cases.jsonl` 与 `evals/results.jsonl`：

| 人 | 题数 | results.jsonl 行数 |
|---|---:|---:|
| Brandeis／Lincoln／Bismarck／Machiavelli／Jefferson／Rousseau／Pestalozzi／Frobel／Dewey | 各 **32** | 0 |
| Kant | **33** | 0 |
| **Marshall** | **0** | 0 |
| **合计** | **321** | **0** |

⇒ **321 题、0 人有答案** —— 与 #115 记的一字不差。

**「10 人」与「11 人」的差就是 Marshall**：他 0 题（#116 已按「装置不成立」结案、零分数产生），
所以他在「已做但未出货」里、**不在判分积压里**。两个数各自都对，**是两把不同的尺子**。

★ 扫描面 **11/11 个工作区都找到了 `evals/`** —— 不是「找不到所以算 0」。

## ★ 我这一轮量坏了两次，都是同一个病

1. **第一版 glob 猜文件名** —— `*question*.json`／`*answer*.json`／`results.jsonl`
   在 11 个工作区上全部数出 **0 题**。真名是 `evals/cases.jsonl` 与 `evals/results.jsonl`。
   **11 个全 0 不是发现，是扫描面为空。** [[filename-matching-is-brittle]]
2. **`d.get('next')` 读队列** —— 真键名是大写 **`NEXT`**，于是我印出「NEXT =（无）」，
   而实际 `NEXT = Gustave Eiffel`。**空默认值又一次吞掉了「我没读到」。**

★★ 这是**今天第三次**：`capabilities` vs `key_capabilities`（102 人全归错类）、
   `next` vs `NEXT`、外加上面的 glob。三次都是**我的临时探针**，不是产品代码 ——
   产品那侧今天已经修了六处同类。**探针不受判据管，而我据探针写结论。**
   [[empty-default-swallows-unknown]]｜[[my-diagnostics-manufacture-false-leads]]

## #16：pending 从 0 变 **1**

`queue_pending = **1**`（Gustave Eiffel，材料建工师，priority 3），`NEXT = Gustave Eiffel`。
⇒ #16 记的「实测 pending = 0」**已过期**；他是 08-18 从延后名单出列回来的（见 Eiffel 那本台账）。
队列自洽：`queue_total 237 = done 40 + pending 1 + deferred 185 + 已做未出货 11`。

**但「有 NEXT」不等于「能开工」**：Eiffel 本机 0 份 `.txt`，抓源要 Owner 的下载授权。

## 可复算

```bash
cd CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline
python3 next_person.py --show 999 | python3 -c "
import json,sys; d=json.load(sys.stdin)
print('pending', d['queue_pending'], '｜NEXT', d['NEXT']['name'])   # ← 键名是大写 NEXT
print('已做未出货', len(d['★ 已做但未出货（不进 NEXT，去看它卡在哪）']))"

cd ../_corpora
for n in brandeis marshall lincoln bismarck machiavelli jefferson kant rousseau pestalozzi frobel dewey; do
  ws=$(ls -d wip-*$n* 2>/dev/null | head -1)
  q=$(cat $ws/**/evals/cases.jsonl 2>/dev/null | grep -c . || echo 0)
  a=$(cat $ws/**/evals/results.jsonl 2>/dev/null | grep -c . || echo 0)
  echo "$n 题 $q 答 $a"
done
# 期望：各 32（kant 33、marshall 0），答案一律 0
```

[[filename-matching-is-brittle]]｜[[empty-default-swallows-unknown]]｜[[zero-hit-gates-must-prove-they-can-hit]]｜[[two-verdicts-decide-by-coverage-not-timestamp]]
