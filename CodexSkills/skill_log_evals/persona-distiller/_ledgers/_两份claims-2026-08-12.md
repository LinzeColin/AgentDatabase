# **6 个人物有两份 `claims.jsonl`，六对内容全不同** —— 核 #95 时撞出来的

日期：2026-08-12。**这不是 #95 本身，是核它的过程中发现的。**

## 一、#95 那条记录：**准的**

原记「Koch 41 条（不是 15）压在一对源上」。按权威文件
（`wip-koch-107/workspaces/robert-koch/evidence/claims.jsonl`，46 条）实测：

| | |
|---|---|
| 全部来源都落在 `src-9115214f10fd` + `src-94dc006b6b8a` 那一对上的 | **41 条** |
| 引到那一对之一的 | **46 条（全部）** |
| 判据 `check_claim_source_independence` 报出的 | **15 条** |

**41 与 15 都对**，差别在射程：判据只查 `MULTI_SOURCE_CATEGORIES` 那 6 类
（mental-model / heuristic / value / work-method / blind-spot / contradiction，共 17 条），
其余 26 条塌缩落在 `fact`(23)、`boundary`(2)、`epistemic`(1)——
**这几类本来就允许单源，不是违规。**

⇒ 准确说法：「**41 条断言只靠一部作品；其中 15 条落在「必须 ≥2 处独立证据」的类别里，是违规**」。

## 二、★★ 撞出来的新问题：两份 `claims.jsonl`

数 Koch 时先读到 `wip-koch-107/claims.jsonl`（外层），**用它数出来的是另一个数**。
逐对比较：

| 人物 | 两份行数 | 内容 |
|---|---|---|
| jenner-104 | 35 / 35 | **不同** |
| koch-107 | 46 / 46 | **不同**（5 条的 `source_ids` 差一个 `src-c8bca1856b9e`） |
| lister-108 | 35 / 35 | **不同** |
| osler-110 | 44 / 44 | **不同** |
| pasteur-106 | 33 / 33 | **不同** |
| virchow-109 | 60 / 60 | **不同** |

**6 个人物、六对全部内容不同。** 行数一样、`claim_id` 集合一样——**只有字段值不同**，
所以「行数一致」这种粗比较看不出来。

★ 判据读的是 `<工作区>/evidence/claims.jsonl`（权威），**判决不受影响**。
★★ **危险的是人**：我数 Koch 时先拿了外层那份，若不核就会把 41 报成 44。

## 三、已做

`check_contract_drift` 的「一个工作区两份账本」推广到 `claims.jsonl`，
冻结这 6 个（都已判过分，不动）、**只挡新增**。
变异：给 #172 造一对 → 当场红并点名两个路径；清掉 → 绿。

⇒ 两种形态是同一个病：**一个人物的同一份东西，仓里有两个版本，而没人说哪个算数。**
