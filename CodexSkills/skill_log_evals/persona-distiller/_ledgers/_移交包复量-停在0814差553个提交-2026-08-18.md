# #87／#127 复量：包停在 **08-14**，比现在少 **553** 个提交（2026-08-18）

## 一句话

`_protected/agentdb-handover-20260814/` 的包**自身完好、7 项全过**，
但它比今天的 HEAD **少 553 个提交** —— 其中**光今天一天就有 63 个**。

★ `_protected/` **只读**：本轮只跑了验包器（它在临时目录里 clone），**没有改动任何文件**。

## 两条待办的数，一条对一条陈旧

| | 原记录 | 实测 |
|---|---|---|
| #87 | 「停在 **08-12**，少 **359** 个提交」 | ❌ 两个数都陈旧 |
| #127 | 「包停在 **08-14**」 | ✅ 对 |
| 实测 | | tip `bc7f4247`@`2026-08-14 15:40`，**差 553** |

## 验包实测：7 项全过

    ✓ 包在（974,369,862 字节）
    ✓ git bundle verify 通过（在空临时仓里跑，判定与 cwd 无关）
    ✓ sha256  a99b02e5…6abaf2
    ✓ tip（包里的 HEAD ref） bc7f4247…78008
    ✓ 真 clone 成功
    ✓ 提交数（从 clone 现算） 2542
    ✓ clone 里 HANDOFF.md 有内容（1859 行）

### ★★★ 但验包器自己印了两条**未判项**，必须一起念

> 「**没给 `--expect-tip` ⇒「是不是最新那个包」这一项未判，不是通过。**
>  旧包自己也全绿（2026-08-13 那个 2,359 提交）。期望值写在封面信里。」
> 「同级还有别的日期的包：`agentdb-handover-20260813/` —— **别传错**」

**自洽 ≠ 最新。** 这正是它 docstring 里写死的那条：
「上面 5 项全过 ≠ 拿的是最新那个包」。08-13 的包 tip 是 `f9a4f67c`，也一样全绿。

## 553 个提交的日期分布

    08-04  50   08-05  20   08-07   2   08-09   5   08-10   3
    08-11   1   08-12   1   08-13   2   **08-14 176**
    08-15  26   08-16  16   **08-17 188**   **08-18  63**

⇒ 包打完（08-14 15:40）之后**又做了 469 个提交**（08-14 当天晚些的 + 08-15 至今）。

## 「184/212 教训已在包内」这一半：**本轮未量**

要复核得把包 clone 出来逐份数。**与「传不传」这个决定无关**（无论 184 还是 212，
包都少 553 个提交），所以本轮**如实标未量，不当已验**。
[[never-verified-the-final-artifact-itself]] 的反面：**不是不验，是说清哪一项没验。**

## 给 Owner

**若要移交，重打一个包比传 08-14 那个更合理** —— 差 553 个提交，含今天全部工作
（本会话 36 个提交也还没推）。重打的命令在 `_pipeline/make_handover_bundle.sh`。

★ 传之前用 `--expect-tip <当时的 HEAD sha>` 跑一次验包器，
**否则「是不是最新那个」永远是未判项**。

## 可复算

    D=~/Documents/Codex/GithubProject/_protected/agentdb-handover-20260814
    bash CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline/verify_handover_bundle.sh "$D"
    git rev-list --count bc7f42479378ff25e29ae995ce250be2d5878008..HEAD     # 期望 553（会随新提交增长）
    git log --format=%ad --date=short bc7f4247..HEAD | sort | uniq -c

[[self-consistent-is-not-latest]]｜[[a-blocked-by-x-label-needs-x-rerun]]｜[[counts-need-their-cutoff-stated]]
