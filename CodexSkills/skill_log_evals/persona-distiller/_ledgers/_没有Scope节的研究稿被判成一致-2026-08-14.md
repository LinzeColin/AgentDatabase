# `emit_lane_scope --check` 对「**根本没有那一节**」的文件打绿

**2026-08-14**｜发现路径：修完 Comenius 的研究稿之后，顺手数了一遍
「六条道的研究稿里有几份带 `## Scope and assigned sources`」。

## 实测

    有六道研究稿的工作区 **39** 个
      六份全有 Scope 节（`--check` 真的在比）        **27**
      **一份都没有**                                 **4**
      部分有（比了一半，另一半静默放行）             **8**

对那 4 个（**六份研究稿一份都没有 Scope 节**）跑 `--check`：

```
$ python3 emit_lane_scope.py <robert-koch> --check
台账 121 行；道文件 6 份
Scope 节与台账一致，无需改动          ← ★ rc=0
```

Pasteur（台账 60 行）、Blackwell（95 行）、Lister 三人一模一样。
**它对着不存在的东西说「与台账一致」。** [[empty-default-swallows-unknown]]

诚实的话应当是：**「6 份里 0 份有 Scope 节 ⇒ 未检查，不是一致」**。

## 这让我今天早些时候报的那个数变得不准

我用它扫全库，报「**一致 32／过期 8**」。
那 32 里**至少 4 个是「什么都没比」**，另有 8 个只比了一部分
（Adams 2/6、Cicero 2/6、Shewhart 2/6、Bessemer 3/6、Coffin 3/6、
Rosenhain 3/6、Whitworth 3/6、Gantt 5/6）。
⇒ **真正被逐份比过的只有 27 个**；「32」把未检查算进了通过。
[[a-gates-scan-set-is-smaller-than-reality]]

★ 工具**写盘那一侧是对的**：它的自测里明写「没有 Scope 标题的文件**原样不动**」——
不擅自往手写文档里插一节，这是对的。**错的只是 `--check` 把「没得比」报成「比过了，一致」。**

## 本轮不改它（说清为什么）

- `quality_check.py` 里引用了 `emit_lane_scope`；改 `--check` 的语义
  等于**动一道接在合成门链条上的判据**，而用户 8-12 的批次指令写着
  「门、席位一概不动」；
- 改完必须全库回归（27 个现在绿的要确认仍绿，12 个会从绿变成「未检查」），
  **那不是今天能收干净的一趟**；
- 更要紧的是**方向**：改完之后会有 12 个工作区从「绿」变成「未检查」，
  其中 Koch／Pasteur／Blackwell／Lister 都是已入库或已判分的人。
  这类改动要在批次之间做，不是在移交当天做。

## 留给下一轮

1. `--check` 的输出与退出码改成三态：**一致 / 过期 / 未检查**，
   并把「6 份里 N 份有 Scope 节」印出来（分母摆在明面上）；
2. 那 49 份没有 Scope 节的研究稿，逐份定：
   **是该补一节（走机械导出）还是本来就该整份手写**。
   4 个「全无」的人（Koch／Pasteur／Blackwell／Lister）是同一批老工作区，
   多半是那一代流程还没有这件工具，属于**历史遗留**而不是有意为之。

## 复现

```bash
python3 - <<'EOF'
import pathlib, re
C = pathlib.Path("CodexSkills/skill_log_evals/persona-distiller/_corpora")
L = ("writings","conversations","expression","decisions","timeline","external")
for ws in sorted(p for p in C.glob("wip-*/workspaces/*") if (p/"references/research").is_dir()):
    mds = [m for m in (ws/"references/research").glob("*.md") if re.sub(r"^\d+-", "", m.stem) in L]
    if not mds: continue
    n = sum(1 for m in mds if re.search(r"^##\s*Scope and assigned sources\s*$",
            m.read_text(encoding="utf-8", errors="replace"), re.M))
    if n < len(mds): print(f"{ws.name:30s} {n}/{len(mds)} 份有 Scope 节")
EOF
```

## 把「没得比就打绿」当一类去查 —— **只查出这一件是坐实的**

三处工具目录共 **309 个 `.py`**，按源码形状扫「集合为空 → 印 ✓ → `return 0`」：

    命中该形状          **44 处**
      绿话里带分母、或明说「不构成通过／未检查」   **22 处**  ← 这一半是对的
      绿话里既无分母也无「未检查」                 **22 处**

★ 那 22 处**只有 11 个文件**：`scripts/` 与 `references/pipeline/checkers/`
是镜像的两棵树，同一份文件被数了两遍。**报「22」不写这一句就是把镜像当成了两个缺陷。**
[[counts-need-their-cutoff-stated]]

### ★★ 这 11 个是**线索，不是结论** —— 我没有真跑过它们

想抽查三件（`check_quote_layer`／`check_evidence_is_per_claim`／`check_probe_precondition`），
两件事都做错了：

1. **参数给错**：它们要的是 `--claims`／`--queue`／`paths...`，我按位置传了工作区路径；
2. **★ 退出码看错**：我写成 `python3 … | head -6` 然后 `echo rc=$?` ——
   **`$?` 取的是 `head` 的码**，于是三件都「rc=0」，我差点据此报「用法错误也打绿」。
   去掉管道重跑，真值是 **rc=3／rc=2／rc=2**，**三件都是对的**。
   [[pipe-to-tail-hides-the-exit-code]] —— 这条我今天在别处刚引用过，转头自己踩了。

⇒ 本轮**坐实的只有 `emit_lane_scope --check` 那一件**（对 4 个工作区实跑、rc=0、
输出「与台账一致」而那一节根本不存在）。上面那 11 个**要逐个用它自己的参数跑过
才能说有没有问题**，本轮没做，**不写成「还有 11 个」**。

### 已经做对的那一半长什么样（照抄这个写法）

    check_quote_in_span     「⚠ **没有一条引文落在有边界记录的文件里** ——
                              本判据这一轮什么也没查到，**不构成通过**」
    check_sole_authorship_overreach  「⚠ 账本里一条合著／集体署名的源都没有 …… 不构成通过」
    check_shared_anchor     「{n} 题里没有跨题共享的语料片段 —— **无从比对，不是通过**」
    find_second_evidence    「一部也没有 —— **而正对照是过的**，所以这是语料的结论，不是尺子坏了」

**四种写法的共同点：把分母印出来，并明说「空」意味着什么。**

## ★★★ 补测：11 个线索**逐个用它自己的参数跑过了**，坐实 **2 个**

上一节写「本轮没做」，随后做了。给每件造**空但合法**的输入（空 jsonl／`{}`／`[]`／空工作区），
**不接管道**取真退出码：

| 工具 | rc | 空输入时说什么 | 判 |
|---|---:|---|---|
| `check_evidence_is_per_claim` | **3** | 「**断言文件是空的——本次未检查**」 | ✅ 诚实 |
| `check_claim_anchors` | **3** | 用法错误，路径不存在 | ✅ 诚实 |
| `check_attribution_basis` | **3** | 「用法错误：需要工作区路径」 | ✅ 诚实 |
| `check_baseline_provenance` | **3** | 「用法错误：需要 target 目录」 | ✅ 诚实 |
| `check_fact_density` | **3** | 「用法错误：需要工作区路径」 | ✅ 诚实 |
| `check_holdout_overlap` | **2** | argparse 缺参 | ✅ 诚实 |
| `check_quote_layer` | **1** | 空文件 → JSONDecodeError（不优雅，但**没打绿**） | ✅ 不误判 |
| `check_answer_honors_constraints` | 0 | 「⚠ **一条约束都没声明——本次…**」 | ✅ 诚实（明说了） |
| **`check_probe_precondition`** | 0 | **「扫了 0 个队列条目……」** 然后 ✓ | ✅ **诚实** |
| **`check_suite_single_drag`** | **0** | **「✓ 有阈值的套组都过了——无需诊断」，全篇无分母** | ❌ **假绿** |
| **`check_ocr_homoglyphs`** | **0** | **「✓ 引文层干净：没有引文含冒充……」，全篇无分母** | ❌ **假绿** |

⇒ **11 个线索 → 坐实 2 个**（加上 `emit_lane_scope --check` 共 **3 个**）。

★ `check_probe_precondition` 是**我那把源码尺子的误报**：它的分母印在
**另一行**（「扫了 0 个队列条目」），而我只看了带 ✓ 的那一行。
—— 同一天第 N 次：**只看一行判不了一份输出**。

★★ 数字一路缩水的全过程记在这里，别只留最后那个数：
**44 处形状 → 22 处无分母 → 11 个文件（镜像去重）→ 8 个跑得动 →
坐实 2 个 ＋ 先前 1 个 = 3 个。** 每缩一次都是我自己的量法被证伪一次。

## 三个坐实的，本轮都不改（同一个理由）

`quality_check.py` 链条上的判据，改语义要全库回归，且会把一批「绿」变成「未检查」——
移交当天不做。留给下一轮，与上面那两件并列。

---

## ★★ 2026-08-15 已处置（本条已关闭）

三个全部改完，**处置办法是同一个：把分母印出来，判定与退出码一个字不动。**
详见 `_重生成台账的护栏落地-连带三个假绿-2026-08-15.md` 与
`_三个假绿收干净-只印分母不动判定-2026-08-15.md`。

| 工具 | 空输入时原来说什么 | 现在说什么 | rc |
|---|---|---|---|
| `check_suite_single_drag` | 「✓ 有阈值的套组都过了」 | 「带阈值的套组 **0** 个……**不构成通过**」 | 0（不变）|
| `check_ocr_homoglyphs` | 「✓ 引文层干净」 | 「读到 N 个文件，扫出 **0** 条引文……**不构成通过**」 | 0（不变）|
| `emit_lane_scope --check` | 「Scope 节与台账一致」 | 「6 份里 **0** 份有该节 ⇒ **「未检查」不是「一致」**」 | 0（不变）|

★ **为什么仍不改判定**：改判定会让一批工作区从绿变「未检查」，其中有已入库／
已判分的人，而用户 8-12 的批次指令写着「门、席位一概不动」。
**诚实由话承载，绿不绿照旧** —— 这条不是妥协，是把两件事分开：
「这道门查到了什么」和「这道门拦不拦」。

★★ **上面那个 4 已过期**。2026-08-15 重新全库量：有六道研究稿的工作区
**50** 个（8-14 是 39），六份全有 **31**、**一份都没有 11**（不是 4）、部分有 **8**。
⇒ **19 个工作区**在拿静默或半绿。一份都没有的那 11 个是：
alexander-fleming、elizabeth-blackwell、florence-nightingale、henry-clifton-sorby、
joseph-lister、louis-pasteur、robert-koch、rudolf-virchow、seth-godin、
william-osler、ws-jenner。[[counts-need-their-cutoff-stated]]
