# 11 个「已做未出货」的三种阻塞 —— **两人今天转绿，剩下的不是同一件事**（2026-08-17）

## 现状（`quality_check --phase synthesis` 逐人实跑）

| 人 | rc | 硬错 | 错码 |
|---|---:|---:|---|
| **louis-brandeis** | **0** | **0** | — |
| **john-dewey** | **0** | **0** | — |
| abraham-lincoln | 1 | 1 | `research.ledger-file-missing` |
| otto-von-bismarck | 1 | 1 | 同上 |
| niccolo-machiavelli | 1 | 1 | 同上 |
| thomas-jefferson | 1 | 1 | 同上 |
| immanuel-kant | 1 | 1 | 同上 |
| jean-jacques-rousseau | 1 | 1 | 同上 |
| johann-pestalozzi | 1 | 1 | 同上 |
| friedrich-frobel | 1 | 1 | 同上 |
| john-marshall | 1 | **18** | `eval.suite-minimum`×16 ＋ `research.ledger-file-missing` ＋ `source.no-holdout` |

## 三种阻塞，处置完全不同

### ① 判分要两名互相独立的评委（2 人：brandeis、dewey）★ 我做不了

两人今天从红转绿（见 `_pipeline/check_scoring_ready.py`：都是 **✓，备注「—」**，
32 题、min 16、SE 2.59）。**装置齐全，下一步就是判分。**

而判分是 `eval_runner.py record --score <float>` —— **分数由外部评委给出**，
本项目的规矩是**两名互相独立的评委、按人物冻结指令、每人上限 3 轮**。

⇒ **我一个 agent 当不了两席**，本会话也不许派子代理。
  这不是「还没做」，是**这一步在结构上不能由我完成**。

### ② 语料正文不在本机（8 人）★ 这是待裁定 #115 的 A/B

八个人各只有 **1 条**硬错，且是同一条：`research.ledger-file-missing`。
那不是产物缺陷 —— 是「语料不进 git」裁定的后果。

⇒ **A 与 B 的差在这 8 个人身上是可见的**：选 A（语料放回 git），这一条
  对 8 人同时清掉；选 B，它会一直红，而**红的理由与产物质量无关**。

### ③ 已按 ㊺ 结案（1 人：marshall）

18 条不是新问题：`eval.suite-minimum`×16 正是「装置不成立、零分数产生」这个
结论的表现，已于 ㊺ 结案。**不是回归。**

## 今天做了什么让 ①那两人转绿

两人卡的是 `corpus.undeclared-duplicate-sources`（brandeis 25 对、dewey 21 对），
**不是语料缺失**。两人都未判分（`results.jsonl` 0 行），不触 ㊵ 冻结。

逐份打开**书名页**核过（不靠文件名相似）：

    brandeis  5 组 / 10 条派生：Business—A Profession ×3、Life Insurance ×2、
              Other People's Money ×3、The Jewish Problem ×2、Fatigue and Efficiency ×5
    dewey     4 组 /  8 条派生：The Child and the Curriculum ×5、
              Democracy and Education ×4、Psychology ×3、Leibniz's New Essays ×2

每组都用并查集验过**各自连通、互不相连**；台账行数一行没变。

★★ **剩下的不是同一部作品，一条也没合并**（合并会毁掉真证据）：

* brandeis：《Business—A Profession》(1914 论文集) 重印了 Life Insurance /
  Savings Insurance / True Americanism / 期刊单篇 —— 单行本各有独立场合与年份；
* brandeis：Goldmark《Fatigue and Efficiency》大段重印了《Women in Industry》
  辩护状（**布兰代斯本人执笔**）—— **作者不同**；
* dewey：`aqr1372…` 是 Findlay 编《The School and the Child — Being Selections
  from the Educational Essays of John Dewey》，**选集收录单篇**。

这些写进了 `meta.json` 的 `attribution_basis.counting_convention`，**逐对点名**。

## ★★★ 顺带修好一个仓里早记过、却一直没修的缺陷

判据的处置文案写着「要么补 `derived_from`，**要么**写 `counting_convention`」——
而 `counting_convention` **在代码里一次也没被读过**。
（memory 的 `error-message-points-at-an-exit-that-isnt-there` 引的就是这句话本身。）

现已实现，但**只认逐对点名**：约定文本要同时出现这一对的两个文件名才算，
写句泛泛的散文不豁免任何一对。6 条断言 + 2 次变异（「全免」/「出路又断开」）各被抓住。

## 可复算

    python3 CodexSkills/registry/codex/persona-distiller/scripts/quality_check.py <工作区> --phase synthesis
    python3 CodexSkills/registry/codex/persona-distiller/scripts/check_source_dedup.py <工作区>
    python3 CodexSkills/skill_log_evals/persona-distiller/_ledgers/_pipeline/check_scoring_ready.py
