# 台账说抽取失败，一手计数不知道 —— 而两个产物的 join 键不一样（2026-08-18）

## 事实

`wip-jefferson-175` 同一份材料，两个产物两种说法：

| 产物 | 说的是 |
|---|---|
| `evidence/source-ledger.jsonl` | `extraction_status: "failed"`，`words: null` |
| `raw/_primary.json` | `档: "一手"`，`words: 0` —— **进了一手计数** |

已裁的「抽取失败的源不再算进覆盖数」（#119，Jefferson 26→25）落在
`check_profile_declared.py` 上 —— **那件读的是台账**。
而 `_primary.json` 是**另一条链**，同一条规则在这条链上**没有执行者**。
[[one-requirement-two-consumers]]｜[[every-requirement-needs-an-owner]]

## ★★★ 我第一次量它，报的是「全库 0 条」

按 `identifier` 直接 join 两个产物 ⇒ **一条也对不上** ⇒ 输出「合计一手 998 条，
其中台账标 failed 的 **0** 条（0.00%）」。看着像个干净的结论。

真因：**两边没有共用字段。**

    台账          `source_id`  : "src-6a3cf5192354"                      ← 本地编号
    _primary.json `identifier` : "version-final-de-la-declaracion-…"     ← IA 标识

唯一的桥是台账 `url` 里的 `archive.org/download/<identifier>`。
按它重做：998 条一手里 **994 条（99.6%）对得上**，命中 **1 条**。

> **一个坏 join 给出的不是报错，是一个干净的答案。**
> 而我恰好是在**已经亲眼看到那一条**之后才去量的 —— 若顺序反过来，
> 「0 条」会被我当成结论写进台账。
> [[zero-hit-gates-must-prove-they-can-hit]]｜[[the-catalog-already-separated-them-i-didnt-read-that-word]]

## ★★ 顺带：我差点用「文件存在」当成「已判分」

查该不该改时我跑了 `find -name results.jsonl`，找到就打印「㊵ 已判分即冻结，不动」。
**而那个文件是 0 行的。** ㊵ 的判别是「`results.jsonl` **非空**」——
文件存在 ≠ 已判分。差一步就以「冻结」为由不改一个其实可以改的东西。
[[empty-default-swallows-unknown]]

## 影响（要说准）

一手占比 **67/73 = 0.9178 → 66/72 = 0.9167**。deep 门 0.65 ——
**不改变任何档位，也不改变任何已下的结论。** 这是个口径缺陷，不是分数缺陷。

## 已落地

`_pipeline/check_primary_excludes_failed_extraction.py`（自测 10/10）：

- 只判**一手**那一档（二手不进一手占比）
- **必须印 join 命中率**；低于 `--min-join`（默认 0.90）判**未核**，不算通过
- 没有台账的工作区判**未核**——实测 19 个里 1 个（`wip-plato-186` 停在阶段 2）
- 自测里专门钉了一条**坏 join 的反例**：键对不上 ⇒ 命中 0 ⇒ 不许报「干净」

实跑：合计一手 **998**｜join 上 **994（99.6%）**｜**违规 1**｜**未核 2 个工作区**
（`wip-churchill-191` join 只有 89%、`wip-plato-186` 无台账）。

## 未做的与为什么

**没有改 `classify_primary.py`**。它是确定性工具，改了之后重跑会改写 19 个工作区的
`_primary.json`，其中多个是已判分产物（㊵）。本件先做**披露**；
真要改判定，属「重跑会不会撤销已冻结的结论」那一类，随重蒸一起做。
[[regenerating-a-file-silently-reverts-human-decisions]]

## 可复算

    cd CodexSkills/skill_log_evals/persona-distiller/_ledgers
    python3 _pipeline/check_primary_excludes_failed_extraction.py            # → rc=1，1 条
    python3 _pipeline/check_primary_excludes_failed_extraction.py --self-test # → 10/10

---

# ★★★ 同日续：**我选错了对照物 —— 真正的缺陷比原来大 4 倍**

## 顺着「未核」查下去，查出了更要紧的一件

上面那版判据把 `wip-churchill-191` 判成「join 只有 89%，未核」。
按纪律「未核 ≠ 通过」去追那 2 份，发现它们的标识都以 `gut` 结尾
（`theriverwar04943gut`、`thestoryofthemal09404gut`）—— Gutenberg 镜像件。

**中途我差点报一个假缺陷**：我看 `raw/` 下只有 `_ids*.txt`，就写下「本地文件没有」。
**那是正常的** —— 语料正文按规矩不进 git，`raw/` 里进版本控制的本就只有指针。
拿 git 树当语料树看，差一点把「合规」读成「缺失」。
[[corpus-lives-outside-git-verify-the-pointers]]

## 真相：**它们在抓取清单里的 status 是 `剔除`**

`raw/_fetch-manifest.json`：

    请求数 24｜已取回 21｜无文本层 1｜剔除 2
    · theriverwar04943gut        status=剔除
    · thestoryofthemal09404gut   status=剔除

**从没取回。** 而 `_primary.json` 把它们记成 **一手**，还各带着 12.9 万 / 9.0 万词。

⇒ `classify_primary.py` 是在**「请求过的 id 清单」**上跑的，不是在**「真拿到的集合」**上跑的。
[[bibliographic-proxy-instead-of-the-measurement]]

## 全库量清（换对照物之后 join 100%）

| 口径 | join 命中 | 违规 |
|---|---|---|
| ① `_fetch-manifest.json` 的 `status != 已取回` | **1051 / 1051（100%）** | **4 份 / 451,983 词** |
| ② `source-ledger.jsonl` 的 `extraction_status == failed` | 994 / 1051（94.6%） | 1 份 / 0 词 |

    ① wip-churchill-191  theriverwar04943gut        剔除   128,672 词
    ① wip-churchill-191  thestoryofthemal09404gut   剔除    89,568 词
    ① wip-dewey-190      democracyandeduc00852gut   剔除   138,063 词
    ① wip-ford-188       mylifeandwork07213gut      剔除    95,680 词
    ② wip-jefferson-175  version-final-de-la-…      failed       0 词

**★ 对照物选对了，「未核」也一起没了**：抓取清单在 19 个工作区里 **19 个都有**，
包括没有出处台账的 `wip-plato-186`。
⇒ 问「这份材料到底拿到没有」，权威记录是**抓取清单**，不是出处台账。
我第一版拿台账去对，既要靠 url 搭桥（994/1051），又制造了两个假的「未核」。

## 影响（要说准，别放大）

| 人 | 一手 | 扣掉后 | 占比 | quick 门 |
|---|---:|---:|---|---|
| churchill-191 | 19 | **17** | 0.8261 → 0.8095 | ≥8 份、≥0.40 |
| dewey-190 | 40 | **39** | 1.0000 → 1.0000 | 同上 |
| ford-188 | 17 | **16** | 0.8947 → 1.0000 | 同上 |

**三个人都没跨过任何门。** 45 万词听着吓人，而它**不改变任何已下的结论** ——
是口径缺陷，不是分数缺陷。[[measurement-errors-all-point-the-same-way]]

## 判据已改到主口径上（自测 14/14）

判的是「**status 不等于「已取回」**」，不是穷举坏值 ——
实测已出现三种坏值（`剔除` / `无文本层` / `失败`），穷举必然漏掉下一种。
[[checkers-assume-a-shape-the-product-outgrows]]

    python3 _pipeline/check_primary_excludes_failed_extraction.py             # → rc=1，5 条
    python3 _pipeline/check_primary_excludes_failed_extraction.py --self-test # → 14/14
