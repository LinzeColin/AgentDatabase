# v0.0.0.74 —— 这个数是 0% 或 100%，先去核工具

日期：2026-08-04

## 触发：一天之内同一形状的错三次

| 何时 | 错法 | 当时的数 | 真值 |
|---|---|---|---|
| v0.0.0.71 | `roster()` 报工作区、`scan()` 报容器 | 「少扫 11 个」 | 15 = 15，一个不缺 |
| #15 范围统计 | `charles-becht-iv` 的 `iv` 是 `l-iv-ermore` 的子串 | 4 人 | **3 人** |
| 锚点全量审计 | **只读外层 zip，核心产物在嵌套 zip 里** | 「**98/100 零锚点**」 | **4/100** |

**共同点：拿到一个看起来完整的数，而它答的是另一个问题。**

## 三条规则

1. 命中数为 **0**
2. 命中数 **= 全体**（且 ≥3）
3. **★ ≥80% 的单位内部命中为 0**——为锚点审计那次专门补的

第 3 条是必须的：那次的 98/100 **既不是 0 也不是全体**，
所以前两条抓不住它。真实信号是「**逐个单位都空**」。

## 实测输出（今天三次错误重放）

```
① 锚点审计: 98/100 单位内部为空
⚠ **98/100 个单位内部命中为 0**
    **逐个单位都空**——先确认每个单位里该打开的东西打开了没有
    （锚点审计那次：核心产物在**嵌套 zip** 里，只读了外层）

② 首创声明第一版: 0/10
⚠ **命中数为 0**

③ 语料射程: 1/1 工作区
  ✓ 不是极端值——这道判据不说话
```

**三次抓住两次。** 第③次不是比例问题，是**扫过的单位数与名册对不上**——
**那归 `check_scan_reach` 管。一道判据抓不住的那一类，要写清归谁管。**

## 真值不许被它拦

```
锚点真值 4/100，0 个单位为空:
  ✓ 不是极端值——这道判据不说话
```

## 自测（9 条反向对照）

```
── ★★ 反向对照 ①：**中间值一律不管** ──
  ✓ 43/100 → 不报    ✓ 1/100 → 不报    ✓ 99/100 → 不报
── ★★ 反向对照 ②：**样本太小时「全中」不算极端** ──
  ✓ 2/2 → 不报    ✓ 3/3 → 报
── ★★ 反向对照 ③：**空集合不许被当成「命中 0」** ──
  ✓ 0/0 → 不报
── ★★ 反向对照 ④：**核过射程就必须能关掉它** ──
  ✓ 给了核验说明 → 不报    ✓ 空白说明关不掉
── ★★ 反向对照 ⑥：**逐个单位都空要举手** ──
  ✓ 98/100 → 报（虽然 98/100 不是比例极端）
── ★★ 反向对照 ⑦：**修好之后的真值不许再报** ──
  ✓ 0 个单位为空（100 个都有锚点）→ 不报

✓ 自测全过
```

`pytest 70 passed`；`✓ 无合同漂移`。

## ★ 它不是门，而且必须能被关掉

`--verified '<怎么核的>'` 直接放行。
**核过射程就必须能关掉它**——否则它会变成一个
**逼人把真实的 100% 改写成别的东西的机器。**
