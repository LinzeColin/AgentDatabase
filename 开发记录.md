# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`42/149`（`28.19%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P3`。
- 当前 Task：`S06-P3-T1`，配置 raw 默认隔离，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P3-T1`（配置 raw 默认隔离）。
- 目标：配置搜索、Codex 路由、CI 和前端构建默认排除 raw 正文。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P1-T2 | 建立浅层公开 raw 数据平面 | completed |
| S06-P1-T3 | 建立 credential exclusion 合同 | completed |
| S06-P2-T1 | 实现不可变 raw ledger | completed |
| S06-P2-T2 | 实现确定性 45 MiB 分片 | completed |
| S06-P2-T3 | 实现 restore/verify | completed |

## 验证与恢复

- 最近完成：`S06-P2-T3`；记录了 `11` 条验证命令、`4` 条结果和 `2` 个证据引用。
- 验证摘要：13 项专用回归覆盖真实 45 MiB+123 bytes 两分片 restore round trip、幂等 replay、两类 legacy manifest、manifest command 不执行、篡改/缺失/额外/symlink、路径与 schema 漂移、冲突不覆盖、archive 内输出阻断、失败清理、post-publish 状态、读取后 part/manifest/output mutation，以及 parent fsync 失败同时发生 output mutation 的 tr...（截断）
- 恢复：提交前仅反向应用 S06-P2-T3 精确 Task patch；提交后使用 git revert <S06-P2-T3 commit>。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
