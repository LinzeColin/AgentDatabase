# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`47/149`（`31.54%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P1`。
- 当前 Task：`S07-P1-T3`，实现 Codex sync state，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P1-T3`（实现 Codex sync state）。
- 目标：建立增量 cursor、内容 hash 去重和 interrupted-run resume。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P3-T1 | 配置 raw 默认隔离 | completed |
| S06-P3-T2 | 建立 push size guard | completed |
| S06-P3-T3 | 建立 raw contract fixtures | completed |
| S07-P1-T1 | 实现 Codex source discovery | completed |
| S07-P1-T2 | 实现 Codex public raw archive | completed |

## 验证与恢复

- 最近完成：`S07-P1-T2`；记录了 `9` 条验证命令、`4` 条结果和 `3` 个证据引用。
- 验证摘要：专用 14/14 覆盖 recoverable sanitized JSONL/SQLite、普通 transcript 保留、credential/path/binary 处理、source hash/stat 不变、append-only 冲突、recent-active 延后、quiescence 重分类、source drift 零发布、part tamper、manifest authorization 漂移、public index/ledger 缺失、unsafe tar member...（截断）
- 恢复：提交前仅反向应用 S07-P1-T2 非 raw 实现 patch；提交后用纠正提交回退代码/配置，但保留 append-only archive、public index 与 raw ledger 证据，不覆盖或删除既有 raw。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
