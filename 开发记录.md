# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`41/149`（`27.52%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P2`。
- 当前 Task：`S06-P2-T3`，实现 restore/verify，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P2-T3`（实现 restore/verify）。
- 目标：从分片恢复并逐文件与整包校验。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P1-T1 | 定义统一 source registry | completed |
| S06-P1-T2 | 建立浅层公开 raw 数据平面 | completed |
| S06-P1-T3 | 建立 credential exclusion 合同 | completed |
| S06-P2-T1 | 实现不可变 raw ledger | completed |
| S06-P2-T2 | 实现确定性 45 MiB 分片 | completed |

## 验证与恢复

- 最近完成：`S06-P2-T2`；记录了 `11` 条验证命令、`4` 条结果和 `2` 个证据引用。
- 验证摘要：14 项专用回归以真实 fixture bytes 验证 45 MiB 边界、两 part 顺序/bytes/SHA-256、跨目录确定性、幂等 replay、冲突不覆盖、source stat、symlink/lock fail closed、目录与 part inode 替换保护以及 CLI 的 partial/post-publish 真实状态。
- 恢复：提交前仅反向应用 S06-P2-T2 精确 Task patch；提交后使用 git revert <S06-P2-T2 commit>。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
