# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`49/149`（`32.89%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P2`。
- 当前 Task：`S07-P2-T2`，更新 Codex Atlas 数据，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P2-T2`（更新 Codex Atlas 数据）。
- 目标：更新 Memory Atlas snapshot、周报和同步状态。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P3-T3 | 建立 raw contract fixtures | completed |
| S07-P1-T1 | 实现 Codex source discovery | completed |
| S07-P1-T2 | 实现 Codex public raw archive | completed |
| S07-P1-T3 | 实现 Codex sync state | completed |
| S07-P2-T1 | 构建 Codex derived outputs | completed |

## 验证与恢复

- 最近完成：`S07-P2-T1`；记录了 `12` 条验证命令、`7` 条结果和 `4` 个证据引用。
- 验证摘要：专用 9/9 覆盖 contract/provenance、真实 T2+T3 fixture 构建、raw bytes 不变、精确复跑零写入、第三 archive 只增量解析一个、archive/registration 篡改、模型参数 hash 重建、并发锁、canonical credential/Windows path 阻断和 symlink 输出父目录 fail closed；相关 Codex/source-registry/CLI 回归 68/68 PASS。
- 恢复：提交前仅反向应用 S07-P2-T1 derived/code/config patch；提交后使用纠正提交回退实现并从 immutable raw 重建 derived。不得修改或删除 archive、public index、raw manifest、ledger 或 sync state。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
