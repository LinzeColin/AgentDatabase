# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`38/149`（`25.50%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P1`。
- 当前 Task：`S06-P1-T3`，建立 credential exclusion 合同，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P1-T3`（建立 credential exclusion 合同）。
- 目标：允许 transcript/raw 公开，同时阻断 credentials、cookies、tokens、passwords 和 private keys。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S05-P3-T1 | 建立 UI 中文术语表与文案字典 | completed |
| S05-P3-T2 | 增加轻量语义可读性检查 | completed |
| S05-P3-T3 | 修复真实编码问题与机器语义泄漏 | completed |
| S06-P1-T1 | 定义统一 source registry | completed |
| S06-P1-T2 | 建立浅层公开 raw 数据平面 | completed |

## 验证与恢复

- 最近完成：`S06-P1-T2`；记录了 `9` 条验证命令、`5` 条结果和 `2` 个证据引用。
- 验证摘要：Canonical audit 以 metadata-only 方式确认 5 个浅层目录、513 个 tracked 文件与 512 个 raw 数据文件；来源分区为 chatgpt 379、codex 132、codex-reviewer 1。
- 恢复：提交前仅反向应用 S06-P1-T2 的精确 Task patch；提交后使用 git revert <S06-P1-T2 commit>。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
