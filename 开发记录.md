# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`46/149`（`30.87%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P1`。
- 当前 Task：`S07-P1-T2`，实现 Codex public raw archive，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P1-T2`（实现 Codex public raw archive）。
- 目标：将 sessions/logs/history 以原始格式或可恢复原始包 append-only 归档。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P2-T3 | 实现 restore/verify | completed |
| S06-P3-T1 | 配置 raw 默认隔离 | completed |
| S06-P3-T2 | 建立 push size guard | completed |
| S06-P3-T3 | 建立 raw contract fixtures | completed |
| S07-P1-T1 | 实现 Codex source discovery | completed |

## 验证与恢复

- 最近完成：`S07-P1-T1`；记录了 `7` 条验证命令、`3` 条结果和 `2` 个证据引用。
- 验证摘要：专用 9/9 覆盖候选优先级、显式 invalid 不回退、default not_found、六类 allowlist、credential 排除、metadata-only 零正文读取、source size/mtime/SHA-256 不变、root/eligible symlink 拒绝和 path-free CLI fail closed。
- 恢复：git revert 本 Task 本地 commit；不得删除已存在 raw。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
