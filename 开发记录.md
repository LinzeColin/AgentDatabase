# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`40/149`（`26.85%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P2`。
- 当前 Task：`S06-P2-T2`，实现确定性 45 MiB 分片，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P2-T2`（实现确定性 45 MiB 分片）。
- 目标：对超过阈值的包分片并生成顺序、bytes 和 SHA-256 manifest。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S05-P3-T3 | 修复真实编码问题与机器语义泄漏 | completed |
| S06-P1-T1 | 定义统一 source registry | completed |
| S06-P1-T2 | 建立浅层公开 raw 数据平面 | completed |
| S06-P1-T3 | 建立 credential exclusion 合同 | completed |
| S06-P2-T1 | 实现不可变 raw ledger | completed |

## 验证与恢复

- 最近完成：`S06-P2-T1`；记录了 `10` 条验证命令、`4` 条结果和 `2` 个证据引用。
- 验证摘要：专用回归覆盖同尺寸源变更、目录 inventory 变更、Codex 非 transcript 读取、ChatGPT 读取、SHA-256、历史 drift/delete/unledgered、独占锁、ledger 前缀不可变和幂等重放；三个适配器 apply fixture 均证明源 bytes/size/mtime 不变且 ledger 注册可见。
- 恢复：提交前仅反向应用 S06-P2-T1 精确 Task patch；提交后使用 git revert <S06-P2-T1 commit>。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
