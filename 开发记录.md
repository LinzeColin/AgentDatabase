# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`45/149`（`30.20%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P1`。
- 当前 Task：`S07-P1-T1`，实现 Codex source discovery，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P1-T1`（实现 Codex source discovery）。
- 目标：从现有配置和实际环境发现 Codex 数据路径，不硬编码单一用户绝对路径。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P2-T2 | 实现确定性 45 MiB 分片 | completed |
| S06-P2-T3 | 实现 restore/verify | completed |
| S06-P3-T1 | 配置 raw 默认隔离 | completed |
| S06-P3-T2 | 建立 push size guard | completed |
| S06-P3-T3 | 建立 raw contract fixtures | completed |

## 验证与恢复

- 最近完成：`S06-P3-T3`；记录了 `12` 条验证命令、`3` 条结果和 `2` 个证据引用。
- 验证摘要：4 项专用回归使用临时合成数据完整覆盖 append、duplicate、tamper、chunk、restore 和 credential_block；45 MiB+17 bytes package 形成 45 MiB 与 17 bytes 两 part，恢复 SHA-256 精确一致，manifest 篡改后 verify/restore 均 fail closed 且既有 output 不变。
- 恢复：提交前仅反向应用 S06-P3-T3 精确 Task patch；提交后使用 git revert <S06-P3-T3 commit>。不得删除或修改生产 raw、archive、ledger 来回滚测试。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
