# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`53/149`（`35.57%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P3`。
- 当前 Task：`S07-P3-T3`，建立 Codex restore proof，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P3-T3`（建立 Codex restore proof）。
- 目标：从空临时目录恢复一份 Codex archive 并重建 derived snapshot。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S07-P2-T1 | 构建 Codex derived outputs | completed |
| S07-P2-T2 | 更新 Codex Atlas 数据 | completed |
| S07-P2-T3 | 迁移旧 Codex 摘要语义 | completed |
| S07-P3-T1 | 实现 sync codex --push-main | completed |
| S07-P3-T2 | 配置 Codex scheduler profile | completed |

## 验证与恢复

- 最近完成：`S07-P3-T2`；记录了 `7` 条验证命令、`5` 条结果和 `3` 个证据引用。
- 验证摘要：Test-first hardening closed dry-run evidence loss, Owner Daily argument leakage, false T1 PASS semantics, dead max-wait and relative Codex source write ordering. Final dedicated scheduler regression 20/20 PASS；相关 scheduler/registry/CLI/validator 56/56、...（截断）
- 恢复：git revert 本 Task 本地 commit；禁用 scheduler profile，不删除 raw。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
