# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`44/149`（`29.53%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P3`。
- 当前 Task：`S06-P3-T3`，建立 raw contract fixtures，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P3-T3`（建立 raw contract fixtures）。
- 目标：覆盖追加、重复、篡改、分片、恢复和凭证阻断。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S06-P2-T1 | 实现不可变 raw ledger | completed |
| S06-P2-T2 | 实现确定性 45 MiB 分片 | completed |
| S06-P2-T3 | 实现 restore/verify | completed |
| S06-P3-T1 | 配置 raw 默认隔离 | completed |
| S06-P3-T2 | 建立 push size guard | completed |

## 验证与恢复

- 最近完成：`S06-P3-T2`；记录了 `11` 条验证命令、`9` 条结果和 `2` 个证据引用。
- 验证摘要：Canonical contract 固定 45 MiB 推荐分片、>50 MiB split-required、>=100 MiB hard stop、严格 <1,610,612,736 bytes 单批目标，以及 8 MiB/batch + 256 bytes/object 协议 reserve；阈值漂移 fail closed。
- 恢复：提交前仅反向应用 S06-P3-T2 精确 Task patch；提交后使用 git revert <S06-P3-T2 commit>。不得通过 force、LFS、history rewrite 或删除 raw 绕过 guard。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
