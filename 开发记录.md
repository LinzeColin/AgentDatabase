# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`39/149`（`26.17%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S06` / `MA-V121-S06-P2`。
- 当前 Task：`S06-P2-T1`，实现不可变 raw ledger，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S06-P2-T1`（实现不可变 raw ledger）。
- 目标：实现 source stat 前后校验、内容 hash、append-only ledger 和幂等去重。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S05-P3-T2 | 增加轻量语义可读性检查 | completed |
| S05-P3-T3 | 修复真实编码问题与机器语义泄漏 | completed |
| S06-P1-T1 | 定义统一 source registry | completed |
| S06-P1-T2 | 建立浅层公开 raw 数据平面 | completed |
| S06-P1-T3 | 建立 credential exclusion 合同 | completed |

## 验证与恢复

- 最近完成：`S06-P1-T3`；记录了 `9` 条验证命令、`5` 条结果和 `2` 个证据引用。
- 验证摘要：Canonical credential exclusion contract 固定 credentials_not_transcript 边界；7 类 account-control material、合同漂移、credential-like staged path、大于 1 MiB public raw 和无值回显均由 8 项专用回归覆盖。
- 恢复：提交前仅反向应用 S06-P1-T3 的精确 Task patch；提交后使用 git revert <S06-P1-T3 commit>。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
