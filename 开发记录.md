# 开发记录

## 当前状态

- 结论：Memory Atlas v1.2.1 仍在本地开发，当前未发布到 GitHub main 或线上。
- Task Pack 进度：`50/149`（`33.56%`）；S05 为 `8/8`。
- 当前 Stage / Phase：`MA-V121-S07` / `MA-V121-S07-P2`。
- 当前 Task：`S07-P2-T3`，迁移旧 Codex 摘要语义，状态 `planned`。
- 发布状态：`local_only_not_released`；仓库版本仍为 `0.2.0`。
- 本页重点：开发记录只显示最近完成、当前门禁和恢复动作；完整历史不在 owner 门面展开。

## 下一步

- 下一 Task：只执行 `S07-P2-T3`（迁移旧 Codex 摘要语义）。
- 目标：保留现有脱敏摘要的兼容读取，但不再把它冒充 full raw backup。
- 验收：完成当前 Task 的 acceptance 与验证命令后，才能进入下一 Task。
- 边界：一次 run 只完成一个 Task；整包与最终复审完成前不 push、不部署、不清缓存。

## 最近完成

| Task | 内容 | 状态 |
|---|---|---|
| S07-P1-T1 | 实现 Codex source discovery | completed |
| S07-P1-T2 | 实现 Codex public raw archive | completed |
| S07-P1-T3 | 实现 Codex sync state | completed |
| S07-P2-T1 | 构建 Codex derived outputs | completed |
| S07-P2-T2 | 更新 Codex Atlas 数据 | completed |

## 验证与恢复

- 最近完成：`S07-P2-T2`；记录了 `10` 条验证命令、`6` 条结果和 `6` 个证据引用。
- 验证摘要：专用 9/9 覆盖合同/模型冻结、snapshot/weekly/state 原子发布、精确复跑零写入、dry-run、derived hash drift、event/facet identity drift、state symlink、state truth 篡改重建和替换后 fsync 失败事务回滚；Codex/registry/CLI/runtime/consolidation focused 131/131 PASS。
- 恢复：提交前反向应用 S07-P2-T2 code/config/UI/governance patch，并从上一提交恢复 snapshot/weekly/codex_atlas state；提交后使用纠正提交回退。本 Task 不删除或重写 canonical T1 derived、raw/archive/public index/ledger/original codex sync state 或 T3 legacy consumers。
- 完整 Stage/Phase/Task、事件和证据仍保存在 canonical governance；本页不复制长日志。
