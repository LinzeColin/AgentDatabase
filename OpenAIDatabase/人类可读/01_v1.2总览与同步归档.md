# 01 v1.2 总览、同步归档与验收

> 合并版本：`v0.0.0.1`。以下源文档严格按原目录顺序串联；正文仅更新因合并失效的文件路径，并增加稳定锚点、来源边界与自动生成区块。

> 范围：v1.2 总览、Agent 同步、Raw 归档、凭证排除、机器账本、ChatGPT fallback 与运行手册。

## 本卷源文件映射

| 顺序 | 原文件 | 本文件锚点 | 类型 |
|---:|---|---|---|
| 1 | 01_v1.2四线14Stage升级总览.md | [src-01-v12-overview](#src-01-v12-overview) | 静态原文 |
| 2 | 05_ChatGPT与Codex及其他Agent自动同步说明.md | [src-05-agent-sync](#src-05-agent-sync) | 静态原文 |
| 3 | 06_Raw明文公开与只读归档说明.md | [src-06-raw-public-archive](#src-06-raw-public-archive) | 静态原文 |
| 4 | 07_凭证排除说明.md | [src-07-credential-exclusion](#src-07-credential-exclusion) | 静态原文 |
| 5 | 08_Raw机器账本说明.md | [src-08-raw-machine-ledger](#src-08-raw-machine-ledger) | 静态原文 |
| 6 | 09_ChatGPT只读同步与官方导出Fallback.md | [src-09-chatgpt-readonly-fallback](#src-09-chatgpt-readonly-fallback) | 静态原文 |
| 7 | 09_验收标准与运行手册.md | [src-09-acceptance-runbook](#src-09-acceptance-runbook) | 静态原文 |

---

<!-- BEGIN SOURCE: src-01-v12-overview; original=01_v1.2四线14Stage升级总览.md; baseline_sha256=965b499e1b4867b1bc0fc0c409bb12618f29ac461bd3b7ed9724a60f7b39d2ae -->
<a id="src-01-v12-overview"></a>
# 01 v1.2 四线14Stage升级总览

## 结论

v1.2 是一次四线、14 Stage 的整合升级。它不是新建一个分裂系统，而是在现有
OpenAIDatabase / Memory Atlas 上建立更清晰的人类入口、机器治理入口、数据同步、
行为智能、可视化、授权 apply 和运行维护能力。

当前 final delivery cleanup 本地部分已完成：GitHub `main`、本地 app 入口和 runtime
manifest 均对齐到当前 GitHub `main` HEAD；以 post-push
`git rev-parse HEAD == git rev-parse origin/main` 和 app install manifest 校验为准。本机已清理可再生
Memory Atlas 前端 build/cache 和 `/private/tmp` 临时证据；保留 app bundle、
Application Support source/runtime 和 Downloads roadmap/task pack。机器证据见
`机器治理/证据与日志/final_delivery/v1_2_final_delivery_cleanup_status.json`。
Cloudflare live deployment and Access challenge 仍缺 operator/live 证据，因此整体 goal
不能标记为 complete。

当前最新阶段是 v1.2 Final Review 已完成。任务 ID 为 `MA-V12-FINAL-REVIEW`，验收 ID 为
`ACC-MA-V12-FINAL-REVIEW`，状态为
`v1_2_final_review_passed_pending_github_main_sync_no_upload_yet`。Validator 为
`validate:v1.2-final-review`。终审覆盖四线14Stage 的 `S01-S14 Review` 链，确认
raw append-only、credential audit、Chinese UX、visual ROI、report contract、proposal apply、
owner-daily 和 final audit 主题均有记录。下一步是 pending GitHub main sync、app reinstall
和 local cleanup；进入该 phase 前必须处理 remote branch reconciliation required。
No GitHub main upload。No remote push。No raw mutation。

历史复验兼容记录：

当前最新阶段是 S13 Review 已完成。任务 ID 为 `MA-V12-S13-REVIEW`，验收 ID 为
`ACC-MA-V12-S13-REVIEW`，状态为
`stage_s13_review_passed_pending_s14_no_github_main_upload`。Validator 为
`validate:v1.2-s13-review`。S13 Review 已完成 Proposal 状态机、Diff narrator 和
Apply 与回滚的整体复审；S13 P1、S13 P2、S13 P3 均通过。复审确认未授权 proposal
不会 apply，Diff narrator 中文可读，失败可进入 rollback_or_needs_revision，raw 不被修改。
下一步是 S14 P1。
No GitHub main upload。No remote push。No raw mutation。

当前最新阶段已推进到 S14 Review 已完成。任务 ID 为 `MA-V12-S14-REVIEW`，验收 ID 为
`ACC-MA-V12-S14-REVIEW`，状态为
`stage_s14_review_passed_pending_v1_2_final_review_no_github_main_upload`。Validator 为
`validate:v1.2-s14-review`。S14 Review 已完成 `owner-daily`、final audit 和开发记录复审：
`atlasctl_unified_cli.v1_2_s14_p1`、`atlasctl_final_audit.v1_2_s14_p2`、
`stage_pass_gate_status.v1_2_s14_p3.json` 均满足 S14 stage gate。开发记录中文可读，
维护命令少而清晰，所有 stage pass gate 状态可查。下一步是 pending v1.2 Final Review。
No GitHub main upload。No remote push。No raw mutation。

历史复验兼容记录：S13 P3 已完成时当前阶段是 S13 P3。任务 ID 为 `MA-V12-S13P3`，
验收 ID 为 `ACC-MA-V12-S13P3`，状态为
`phase_s13_p3_apply_rollback_completed_pending_s13_review`。Validator 为
`validate:v1.2-s13-p3`。S13 P3 已完成 Apply 与回滚：`sample_unauthorized`
未授权 fail-closed，`sample` 授权 dry-run 可进入 apply、validation 和 rollback point
路径，模拟 validation failure 会进入 `rollback_or_needs_revision`。真实 pending proposal
未获人类授权前不 apply。当时下一步是 pending S13 Review。S13 Review 已完成，下一步是 S14 P1。
No GitHub main upload。No remote push。No raw mutation。

历史复验兼容记录：S13 P2 已完成时当前阶段是 S13 P2。任务 ID 为 `MA-V12-S13P2`，
验收 ID 为 `ACC-MA-V12-S13P2`，状态为
`phase_s13_p2_diff_narrator_completed_pending_s13_p3`。Validator 为
`validate:v1.2-s13-p2`。S13 P2 已完成 Diff narrator：每个 proposal 都有中文解释，
覆盖改了什么、为什么改、影响什么、如何验证、如何回滚；机器 diff 保留在治理证据文件，
不进入人类首页。当时下一步是 S13 P3。S13 P3 已完成，下一步是 S13 Review。
No GitHub main upload。No remote push。No raw mutation。No proposal apply execution。

历史复验兼容记录：S13 P1 已完成时当前阶段是 S13 P1。任务 ID 为
`MA-V12-S13P1`，验收 ID 为 `ACC-MA-V12-S13P1`，状态为
`phase_s13_p1_proposal_state_machine_completed_pending_s13_p2`。Validator 为
`validate:v1.2-s13-p1`。S13 P1 已完成 Proposal 状态机，建立
`draft → pending_human_review → approved_by_human → applying → applied → validated → committed`
以及 `failed_validation → rollback_or_needs_revision` 的状态合同，并接入 proposal expiry。
当前 proposal 默认处于 `pending_human_review`，未进入 `approved_by_human` 前不能 apply。
此句只用于保留已完成 phase 的复验语义，不代表当前阶段。当时下一步是 S13 P2。
S13 P2 已完成，下一步是 S13 P3。
No GitHub main upload。No remote push。No raw mutation。No proposal apply execution。

历史复验兼容记录：S12 Review 已完成时当前阶段是 S12 Review。任务 ID 为
`MA-V12-S12-REVIEW`，验收 ID 为 `ACC-MA-V12-S12-REVIEW`，状态为
`stage_s12_review_passed_pending_s13_no_github_main_upload`。Validator 为
`validate:v1.2-s12-review`。S12 Review 已复审通过 Command Palette、Personalization Prompt
和 ChatGPT 深度探索；S12 P1、S12 P2、S12 P3 均通过，`prefill_only` no-send，
`auto_submit` FAIL_CLOSED。此句只用于保留已完成 review 的复验语义，不代表当前阶段。
当时下一步是 S13 P1。S13 P1 已完成，下一步是 S13 P2。
No silent send。No cookie/token/secret export。No GitHub main upload。No remote push。
No raw mutation。No proposal apply execution。

历史复验兼容记录：S12 P3 已完成时当前阶段是 S12 P3。任务 ID 为 `MA-V12-S12P3`，验收 ID 为
`ACC-MA-V12-S12P3`，状态为
`phase_s12_p3_chatgpt_deep_explore_completed_pending_s12_review`。Validator 为
`validate:v1.2-s12-p3`。S12 P3 已完成 ChatGPT 深度探索入口：命令面板新增用户触发入口，
`atlasctl.py chatgpt-deep-explore` 可生成最新记忆分析报告、深度探索提示和 ChatGPT launch
URL。默认 `prefill_only`，`auto_submit` 受配置和显式确认控制。下一步是 S12 Review。
此句只用于保留已完成 phase 的复验语义，不代表当前阶段。S12 Review 已完成，下一步是 S13 P1。

历史复验兼容记录：S12 P2 已完成时当前阶段是 S12 P2。任务 ID 为 `MA-V12-S12P2`，验收 ID 为
`ACC-MA-V12-S12P2`，状态为
`phase_s12_p2_personalization_prompt_completed_pending_s12_p3`。Validator 为
`validate:v1.2-s12-p2`。S12 P2 已完成 Personalization Prompt：ChatGPT、Codex、other agent
三类 prompt 均包含中文人类说明和机器可复制文本；来源包含 latest memory、behavior、latent
和 self_iteration。下一步是 S12 P3。No GitHub main upload。No remote push。No raw mutation。
No proposal apply execution。

历史复验兼容记录：S12 P1 已完成时当前阶段是 S12 P1。任务 ID 为 `MA-V12-S12P1`，
验收 ID 为 `ACC-MA-V12-S12P1`，状态为
`phase_s12_p1_command_palette_completed_pending_s12_p2`。Validator 为
`validate:v1.2-s12-p1`。S12 P1 已完成命令面板：同步 ChatGPT、同步 Codex、生成本周报告、
查看待授权 proposal、生成 personalization prompt。此句只用于保留已完成 phase 的复验语义，
不代表当前阶段。当时下一步是 S12 P2。

历史复验兼容记录：S11 Review 已完成时当前阶段是 S11 Review。任务 ID 为
`MA-V12-S11-REVIEW`，验收 ID 为 `ACC-MA-V12-S11-REVIEW`，状态为
`stage_s11_review_passed_pending_s12_no_github_main_upload`。Validator 为
`validate:v1.2-s11-review`。S11 Review 已完成：P0 图谱集合覆盖 12 张图，每张图都有
中文问题和行动说明、Visual ROI Gate 与 `source/time/project/task` 过滤；这些图共同支持
行为、ROI、协作、潜性、证据、治理决策阅读。此句只用于保留已完成 review 的复验语义，
不代表当前阶段。当时下一步是 S12 P1。S12 P1 已完成，下一步是 S12 P2。

历史复验兼容记录：S11 P4 完成时当前阶段是 S11 P4。任务 ID 为 `MA-V12-S11P4`，
验收 ID 为 `ACC-MA-V12-S11P4`，状态为
`phase_s11_p4_human_question_map_completed_pending_s11_review`。Validator 为
`validate:v1.2-s11-p4`。Human Question Map 已把 S11 P1-P3 的 12 张 P0 图谱统一绑定
中文问题、行动价值、Visual ROI Gate 和 `source/time/project/task` 过滤。下一步是
S11 Review。No GitHub main upload in this phase。No raw mutation。No proposal apply execution。

S01 整体复审已通过。S02 P1 已完成数据源模型定义，S02 P2 已完成 source registry，
S02 P3 已完成人类同步说明。S02 Review 已通过。S03 P1 已完成公开 raw 路径定义。
S03 P2 已完成凭证排除。S03 P3 已完成机器账本。S03 Review 已通过。
S04 P1 已完成 ChatGPT 只读同步。S04 P2 已完成 Codex local sync 和
future-agent adapter。S04 P3 已完成 GitHub backup dry-run/apply 本地控制面。
S04 Review 已通过，S04 整体复审已通过。S05 P1 已完成 Facet schema。
S05 P2 已完成 facet extractor，并生成
`data/derived/behavior_intelligence/events.json`。S05 P3 已完成 lightweight
`evidence_refs`，每条 event 保留可追溯证据引用。S05 Review 已通过，S05 整体复审已通过。
S06 P1 已完成 Cluster builder，输出 `data/derived/behavior_intelligence/clusters.json`。
S06 P2 已完成低价值循环、Decision Debt Ledger 和 Action Half-Life，输出
`data/derived/behavior_intelligence/low_value_loops.json`。S06 P3 已完成机会发现和
为什么不是现在 卡片，输出 `data/derived/behavior_intelligence/opportunities.json`。
S06 Review 已完成，`data/derived/visualization/memory_atlas.json` 已包含
`behavior_intelligence`，Memory Atlas 能显示有证据的主题簇、低价值循环和机会线索。
S07 P1 已完成 Personal Economic Proxy，输出
`data/derived/economic_proxy/personal_economic_proxy.json`，公式来源是
`机器治理/参数与公式/personal_economic_proxy.v1_2_s07_p1.json`。S07 P2 已完成
Information ROI 与 Visual ROI Gate，输出
`data/derived/information_roi/information_roi_gate.json`，Visual ROI Gate 要求每个 P0
图表绑定 human question 和 action；没有决策价值的图表不进 P0。S07 P3 已完成
Formula What-if 配置预览，输出 `data/derived/economic_proxy/formula_what_if_preview.json`，
以 proposal-only 方式查看时间节省、复用价值、长期复利、返工成本和低价值循环惩罚等权重假设。
S07 Review 已完成，确认 Personal Economic Proxy、Information ROI、Visual ROI Gate 和
Formula What-if 满足 S07 stage gate，外部经济数据库只保留 v2 占位，不声称精确收入预测。
S08 P1 已完成 Codex/Agent 协作质量指标，输出
`data/derived/agent_collaboration/agent_collaboration_quality_report.json`，用
`planning_clarity`、`execution_clarity`、`review_burden`、`rework_count`、
`scope_clarity`、`testability` 和 `rollbackability` 解释协作质量，并保留
`chatgpt`、`codex`、`other_agent` 通用 source 字段。S08 P1 不创建复杂 Delegation
Contract UI，不创建多 agent 系统，不修改 raw，不定义授权 apply 边界。S08 P2 已完成
授权边界，输出 `data/derived/agent_collaboration/agent_authorization_boundary_report.json`，
明确 raw 不可修改，proposal 需人类授权并进入 `approved_by_human` 后才能 apply。
S08 P2 不执行 proposal apply，不创建复杂 Delegation Contract UI，不创建多 agent 系统，
不生成 stage flight recorder。S08 P3 已完成 stage flight recorder，输出
`data/derived/agent_collaboration/stage_flight_recorder.json`，用 10 个轻量字段记录
S08 P1/P2/P3 运行证据，不携带 raw/transcript 载荷，不生成臃肿人类文档，只在开发记录中
总结必要信息。S08 Review 已完成，确认 Codex/Agent 协作质量、授权边界和 lightweight
stage flight recorder 均满足 S08 stage gate；系统能解释 ChatGPT/Codex/其他 agent 的
协作质量与边界，且没有高负担治理框架。S09 P1 已完成，输出
`data/derived/behavior_intelligence/latent_signals.json`，每条 latent signal 都有
supporting evidence、contradicting evidence、alternative explanation、confidence、
Evidence Strength Badge 和 next validation。S09 P2 已完成，输出
`data/derived/behavior_intelligence/self_iteration_suggestions.json`，建议覆盖 memory、
config、AGENTS、style 和 personalization，每条建议都有 action half-life，proposal
均设置 warn/stale/archive 和 `expires_at`，且保持 pending human review。S09 P3 已完成，
输出 `data/derived/behavior_intelligence/decision_debt_ledger.json`，包含 8 条决策债候选，
每条都有证据、linked self-iteration suggestions、最小下一步、预期交付件和停止条件。
历史复验兼容记录：S09 P3 已完成，`decision_debt_ledger.json` 已生成，下一步是 S09 Review。
	S09 Review 已完成，复审确认 `latent_signals.json`、`self_iteration_suggestions.json` 和
	`decision_debt_ledger.json` 均满足 S09 stage gate，且没有 raw mutation、proposal apply
	execution、pressure list、psychological diagnosis output 或 personality label output。
	S10 P1 已完成首页 arrival briefing，首屏先回答“上次来以后发生了什么”，覆盖新增重要资料、
	增强结论、减弱或过期结论、待授权 proposal 和同步失败；机器细节默认折叠。
	S10 P2 已完成全局中文和 Chinese UX linter。S10 P3 已完成机器字段默认折叠和
	高级详情入口。S10 Review 已完成，复审确认首页能回答上次来以后发生了什么、核心 UI 默认中文、
	机器字段默认折叠和 Chinese UX linter 均满足 S10 stage gate。S11 P1 已完成 Clio-like
	visuals，首页新增 `cluster_tree`、`bubble_map` 和 `topic_cluster_explorer`。
	历史复验兼容记录：S11 P1 已完成时，下一步是 S11 P2。
	S11 P2 已完成 Economic-like visuals，首页新增 `task_treemap`、
	`automation_vs_augmentation`、`roi_scatter` 和 `opportunity_radar`。每个图都有中文问题和
	行动说明，并可按 `source/time/project/task` 过滤。历史复验兼容记录：S11 P2 已完成时，
	下一步是 S11 P3。S11 P3 已完成 Workflow/latent/governance visuals，首页新增
	`agent_decision_sankey`、`friction_heatmap`、`latent_radar`、`evidence_timeline` 和
	`formula_explorer`。每个图都有中文问题和行动说明，并可按 `source/time/project/task`
	过滤。下一步是 S11 P4。S11 Review 已完成，下一步是 S12 P1。

## 四线

| 线 | 内容 | 目标 |
|---:|---|---|
| 1 | Anthropic 化行为智能 | 从 transcript 和 derived data 中形成 facets、clusters、latent signals、自我迭代和协作质量解释。 |
| 2 | 信息 ROI、全中文和多维可视化 | 打开系统先看到结论、变化、证据和下一步，而不是 ID、schema 和路径堆叠。 |
| 3 | ChatGPT、Codex、后续其他 agent 自动备份进 GitHub | 通过 source registry、raw append-only、manifest/hash 和 derived reports 建立公开备份链路。 |
| 4 | UIUX 多模态交互、参数公式治理、授权后自动 apply、运行维护简化 | 用 command palette、proposal gate、formula what-if 和 atlasctl 降低维护成本。 |

## 14 Stage 执行规则

- 每次 run 最多只完成一个 phase。
- 一个 Stage 的全部 phase 完成后，必须做 Stage 复审。
- 复审暴露的问题必须先解决，才能进入下一个 Stage。
- 整体完成后才上传 GitHub main。
- 整体完成后才重装 app 入口。
- 本机清理和缓存减压属于整体完成后的收尾工作。

## 当前状态

S01 P1、S01 P2、S01 P3 和 S01 Review 已完成。S02 P1 已完成数据源模型：
`source_id`、`source_type`、`agent_name`、`raw_root`、`sync_mode`、
`public_backup_mode`、`connector_capability`，source_type 支持
`chatgpt、codex、other_agent`。

S01 的需求冻结清单继续记录旧隐私边界替换为用户授权后的 raw/transcript 公开和凭证排除。

S02 P2 已完成。source registry 位于
`机器治理/同步与备份/sync_source_registry.json`，包含：

- `chatgpt`：ChatGPT browser connector 与 official export fallback。
- `codex`：Codex local sync。
- `future_agent_template`：后续 other_agent 的 future_agent_adapter。

所有 source 都使用 `plaintext_public` public backup mode，并保留 transcript/credential
边界。

S02 P3 已完成。人类同步说明位于
`人类可读/01_v1.2总览与同步归档.md#src-05-agent-sync`，明确写出
ChatGPT、Codex、后续其他 agent 数据备份进 GitHub，说明 future_agent_template 如何接入，
并重申 `credentials_not_transcript`。

S02 Review 已通过。复审确认：

- S02 P1 数据源模型保留 `source_id`、`source_type`、`agent_name`、`raw_root`、
  `sync_mode`、`public_backup_mode`、`connector_capability`。
- S02 P2 source registry 不是仅 chatgpt/codex 硬编码，包含 `future_agent_template`。
- S02 P3 人类说明保留 ChatGPT、Codex、后续其他 agent 数据备份进 GitHub、
  future_agent_template 和 transcript/credential 边界。

S03 P1 已完成。它定义：

- `data/public_raw/chatgpt`
- `data/public_raw/codex`
- `data/public_raw/agents/{agent_id}`
- `manifest/hash` 文件合同
- `append-only` 规则
- `hash drift fail` 规则

S03 P1 不导入真实 transcript，不实现 credential gate，不生成 manifest ledger，
不实现 connector，不上传 GitHub main。

S03 P2 已完成。它定义：

- credential is not memory。
- `credentials_not_transcript`。
- 普通 transcript 可以进入公开 raw。
- 凭证 pattern 导致 gate fail。
- `scripts/privacy_guard.py` 提供共享扫描、断言、脱敏和 repo audit。
- `scripts/sync_codex_memory_data.py` 复用共享凭证脱敏入口。

S03 P2 不生成 manifest ledger，不实现 connector，不新增复杂 UI，不上传 GitHub main。
S03 P3 已完成。它定义：

- raw manifest/hash 可生成。
- 每个非空 manifest row 映射 source/file/hash/imported_at。
- `scripts/raw_archive_manifest.py` 提供 generate 与 audit。
- 修改已有 raw 文件导致 validation fail。
- 删除已登记 manifest entry 导致 validation fail。
- raw manifest 是机器文件，不是人类主要页面。

S03 P3 不导入真实 transcript，不实现 connector，不新增 UI，不上传 GitHub main。

S03 Review 已通过。复审确认：

- raw 可公开备份。
- append-only 规则、hash drift fail 和 deleted manifest entry fail 均可验证。
- credential exclusion 已接入扫描、同步脱敏和审计，credential pattern 会失败。
- raw manifest/hash 可生成，并映射 source/file/hash/imported_at。
- human files 没有被 raw manifest 明细污染。

下一步是 S04 P1。

S04 P1 已完成。它定义：

- ChatGPT 只读同步。
- official export fallback。
- 密码/验证码立即停止。
- 不发送消息、不删除、不归档、不重命名会话。
- `scripts/atlasctl.py sync --source chatgpt --dry-run` 的低负担入口。

S04 P1 不实现 Codex local sync，不实现后续 agent adapter，不实现 GitHub backup apply，
不上传 GitHub main。下一步是 S04 P2。

S04 P2 已完成。它定义：

- Codex local sync dry-run 与 apply 输出合同。
- future-agent adapter 的最小本地 JSON 输入接口。
- 每个来源的 raw + derived + run log 输出。
- `scripts/atlasctl.py sync --source codex --dry-run`。
- `scripts/atlasctl.py sync --source future-agent --dry-run`。
- `scripts/atlasctl.py build-atlas --dry-run`。

S04 P2 不实现 GitHub backup dry-run/apply，不上传 GitHub main。

S04 P3 已完成。它定义：

- `scripts/github_backup.py`。
- `scripts/atlasctl.py push --dry-run`。
- `scripts/atlasctl.py push --apply`。
- backup scope 覆盖 `data/public_raw`、`data/derived`、`data/run_logs`、`docs/reviews` 和 `reports`。
- dry-run 不写文件。
- apply 只本地 git add/commit，不远端 push。
- 失败时输出中文原因和 fallback 建议。

S04 P3 不执行远端 push，不上传 GitHub main，不重装 app。

S04 Review 已通过。S04 整体复审已通过，覆盖 ChatGPT 只读同步、Codex local sync、
future-agent minimal adapter、raw + derived + run log 输出、build-atlas dry-run 和
GitHub backup dry-run/apply。复审产物为
`docs/reviews/memory_atlas_v1_2_s04_review.md`，validator 为
`validate:v1.2-s04-review`。

S05 P1 已完成。Facet schema 位于
`机器治理/数据契约/facet_event_schema.v1_2_s05_p1.json`，中文说明位于
`人类可读/02_同步备份与行为治理.md#src-12-facet-event-semantics`。它定义 `source`、`topic`、
`intent`、`task_type`、`project`、`output_type`、`language`、`tool`、
`turn_count`、`friction`、`value_signal` 和 `future_agent_source`，但不实现
extractor、不生成 events、不修改 raw。

S05 P2 已完成。facet extractor 位于 `scripts/extract_memory_atlas_facets.py`，
CLI 入口为 `scripts/atlasctl.py analyze --stage facets`，输出为
`data/derived/behavior_intelligence/events.json`。当前输出覆盖 ChatGPT 201 条和
Codex 16 条；future_agent 当前无 public raw，只记录 missing reason。

S05 P3 已完成。`data/derived/behavior_intelligence/events.json` 当前包含
`evidence_refs`，每条 event 均保留 `source_id`、`record_id` 以及 raw、manifest、
derived 或 missing reason 级别的轻量证据引用。S05 P3 不实现 Raw-to-Insight Replay UI，
不修改 raw，不生成 fake events。

S05 Review 已通过。复审确认 S05 P1 schema、S05 P2 extractor 和 S05 P3
evidence_refs 共同满足：canonical event 可覆盖 ChatGPT/Codex/future agent；
每条 event 有 evidence ref 或缺失原因；人类文件能解释 facet 含义；不输出纯机器字段给首屏。
S05 整体复审已通过。

S06 P1 已完成。Cluster builder 位于 `scripts/build_memory_atlas_clusters.py`，
CLI 入口为 `scripts/atlasctl.py analyze --stage clusters`，输出为
`data/derived/behavior_intelligence/clusters.json`。当前输出包含主题簇和层级簇，
支持 `source/time/project/task/language` 过滤合同；每个 cluster 均有中文摘要、
代表事件和 `evidence_refs`。S06 P1 不识别低价值循环，不生成机会卡片，不修改 raw。

S06 P2 已完成。低价值循环 builder 位于
`scripts/build_memory_atlas_low_value_loops.py`，CLI 入口为
`scripts/atlasctl.py analyze --stage low-value-loops`，输出为
`data/derived/behavior_intelligence/low_value_loops.json`。当前输出包含低价值循环候选、
Decision Debt Ledger 和 Action Half-Life，覆盖 `repeated_rework`、
`discussion_without_landing`、`over_optimization` 和 `scope_creep`。S06 P2 不生成
opportunity cards，不做心理诊断，不修改 raw。

S06 P3 已完成。机会发现 builder 位于 `scripts/build_memory_atlas_opportunities.py`，
CLI 入口为 `scripts/atlasctl.py analyze --stage opportunities`，输出为
`data/derived/behavior_intelligence/opportunities.json`。当前输出包含 12 条候选机会
和 12 张 为什么不是现在 卡片，覆盖 `automation`、`productization`、`template`、
`compounding` 和 `defer`。S06 P3 不接外部经济数据库，不做心理诊断，不生成无穷压力清单，
不修改 raw。

S06 Review 已完成。复审产物为 `docs/reviews/memory_atlas_v1_2_s06_review.md`，
validator 为 `validate:v1.2-s06-review`。复审确认 S06 P1/P2/P3 输出均有证据，
`data/derived/visualization/memory_atlas.json` 的 `behavior_intelligence` 可供 Memory
Atlas 展示主题簇、低价值循环和机会线索。下一步是 S07 P1。

整体完成后才上传 GitHub main，整体完成后才重装 app 入口。
<!-- END SOURCE: src-01-v12-overview -->

---

<!-- BEGIN SOURCE: src-05-agent-sync; original=05_ChatGPT与Codex及其他Agent自动同步说明.md; baseline_sha256=5d7789b21ab2bad780286ca42ef92a189b4a418fd63974f14a746d0a79b194eb -->
<a id="src-05-agent-sync"></a>
# ChatGPT 与 Codex 及其他 Agent 自动同步说明

## 当前结论

本页是 Memory Atlas v1.2 S02 P3 的人类同步说明。

- task: `MA-V12-S02P3`
- acceptance: `ACC-MA-V12-S02P3`
- status: `phase_s02_p3_human_sync_explanation_completed_pending_s02_review`
- registry: `机器治理/同步与备份/sync_source_registry.json`
- next: pending S02 Review

本页不是 connector 实现，不会读取浏览器、不会写 raw archive、不会上传 GitHub main。
它只解释 ChatGPT、Codex、后续其他 agent 数据备份进 GitHub 的范围、边界和接入规则。

## 同步范围

1. ChatGPT
   - 使用 source registry 中的 `chatgpt` source。
   - 同步策略是 ChatGPT browser connector + official export fallback。
   - browser connector 的边界是只读 conversation/title/metadata，不抓取密码、cookie、session token。
   - official export fallback 用于浏览器只读同步不可用时的人工导出导入路径。

2. Codex
   - 使用 source registry 中的 `codex` source。
   - 同步策略是 Codex local sync。
   - 范围是本地 Codex session transcripts、消息文本、工具摘要和必要 metadata。
   - 不采集 tokens、API keys、env secrets、本机临时缓存或本地 app 私有状态。

3. 后续其他 agent
   - 使用 source registry 中的 `future_agent_template`。
   - source_type 必须是 `other_agent`，connector_type 必须是 `future_agent_adapter`。
   - 允许能力可来自 local_file、browser_readonly、api 或 manual_import。
   - 新 agent 必须先注册为 source，再进入后续 raw/manifest/derived/report 流程。

## 哪些数据会进入 GitHub

进入 GitHub 的前提是用户授权、source registry 通过、后续 raw append-only gate 通过。
S02 P3 本身不写 raw，只说明未来会进入 GitHub 的类别：

| Source | GitHub raw root | 可公开内容 |
|---|---|---|
| ChatGPT | `data/public_raw/chatgpt` | conversation_title、conversation_content、message_text、timestamp、speaker_role、metadata、tool_call_summary、attachment_reference |
| Codex | `data/public_raw/codex` | conversation_title、conversation_content、message_text、timestamp、speaker_role、metadata、tool_call_summary、attachment_reference |
| other_agent | `data/public_raw/agents/{agent_id}` | 与 transcript 等价的消息、标题、时间、角色、metadata、工具摘要和附件引用 |

所有 source 的 `public_backup_mode` 必须是 `plaintext_public`。这表示用户授权后的 transcript
可以明文公开进入 GitHub，但不表示凭证、密钥或账户控制信息可以进入 GitHub。

后续阶段会把 raw manifest、hash、derived data 和 reports 接入 GitHub 备份链路；S02 P3 不创建这些运行产物。

## transcript/credential 边界

规则是 `transcript/credential` 分离，credential boundary 是 `credentials_not_transcript`。

永远不能提交：

- cookies
- session tokens
- passwords
- API keys
- private keys
- OAuth tokens
- browser_credential_store
- 本机登录态、验证码、浏览器凭证库、env secrets

这些内容不是 agent 数据源内容。任何同步、导出、导入、GitHub backup 或 deep explore payload
一旦包含这些内容，都必须 fail closed。

## 后续 agent 如何接入

后续 agent 不能绕过 source registry。接入步骤：

1. 在 `sync_source_registry.json` 中按 `future_agent_template` 新增 source。
2. 填写 `source_id`、`source_type`、`agent_name`、`raw_root`、`sync_mode`、
   `public_backup_mode`、`connector_capability`。
3. 保持 source_type 为 `other_agent`，并保留 transcript_boundary 与 credential_boundary。
4. allowed action 可以包含 `register_source`；forbidden action 必须包含 `capture_credentials`。
5. 通过 S02/S03/S04 的 validator、credential gate、raw append-only gate 后，才能进入 GitHub 备份链路。

## 本 phase 边界

- No GitHub main upload in this phase.
- No connector implementation.
- No raw archive change.
- No app reinstall.
- 不进入 S02 Review；下一轮才能做 S02 Review。
- 不实现自动登录、自动发送、删除、归档、重命名 ChatGPT 会话。
- 不要求 Codex 获取或保存密码、验证码、cookie、session token。
<!-- END SOURCE: src-05-agent-sync -->

---

<!-- BEGIN SOURCE: src-06-raw-public-archive; original=06_Raw明文公开与只读归档说明.md; baseline_sha256=0404dfbf6f641b7a17a61db385455c71750fb9d96b1e1040aa85f5f2535bcc65 -->
<a id="src-06-raw-public-archive"></a>
# Raw 明文公开与只读归档说明

任务 ID：`MA-V12-S03P1`。

验收 ID：`ACC-MA-V12-S03P1`。

状态：`phase_s03_p1_public_raw_path_defined_pending_s03_p2`。

S03 P1 只定义公开 raw 路径和只读完整性规则，不导入真实 transcript。

## 公开路径

- ChatGPT：`data/public_raw/chatgpt`
- Codex：`data/public_raw/codex`
- 后续其他 agent：`data/public_raw/agents/{agent_id}`

机器策略文件是 `raw_public_archive_policy.v1_2_s03_p1.json`，位于
`机器治理/同步与备份/`。

## 只读规则

- 新 raw 文件以后可以追加，规则是只追加。
- 已存在 raw 文件不能修改。
- 已存在 raw 文件不能删除。
- 已存在 raw 文件不能覆盖。
- 已存在 raw 文件 hash 变化时必须 hash drift fail。
- raw archive 永远不是 proposal apply target。

## 本 phase 不做什么

- 凭证检查在 S03 P2。
- manifest 生成在 S03 P3。
- connector 和真实同步在 S04。
- No GitHub main upload in this phase。

本页只给人类解释路径和边界，不展示机器 manifest 明细。
<!-- END SOURCE: src-06-raw-public-archive -->

---

<!-- BEGIN SOURCE: src-07-credential-exclusion; original=07_凭证排除说明.md; baseline_sha256=c51de298788567fb07c13843c2c803a8634d3eab519efbfbfc2c681aaf5c70ce -->
<a id="src-07-credential-exclusion"></a>
# 凭证排除说明

任务 ID：`MA-V12-S03P2`。

验收 ID：`ACC-MA-V12-S03P2`。

状态：`phase_s03_p2_credential_exclusion_completed_pending_s03_p3`。

## 结论

S03 P2 固定一条边界：credential is not memory。

`credentials_not_transcript` 的意思是：普通 transcript 可以进入公开 raw，
但 cookie、session token、password、api key、private key、oauth token 和
browser credential store 不能进入 GitHub。

## 会被拦截的内容

- cookie。
- session token。
- password。
- api key。
- private key。
- oauth token。
- browser credential store。

## 不应该被拦截的内容

- 普通对话 transcript。
- 讨论 token budget、API key 管理原则、密码管理器概念的文字。
- 不包含真实凭证值的同步说明、需求说明和审计记录。

## 机器实现

- 策略文件：`机器治理/同步与备份/credential_exclusion_policy.v1_2_s03_p2.json`。
- 扫描与审计：`scripts/privacy_guard.py`。
- Codex 同步脱敏入口：`scripts/sync_codex_memory_data.py`。
- Validator：`validate:v1.2-s03-p2`。

## 边界

No GitHub main upload in this phase。

本 phase 不生成 S03 P3 manifest ledger，不实现 connector，不新增复杂 UI，
不导入真实 transcript，不修改或删除已有 raw。

下一步是 S03 P3。
<!-- END SOURCE: src-07-credential-exclusion -->

---

<!-- BEGIN SOURCE: src-08-raw-machine-ledger; original=08_Raw机器账本说明.md; baseline_sha256=6acd8a2a0216329704326fef41099ec07051ba3800c493508631e1476439b66e -->
<a id="src-08-raw-machine-ledger"></a>
# Raw机器账本说明

Task ID: `MA-V12-S03P3`.

Acceptance ID: `ACC-MA-V12-S03P3`.

Status: `phase_s03_p3_machine_ledger_completed_pending_s03_review`.

本页只解释 S03 P3 的机器账本边界，不展示 raw manifest 逐行明细。

S03 P3 已把 raw manifest 和 hash ledger 生成为机器文件。机器账本用于记录
source/file/hash/imported_at 映射，让后续 agent 可以判断公开 raw 是否只追加、
是否发生 hash drift、是否有 manifest entry 被删除。

当前仓库还没有真实 raw transcript 文件，因此 baseline machine ledger 可以为空。
`README.md`、`.gitkeep`、`.DS_Store` 这类说明或占位文件不会被锁成 transcript raw。

S03 P3 的边界：

- raw manifest 是机器文件，不是人类主要页面。
- 新 raw 文件允许追加。
- 已有 raw 文件 hash 改变会让 audit fail。
- 已登记 raw 文件被删除会让 audit fail。
- 本 phase 不实现 connector，不导入真实 transcript，不新增 UI。
- No GitHub main upload in this phase.

下一步是 S03 Review。
<!-- END SOURCE: src-08-raw-machine-ledger -->

---

<!-- BEGIN SOURCE: src-09-chatgpt-readonly-fallback; original=09_ChatGPT只读同步与官方导出Fallback.md; baseline_sha256=e4369aff7ea93c12faea05927e2d62d837821847bfdc32979da1ae1686f173a7 -->
<a id="src-09-chatgpt-readonly-fallback"></a>
# ChatGPT只读同步与官方导出Fallback

任务 ID：`MA-V12-S04P1`。

验收 ID：`ACC-MA-V12-S04P1`。

状态：`phase_s04_p1_chatgpt_sync_completed_pending_s04_p2`。

## 结论

S04 P1 已建立 ChatGPT 只读同步的最小可运行入口。当前实现不尝试登录、不读取密码、
不保存 cookie 或 session token，也不会发送消息/删除/归档/重命名会话。
浏览器 connector 的硬边界是不得发送消息/删除/归档/重命名会话。

浏览器 connector 在本阶段是只读 contract：只能读取已登录状态下的 conversation title、
conversation content 和 metadata；一旦遇到密码/验证码立即停止。

可执行 fallback 是 official export ZIP/conversations.json fallback。`scripts/sync_chatgpt_memory_data.py`
可以解析官方导出的 `conversations.json` 或 ZIP，在 dry-run 下只统计不写文件，在 apply
模式下写入 `data/public_raw/chatgpt/`、`data/derived/chatgpt/chatgpt_sync_summary.json`
和 `data/run_logs/sync_runs/`。

## 使用边界

- `python scripts/atlasctl.py sync --source chatgpt --dry-run` 只返回可运行合同，不写文件。
- `python scripts/sync_chatgpt_memory_data.py --official-export <path> --dry-run` 验证导出文件可解析，不写文件。
- apply 模式必须提供 official export；没有输入时不能生成伪数据。
- 任何 credential pattern 会先失败，不进入 public raw。
- 已存在 public raw 文件不允许被覆盖。

## 本阶段不做

- 不实现 Codex local sync；那是 S04 P2。
- 不实现后续 agent adapter；那是 S04 P2。
- 不实现 GitHub backup dry-run/apply；那是 S04 P3。
- No GitHub main upload in this phase。

下一步是 S04 P2。
<!-- END SOURCE: src-09-chatgpt-readonly-fallback -->

---

<!-- BEGIN SOURCE: src-09-acceptance-runbook; original=09_验收标准与运行手册.md; baseline_sha256=63ae32848bab32a51ea4cf303e5e17a09353affedf3989211c6007529b7fd655 -->
<a id="src-09-acceptance-runbook"></a>
# 09 验收标准与运行手册

## 当前状态

本文件是 Memory Atlas v1.2 S14 P3 的中文运行手册。任务 ID 为 `MA-V12-S14P3`，
验收 ID 为 `ACC-MA-V12-S14P3`，状态为
`phase_s14_p3_development_record_completed_pending_s14_review`。本 phase 只完成开发记录
与维护说明，不进入 S14 Review，不上传 GitHub main，不重装 app，不清理本机。

S14 P3 的验收口径：

- 开发记录中文可读。
- 维护命令少而清晰。
- 所有 stage pass gate 状态可查。
- 机器证据位于 `机器治理/证据与日志/stage_pass_gates/stage_pass_gate_status.v1_2_s14_p3.json`。
- 下一步是 pending S14 Review。

No GitHub main upload。No remote push。No raw mutation。

## 最少维护命令

以下命令是 S14 P3 以后交接给任意 agent 的最小维护面。默认工作目录是
`/Users/linzezhang/Documents/Codex/2026-07-05/1-airm2-2-codexproject-https-github/work/CodexProject`。

```bash
python3 scripts/atlasctl.py run --profile owner-daily --dry-run
```

```bash
python3 scripts/atlasctl.py audit
```

```bash
pnpm --dir OpenAIDatabase/apps/memory-atlas run validate:v1.2-s14-p3
```

```bash
npm run build --prefix apps/memory-atlas
```

```bash
python3 -B -m unittest discover OpenAIDatabase/tests -q
```

```bash
cat OpenAIDatabase/机器治理/证据与日志/stage_pass_gates/stage_pass_gate_status.v1_2_s14_p3.json
```

## 状态查询

`stage_pass_gate_status.v1_2_s14_p3.json` 记录 S01-S13 的 review gate 状态和 S14 的
phase gate 状态。S14 当前停在 `phase_s14_p3_development_record_completed_pending_s14_review`，
还没有完成 S14 Review，因此不能声称整个 S14 或 v1.2 已最终完成。

## 回滚

若 S14 P3 失败，回滚本地 S14 P3 commit 即可。不要删除 raw archive，不要上传 GitHub main，
不要重装 app，不要深度清理本机。回滚后下一步仍应从 S14 P3 重新开始，而不是跳到 S14 Review。
<!-- END SOURCE: src-09-acceptance-runbook -->
