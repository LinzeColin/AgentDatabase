Keep the implementation on Stage 1: deliver a governance-first, derived-only dynamic personal profile Skill v0.0.0.1 that reads only allowlisted redacted derived data, writes only one dual-plane DYNAMIC_PROFILE.md, runs every three calendar days with a no-change cost gate, never mutates stable memory, and turns validated profile changes into one temporary Agent action or one trial-ready Prompt/Workflow/Skill candidate. Do not introduce raw-data ingestion, LLM/API calls, databases, extra ledgers, rejected opportunities, or scope expansion.
